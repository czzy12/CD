# AGENTS.md

本文件给 coding agent 使用，记录本项目的协作规则和入口。项目状态、历史细节和样本排查不要堆在这里。

## 项目定位

本项目是银行流水 PDF / Excel / 微信流水解析工具，目标是把多银行流水解析为统一交易模型，并提供 GUI、月度统计、异常提示、流水调整、Excel 导出和收入佐证 JSON 输出。

本项目后续作为车贷报告自动化总项目的流水子模块。总项目不应重新实现流水识别。

## 工作原则

- 小范围修改，避免无关重构。
- 不做需求外功能，不做 speculative abstraction。
- 不删除已有银行适配逻辑，除非明确确认无用。
- 遇到不确定的银行格式或业务口径，先说明假设。
- GUI 改动默认只影响展示层，不改变解析结果或导出数据，除非需求明确。
- 可信度只作为 GUI 提示，不写入 Excel，不作为业务硬判断。
- 不把逐文件金额、笔数、客户本地路径等细账长期写入 `PROJECT_STATUS.md`。

## 关键入口

- `gui_v2.py`：当前源码 GUI 入口。
- `bankflow_v2/models.py`：标准交易模型。
- `bankflow_v2/auto_detect.py`：银行类型识别。
- `bankflow_v2/pipeline.py`：解析调度入口。
- `bankflow_v2/summary.py`：汇总和异常校验。
- `bankflow_v2/adjustment.py`：流水调整/测算层。
- `bankflow_v2/income_proof_export.py`：收入佐证 JSON 输出。
- `tools/regression.py`：统一回归入口。
- `tools/regression_cases.json`：回归样本清单。
- `PROJECT_STATUS.md`：当前状态概要。
- `技术变更记录.md`：详细问题、原因、解决方式和验证结果。
- `银行适配手册.md`：银行格式和适配规则。
- `INTEGRATION_CONTRACT.md`：与车贷报告自动化总项目的集成契约。

## 银行适配要求

新增或修复银行格式时，通常需要同步检查：

- `bankflow_v2/auto_detect.py`
- `bankflow_v2/pipeline.py`
- 对应银行解析器
- `tools/regression_cases.json`
- `银行适配手册.md`
- `技术变更记录.md`

解析器应尽量保留原始字段、原始金额、原始余额和问题提示，方便复核。

## 验证要求

常规 GUI 或文档改动至少运行：

```powershell
python -m py_compile gui_v2.py tools\regression.py
```

涉及解析、识别、汇总、调整或导出时运行：

```powershell
python tools\regression.py --all --allow-missing
```

涉及单个银行适配时，优先运行对应 tag：

```powershell
python tools\regression.py --tag xxx --allow-missing
```

## 文档记录要求

重要更新要记录：

- 遇到的问题
- 原因分析
- 做了哪些调整
- 验证命令和结果
- 影响范围

记录位置：

- 当前状态概要：`PROJECT_STATUS.md`
- 详细过程入口：`技术变更记录.md`；正文按月份位于 `docs/change-history/`
- 银行格式规则入口：`银行适配手册.md`；详细规则位于 `docs/bank-rules/`
- 总项目接口：`INTEGRATION_CONTRACT.md`

### 文档按需读取规则

- 默认先读取 `PROJECT_CONTEXT.md` 的“完成记录固定位置”，再读取 `PROJECT_STATUS.md`、`技术变更记录.md` 和 `银行适配手册.md` 的短入口；不得在任务开始时通读历史归档。
- 银行适配任务先在 `银行适配手册.md` 搜索银行、版式或解析器，再只读取命中的 `format-*` 或 `field-*` 章节。
- 问题排查先在 `技术变更记录.md` 搜索银行、问题关键词或 `chg-*`，再只读取对应月份文件的稳定 ID 章节。
- 乱码、余额链、跨页表头、误识别等跨银行问题读取 `docs/解析踩坑与通用规则.md`；具体发生经过和验证证据仍沿 `chg-*` 追溯。
- 只有项目级审计、规则冲突无法解释或用户明确要求完整历史时，才扩大到全部月份归档；禁止把归档中的旧“当前状态”直接当作现状。
- 新增记录时必须同时增加入口索引和详细正文；不得只写归档而留下无法检索的孤立章节。

### 字段工作记录体系

以下文件职责互斥，禁止把同一批逐来源状态复制到所有文件：

| 文件 | 唯一职责 |
| --- | --- |
| 人工确认工作簿 | 逐来源字段决策、实现、验证状态总账；三类状态必须分列 |
| 外部《阶段进展与续接记录》 | 顶部当前有效快照、下一步入口、历史停点；历史章节只作审计 |
| 外部《PDF流水核实能力重构规划》 | 稳定路线图、阶段目标和阶段完成度 |
| `银行适配手册.md` | 当前代码规则索引；详细银行版式和文字字段规则位于 `docs/bank-rules/` |
| `技术变更记录.md` | 问题索引和当前阶段最近变更；详细原因、修改和验证历史位于 `docs/change-history/` |
| `PROJECT_STATUS.md` | 重大能力里程碑和最近验证摘要 |

固定状态字段：

- 决策状态：`已确认 / 部分确认 / 待确认 / 暂缓（缺原件）`；只表示字段归属、保留/排除和边界是否确认。
- 实现状态：`已实现 / 已实现（本地未提交） / 部分实现 / 待实现 / 未实现 / 暂缓（缺原件）`；只表示代码是否按确认结论落地。
- 验证状态：`专项通过+统一回归通过 / 字段专项通过+统一回归通过 / 字段专项通过 / 字段专项待做 / 统一回归待做 / 未验证 / 暂缓（缺原件）`；数字回归通过不得替代字段专项通过。

事件触发矩阵：

| 事件 | 必须更新 | 条件更新 |
| --- | --- | --- |
| 完成字段确认 | 人工确认工作簿；续接记录顶部快照 | 规划仅在阶段完成度变化时更新 |
| 完成解析实现 | 人工确认工作簿；银行适配手册；技术变更记录 | `PROJECT_STATUS.md` 仅重大能力里程碑更新；续接记录更新当前入口 |
| 完成专项验证 | 人工确认工作簿；技术变更记录 | 续接记录更新当前快照；失败暴露规则冲突时同步银行适配手册 |
| 完成统一回归 | 人工确认工作簿；技术变更记录；续接记录顶部快照 | `PROJECT_STATUS.md` 仅验证基线或里程碑变化时更新；规划仅阶段完成时更新 |
| 发现版式冲突 | 人工确认工作簿把决策改为部分确认或待确认；续接记录写明阻塞入口 | 若当前代码规则已不准确则更新银行适配手册；产生代码修改后更新技术变更记录 |
| 暂缓缺原件 | 人工确认工作簿三类状态分别记为暂缓；续接记录写明缺件和禁止替代 | 规划仅影响阶段范围或完成度时更新 |

执行顺序：先更新工作簿状态总账，再写技术或规则事实，最后刷新续接记录顶部快照；不要以旧历史章节作为默认入口。

## 运行注意

- `启动GUI.bat` 通过 `python gui_v2.py` 启动源码 GUI。
- `dist\BankFlowGUI\BankFlowGUI.exe` 不一定代表最新源码。
- 当前不稳定支持扫描图片 PDF OCR。
