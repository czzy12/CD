# PDF银行流水识别项目上下文

## 当前目标

重构为 v2 高准确度核心，优先保证统计字段准确，不追求提取流水中的所有字段。

核心字段：

- 交易时间
- 收入金额
- 支出金额
- 余额

辅助字段：

- 银行
- 页码
- 行号
- 原始时间文本
- 原始金额文本
- 原始余额文本
- 校验状态
- 异常原因

## 架构方向

不要使用大模型一次性从 PDF 生成最终表格。采用：

1. PDF 文本/表格提取
2. 银行识别
3. 银行适配器解析候选交易
4. 标准化为统一字段
5. 用余额连续性校验金额和余额
6. 异常行隔离
7. 生成明细和统计结果

## 统计口径

期初余额不能直接取第一笔交易记录的余额，因为流水中的余额通常是“该笔交易后余额”。

统一计算：

```text
期初余额 = 第一笔交易后余额 - 第一笔收入 + 第一笔支出
期末余额 = 最后一笔交易后余额
余额变动 = 期末余额 - 期初余额
净额 = 收入合计 - 支出合计
```

校验要求：

```text
余额变动 = 净额
```

例：工商对公样本第一笔为支出 2055.00，交易后余额 421682.47，则期初余额为 421682.47 + 2055.00 = 423737.47。

## v2 当前进度

已新增 `bankflow_v2/` 和 `tools/gold_check.py`。

`bankflow_v2` 当前只实现工商银行解析器：

- 保留完整交易时间
- 按 PDF 原始顺序处理
- 对金额和余额生成候选值
- 使用公式 `上一笔余额 + 收入 - 支出 = 当前余额` 选择正确候选

新增建设银行 v2 解析器：

- 文件：`bankflow_v2/ccb.py`
- 样本：`建设.pdf`
- 表格列：交易日期、交易金额、账户余额
- 当前烟测结果：222 笔，收入 51 笔 746916.97，支出 171 笔 763498.86，期初余额 24305.05，期末余额 3723.16
- 余额连续性校验：222 笔全部 `ok`

同时修正 `bankflow_v2/number_parser.py`：余额候选允许 `0.00`，用于处理账户余额归零的交易。

新增农业银行 v2 解析器：

- 文件：`bankflow_v2/abc.py`
- 样本：`D:\Codex data\PDF流水\农行.pdf`
- 人工确认 Excel：`D:\Codex data\PDF流水\农行.xlsx`
- 兼容交易行有时间和无时间两种格式，例如 `20251221 结息 +0.24 63.51`
- 当前黄金对账结果：85 笔，收入 22 笔 961610.32，支出 61 笔 961513.36，净额 96.96
- 期初余额 39975.08，期末余额 112.00
- 余额连续性校验：85 笔全部 `ok`

新增农业银行对公 v2 解析器：

- 文件：`bankflow_v2/abc_corp.py`
- 样本：`C:\Users\lenovo\Desktop\练习\游相位\农业公户.pdf`
- PDF 为无表格文本结构，表头显示收入金额/支出金额/账户余额，但 pdfplumber 文本会吞掉空金额列。
- 该格式还存在日期拆行，例如 `2026-03-` 与下一行 `04 16:54:25` 组合为完整时间。
- 解析策略：按完整/半截日期分块，用余额连续性判断单个金额是收入还是支出；首笔或断点用摘要关键词 `转存/转取/费用/公共缴费/扣税` 辅助判断。
- 当前烟测结果：371 笔，收入 35 笔 9160532.81，支出 336 笔 8571888.47，净额 588644.34。
- 期初余额 108.43，期末余额 588753.77。
- 校验状态：371 笔全部 `ok`。
- 尚未提供人工确认 Excel，后续需要黄金对账确认统计口径。

新增建设银行对公 v2 解析器：

- 文件：`bankflow_v2/ccb_corp.py`
- 校验工具：`tools/corp_merge_check.py`
- 样本目录：`D:\Codex data\PDF流水\对公需合并`
- PDF：
  - `25.10月-12月流水.pdf`
  - `26.1月-3月流水.pdf`
  - `26.3月-5月.pdf`
- 人工确认 Excel：`D:\Codex data\PDF流水\对公需合并\合计.xlsx`
- 该样本需要合并多份 PDF，且 `26.1月-3月流水.pdf` 与 `26.3月-5月.pdf` 在 3 月存在重叠。
- 合并策略：先按人工表月份范围过滤，再按账户明细编号/交易流水号去重。
- 当前黄金对账结果：1511 笔，借方 1378 笔 14356908.22，贷方 119 笔 14355796.38，净额 -1111.84。
- 期初余额 33086.91，期末余额 31979.57。
- 校验状态：1511 笔全部 `ok`。

复跑命令：

```powershell
python tools\corp_merge_check.py --dir "D:\Codex data\PDF流水\对公需合并"
```

新增邮储银行 v2 解析器：

- 文件：`bankflow_v2/psbc.py`
- 样本：`D:\code\Codex data\流水核对\邮储银行历史明细（20251112-20260512）.pdf`
- 人工确认 Excel：`D:\code\Codex data\流水核对\邮储银行历史明细（20251112-20260512）.xlsx`
- 表格列：交易时间、交易金额、交易余额
- 黄金对账结果：94 笔，收入 24 笔 183961.86，支出 70 笔 162085.04，净额 21876.82
- 余额连续性校验：94 笔全部 `ok`

新增民生银行对公 v2 解析器：

- 文件：`bankflow_v2/cmbc_corp.py`
- 样本：`D:\code\Codex data\流水核对\民生公户.pdf`
- 人工确认 Excel：`D:\code\Codex data\流水核对\民生公户.xlsx`
- PDF 为无表格文本结构，交易日期和时间分两行；金额为借/贷分列
- 黄金对账结果：89 笔，收入 21 笔 2167655.50，支出 68 笔 2129397.82，净额 38257.68
- 余额连续性校验：89 笔全部 `ok`

新增招商银行 v2 解析器：

- 文件：`bankflow_v2/cmb.py`
- 样本：`D:\code\Codex data\流水核对\招商银行交易流水(申请时间2026年05月07日18时57分03秒).pdf`
- 人工确认 Excel：`D:\code\Codex data\流水核对\招商银行交易流水(申请时间2026年05月07日18时57分03秒).xlsx`
- PDF 无表格框，使用文本行正则解析
- 该样本 Excel 自身存在无框表格导出问题：部分金额/余额丢小数点，部分多笔交易被压缩到同一行
- `tools/gold_check.py` 已增强：自动定位表头、兼容列偏移、展开压缩多笔行，并用余额连续性修正黄金 Excel 中的明显小数点缺失
- 黄金对账结果：489 笔，收入 272 笔 5179378.90，支出 217 笔 5179283.11，净额 95.79
- 余额连续性校验：489 笔全部 `ok`

## 第一份黄金样本

样本：

- PDF：`陈洁银行卡.pdf`
- 人工确认 Excel：`陈洁银行卡.xlsx`

Excel 为人工手动调整/确认后的黄金样本，不是 PDF 原始导出数据。

当前本地位置：

- `D:\Codex data\PDF流水\陈洁银行卡.xlsx`

该 Excel 中 `Sheet1` 为人工确认明细，字段为年、月份、交易日期、收入/支出金额、收支；没有余额列。`Sheet6` 为透视统计表。

验收标准：

- 总笔数：61
- 收入：18 笔，18794.20
- 支出：43 笔，18248.63
- 净额：545.57
- 期初余额：1162.18
- 期末余额：1717.75

本地已通过：

```powershell
python tools\gold_check.py
```

输出结果为 `PASS`。

注意：当前 GitHub 仓库中包含 `陈洁银行卡.pdf`，但未包含 `陈洁银行卡.xlsx`。本机可用 `D:\Codex data\PDF流水\陈洁银行卡.xlsx` 复现黄金样本 PASS。`tools/gold_check.py` 已兼容这份人工确认 Excel：当黄金样本没有余额列时，只比较日期、金额、收入、支出和统计汇总，余额由解析结果自身的连续性校验保证。

## 下一步建议

1. 将第二份黄金样本加入项目，建议优先建设；建设银行 v2 解析器已有烟测结果，但仍需要人工确认 Excel 做黄金对账。
2. 每个银行都准备一组 PDF + 人工确认 Excel。
3. 新银行适配后必须通过黄金样本对账。
4. 核心稳定后再整合 GUI 和最终 Excel 输出。

## 新增待确认样本

本地目录：`D:\Codex data\PDF流水`

- `建行.pdf`：曾命名为 `农行1.pdf`，但 PDF 文本实际为“中国建设银行个人活期账户全部交易明细”。
  - 使用 v2 `ccb` 解析器烟测通过。
  - 人工确认 Excel：`D:\Codex data\PDF流水\建行.xlsx`
  - 黄金对账结果：227 笔，收入 23 笔 56259.77，支出 204 笔 50945.76，净额 5314.01。
  - PDF 首页合计为总收入 56259.77、总支出 50945.76，已对齐。
  - 余额连续性校验：227 笔全部 `ok`。
- `农行.pdf`：PDF 文本为“中国农业银行账户活期交易明细清单”。
  - 使用 v2 `abc` 解析器黄金对账通过。
  - 人工确认 Excel：`D:\Codex data\PDF流水\农行.xlsx`
  - 黄金对账结果：85 笔，收入 22 笔 961610.32，支出 61 笔 961513.36，净额 96.96。
  - 余额连续性校验：85 笔全部 `ok`。

注意：人工确认 Excel 的透视表下方余额、期初余额、期末余额等，是人工拉取用于核实的辅助值，不是流水自带字段。黄金对账可以用这些余额核实解析连续性，但不要把它们当作 PDF 原始流水字段来源。

## GUI 第一版进度（2026-05-16）

- 新增 `gui_v2.py`，使用 PyQt6 做第一版桌面界面。
- 支持选择多个 PDF、选择文件夹、拖入 PDF/文件夹。
- 自动识别银行格式后调用 `bankflow_v2.pipeline.extract_transactions`。
- 界面分为：文件汇总、月度统计、流水明细、异常提示。
- 表格支持选中单元格后按 `Ctrl+C` 复制，复制内容为制表符分隔文本，可直接粘贴到 Excel 或其他软件。
- 月度统计最后追加 `总计` 行，使用合并去重后的全部流水按统一口径汇总。
- Excel 导出包含：`汇总`、`月度统计`、`明细`、`异常提示` 四个 sheet。
- 统计口径沿用当前确认规则：
  - 收入笔数：`金额 >= 0`
  - 支出笔数：`支出 > 0`
  - 期初余额：第一笔交易后余额 - 第一笔收入 + 第一笔支出
  - 期末余额：最后一笔交易后余额
- 数据可能有问题时会标记 `需复核`，当前包括：
  - 银行格式未识别
  - PDF 读取/解析失败
  - 没有解析到流水
  - 金额或余额缺失
  - 解析器返回 review 状态
  - 按时间排序后的余额连续性断点
- 多 PDF 合并明细会做疑似重复流水去重；重复项会进入 `异常提示`，说明首次来源。
- 注意：第一版 GUI 目前只做重复流水去重，还没有做“按每份 PDF 的有效月份范围裁剪”。例如建设对公“需合并”样本此前黄金对账按人工确认表限定月份范围，若要 GUI 完全自动对齐该类人工合并表，后续需要增加月份范围设置或从文件名识别范围。
- `启动GUI.bat` 已改为启动 `gui_v2.py`。
- 计划后续用 PyInstaller 打包为可在其他 PC 运行的 exe。
- 第一版 exe 已用 PyInstaller 构建为文件夹版：
  - exe：`D:\Codex data\CD\dist\BankFlowGUI\BankFlowGUI.exe`
  - 注意：复制到其他电脑时需要带上整个 `BankFlowGUI` 文件夹，不能只复制单个 exe，因为依赖在 `_internal` 目录。
  - spec：`BankFlowGUI.spec`
  - 构建命令：`python -m PyInstaller --noconfirm --clean --windowed --name BankFlowGUI gui_v2.py`

新增工商银行对公 v2 解析器：

- 文件：`bankflow_v2/icbc_corp.py`
- 已接入 `bankflow_v2/pipeline.py`，bank id 为 `icbc_corp`。
- 覆盖两种工商对公表头：
  - `凭证号/对方账号/交易时间/借贷标志/发生额/余额`
  - `交易时间/本方账号/.../借/贷/借方发生额/贷方发生额/余额`
- 样本目录：`D:\Codex data\PDF流水\工商对公`
- 当前烟测结果与人工核算一致：
  - 总笔数：262
  - 收入：29 笔，5,228,312.64
  - 支出：233 笔，5,480,637.04
  - 净额：-252,324.40
  - 期初余额：423,737.47
  - 期末余额：171,413.07
  - 异常提示：0

新增自动识别模块：

- 文件：`bankflow_v2/auto_detect.py`
- 当前已烟测识别：
  - 工商对公：`icbc_corp`
  - 交通银行：`bocom`
  - 邮储银行：`psbc`
  - 招商银行：`cmb`
  - 农业银行个人：`abc`
  - 农业银行对公：`abc_corp`

新增汇总/异常模块：

- 文件：`bankflow_v2/summary.py`
- 提供 `summarize`、`monthly_summaries`、`collect_issues`、`money`。
- GUI 和导出 Excel 均使用该模块，避免界面和命令行统计口径分叉。

烟测命令：

```powershell
python -m py_compile bankflow_v2\icbc_corp.py bankflow_v2\auto_detect.py bankflow_v2\summary.py gui_v2.py
```

已通过。另用工商对公样本生成过 `gui_v2_smoke.xlsx` 验证导出函数可用，该文件只是临时烟测产物，不需要纳入 Git。

## 注意

旧版 `main.py` 和 `gui_app.py` 仍引用 `output.aggregator`，但当前仓库没有该文件，因此旧版 CLI/GUI 可能无法直接运行。后续应优先推进 v2 核心，再回头接 GUI。

## 新增待处理：甘肃银行图片型电子对账单（2026-05-17）

- 样本：`D:\Codex data\PDF流水\甘肃银行电子对账单.pdf`
- PDF 共 8 页，`pdfplumber` 抽取结果为 `text=0`、`chars=0`，每页约 20 个图片对象；属于扫描/图片型 PDF，没有可抽取文字层。
- 首页视觉确认字段包括：交易时间、交易金额、金额、对方账号/户名、对方户名、收入/支出、用途、附言；表头显示笔数 370。
- 已确认直接 OCR 图片 PDF 的准确率不足：曾用 RapidOCR PyTorch 后端抽取 345/370 笔，余额连续性仍有大量异常，因此不纳入稳定版 GUI。
- 稳定策略改为：用户可先用其他软件将图片 PDF 转成 Excel，再用本 APP 导入 Excel 做统一统计/导出。
- 已新增 Excel 输入：
  - `bankflow_v2/excel_input.py`：识别常见列名（交易时间/交易日期、交易金额、收入/支出、余额等），转换为统一 `Transaction`
  - `gui_v2.py`：选择文件/拖拽/选择文件夹均支持 `.pdf`、`.xlsx`、`.xlsm`
- GUI 新增日期筛选：默认启用“近半年”（当前日期往前 6 个月至今天），用户可手动修改起止日期；取消勾选则全量输出。

## GUI 输入行为和微信流水（2026-05-17）

- GUI 导入行为已调整：每次点击“选择文件”“选择文件夹”或拖入文件/文件夹，都会替换当前待处理列表和上次结果，不再累加旧文件。
- 默认日期范围口径已改为自然月近 6 个月：当前月 + 前 5 个完整月。例如 2026-05-17 默认 `2025-12-01` 到 `2026-05-31`。
- 新增微信流水解析器：
  - 文件：`bankflow_v2/wechat.py`
  - 已接入 `bankflow_v2/auto_detect.py` 和 `bankflow_v2/pipeline.py`
  - 样本：`C:\Users\lenovo\Desktop\微信流水.pdf`
  - 识别关键词：`微信支付交易明细证明`
  - 微信流水没有余额列，解析器会标记 `balance_optional`，不把余额缺失作为异常。
- 微信 `收/支/其他` 中的 `其他` 不再作为中性项；默认也归入收入/支出统计。当前规则：`其他` 中含零钱通转出/退款/收益等按收入，否则按支出。
- 当前样本烟测：455 笔，收入 77 笔 423,023.61，支出 378 笔 547,312.18，净额 -124,288.57，异常 0。
- 未适配 PDF/Excel 不会中断批量处理；GUI 会保留该文件在汇总里，状态为 `需复核`，说明栏提示未适配/未识别原因，其他已适配文件正常统计。
- GUI 汇总展示调整：
  - `月度统计` 和 `文件汇总` 保持分开，页签顺序为：`月度统计`、`文件汇总`、`流水明细`、`异常提示`。
  - `月度统计` 中的收入、支出、净额、期初余额、期末余额统一显示/导出为 `万元`，列名标注 `(万元)`，保留两位小数。
  - `文件汇总` 和 `流水明细` 仍保留元单位，方便核对原始明细。
  - 拖拽导入保留，并增加主窗口级 DropWidget 接收拖拽；每次点击选择文件/文件夹或拖入文件/文件夹，都会替换当前待处理列表和上次结果，不再累加。

## 本地/网盘资料同步规则（2026-05-17）

- 源码仓库固定使用：`D:\Codex data\CD`。
- 本地/网盘资料统一入口固定使用：`D:\Codex data\CD_assets`。
- `CD_assets` 用于保存不适合进入 GitHub 的资料，包括：
  - `PDF流水`：原始 PDF 流水样本。
  - `Excel流水`：图片 PDF 转出的 Excel、人工整理表、第三方软件转换结果。
  - `打包成品`：exe、zip、发布包。
  - `项目备份`：重要版本源码 zip、交接备份。
  - `测试输出`：临时导出的 Excel、截图、对账结果。
- GitHub 只同步源码、配置和说明文档；不要再把 PDF、Excel、docx、exe、zip、build/dist 产物提交到源码仓库。
- 每次把源码或清理提交推送到 GitHub 后，需要主动提醒用户同步或备份 `D:\Codex data\CD_assets`，这样更换 PC 时可以继续项目。
- 如果用户以后把 `CD_assets` 放到 OneDrive，优先保持 `D:\Codex data\CD_assets` 作为入口路径不变，避免多台电脑路径混乱。

## 通用 PDF 兜底解析与新增样本（2026-05-20）

- 新增通用 PDF 解析兜底：未识别、专用解析失败、或专用解析得到 0 条流水时，GUI 会尝试按“交易时间 + 交易金额 + 账户余额”的通用文本行结构解析。
- 新增识别标签：
  - `cmbc`：民生银行个人。
  - `cib`：兴业银行。
- 银行识别规则调整：招商银行不再用全文任意出现的“招商银行”直接判定，避免把交易对方行名误判为开户行。
- 通用解析成功时，GUI 状态显示为 `通用识别`；如余额断点或字段异常，仍显示 `需复核`。
- 本地黄金样本不进 GitHub，固定保存在 `D:\Codex data\CD_assets\PDF流水`：
  - `詹兰民生.pdf` / `詹兰民生.xlsx`
    - 识别：民生银行个人。
    - 明细：130 笔。
    - 收入：26 笔，766,712.11。
    - 支出：104 笔，411,862.88。
    - 净额：354,849.23。
    - 期初余额：3,017.56。
    - 期末余额：357,866.79。
    - 余额断点：0。
    - 与人工 Excel 全量核对一致；人工表部分时间秒数或具体时间可忽略，核算以日期、金额、余额为准。
  - `兴业银行1.pdf` / `兴业银行1.xlsx`
    - 当前 PDF 文字层被水印/竖排字严重拆散，普通通用文本行解析无法直接抽出明细。
    - 人工 Excel 可作为兴业适配黄金样本。
    - 明细：378 笔。
    - 收入：74 笔，1,121,478.99。
    - 支出：304 笔，1,140,536.30。
    - 净额：-19,057.31。
    - 期初余额：76,360.23。
    - 期末余额：57,302.92。
    - 余额断点：0。
    - 与 PDF 首页汇总区收入/支出/净额一致。
## 批量验证与工行个人历史明细适配（2026-05-21）

- 当前新增测试资料目录：`D:\Codex data\CD_assets\PDF流水\打包测试`，共约 109 份 PDF，测试输出统一放在 `D:\Codex data\CD_assets\测试输出`，不纳入 Git。
- 批量验证工具新增：`tools/batch_validate.py`。
  - 递归扫描 PDF，调用 `auto_detect.detect_bank_type` 与 `pipeline.extract_transactions`。
  - 未识别/专用解析失败时尝试 `generic_pdf` 通用兜底。
  - 输出中文 Excel 报告，包含 `概览`、`汇总`、`异常明细`。
  - 支持 `--ignore-parse-errors` 将加密/密码 PDF 标为 `忽略-加密PDF`。
  - 支持 `--manual-pass` 将人工核对通过文件标为 `已人工核对通过`。
  - 图片/扫描型 PDF 统一标记 `忽略-图片PDF`，当前不做 OCR。
- 最新批量报告：`D:\Codex data\CD_assets\测试输出\打包测试_批量验证_中文_工行v1.xlsx`。
  - 生成命令：
    ```powershell
    python tools\batch_validate.py "D:\Codex data\CD_assets\PDF流水\打包测试" --out "D:\Codex data\CD_assets\测试输出\打包测试_批量验证_中文_工行v1.xlsx" --ignore-parse-errors --manual-pass "中国银行5.pdf" --manual-pass "工商银行历史明细（申请单号：26051516345654037193）.pdf"
    ```
  - 当时报表状态：`通过` 51、`未识别银行` 24、`需要复核` 15、`忽略-加密PDF` 6、`未抽取到流水` 5、`忽略-图片PDF` 5、`已人工核对通过` 2、`通用解析需复核` 1。
- 核对口径确认：
  - `民生公户个人.pdf` 收入差异来自两笔 `结息`，分别为 `0.58` 与 `2.08`；用户确认结息应保留为收入，程序口径正确。
  - `淮坊银行.pdf` 这类三金额列通用格式，期初应由第一笔交易倒推：第一笔收入 190000、余额 190000，则期初为 0。用户确认 01-12 当天实际顺序是收入 190000、支出 190000、收入 190000、支出 190000。
  - `中国银行5.pdf` 通用解析已人工核对无误，人工核对表在 `C:\Users\lenovo\Desktop\流水测试\打包测试\已核对`。
- 通用 PDF 兜底更新：`bankflow_v2/generic_pdf.py`。
  - 新增支持 `日期 时间 收入 支出 余额` 三金额列结构。
  - 修复淮坊/潍坊类样本期初误取第一笔余额的问题。
- 工行个人历史明细适配进展：
  - `bankflow_v2/auto_detect.py` 已新增工行个人历史明细识别规则，关键词包括 `中国工商银行借记账户历史明细` 以及表头 `交易日期账号储种序号币种钞汇摘要地区收入/支出金额余额`。
  - 工行样本视觉表格规律稳定，但 PDF 文本层会被斜向水印数字、二维码和红章干扰；当前策略是基于文本表格列位置与余额连续性做候选金额选择，不做 OCR。
  - `bankflow_v2/number_parser.py` 已强化脏金额解析：
    - 清理金额中的非金额字符。
    - 支持从异常逗号金额生成候选，例如 `-15,2000.00` 可候选为 `-15000.00`。
    - 支持余额前缀/后缀缺失时结合连续性选择。
    - 通过 `choose_amount_and_balance` 在金额候选、余额候选、上笔余额之间做连续性匹配。
- 工行黄金样本：
  - PDF：`D:\Codex data\CD_assets\PDF流水\打包测试\工商银行历史明细（申请单号：26051516345654037193）.pdf`
  - 人工核对 Excel：同名 `.xlsx`，`Sheet1` 表头为 `交易日期`、`收入/支出金额`、`余额`、`收支`。
  - 当前程序解析与人工表完全一致：
    - 明细 1148 笔。
    - 收入 78 笔，金额 `1,616,841.29`。
    - 支出 1070 笔，金额 `1,571,813.04`。
    - 余额连续异常 0。
    - 与人工表逐笔差异 0。
- 工行批量样本现状：
  - 打包测试中约 15 个工行历史明细 PDF，其中 5 个为密码/加密文件，按用户要求忽略并剔除测试。
  - 可读工行样本中，部分已余额连续异常为 0；仍有少数样本存在若干连续性问题，需要继续查看具体异常行再调规则。
  - 后续顺序：先继续收敛工行，再处理华夏银行、浦发银行；其他未识别银行先标记“暂不处理”。
- 源码当前未提交到 Git。已变更/新增文件：
  - `bankflow_v2/auto_detect.py`
  - `bankflow_v2/generic_pdf.py`
  - `bankflow_v2/number_parser.py`
  - `tools/batch_validate.py`
  - `PROJECT_CONTEXT.md`

## 工行个人历史明细回归通过（2026-05-22）

- 当前分支：`work/2026-05-21-batch-icbc`。
- 在 2026-05-21 提交 `d516238 Add batch validation and ICBC parsing updates` 基础上继续收敛工行。
- 新增工行专项回归脚本：`tools/icbc_regression.py`。
  - 仅扫描 `D:\Codex data\CD_assets\PDF流水\打包测试` 中 `*工商银行历史明细*.pdf`。
  - 输出每份文件的解析笔数、收入笔数/金额、支出笔数/金额、余额连续异常数。
  - 对检测为空且读取失败的文件标记为 `IGNORED`，用于按用户口径忽略密码/加密 PDF。
- 本轮发现剩余异常集中在工行 PDF 水印数字混入余额小数位：
  - 例如 `20A200,429.019` 应结合连续性候选为 `200,429.09`，旧逻辑会优先取 `200,429.01`。
  - `200127,490.761` 应候选为 `127,490.71`，旧逻辑会取 `127,490.76`。
- 已修复 `bankflow_v2/number_parser.py`：
  - `balance_candidates` 现在也支持三位小数噪声候选。
  - 对 `xxx.abc` 同时生成 `xxx.ab` 与 `xxx.ac` 两类候选，再由余额连续性选择正确值。
- 工行专项回归最新结果：
  - 工行 PDF 总数：15。
  - 密码/读取失败忽略：5。
  - 可读工行样本：10。
  - 可读样本通过：10。
  - 需复核：0。
  - 失败：0。
- 结论：按当前“密码 PDF 忽略、非 OCR 图片 PDF 不处理”的口径，工行个人历史明细本轮测试已通过。后续可进入华夏银行适配。

## 浦发银行个人/对公初版适配（2026-05-22）

- 工行已先行确定：新增 `新工行.pdf` 与 `新工行2.pdf` 验证通过。
  - `新工行.pdf`：工行个人，1406 笔，收入 384 笔 `414,804.81`，支出 1022 笔 `420,547.89`，期初 `8,786.09`，期末 `3,043.01`，异常 0。
  - `新工行2.pdf`：工行对公，406 笔，收入 188 笔 `34,155,752.51`，支出 218 笔 `33,766,329.80`，期初 `41,864.42`，期末 `431,287.13`，异常 0。
- 浦发样式分两类：
  - 个人：`上海浦东发展银行个人客户交易流水专用回单`，英文标题 `Transaction Statement of Shanghai Pudong Development Bank`，表格列为交易日期、交易时间、交易金额、账户余额等。
  - 对公：`上海浦东发展银行电子对账单`，英文标题 `Shanghai Pudong Development Bank Electronic Statement`，表格列为借方、贷方、账户余额；借方按支出，贷方按收入。
- 新增解析器：`bankflow_v2/spdb.py`。
  - `extract_spdb` 处理浦发个人专用回单。
  - `extract_spdb_corp` 处理浦发对公电子对账单。
  - 已接入 `bankflow_v2/auto_detect.py` 与 `bankflow_v2/pipeline.py`，bank_id 分别为 `spdb`、`spdb_corp`。
- 新增专项回归脚本：`tools/spdb_regression.py`。
- 当前 `D:\Codex data\CD_assets\PDF流水\打包测试` 中浦发样本回归结果：
  - `浦发 (2).pdf`：`spdb`，26 笔，收入 17 笔 `279,160.94`，支出 9 笔 `238,276.18`，期初 `96,621.04`，期末 `137,505.80`，异常 0。
  - `浦发.pdf`：`spdb_corp`，457 笔，收入 81 笔 `17,541,811.36`，支出 376 笔 `19,156,906.20`，期初 `1,618,027.32`，期末 `2,932.48`，异常 0。该结果与对公样本首页汇总借方/贷方/期末一致。
  - `浦发2.pdf`：`spdb`，94 笔，收入 34 笔 `815,728.96`，支出 60 笔 `652,104.46`，期初 `89,905.56`，期末 `253,530.06`，异常 0。
  - `浦发3.pdf`：`spdb`，26 笔，收入 17 笔 `279,160.94`，支出 9 笔 `238,276.18`，期初 `96,621.04`，期末 `137,505.80`，异常 0。
- 结论：现有浦发 4 份样本初版解析通过；后续如果新增对公版式差异较大的浦发 PDF，需要继续单独跑回归确认。
