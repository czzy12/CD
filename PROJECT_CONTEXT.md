# PDF银行流水识别项目上下文

## 当前目标

重构为 v2 高准确度核心，优先保证统计字段准确，不追求提取流水中的所有字段。

核心字段：

- 交易时间
- 收入金额
- 支出金额
- 余额

辅助字段：

- 银行
- 页码
- 行号
- 原始时间文本
- 原始金额文本
- 原始余额文本
- 校验状态
- 异常原因

## 架构方向

不要使用大模型一次性从 PDF 生成最终表格。采用：

1. PDF 文本/表格提取
2. 银行识别
3. 银行适配器解析候选交易
4. 标准化为统一字段
5. 用余额连续性校验金额和余额
6. 异常行隔离
7. 生成明细和统计结果

## v2 当前进度

已新增 `bankflow_v2/` 和 `tools/gold_check.py`。

`bankflow_v2` 当前只实现工商银行解析器：

- 保留完整交易时间
- 按 PDF 原始顺序处理
- 对金额和余额生成候选值
- 使用公式 `上一笔余额 + 收入 - 支出 = 当前余额` 选择正确候选

## 第一份黄金样本

样本：

- PDF：`陈洁银行卡.pdf`
- 人工确认 Excel：`陈洁银行卡.xlsx`

Excel 中 `Sheet1` 为人工确认明细，`Sheet2` 透视表为准确统计。

验收标准：

- 总笔数：61
- 收入：18 笔，18794.20
- 支出：43 笔，18248.63
- 净额：545.57
- 期初余额：1162.18
- 期末余额：1717.75

本地已通过：

```powershell
python tools\gold_check.py
```

输出结果为 `PASS`。

## 下一步建议

1. 将第二份黄金样本加入项目，建议优先招商或建设。
2. 每个银行都准备一组 PDF + 人工确认 Excel。
3. 新银行适配后必须通过黄金样本对账。
4. 核心稳定后再整合 GUI 和最终 Excel 输出。

## 注意

旧版 `main.py` 和 `gui_app.py` 仍引用 `output.aggregator`，但当前仓库没有该文件，因此旧版 CLI/GUI 可能无法直接运行。后续应优先推进 v2 核心，再回头接 GUI。
