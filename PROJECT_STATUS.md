# PDF流水项目当前状态

更新时间：2026-08-07

## 当前仓库

当前隔离开发仓库：

```text
D:\Investigator PDF\CD-bankflow-refactor
```

当前分支：

```text
work/deepseek-12b2-followup
```

远端跟踪：

```text
origin/work/2026-07-18-bankflow-verification
```

已提交的当前实现：

```text
HEAD 以 git log --oneline -1 为准；chg-20260807-14 起为经营语义知识库架构 v1 收口；
chg-19（C2.1 契约收口）与 chg-20（Gate D 真实 AI validation）为本轮本地新增提交
```

当前分支为 `work/deepseek-12b2-followup`；`5fb4685`（chg-14）起经 Gate A、Foundation Remediation（chg-17）、
Gate B.1/C1/C2（chg-18）与续接记录已全部推送至 `origin/work/2026-07-18-bankflow-verification`
（远端 HEAD 以 `git rev-parse origin/work/2026-07-18-bankflow-verification` 为准）。
`backup/codex-24a9d96` 仍指向 `24a9d96`。工作区唯一未跟踪文件为仓库根目录 `CD-bankflow-refactor.bundle`（保留原样）。
本轮本地新增 `c773a3d`（C2.1）与 Gate D 代码/文档提交，均未 push。

AI 经营关联验收链：2026-07-28 曾在检查点 `68e0ac8` 完成 v11 验收；当前代码在其后有 `6e149d6/7b08441` 经营上下文准入改动，旧缓存离线重放 286 项全部未命中，不能沿用。2026-08-07 已用当前代码完成整条验收链（`chg-20260807-05`）：development 50 离线重放、reserved 50 真实调用 + 离线重放、286 语义 / 1115 笔完整验收 + 离线重放 + 输入审计全部通过；产物在 `outputs/mvp-acceptance-2026-08-07/`（仓库外）。真实调用在验收进程内显式授权，产品 AI 默认关闭语义未改变。

Foundation 状态（2026-08-07，`chg-20260807-17`）：Foundation Remediation 已完成，结论 GREEN（G1-G8 全部满足）。华夏 6 列回归恢复（57 笔）、对公 19 文件 4,316 笔 raw evidence 补齐、negative income/expense 与 double nonzero 全库 0、skipped/unparsed 具备 source diagnostics 可观测性、metadata 缺失显式 unavailable、无新 Critical/High regression、transaction_id 稳定性验证通过（4,316 笔 ID 因 raw evidence 修复变化但无已发布引用，其余 77,316 笔不变）、核心 downstream 测试全部通过。全量回归：81,746 笔（CD_assets 61,740 + MVP-input 20,006）、172 同 sha256 旧基线对比 0 regression。剩余 MEDIUM/LOW 发现（abc/generic raw_fields partial、兴业水印、45 笔 0 金额、ccb_corp 0530 unexplained 等）不阻塞 GREEN，单独记录。

Schema 1.17 状态（2026-08-07，`chg-20260807-18`）：Gate B.1 冻结 + Gate C1 契约修订 + Gate C2 shadow writer 修订完成。
`business_semantics_resolutions` 使用 deterministic resolution_id、内容可审计 relation_id（relevance 变化即变化）、
`value.migration_status` 正式落位（parsed / not_parsed）、concept/relation resolution source 由 resolver 直接输出、
`review_status ∈ {approved, unresolved, candidate}` + 可选 `candidate_ref`、per-entry industry profile 支持；
326 legacy replay（per-entry）：agreement 70.55%、mismatch 96、undetermined 40、escalation 36、life_positive 0、AI fallback 40；
legacy production result 在 1.16/1.17 下不变；production_resolver 仍为 legacy_v11，knowledge_v1 仍为 shadow。
Python 全量 462 项通过（2 SKIP）；统一回归 71/0/20/1；compileall 与 git diff --check 通过；未 push。

C2.1 契约收口状态（2026-08-07，`chg-20260807-19`）：unresolved 最小 resolution 正式落位
（`migration_status=not_parsed + resolutions=[]` 表示从未运行；`parsed + review_status=unresolved`
表示已运行但本地无法确定；支持 Concept unresolved 与 Concept resolved + Relation unresolved）；
96 mismatch 全量分类 = knowledge_undetermined 40 + strength_escalation 36 + strength_downgrade 20
（此前“剩余 20 条”即 downgrade）+ 其余 0，无 unexplained 桶；relation_id 冻结为
Canonical Relation Snapshot Identity（长期逻辑键为 industry_id + concept_id，relevance/review_status
变化产生新 ID）；Python 全量 472 项、统一回归 71/0/20/1 通过；已本地 commit（c773a3d），未 push。

Gate D 真实 AI Fallback Validation 状态（2026-08-07，`chg-20260807-20`）：新增 privacy preflight、
PII guard、`tools/run_knowledge_ai_validation.py`（--dry-run / --confirm-real-data）与
`tools/knowledge/freeze_unseen_manifest.py`；真实调用 3 次全部成功（57 条可发送：
40 主集 + 20 unseen - 1 重叠 - 2 privacy blocked），产生 57 个 concept 候选（55 existing + 2 new
property_management）与 2 个 relation 候选，全部 pending；0 失败/0 重试/0 敏感外发/
0 自动 approve；approved cache 0 写入；审核包在
`D:\Investigator PDF\outputs\knowledge-v1\ai-validation-20260807\`（仓库外）；
Python 全量 487 项、统一回归 71/0/20/1 通过；已本地 commit，未 push。

Gate D.1A Privacy Block Audit 状态（2026-08-07，`chg-20260807-21`）：2 条 blocked case 审计为
false_positive（商户小票 product_description 中 16 位 Luhn 失败订单/流水号）；PII guard 增加
typed business field + Luhn 失败 + 非纯数字上下文 + 无卡号提示词的保守例外，同时修复数字串紧贴
中文时 `\b` 漏检缺陷；Luhn 有效卡号永远阻断、裸数字永远阻断、自由文本字段严格阻断；
审计汇总 true_positive=0 / false_positive=2 / ambiguous=0 / safely_remediated=2 /
unauthorized_sensitive_outbound=0；本轮未重新调用 AI；Python 全量 492 项通过；已本地 commit，未 push。

Gate D.1B Human Review Package 状态（2026-08-07，`chg-20260807-22`）：59 条真实 AI candidate
（57 concept + 2 relation）已进入独立人工审核队列，与 12 条 legacy relation pending 完全隔离；
审核包含 review_summary、concept/relation review queue、candidate_review_sheet.md、
空 decisions（reviewed_by=human）、quality metrics 结构（not_yet_computable）；
无任何自动裁决；审核包在
`D:\Investigator PDF\outputs\knowledge-v1\candidate-human-review-20260807\`（仓库外）；
已本地 commit，未 push。

AI 运行配置已持久化到 `%LOCALAPPDATA%\BankFlowReview\ai_runtime.json`（DPAPI 加密，不进仓库），新增 `tools/save_deepseek_ai_config.ps1` 与 `tools/load_deepseek_ai.ps1`；每个新会话先运行 load 脚本即可。

2026-07-28 第4至第8项已统一提交为 `4cf71ef feat: close bankflow verification rounds four through eight`，续接记录提交为 `f6640d4 docs: record bankflow rounds four through eight checkpoint`；第9项已提交为 `4241ba9 feat: close traceable evidence output`，续接记录提交为 `7833959 docs: record traceable evidence checkpoint`。以上均未推送；领先数继续以 `git status`、`git log -1 --oneline` 和远端差异实际核验。v1C 本人账户精确匹配基线为 `893c5d1`，建行个人完整对手账号确定性拆分提交为 `941c2b7`，均尚未推送。

- 2026-07-27：流水核查 MVP 第1轮已在本地实现：新增来源分层的最小 `case_context`；工行个人与微信首页明确期间写入既有 `StatementMetadata`；任如冰工行“渠道”在唯一归一后写入 `transaction_method`，原值继续保留。真实任如冰工行109笔渠道全部可用，两份微信期间正确；定向单元测试25 PASS，工行专项回归2 PASS / 0 FAIL / 2 SKIP。未运行全量单元测试和统一回归，未提交、未推送；人工确认工作簿因规定工具导入超时待同步，详见 `chg-20260727-07`。
- 2026-07-27：已完成 `D:\客户报告\历史客户` 全量只读批处理：160 个末级目录、133 份可解析文件；85 份非流水/无文本/密码或不可读文件按来源处理摘要忽略，不计失败；213 份可靠抬头账户不可得仅作技术覆盖不足单列，20 份逐文件超时单列待重跑。95 个目录发现 110 个可靠抬头账户；12 个目录的 19 个账户对均实际尝试 v1D，得到 8 组唯一双边候选、69 笔单边候选、3 笔歧义候选。可靠抬头账户自动进入本批 v1C，上述 133 份解析文件合计 106 笔精确匹配观察。未使用姓名、目录名、摘要、尾号、掩码、混合文本或任何日期/金额容差；零配对是正常结果。全量单元测试 97 PASS；统一回归 71 PASS / 0 FAIL / 21 SKIP（20 个既有路径缺失、佛山 1 项主动暂缓）。详见 `chg-20260727-02`。
- 2026-07-27：经原件核验补齐中行对公与招商对公完整抬头元数据；招商个人掩码账号可作为同案分析来源，但独立提示且不计入完整账号 v1C/v1D。详见 `chg-20260727-02`。
- 2026-07-27：仅为已核验 `ccb_corp` 固定版式增加受限 `StatementMetadata`：第 1 页唯一 `本方户名`，以及每页交易表首列 `账号`归一后唯一、完整且跨页一致；缺列、掩码、多值、异常或不一致均整体保持为空。新增只读案例候选扫描器和命令行入口，逐 PDF 独立超时并输出不可用原因。未改交易、金额、方向、去重、统计或 v1D 精确配对边界。详见 `chg-20260727-01`。

最近重要变更：

- 2026-08-07：Gate D.1B 完成（`chg-20260807-22`）：59 条真实 AI candidate 独立人工审核包
  （57 concept + 2 relation，12 legacy 隔离，provenance 完整，decisions 空待人工）；
  Python 全量 492 项通过；已本地 commit，未 push。
- 2026-08-07：Gate D.1A 完成（`chg-20260807-21`）：2 条 privacy blocked 审计为 false_positive；
  PII guard 增加 typed 字段 + Luhn 失败保守例外并修复中文邻接漏检；Luhn 有效卡仍永远阻断；
  审计产物 privacy_block_audit.json/.md；Python 全量 492 项通过；已本地 commit，未 push。
- 2026-08-07：Gate D 完成（`chg-20260807-20`）：第一次真实 AI Fallback Validation。
  privacy preflight + PII guard；57 条可发送（2 条 product_description 长数字被银行卡模式保守拦截）；
  3 次调用全部 ok；57 concept 候选（55 existing / 2 new 通用概念）+ 2 relation 候选，全部 pending；
  0 失败 / 0 重试 / 0 敏感外发 / 0 自动 approve；approved cache 0 写入；
  Python 全量 487 项、统一回归 71/0/20/1 通过；已本地 commit，未 push。
- 2026-08-07：C2.1 契约收口完成（`chg-20260807-19`）：unresolved 持久化（never-parsed 与
  parsed-unresolved 可区分）、96 mismatch 全量分类（40+36+20，无 unexplained）、
  relation_id = Canonical Relation Snapshot Identity 冻结；Python 全量 472 项、统一回归 71/0/20/1 通过；
  已本地 commit（c773a3d），未 push。
- 2026-08-07：Gate B.1 + C1 + C2 完成（`chg-20260807-18`）：schema 1.17 契约冻结并修订
  （deterministic resolution_id、relation_id 内容哈希、migration_status 落位、resolver source 直出、
  review_status/candidate_ref、per-entry profile）；326 legacy replay 与 Gold Set 重跑通过；
  legacy production result 保持不变；production_resolver=legacy_v11、knowledge_v1=shadow；
  Python 全量 462 项通过；已本地 commit（B1/C1/C2），未 push。
- 2026-08-07：Foundation Remediation 完成（`chg-20260807-17`）：华夏 6/9 列版式自适应（6 列 57 笔恢复）、
  对公 19 文件 4,316 笔 raw evidence 补齐、icbc_corp 负值方向契约修复（全库 negative=0）、
  source diagnostics（skipped/unparsed 可观测）、metadata 显式 unavailable；
  Python 全量 447 项、统一回归 71/0/20/1、81,746 笔全量 invariant 与 172 同文件 differential 通过，
  Foundation 结论 GREEN；已本地 commit，未 push。
- 2026-08-07：schema 1.17 已实施（`chg-20260807-16`）：独立 `business_semantics_resolutions`
  观察 + diagnostics legacy comparison + `migrate_schema_116_to_117`（不伪造历史）；
  1.16/1.17 双向兼容；Python 全量 437 项通过；已本地 commit（契约/迁移与 shadow 写入各一），未 push。
- 2026-08-07：Gate A 完成（`chg-20260807-15`）：per-entry 行业画像修正后 100 条 mismatch
  人工裁决，knowledge_v1 vs Gold Set accuracy 99%（63 条泛化 Gold 条目）；第三轮 shadow
  agreement 69.33%、理论 AI fallback 40（语义 40 / 关系 0）；知识校准（具体概念优先、
  名称弱关联封顶、水泥阻断、17 个通用别名 + 2 概念 + consulting 关系降级）；
  Python 全量 425 项通过；已本地 commit，未 push。
- 2026-08-07：经营语义知识库架构 v1 独立阶段完成（`chg-20260807-14`）：四层知识库、双轨缓存、
  AI 双任务契约、候选审核、validator、legacy shadow 对比与迁移工具；真实 326 条缓存离线 shadow
  一致率 35.89% → 人工验收扩充后 62.88%；170 条候选复核后 158 拒绝 / 12 冲突 pending；
  schema 1.17 设计草案已出（未实施）；Python 全量 421 项通过；已本地 commit，未 push。
- 2026-08-07：AI 验收对齐方案待确认（行业桶共享缓存 + 新客户增量联网开关），确认前不改造缓存键；详见 `docs/续接_2026-08-07_新窗口.md` 顶部“当前执行定位”。
- 2026-08-07：设置页支持预检阶段句柄读写/清空经营上下文，并新增同名案件工作区回退（`chg-20260807-13`），修复韩鹏飞“当前案件没有关联目录”提示；Python 383、前端 27、真实 QA 通过；已本地提交，未 push。
- 2026-08-07：下定购车模块“此前收入”显示改为“下定前收入”（`chg-20260807-12`，仅展示层文案）；Python 381 项通过；已本地提交，未 push。
- 2026-08-07：修复设置页在预检/分析页点击无反应（`chg-20260807-11`），设置提升为最高渲染优先级；证据第三层取消“展开完整允许内容”折叠、默认完整显示，原始内容按 `|` 逐行中文排版（去掉 raw_text 英文标签）。前端 27、Python 381、真实 QA 通过；已本地提交，未 push。
- 2026-08-07：工作台可执行优化批（`chg-20260807-10`）：设置页 AI 状态面板、经营痕迹“不可用”中文原因、构建阶段文案注明 AI 离线重放、回归输出缺失清单、清空上下文改为未确认空记录（修复清空后重建报错）。Python 381、前端 27、统一回归 71/0/20/1、真实 QA 通过；已本地提交，未 push。
- 2026-08-07：修复工作台分析卡在“构建标准结果”（`chg-20260807-09`）。根因是已确认经营上下文触发工作台全量在线 AI（韩鹏飞 653 语义约 14 批）；现工作台 AI 改为验收缓存离线重放（零网络），运行时缓存已合并曹国民/任如冰/李娟验收缓存，韩鹏飞两场景全流程 18-20 秒完成；Python 379、统一回归 71/0/20/1、真实 QA 通过；未 commit/push。
- 2026-08-07：工作台取消 AI 默认关闭（`chg-20260807-08`）：启动器加载持久化配置后分析自动启用 AI，无配置时离线降级；新增字段级抽查工具与“来源×字段→统一字段”矩阵（曹国民四来源 40 笔、61 字段 0 未溯源）；人工核实证据笔数、经营痕迹确定性/AI 徽标与筛选、申报对照仅展示标签、历史案件重新分析、设置页清空上下文、金额千分位、提取状态中文化；统一回归 `--allow-missing` 显式分离缺失样本（71/0/20/1）；任如冰（烟酒零售/超市）固定 20 种真实抽检 20/20 + 离线重放一致。Python 全量 379、前端 27、真实 QA 通过；未 commit/push。
- 2026-08-07：取消经营上下文“确认状态”（`chg-20260807-07`）。预检页与设置页不再提供未确认/已确认选择，填写并保存即视为已确认（后端固定 `confirmation_status=confirmed`，兼容既有 AI 准入逻辑）；Python 全量 371 项、前端 27 项、真实 WebView2 完整实测通过；已本地提交，未 push。
- 2026-08-07：工作台展示修复（`chg-20260807-06`）。经营痕迹补全交易上下文与中文分类、购车命中文本去重、申报对照分类列显示对照项、人工核实中文分类并展示证据交易金额、证据页去掉 `raw_fields[N]`；Python 全量 371 项、前端 27 项、统一回归基线一致、真实 WebView2 完整实测通过；已本地提交，未 push。
- 2026-08-07：AI 经营关联验收链完成（`chg-20260807-05`）。development 50 离线重放、reserved 50 真实调用 + 离线重放、286 语义 / 1115 笔完整验收 + 离线重放 + 输入审计全部通过；回归为 Python 全量 368 项、统一回归 71 PASS / 20 项既有路径缺失 / 1 项佛山暂缓、前端 27 项、真实 WebView2 完整实测（含新增设置页经营上下文场景）全部通过。工作台新增“设置”页，可查看/编辑当前案件经营上下文（沿用 `chg-20260807-02` 的闭环与工作区保存，不写客户原始资料）。已本地提交，未 push。另完成生活轨迹/用车痕迹/消费水平立项讨论：AI 生活轨迹单独立项、用车痕迹并入生活轨迹数据层不单独 AI 立项、消费水平单独确定性统计立项；均需用户确认 schema 方案后执行。详见 `docs/续接_2026-08-07_新窗口.md`。
- 2026-08-06/07：12B-2 完整分析流程接入报告已补充；真实 WebView2 窗口实测通过协作取消（MVP-input 14 个来源中完成 2 个后取消，旧案件保持不变）、任务级失败恢复（旧案件保持不变）与完整分析成功自动进入工作台（20006 笔、9 个模块）。修复前端“分析完成未自动进入工作台”竞态（`chg-20260806-02`）、测试入口 bat 编码问题（`chg-20260806-01`）并完成工作台 UI 修正（滚轮翻页、原始字段分隔符过滤、筛选行换行、底部显示完整，`chg-20260806-03`、`chg-20260807-01`）；新增 `tools/run_12b2_flow_qa.py` 与 `运行12B2完整流程实测.bat` 作为可运行测试入口；未打包、未提交、未推送。详见 `docs/Web_GUI_12B2_完整分析流程接入报告.md`。
- 2026-08-07：完成历史案件页、经营上下文闭环与报告导出（`chg-20260807-02`）；Python 全量 363 项、前端 26 项与真实 WebView2 完整实测全部通过；经营上下文仅预检录入、确认人/时间不强制、手动重建观察；报告导出 Markdown 验收视图；未提交、未推送。详见 `docs/Web_GUI_12B2_完整分析流程接入报告.md`。
- 2026-08-07：修复列表明细中文显示、历史案件打开失败、预检 TXT 上下文展示（`chg-20260807-03`）；分析后自动保存工作区标准结果，历史案件可回退打开；Python 全量 366 项、前端 26 项与真实 WebView2 完整实测全部通过；未提交、未推送。详见 `docs/Web_GUI_12B2_完整分析流程接入报告.md`。
- 2026-08-03：WebView2基础审核工作台完成正式源码迁移（本地未提交）。新增独立正式入口和启动器，技术切片与旧Qt入口保持不变；Python增加ModuleRegistry、统一分页/筛选、来源复核DTO、会话ID/revision和过期请求隔离，React拆分为app/bridge/components/styles并保持V5 Linear视觉。两个批准schema 1.16结果完成025→129→025进程内验收，来源复核、至少两个可用模块、transaction_id证据、切案清理、主题、阻网及125%/150%缩放通过；未调用AI/外部API、未重算业务、未打包、未提交、未推送。详见`chg-20260803-02`。
- 2026-08-03：独立Web/WebView2真实结果切片及来源复核链路完成。Worker来源状态进入schema 1.16 `source_files[]`，CaseSession只向React返回轻量DTO；case-025显示11来源、其中1来源需复核，025→129→025切案无提示、选中项或Inspector残留。源码WebView2、334项全量测试、TypeScript和生产构建通过。影响限于独立切片，正式GUI尚未迁移；下一步为工作台正式迁移。详见`chg-20260803-01`。
- 2026-08-01：用户确认GUI步骤10至12按当前交互收口并授权提交及同步Obsidian。默认列表仍遵循步骤10的脱敏边界；用户主动点击“展开完整证据”后直接显示未脱敏原始字段，不增加共享屏幕二次确认或额外脱敏。150ms卡片悬停与220ms页面切换速度经用户确认暂按现状保留。功能和验证基线仍为`chg-20260731-02`；本次只补充确认、提交和续接记录，未修改业务规则，不推送。详见`chg-20260801-01`。
- 2026-07-31：连续完成GUI步骤10至步骤12（本地未提交）。证据中心继续只读使用schema 1.16证据索引和原交易行序号，补齐异常引用的ID、引用消费者及“无法打开原交易”说明；默认视图保持脱敏，只有用户主动展开时显示完整未脱敏原始字段。首页主操作卡、Dashboard组合卡、主要发现卡和人工核实事项卡使用150ms `OutCubic`整体左上3px及硬阴影延长；顶部与模块页面使用55ms轻淡出加165ms淡入上移，不进入表格行、分页或交易刷新。三级表格最低可视高度360px、行高34px并使用受控列宽；证据抽屉按窗口宽度在400至560px内适配，窄窗同步收紧外边距。吴信鹏999笔真实结果为20页、每页50笔，999个唯一索引、11915条有效引用、0悬空、0歧义，transaction_id定位、默认脱敏及按需展开通过；下定仍为4笔，7月27日问界支出和7月28日退款收入均保留。43项GUI定向测试、279项全量单元测试、兴业专项2 PASS / 0 FAIL / 0 SKIP、完整统一回归71 PASS / 0 FAIL / 21 SKIP、语法、离屏启动、运行态动效和`git diff --check`通过。完整回归本机耗时426.7秒，前两次仅因180秒和300秒执行上限过短被终止。未调用模型、未修改schema或AI规则、未提交、未推送。详见`chg-20260731-02`。
- 2026-07-31：处理GUI步骤6至10验收反馈并按用户确认更新购车口径（本地未提交）。工作地点、住址等未来生活轨迹项使用独立“仅展示”状态；车型保留在案件上下文供后续用车轨迹使用，不再作为当前申报对照的独立展示项。证据中心正常时只显示“证据索引完整”，仅在异常时用白话说明结果引用找不到原交易或ID重复/歧义；三级明细去除重复长概要并限制说明区高度，真实离屏表格高度432px。吴信鹏兴业68页8列强水印流水由错误的20笔恢复为999笔，对手名称、摘要/用途和方向以可靠字段接入既有字段匹配路径。购车观察现对下定、订金、定金、购车款、首付款、补款或问界的可靠字段命中同时接受收入和支出，统一计入下定总数并保留子类；吴信鹏为下定4笔、其中订金2笔，7月27日10,000元问界支出和次日10,000元问界退款收入均为直接命中，退款仅提示、不判断订单取消，且不再要求订单或付款凭证二次确认。40项GUI测试、276项全量单元测试、兴业专项2 PASS / 0 FAIL / 0 SKIP、完整统一回归71 PASS / 0 FAIL / 21 SKIP、语法和差异检查通过；未调用模型、未提交、未推送。详见`chg-20260731-01`。
- 2026-07-30：按用户要求连续完成GUI步骤6至步骤10（本地未提交）：下定购车、交易对手、资金与余额、申报对照和证据中心均只读接入schema 1.16既有观察、指标和证据。下定按六类关键词统计并分类后进入含脱敏原始上下文和transaction_id的明细，任如冰真实2来源2075笔验收得到1笔2026-01-09的10,000元问界候选及2笔此前收入并列；交易对手提供双方向Top、双口径占比、月份、不可识别金额和逐对手交易；资金与余额保持单一组合卡并提供大额交易、资金路径、余额、结息、月度变化五页签；申报对照统一四状态及来源/覆盖/证据；证据中心基于原交易行序号分页，支持来源、日期、方向、引用状态、transaction_id和消费者筛选。完整账号默认脱敏，所有交易类明细复用transaction_id索引和按需`EvidencePanel`。47项GUI/标准结果定向测试、272项全量单元测试、语法、离屏启动和`git diff --check`通过；统一回归71 PASS / 0 FAIL / 21 SKIP。未修改schema、解析后端或AI规则，未调用模型，未接入生活轨迹/用车轨迹/高德，未提交、未推送。详见`chg-20260730-10`。
- 2026-07-30：按用户最新步骤5范围完成“现有模块概要层迁移”（本地未提交），本轮只处理人工核实、敏感交易和经营关联，未接入下定购车、交易对手或资金模块。人工核实概要先显示待核实数量、主要触发类型、唯一证据数、最重要的1至3项和数据可用状态；敏感交易概要新增受控类别数量、主要命中词、来源、月份和待核实上下文，并按类别进入同一分页明细；经营关联概要补齐工作单位、申报工作描述、人工确认经营内容、产品/服务、AI状态、确定性正向、strong/medium/weak/undetermined/none、确定性排除、主要相关对手、用途、月份和最多3项关注内容，默认入口为“相关候选”。三类明细继续共用`QTableView`、`QAbstractTableModel`、50条分页、transaction_id回跳和按需`EvidencePanel`；未修改schema 1.16、解析后端、经营AI规则或授权逻辑，未调用模型。42项GUI/标准结果定向测试、267项全量单元测试、语法、离屏启动和`git diff --check`通过；统一回归71 PASS / 0 FAIL / 21 SKIP。详见`chg-20260730-09`。
- 2026-07-30：完成GUI步骤4重复信息校正，并将步骤2至步骤4收口为本地基线提交（提交号以`git log -1 --oneline`为准）。四张组合画像卡只保留客观汇总和状态，不再重复展示下方人工核实问题正文；“需关注事项”改为“人工核实待办”，按既有`trigger_observation_type`和`question_type`结构化归组，不进行中文文案匹配。韩鹏飞4项人工问题收敛为3组待办：主要交易关系2项（4笔唯一证据）、资金与余额1项（221笔）、核实与申报1项（21笔）；点击待办直接进入人工核实分页列表，不再返回同一模块概要。真实案例仍为3来源、3135笔、21项敏感候选、3135笔唯一交易索引、39431条有效引用、0悬空、0歧义，AI未调用。40项GUI/标准结果定向测试、265项全量单元测试、语法、离屏启动和差异检查通过；统一回归71 PASS / 0 FAIL / 21 SKIP。下一续接点固定为步骤5“下定购车”执行前，本轮不执行步骤5、不推送。详见`chg-20260730-08`。
- 2026-07-30：完成GUI步骤4“案件整体画像Dashboard与需关注事项”（本地未提交）。案件头部明确显示案例名称、资料期间、来源数、交易笔数和分析状态，完成页只保留“已完成 / 证据完整 / schema 1.16”徽标，不显示100%进度条。步骤3A四张组合卡继续作为整体画像，购车与经营卡补充单位及经营内容对照状态。新增并实际复用`ModuleSummaryHeader`、`SummaryMetric`、`KeyFindingCard`、`AttentionItemCard`和`CandidateCategoryButton`；需关注事项最多5条，逐项显示客观触发事实、建议核实、对应模块、证据数量和数据可用状态。韩鹏飞真实验收为2025-01-21至2026-01-21、3来源、3135笔、4条关注事项、4项人工核实、21项敏感候选，证据仍为3135唯一索引、39431有效引用、0悬空、0歧义；40项GUI/标准结果定向测试和265项全量单元测试通过，AI保持未调用。未接入后续业务详情、未修改schema或后端规则、未增加动画、未提交、未推送。详见`chg-20260730-07`。
- 2026-07-30：完成插入步骤3A“案件概览信息结构校正”（本地未提交）。删除案件标题下方“概览 / 购车与申报 / 余额与资金 / 交易对手 / 经营与敏感 / 生活轨迹 / 用车信息”重复二级标签，顶部仍只保留“首页 / 当前案件 / 历史案件 / 设置”。Dashboard原八张独立业务卡收敛为“核实与申报 / 购车与经营 / 资金与余额 / 主要交易关系”四张2列组合卡；卡片只进入模块概要，人工核实、敏感和经营候选继续经分页明细打开共享证据面板。证据完整性保持窄摘要，生活轨迹和用车信息降为不可点击的后续状态块。现有人工经营上下文记录作为只读显示来源，已人工确认时卡片恢复确认状态和主营内容，补充入口仍复用步骤3同一信号。韩鹏飞真实离线验收保持3来源、3135笔、4项人工核实、21项敏感候选、3135唯一索引、39431有效引用、0悬空、0歧义；39项GUI/标准结果定向测试、264项全量单元测试和语法检查通过。未修改schema或后端规则，未调用模型，未提交、未推送。详见`chg-20260730-06`。
- 2026-07-30：GUI步骤3在既有经营上下文闭环上补齐最新页面契约（本地未提交）。`CasePreparationPage`新增案例名称和“当前缺少信息”，韩鹏飞仅有公司名称时明确提示实际主要经营内容必填；人工AI授权字段统一为`enable_ai_business_analysis`并兼容恢复旧`ai_business_assistance_enabled`文件。`ModuleSummaryPage`新增复用同一工作区信号的“补充经营信息”入口，不绑定Dashboard独立业务卡布局。真实韩鹏飞三来源重新解析3135笔；临时案件工作区成功保存和恢复人工占位确认，原4份客户资料SHA-256前后不变，局部重算前后`original_transactions`逐项相同，AI保持关闭且结果为`ai_data_authorization_missing`，经营卡由“需补充经营内容”恢复为47项确定性候选。38项GUI/标准结果定向测试、263项全量单元测试、语法和离屏检查通过；未接入后续模块、未调用模型、未推送。详见`chg-20260730-05`。
- 2026-07-30：独立流水核查工作台完成GUI步骤2顶部导航与三级页面骨架（本地未提交）。长期占宽的左侧导航改为“首页 / 当前案件 / 历史案件 / 设置”顶部一级导航，未加载结果时当前案件禁用；案件标题和状态下新增“概览 / 购车与申报 / 余额与资金 / 交易对手 / 经营与敏感 / 生活轨迹（后续） / 用车信息（后续）”二级标签。新增`ModuleSummaryPage`与`TransactionListPanel`，Dashboard或二级标签先进入模块概要，用户主动选择候选分类后才进入人工核实、敏感交易或经营关联列表，再通过既有transaction_id打开共享`EvidencePanel`。经营默认正向候选只含确定性正向及AI strong/medium/weak；undetermined、AI none/无关联及确定性排除分别进入待人工判断和已排除入口，全部结果仍可查看。韩鹏飞真实案例保持3来源、3135笔、4项人工核实、21项敏感候选；经营默认正向47项、排除251项，首条证据成功展开，证据仍为3135唯一索引、39431有效引用、0悬空、0歧义。37项GUI/标准结果定向测试、262项全量单元测试、语法检查、离屏截图和GUI启动通过。生活轨迹和用车信息只显示后续状态；未接入新业务模块、未增加动画、未修改schema 1.16或经营AI规则、未调用模型、未推送。详见`chg-20260730-04`。
- 2026-07-30：暂停“下定购车”GUI接入，完成经营关联分析前确认与人工上下文闭环。新建案件改为目录快速扫描后先进入`CasePreparationPage`，展示工作单位、明确工作内容、原始文字/来源和经营上下文状态；人工确认通过GUI录入并独立保存为案件级`manual_case_context.json`，不修改客户TXT、PDF或流水。只有单位名称时Dashboard显示“需补充经营内容”，47笔韩鹏飞命中改称确定性文字/企业名称候选。暂不补充仍可运行其他模块，经营语义判断保持待确认。修改确认后不重解析PDF，基于既有交易只重建经营关联、申报对照、相关人工核实和证据审计，`original_transactions`保持不变。确认页两张主卡改为左右布局，AI字段说明完整显示；确认人明确标为必填，校验失败时红框聚焦并显示醒目原因。AI默认关闭，只有GUI明确勾选并确认后才装载运行配置；未勾选时不读取AI环境配置，结果状态明确写为“本次分析未获得GUI明确授权”。为避免PowerShell环境继承失败，勾选后可在GUI输入仅本次进程使用的DeepSeek API Key；密码输入不写入`manual_case_context.json`、标准结果或日志。韩鹏飞真实临时工作区验收为3来源、3135笔、47笔确定性文字/名称候选、0笔AI观察，前后证据均为3135唯一索引、39431有效引用、0悬空、0歧义；260项全量单元测试、语法检查和GUI离屏启动通过。未修改schema 1.16字段含义、未启动生活轨迹、未接高德、未调用外部模型、未推送。详见`chg-20260730-03`。
- 2026-07-30：独立流水核查工作台接入经营关联详情：`ResultListModel`和统一`ModuleDetailPage`只读展示 schema 1.16 已有的确定性单位/行业命中和已接受 AI 观察，按判断来源、关联分类、证据强度及交易证据列示，点击行继续通过 transaction_id 唯一索引展开共享证据面板。AI 可用状态及不可用原因与确定性结果分层展示；AI 未授权或经营上下文待确认时不隐藏已有确定性命中，也不在 GUI 恢复模型调用。确定性排除只计入状态摘要，不混入主候选表。韩鹏飞真实案例为47笔确定性命中、0笔AI观察、251笔确定性排除，状态保持`business_context_confirmation_required`；主表47行且首行证据回跳成功。252项全量单元测试、语法检查和GUI离屏启动通过。未修改schema、Worker或解析后端，未调用外部模型、未启动生活轨迹、未推送。详见 `chg-20260730-02`。
- 2026-07-30：独立流水核查工作台完成后续模块接入前的小范围信息架构整理：左侧删除可见的重复“分析结果”入口，改为“当前案件”分组下的案件概览、人工核实和证据中心；`CaseDashboardPage`继续作为唯一案件总览页。六项关键数据合并为单一 `KeyMetricsPanel`；八张业务摘要卡删除编号与 `ANALYSIS` 标识，并按优先处理、业务与购车、资金情况分组。证据总表移出普通业务卡，改为明确区分交易唯一索引、有效证据引用、悬空和歧义数量的证据完整性摘要。韩鹏飞真实案例保持3个来源、3135笔交易、4项人工核实、21项敏感候选，证据为3135笔唯一交易索引、39431条有效引用、0悬空、0歧义；248项全量单元测试、语法检查和GUI离屏启动通过。未修改schema、Worker或解析后端，未调用外部模型、未启动生活轨迹、未推送。详见 `chg-20260730-01`。
- 2026-07-29：独立流水核查工作台已完成整体信息架构重构：未加载案件时只显示首页；新增独立任务处理页；左侧改为首页/当前案件/历史案件/设置的全局导航；案件完成后进入 Dashboard，并以9张摘要卡进入统一模块详情。人工核实和敏感交易已迁移到统一 `ModuleDetailPage`；证据详情默认折叠，点击交易后通过 `QSplitter` 展开，支持关闭及上一条/下一条。打开已有案件优先读取目录内 schema 1.16 JSON，不重复解析。韩鹏飞真实案例离线验收为3个来源、3135笔交易、4项人工核实、21项敏感候选，证据回跳个人建行第20页第10行且完整；246项全量单元测试和统一回归71 PASS / 0 FAIL / 21 SKIP通过。schema、Worker和解析后端未改；未调用外部模型、未启动生活轨迹、未推送。详见 `chg-20260729-03`。
- 2026-07-29：独立流水核查工作台第一轮已完成并提交。当前有效入口仅为 `启动流水核查工作台.bat → gui_verification_app.py`；旧 `启动GUI.bat → gui_v2.py` 不参与新项目。概览、人工核实、敏感交易、分页证据、默认精简/按需展开、脱敏、进度、取消、来源错误、schema 1.16 JSON加载及深色系统下固定浅色主题均已收口。人工核实和敏感交易列表不显示交易ID，ID只在内部索引；右侧默认只显示业务字段与来源页/行。BAT从 `cmd.exe` 实际启动通过，240项单元测试通过；统一回归仍为71 PASS / 0 FAIL / 21 SKIP。工作区收口后下一入口为GUI第2轮“04经营关联 + 05下定购车”，开始时先核验Git和当前快照，不调用外部模型、不启动生活轨迹、不推送。详见 `chg-20260729-02`。
- 2026-07-29：完成独立 PyQt6 流水核查工作台第一轮最小纵向切片：新入口固定为 `启动流水核查工作台.bat → gui_verification_app.py`，旧 `启动GUI.bat → gui_v2.py` 不参与新项目启动或运行。独立 `VerificationWorker(QThread)` 解析后显式以 `ai_config={}` 生成 schema 1.16。工作台只读消费标准结果，提供概览、人工核实、敏感候选、50 条分页、transaction_id 证据直查、来源页/行、默认脱敏、进度、协作取消、来源错误、标准结果JSON打开和版本不兼容提示；未建立平行交易模型。任如冰真实案例 2 个来源、2075 笔原始交易、5 项人工核实、11 项敏感候选，成功回跳微信第3页第3行；独立 Worker 解析与结果构建约22.213秒，首次绑定绘制约76.0毫秒。独立入口固定浅米色调色板，不继承 Windows 深色主题；BAT 实际启动冒烟通过。证据面板已明确为输出区，无交易ID事项会显示原因。240项单元测试及统一回归71 PASS / 0 FAIL / 21 SKIP通过；未调用外部模型、未启动生活轨迹、未推送。详见 `chg-20260729-01`。
- 2026-07-29：完成 schema 1.16 GUI 接入规划并记录于 `docs/流水核查MVP_GUI接入设计_v1.md`。当前有效 GUI 为 `启动GUI.bat -> gui_v2.py`，技术栈为 PyQt6，解析使用既有 `QThread`；现有 GUI 尚未消费标准结果。后续沿用当前框架，按标准结果只读接入、摘要模块、候选模块、分页证据、脱敏、进度/取消和真实案例验收分轮实施。接入期间冻结 schema 1.16 现有字段含义；基础结果构建显式禁用 AI，不启动生活轨迹或解冻其他冻结能力。本轮未修改 GUI 代码、未调用外部模型、未推送。
- 2026-07-28：第9项可追溯交易证据输出已本地收口并提交为 `4241ba9`，标准结果升至schema 1.16：新增交易ID到既有原交易序号及来源/页行定位的索引，并逐项审计事实、指标、观察和人工事项引用；重复ID不静默覆盖，缺失、悬空和歧义显式降级。任如冰2075笔、韩鹏飞3135笔均100%建立唯一索引及定位，分别审计23186和39431个证据链接，悬空/歧义/重复均为0。225项全量单元测试通过，统一回归71 PASS / 0 FAIL / 21 SKIP；详见 `chg-20260728-13`。未调用外部模型，未推送。
- 2026-07-28：人工核实事项与需关注提示已本地收口，标准结果升至schema 1.15：确定性生成稳定问题ID、问题正文、触发原因、核实要点、待核实状态和证据；同类项合并，系统仅展示字段不触发问题。主要对手、低留存路径和敏感文字可提示“需关注（仅供参考）”，不形成风险评分或结论。223项全量单元测试通过，统一回归71 PASS / 0 FAIL / 21 SKIP；详见 `chg-20260728-12`。
- 2026-07-28：客户申报信息与流水事实对照已按确认口径收口，标准结果升至schema 1.14：自动对照只处理工作单位、申报行业/经营内容和下定定金支出；工作地点、住家地址、上牌地点、车型、经销商、门店和下定描述只展示。修复普通购车款被误列为下定候选的边界；真实样本离线核验通过。64项相关定向测试、221项全量单元测试通过，统一回归71 PASS / 0 FAIL / 21 SKIP；详见 `chg-20260728-11`。
- 2026-07-28：敏感交易关键词及上下文第一版已收口并包含于 `4cf71ef`：独立输出完整候选、逐来源覆盖、当前交易覆盖期、命中字段和证据，区分无命中与字段不可用；不匹配单字，不输出风险结论。Markdown展示层统一隐藏手机号、证件号和完整账号，结构化证据不改。45项定向测试、219项全量单元测试通过，统一回归71 PASS / 0 FAIL / 21 SKIP；详见 `chg-20260728-10`。
- 2026-07-28：修复部分大额入账缺少来源ID时静默跳过的边界，结构化结果和Markdown现明确达到阈值数、成功构造数及跳过数。“余额、结息和月度变化”第一版同步收口：Markdown复用既有收入连续性和近期收支指标，展示月均收支、两个三月窗口、日末余额四项统计及证据、逐笔结息和季度变化；微信余额明确不适用。任如冰、韩鹏飞真实本地结果独立复算一致；全量单元测试215项通过，统一回归71 PASS / 0 FAIL / 21 SKIP。详见 `chg-20260728-09`。
- 2026-07-28：快进快出和大额资金停留第一版已收口并包含于 `4cf71ef`：保留1/3/7日收入后支出时间共现、1万元大额清单、3万元入账后同来源1/3/7日拆分支出和余额路径，修正余额增量留存公式为 `max(日末余额-入账前余额, 0) / 入账额`，目标日无交易时使用同来源目标日结束前最后一笔余额快照；缺少 `source_file_id` 时不构造路径。Markdown新增时间共现汇总、完整大额交易清单及逐笔路径、比例、余额、证据和非归因说明，不计算或断言实际资金停留时长。任如冰真实本地验收为106笔大额、49笔路径、41笔低留存候选；韩鹏飞为358、74、52。全量单元测试211项通过，统一回归71 PASS / 0 FAIL / 21 SKIP；详见 `chg-20260728-08`。
- 2026-07-28：主要交易对手与集中度第一版已收口并包含于 `4cf71ef`：收入/支出Top 5新增可识别金额覆盖、双口径金额占比、月份、证据和逐方向不可用原因；集中度指标排除空值、掩码、占位、代码前缀短姓名和账户标签，Markdown中的账号仅显示尾号。任如冰与李娟真实本地报告通过；全量单元测试207 PASS，统一回归71 PASS / 0 FAIL / 21 SKIP；详见 `chg-20260728-07`。
- 2026-07-28：客户信息来源边界已收紧并本地提交为 `7b08441`：本人/个人分析、公司意见及其他人工判断不进入客户字段或经营上下文。下定与购车关键词第一版随后提交为 `4c2f3ee`：新增订金、购车款、首付款和补款，候选输出方向、命中原文及同/跨来源此前收入；任如冰真实本地案例命中1笔10,000元问界支出。全量单元测试204 PASS，统一回归71 PASS / 0 FAIL / 21 SKIP；详见 `chg-20260728-06`。
- 2026-07-27：本地收口并准备新窗口。建行对公当前练习样本已人工核验：第 1 页有明确 `本方户名` 与交易表首列 `账号`，16 页账号归一后唯一且跨页一致；当前代码尚未据此创建 `StatementMetadata`，因此仍不参与案件账户发现或 v1D。下一窗口先为该固定版式做受限实现与负向验证，再增加带逐文件超时的只读候选扫描器；历史样本库只能用于版式筛选，账户角色仍须通过 `account_ref` 人工确认。
- 2026-07-26：完成 StatementMetadata 覆盖审计并建立 `docs/StatementMetadata覆盖审计_v1.md`，已随当前本地收口提交。工行对公企业存款对账单新增首页唯一 `户名 / 账号` 元数据；恢复工行个人/对公、对公客户明细、河北个人、霸州舜丰对公、兰州对公、天津农商个人、邢台、浦发个人的独立交易对手账号列映射。混合`对手信息`仍不猜测。真实工行对公样本 76 笔不变、45 笔完整对手账号；练习资料尚无第二个 v1D 双账户案例。目标测试 43 PASS、全量单元测试 93 PASS、统一回归 71 PASS / 0 FAIL / 21 SKIP；详见 `chg-20260726-13`。
- 2026-07-26：建立 `StatementMetadata` 账户抬头覆盖 v1，已随当前本地收口提交。农业银行个人当前确认固定版式从首页唯一 `户名 / 账户` 标签、工商银行个人当前确认固定版式从首页唯一 `户名 / 卡号` 标签提取文件级户名和完整账号；账号仅去除空白和连字符后为 12 至 32 位纯数字时标记为 1.0，并保留原始字段、首页来源和置信度。张鑫农行与姚志国工行真实样本均发现 1 个账户，交易明细逐项保持不变；两类交易侧对手账号能力未改变，不能据此进入双边候选配对。新增测试 4 PASS、目标测试 23 PASS、全量单元测试 92 PASS、统一回归 71 PASS / 0 FAIL / 21 SKIP；详见 `chg-20260726-12`。
- 2026-07-21：中信个人、工行个人、建行个人、兴业、招商个人、民生对公、华夏个人、平安个人、浦发个人、浦发对公 10 类已确认流水字段实现已提交并推送为 `277be1b`；提交前专项单元测试 10 个测试方法及正式回归 `72 PASS / 0 FAIL / 20 SKIP` 通过。
- 2026-07-22 至 2026-07-23：两批已确认字段重构完成并推送为 `7ac8b6a`；当前规则、技术记录与工作簿状态均已同步。
- 2026-07-24：佛山农村商业银行、九江银行、泸州银行个人、南京银行对公、宁波银行、上海银行交易明细、盛京银行、邢台银行 8 类确认字段已按明确表头/坐标实现并完成字段专项验证，已提交并推送为 `ae77782`；相关正式回归均通过。完整统一回归因本机 180 秒超时未取得汇总，不能表述为统一回归通过。
- 2026-07-24：天津农村商业银行个人账户交易明细、兰州银行对公账户对账单已按人工确认的明确表头/坐标实现；分别排除交易附言/对手账号和序号/对方帐号。通用 PDF 不扩展文字字段分析，保留为数字字段兜底与人工复核入口。
- 2026-07-24：在 `ded0a8a` 建立可复现完整测试基线：编译通过，`unittest discover` 为 55 PASS；统一回归完成于约 194 秒，结果为 71 PASS / 1 FAIL / 20 SKIP。唯一 FAIL 为佛山农村商业银行个人交易流水的既有期望值差异；20 SKIP 均为历史桌面路径样本缺失，未发生超时。
- 2026-07-24：已对 `D:\Codex data\CD_assets\PDF流水` 的 183 份历史 PDF 完成逐文件核验：168 份可解析；5 份纯图片 PDF 与经人工确认的 6 份密码保护 PDF 已排除，均不计入失败或未完成；4 份需复核（2 份无交易、2 份未识别）。另有 9 份已解析样本保留汇总层异常提示，逐文件 CSV/JSONL 与原始日志位于 `outputs\verification-runs\2026-07-24\full-history-audit-0fe55ed\`，不提交。
- 2026-07-25：`03efcb8` 已提交并推送建行对公冲正方向与兴业强水印余额定向修复。建行负借方冲正归收入、负贷方冲正归支出；已确认兴业强水印免责声明版余额仅作参考，跳过余额链与总闭合校验。最小回归 3 PASS、`ccb_corp` 标签 5 PASS、`cib` 标签 2 PASS；完整回归仍为 71 PASS / 1 FAIL / 20 SKIP，唯一 FAIL 仍为冻结的佛山农村商业银行第 23 页疑点，未改解析或期望。
- 2026-07-26：可追溯核实 MVP 第一项已实现但尚未提交：`Transaction` 增加 `transaction_id`、`source_file_id` 和 `evidence_locator`；来源文件 ID 使用内容 SHA-256，交易 ID 由原始交易证据稳定生成。PDF 管线和工作台的 PDF/Excel/通用兜底入口均接入；未改银行解析、文字字段规则或佛山期望。定向测试 16 PASS、编译通过，详见 `chg-20260726-01`。
- 2026-07-26：可追溯核实 MVP 两项能力已提交为 `4188563`：新增稳定交易/来源/页行证据标识及 `result_export.py` 的 `schema_version: "1.0"` 原始交易证据 JSON；不混入调整、事实或风险结论。单元测试 65 PASS，完整回归仍为既有 71 PASS / 1 FAIL / 20 SKIP；详见 `chg-20260726-01`、`chg-20260726-02`。
- 2026-07-26：可追溯核实 MVP 第三项已提交为 `bdf6f06`：`result_export.py` 将基于 `original_transactions` 输出 `schema_version: "1.1"` 的确定性事实（笔数、收支、净额、期间及可用余额），每项均含 `evidence_transaction_ids`；待人工核实事项也区分交易/汇总范围并携带相关交易 ID。不作风险定性或大模型结论，未改解析、去重、统计口径或佛山农商行冻结项。定向测试 8 PASS、单元测试 66 PASS；完整回归仍为 71 PASS / 1 FAIL / 20 SKIP。
- 2026-07-26：确定性指标包 v1A 已提交为 `50c96b9`：标准 JSON 升至 `schema_version: "1.2"`，`result.indicators[]` 新增含边界的 1/3/7 天收入后支出时间邻近观察、收入/支出对手集中度及指标可用性/证据覆盖。时间邻近只表示时间共现，不声称支出来源于某笔收入；对手指标只使用非空且 `field_confidence == 1.0` 的现有账号/名称，缺失时报告覆盖不足。未改解析、去重、统计口径、文字字段规则或冻结项。定向测试 11 PASS、单元测试 69 PASS；完整回归仍为 71 PASS / 1 FAIL / 20 SKIP，详见 `chg-20260726-04`。
- 2026-07-26：已建立 `docs/流水事实指标字典_v1.md`，统一区分纯数字指标、可靠文字观察、外部事件/案件参数和跨资料比对。确定性指标包 v1B 已提交为 `d008168`：标准 JSON 升至 `schema_version: "1.3"`，新增收入月份连续性、逐来源文件日末余额快照观察、1/100/1000 元金额形态和数据末月锚定的 3 个月近期收支变化。全部只读 `original_transactions`，带参数、证据交易 ID 和字段覆盖，不作风险定性。定向测试 11 PASS、单元测试 73 PASS；完整回归仍为 71 PASS / 1 FAIL / 20 SKIP，详见 `chg-20260726-05`。
- 2026-07-26：v1B 已用农行个人、农行对公、微信三类真实样本完成证据包验收，四项公式、参数、证据 ID、字段覆盖和不可用原因均独立复算一致，稳定证据标识重复解析一致。微信样本暴露 schema 1.3 未导出中性交易标志的可复算缺口；最小测试复现后仅为每笔标准交易新增 `neutral` 布尔字段，并将接口升至 `schema_version: "1.4"`。当前单元测试 74 PASS，完整回归仍为 71 PASS / 1 FAIL / 20 SKIP；唯一 FAIL 仍为冻结的佛山农商 535 vs 533 既有差异。详见 `chg-20260726-06`。
- 2026-07-26：按用户决定，佛山农村商业银行个人交易流水暂不执行自动回归，与未确认文字字段及人工复核事项一并后续专项处理。案例仍保留原样本、期望和 `535 vs 533` 历史证据，通过显式 `skip_reason` 直接跳过，不改解析、去重或期望。当前单元测试 75 PASS；统一回归为 71 PASS / 0 FAIL / 21 SKIP，其中 20 项为样本路径缺失，1 项为佛山主动暂缓，不能表述为佛山通过。详见 `chg-20260726-07`。
- 2026-07-26：v1B 真实样本验收、schema 1.4 中性交易可复算修正、佛山回归主动暂缓及对应测试已提交为 `44cc76b`，尚未推送。
- 2026-07-26：v1C 已本地提交为 `893c5d1`，尚未推送：标准 JSON 升至 `schema_version: "1.5"`，新增独立 `result.observations[]`，只用已确认完整账号精确匹配本人账户转入/转出候选。随后新增案件文件夹可靠账户抬头自动发现、按 `account_ref` 一次性角色确认和 sidecar/manifest 接口；文件夹不自动认定同一主体，用户无需手工录入完整账号。农行对公独立 `对方账号 / 对方户名 / 对方开户行`列已修正映射，真实公司样本 409 笔中可靠完整账号覆盖 262 笔并精确匹配 20 笔候选，收入合计与 PDF 页脚一致；建行个人混合列旧排除规则未变，1455 笔仍为零覆盖并正确输出不可用。更新证据包位于不可提交的 `outputs\verification-runs\2026-07-26\v1c-abc-account-mapping\`。单元测试 85 PASS，统一回归 71 PASS / 0 FAIL / 21 SKIP；20 项路径缺失，佛山 1 项仍主动暂缓且不能表述为通过。详见 `chg-20260726-08`、`chg-20260726-09`。
- 2026-07-26：建行个人完整对手账号确定性拆分已提交为 `941c2b7`，尚未推送。固定七列 `对方账号与户名`仅在唯一斜杠、左侧为 12 至 32 位完整数字账号且右侧户名非空时分别映射，掩码、非数字、长度不足或边界不唯一继续排除，完整原文仍保留；`generic_pdf` 未改。真实客户 1455 笔中可靠账号覆盖 222 笔，v1C 精确匹配已确认公司账户 28 笔，原收支、余额和交易笔数完全不变。单元测试 86 PASS，统一回归 71 PASS / 0 FAIL / 21 SKIP；详见 `chg-20260726-10`。
- 2026-07-26：v1D 跨账户双边候选核对已实现，标准 JSON 升至 `schema_version: "1.6"`。仅在双方来源文件精确归属已确认账户、可靠完整对手账号互相精确匹配、同一自然日、金额完全相等且方向相反时输出唯一配对；其余保留单边或歧义，不采用姓名、混合文字、日期/金额容差、拆分合并或手续费容差。韩鹏飞案例验收为 20 组唯一双边候选、8 笔单边候选、0 个歧义；不表示资金来源、资金闭环、实际控制、风险或案件结论。单元测试 88 PASS，统一回归 71 PASS / 0 FAIL / 21 SKIP；佛山农商仍主动暂缓。详见 `chg-20260726-11`。
- `PROJECT_CONTEXT.md`：轻量项目入口。
- `PROJECT_STATUS.md`：当前状态概要，只保留必要过程和结果。
- `INTEGRATION_CONTRACT.md`：与车贷报告自动化总项目的集成契约。
- `技术变更记录.md`：问题与最近变更索引；详细正文按月份位于 `docs/change-history/`。

## 当前能力概览

当前项目已经具备：

- PDF 银行流水识别。
- Excel 流水导入。
- HTML 伪 `.xlsx` 流水导入兜底。
- 微信流水识别。
- 多银行专用解析器。
- 通用 PDF 兜底识别。
- 余额连续性校验。
- 月度统计。
- 文件汇总。
- 异常提示。
- GUI 可信度提示。
- 流水调整/测算层。
- GUI。
- Excel 导出。
- 收入佐证 JSON 导出。
- 工资类收入佐证 JSON 导出。
- 统一回归测试。
- PyInstaller 打包。

## 当前关键模块

| 模块 | 文件 |
|---|---|
| 标准交易模型 | `bankflow_v2/models.py` |
| 银行识别 | `bankflow_v2/auto_detect.py` |
| 解析入口 | `bankflow_v2/pipeline.py` |
| 汇总统计 | `bankflow_v2/summary.py` |
| 流水调整 | `bankflow_v2/adjustment.py` |
| Excel 导入 | `bankflow_v2/excel_input.py` |
| GUI | `gui_v2.py` |
| 统一回归 | `tools/regression.py` |
| 回归样本 | `tools/regression_cases.json` |

## 当前支持银行与格式

以 `银行适配手册.md` 为详细准则。

当前主要覆盖：

- 国有行和股份行常见个人/对公 PDF：工商、建设、农业、中国、交通、邮储、招商、兴业、平安、民生、中信、浦发、上海、华夏。
- 城商行/农商行专项格式：齐鲁、成都农商、九江、佛山农商、兰州、宁波、南京、中原、桂林银行、重庆银行等。
- 微信流水、Excel 导入、HTML 伪 `.xlsx` 导入和通用 PDF 兜底。

## 最新功能状态

### 流水调整/测算层

已实现：

- `bankflow_v2/adjustment.py`
- GUI 右侧流水调整面板。
- 收入调整（微信）。
- 收支平衡调整（个/公）。
- 固定分配/确定性随机分配。
- 调整后月度统计。
- Excel 导出保留原始和调整后统计。

调整层不修改原始流水明细，不污染银行解析器。

### GUI 现代化

已实现：

- 顶部主次操作栏。
- 日期筛选模块。
- 左主内容区 + 右调整面板。
- 摘要仪表板。
- 调整预览结果卡。
- 下划线风格 tabs。
- 顶部可信度字段，显示 `极高/高/中/低/未识别`，不显示具体百分比。
- 完全识别且没有异常、没有复核提示时显示 `极高`。
- 顶部工具栏保留 `佐证填写`，不再显示 `导出 Excel` 按钮。
- 月度统计 GUI 隐藏期初/期末余额两列。
- 调整启用后，月度统计仍保留按流水类型分组显示，下面再追加 `全部` 调整统计。
- GUI 不再显示 `流水明细` tab；Excel 导出仍保留明细。
- `日期范围内没有流水` 不参与可信度评分。
- 收支余额闭合校验：`收入合计 - 支出合计 = 期末余额 - 期初余额` 作为强可信加分，不闭合则扣分。
- 汇总层补充总收支余额闭合复核提示：当总净额和期末/期初余额变动不一致时，复核清单提示 `收支余额不闭合`。
- 最近两个月都有流水且月度收支余额闭合、无复核时额外加分；没有最近两个月流水仍不扣分。
- 微信流水不要求期初/期末余额，天然无余额列不影响可信度等级。
- 未识别且通用识别也没有流水时，可信度显示为 `未识别`，不再显示 `低 0%` 或弱可信基础分。

### 收入佐证 JSON 接入

已实现：

- GUI 可导出收入佐证 JSON。
- 默认按近 6 个完整月份筛选。
- 个人、微信、对公流水分组。
- 民生对公可识别为 `cmbc_corp`，明确标签后的 8-22 位账号可识别。
- 同一类型同一账号的多份 PDF 在 `accounts[]` 中只保留一组。
- 账户去重不影响交易明细和月度汇总。
- 源码 GUI 按钮已改为“佐证填写”，自动保存 `.income_proof.json` 到桌面 `银行流水解析结果` 文件夹，并打开 Word 填写表预填 JSON。
- 期末余额按同一流水类型同一账号取最新交易日期的余额，避免同账号多份 PDF 的期末余额叠加。
- 微信流水默认输出按模拟余额口径展示和导出：从 0 余额开始，按原始支出形态分布支出，保证累计模拟余额保持小额正数；收入调整后沿用同一口径。
- 交通银行个人流水账号识别支持中英混排账号标签。
- 交通银行借贷方向兼容金额正负号，避免方向列和金额符号同时表达时收入/支出反向。
- 农业银行 `自动抹账` 明细按余额差识别冲正方向，避免余额连续但方向相反时误判。
- 建设银行对公 `中国建设银行账户明细信息` 17 列表恢复完整解析。
- 中信银行对公 `账户交易明细` 支持 `借方发生额 / 贷方发生额 / 余额` 分列表格。
- 招商银行对公 `账务明细清单` 支持 `YYYYMMDD + 业务类型 + 带正负号金额 + 余额` 文本行解析。
- 中国银行 PDF 转 Excel 的 HTML 伪 `.xlsx` 可通过 Excel 导入解析，支持 `记账日期 / 记账时间 / 币别 / 金额 / 余额` 列。
- 中原银行 `账户交易流水` 支持表格层 `交易日期 / 交易时间 / 金额 / 收支状态 / 余额` 解析。
- 成都农村商业银行对公 `客户明细` 支持表格列内换行的 `借方金额 / 贷方金额 / 余额` 分列解析。
- 桂林银行对公 `企业客户交易清单` 支持坐标抽取 `交易日期 / 对方账号 / 对方户名 / 收入 / 支出 / 余额`，保留负数支出冲正符号。
- 重庆银行个人 `账户交易明细` 支持表格层 `交易日期 / 交易金额 / 活期账面余额 / 交易类型/摘要` 解析。

注意：当前未重新打包 EXE；`启动GUI.bat` 通过 `python gui_v2.py` 启动源码 GUI，因此本地启动 GUI 会使用最新源码。`dist\BankFlowGUI\BankFlowGUI.exe` 不代表当前最新源码状态。

## 当前验证结果

2026-07-26 最近执行：

```powershell
python -m unittest discover -s tests
python tools\regression.py --all --allow-missing
```

结果：

```text
编译：通过
单元测试：86 PASS / 0 FAIL
统一回归：71 PASS / 0 FAIL / 21 SKIP（共 92 项，约 148 秒）
```

最近专项验证：

- 佛山农村商业银行个人交易流水的历史事实仍为实际 535 笔与期望 533 笔不一致；现按用户决定主动暂缓，不执行解析或比较，与未确认文字字段事项一并后续处理。不得表述为通过。
- 21 个 SKIP 中，20 个为既有样本路径缺失，1 个为佛山主动暂缓；字段专项结果与统一数字回归必须分别记录。
- 本次统一回归已取得汇总且自动执行集合无失败；该结论不覆盖佛山暂缓项和缺失样本。
- 全量历史样本核验：183 份均有记录；纯图片和密码保护 PDF 仅作来源处理记录，不纳入错误或未完成。当前实际结果为 168 份可解析、5 份纯图片排除、6 份人工确认的密码保护 PDF 排除、4 份需复核；未修改解析器或期望值。

## 当前不支持或不作为稳定核心

- 扫描/图片 PDF 直接 OCR。
- 工资类完整原字段专项导出。
- 完整车贷报告生成。
- 征信说明。
- 企业信息说明。

图片 PDF 当前建议：

```text
先用外部工具转 Excel -> 再导入本项目
```

## 与车贷报告自动化总项目的关系

本项目后续是总项目中的流水子模块。

推荐链路：

```text
PDF / Excel / 微信流水
  -> Transaction
  -> Summary / Adjustment
  -> 标准 JSON
  -> 收入佐证 Word
  -> 车贷报告自动化总项目读取
```

总项目不应重新实现流水识别。

## 下一步优先级

当前执行入口不在本文件展开，详见外部《阶段进展与续接记录》顶部“当前有效快照”和人工确认工作簿。

此前列出的 8 项已完成。文字字段待办和佛山农商行均继续暂缓；v1B 真实样本证据包验收已完成，标准接口因补齐中性交易可复算字段升至 schema 1.4。本轮完成记录和本地提交后在新窗口再决定是否进入 v1C；在新决定前不得修改解析器、去重、暂缓项或新增混合文字猜测。

农信旧版个人和农村商业银行对公因可信原件缺失继续暂缓；不得用其他来源或相似银行样本替代。
## 更新文档规则

本文件只记录重大能力里程碑、当前可用能力和最近验证摘要，不保存逐来源字段状态、逐样本细账或下一窗口操作日志。

- 逐来源字段的决策、实现、验证状态以人工确认工作簿为唯一总账。
- 当前有效快照、下一步入口和历史停点写入外部《阶段进展与续接记录》；历史章节保留，不回写成当前状态。
- 稳定路线图和阶段完成度写入《PDF流水核实能力重构规划》；不记录逐次实现细节。
- 当前代码实际解析规则写入 `银行适配手册.md`。
- 实现原因、修改内容、验证命令和结果写入 `技术变更记录.md`。
- 总项目接口变化才更新 `INTEGRATION_CONTRACT.md`。

具体事件触发矩阵见 `AGENTS.md`；不是每个事件都更新全部文件。

## 历史问题与解决记录

日期流水账不再在本文件重复维护。问题现象、原因分析、解决方式和验证证据完整保留在 `技术变更记录.md`；当前生效的银行解析规则见 `银行适配手册.md`。

后续排查必须先按问题或银行关键词搜索 `技术变更记录.md`，不得因为本状态摘要已精简而忽略历史踩坑。

# 2026-07-27 只读账户发现复跑与版式核验（chg-20260727-03）

- 以本地 HEAD 的实际状态为基线，未推送。对历史客户中中国银行对公、招商银行对公和招商银行个人定向重跑账户发现：中行对公 10 个目录、59 份已扫描文件、8 个可靠完整抬头账户；招商对公 3 个目录、11 份已扫描文件、1 个可靠完整抬头账户，另 3 份缺可靠抬头；招商个人 13 个目录没有可靠完整抬头账户，6 份来源形成 5 个不同掩码账户，另 7 份缺可靠抬头。
- 招商个人掩码账户专项验收：所有 5 个掩码账户均输出 `masked_case_account_included_with_warning`，且均不进入完整账号 v1C 候选或 v1D 双边配对。任何独立完整账号结果仍只来自可靠完整抬头和可靠完整对手账号。
- 原 20 份文件超时以 120 秒逐文件重跑：17 份完成；3 份仍为 `source_processing_timeout`，保留来源处理状态，不补号、不改解析或统计。
- 按 `StatementMetadata` 覆盖清单归并并优先核验两组同案多可靠抬头账户版式：工行对公 + 建行个人得到 7 组唯一双边候选、1 笔单边、3 笔歧义；农行个人 + 建行个人 + 中信个人得到 1 组唯一双边候选、17 笔单边、0 笔歧义。均仅为可复核候选，不作姓名、目录、摘要、掩码或资金关系推断。
- 产物：`D:\Investigator PDF\outputs\verification-runs\2026-07-27\history-customers-targeted-header-refresh\`、`history-customers-extended-timeout-rerun\`、`high-value-multi-account-layout-verification\`。
- 验证：`python -m unittest discover -s tests` 为 99 项通过；`python tools\regression.py --all --allow-missing` 为 71 PASS / 0 FAIL / 21 SKIP（20 个既有路径缺失、佛山农村商业银行 1 项主动暂缓）。
- 外部 Obsidian《阶段进展与续接记录》顶部快照已同步本轮结论；下一窗口仍以该快照和实际 Git 状态共同核验。

# 2026-07-27 延长超时来源可行性复核与补充候选筛选（chg-20260727-04）

- 3 份仍超时的微信来源均具有文字层，但页数分别为 1798、808、694；完整解析超过 120 秒，继续保持 `source_processing_timeout`。除非另行授权更大逐文件预算或外部转换路线，不再自动重跑。
- 对延长重跑后完成的 5 份来源所在目录刷新账户发现：3 个目录各仅有一个可靠账户；2 个目录各有建行个人与建行对公两个可靠账户，但双方均缺少相互完整对手账号覆盖。新增合格 v1D 高价值候选为 0。
- 未修改解析器、去重、统计或 v1C/v1D 边界；全量测试继续以 `chg-20260727-03` 的 99 项单元测试通过和 71 PASS / 0 FAIL / 21 SKIP 为基线。
- 产物：`D:\Investigator PDF\outputs\verification-runs\2026-07-27\history-customers-extended-timeout-rerun\remaining-timeout-feasibility.json` 与 `post-timeout-case-refresh\post-timeout-case-refresh-summary.json`。

# 2026-07-27 微信支付扣款银行流水独立关联（chg-20260727-05）

- 标准 JSON 升至 schema 1.7，新增独立观察 `wechat_payment_bank_debit_link_candidates`：微信侧必须有姓名、完整身份证号、微信号三项已确认归属和来源证据；银行侧必须有可靠完整抬头账户。微信支付银行卡尾号唯一对应、同日、精确同额、银行文字同时含“财付通/微信支付”及唯一商户组成部分时，才输出可复核候选；歧义不强配。
- 该观察不进入 v1C/v1D，也不表示本人账户互转、资金来源、资金闭环、实际控制或案件结论；输出不回显完整银行卡号、身份证号或微信号。支付宝关联固定保持“原件字段待确认、不自动匹配”。
- 已完成编译和 29 项相关单元测试；按本轮指令未运行全量单元测试和统一回归。截图构造的同额商户样例仅验证规则，真实双源 PDF 原件验收待用户提供可定位的成对来源。

# 2026-07-27 微信首页身份元数据与真实双源验收（chg-20260727-06）

- 微信解析器新增仅首页读取的身份三要素元数据：明确 `兹证明 / 居民身份证或身份证号 / 微信号`标签唯一且身份证完整时，才形成已确认微信来源；不把微信号或身份证写入标准结果 JSON，也不把微信文件创建为银行账户。
- 案件上下文自动携带该微信来源，同时为微信扣款观察单独携带可靠完整银行抬头账户；这不改变 v1C/v1D 的完整账号、对手账号或人工角色确认边界。
- 真实双源验收：一组微信+建行练习原件得到 26 个唯一关联、4 个歧义候选；另一组满足来源前提但无全条件交易候选。页面视觉抽检确认了微信身份/交易方式和建行完整抬头/财付通微信支付扣款文字。已通过 37 项相关测试；按指令未运行全量单元测试和统一回归。
# 2026-07-27 流水核查 MVP 第2轮本地进展

- 标准 JSON 升至 schema 1.8，新增受控关键词候选、逐来源行业文字搜索覆盖和下定前1/3/7日资金候选；全部直接读取现有标准交易和证据字段，不改解析总流程或交易模型。
- 任如冰真实案例已定位问界1万元支出，并列此前微信1万元同额收入和工行103.87万元大额收入；韩鹏飞真实三源已命中停车、工资、报销和申报单位候选。所有结果均为中性候选，不作资金来源或风险定性。
- 第1、2轮相关定向单元测试54项通过，语法编译和工作区格式检查通过；未运行全量单元测试或统一全量回归，未提交、未推送。
- v1C、v1D、微信银行扣款关联和支付宝关联继续冻结。下一轮为可授权、可降级的AI经营关联候选。
# 2026-07-27 流水核查 MVP 第3轮本地进展

- 标准 JSON 升至 schema 1.9，新增模型无关的AI经营关联观察接口；默认不调用模型，必须同时满足数据授权、留存策略、模型配置和调用适配器门槛。
- 显式申报单位/行业的可靠文字精确命中优先保留；模型返回只能引用真实交易和输入字段，虚构、漏项或非法分类整批拒绝。模型不可用不影响第2轮确定性结果。
- 三案例只读核对均返回 `ai_data_authorization_missing`，未进行网络传输；分别保留任如冰25笔、曹国民155笔、韩鹏飞47笔确定性直接命中。
- 实际模型提供方、部署位置、数据授权和留存策略仍需业务确认。第4、5轮可在AI降级状态下继续开发和验收。
# 2026-07-27 流水核查 MVP 第4轮本地进展

- 标准 JSON 升至 schema 1.10，补齐1万元大额清单、3万元入账后1/3/7日拆分转出与余额留存、日末余额四项统计、季度结息、收支Top 5、跨来源同名出现及明确用途候选。
- 任如冰工行得到52个日末余额日和2笔结息；韩鹏飞“张鑫”在微信、公户、个人建行三类来源可靠同名出现，主要企业对手和工资/报销/税费/货款等均可带证据展示。
- 所有资金路径只报告同来源时间金额和余额共现，不认定支出资金来源；同名出现不认定关系、归属或闭环。
- 第1至第4轮相关定向测试65项通过；未运行全量测试或统一回归，未提交、未推送。
# 2026-07-27 流水核查 MVP 五轮本地实现完成

- 标准 JSON 升至 schema 1.11，已完成案件上下文、受控关键词与覆盖率、AI防护/降级、资金余额观察、申报对照和Markdown验收视图。
- 任如冰、韩鹏飞两份本地Markdown报告已生成并抽检；客户资料、客户经理描述和调查报告角色分离，未发现/不可用不解释为申报虚假。
- 第1至第5轮相关定向测试68项通过，语法编译和工作区格式检查通过；未运行全量单元测试或统一全量回归。
- 当前仍未提交、未推送；v1C、v1D、微信银行扣款关联和支付宝关联未扩展。真实AI调用只允许在模型、数据授权、企业名称发送范围和留存确认均显式配置后进行。
- 2026-07-27：在用户明确选择 DeepSeek、允许发送企业/商户名称并要求启用授权后，新增官方 OpenAI 兼容的 `deepseek-v4-flash` 适配器。环境变量开关默认关闭；启用后按50笔分批、60秒超时调用，任何批次失败或结构不合格都整次丢弃模型候选，确定性结果不受影响。提供隐藏输入 Key 的当前 PowerShell 配置脚本和仅使用虚构数据的连通性检查；本进程无完整 Key，未执行真实网络调用。相关定向测试37项通过，未运行全量单元测试或统一回归，未提交、未推送。详见 `chg-20260727-12`。
- 2026-07-27：用户侧虚构数据连通性检查已返回 `connection=ok`。新增真实案例单批验收入口：排除确定性精确命中后从全部可发送候选中均匀抽取最多50笔，只调用一个批次，并输出逐笔分类、理由、使用字段和证据定位的本地Markdown；必须显式确认真实数据。抽样/适配器/降级相关定向测试15项通过；Codex进程无Key，未代用户执行真实调用。未运行全量测试或统一回归，未提交、未推送。详见 `chg-20260727-13`。
- 2026-07-27：任如冰真实AI单批已由用户侧成功执行：2075笔原交易中2050笔符合当前AI输入资格，均匀抽样50笔全部通过结构校验；0笔直接相关、3笔可能相关、7笔未发现关联依据、40笔无法判断。当前样本大量只有“转账/扫码付款/商户消费”等通用类型，信息密度偏低；3笔二维码收款仅凭收款方式被判为烟酒经营可能相关，依据不足。结论为暂不扩大完整调用，先收紧AI输入资格和提示边界，再做第二批质量验收。报告位于不可提交的 `outputs\mvp-acceptance-2026-07-27\任如冰_AI经营关联小批量验收.md`。
- 已据首批结果将提示词升至 `business-relevance-mvp-v2`：只有至少一个非通用可靠文字字段的记录才进入AI候选池；仅含转账、二维码收款、扫码付款、商户消费、红包、其他、普通汇入/汇款等值的交易留在本地，不调用模型。提示明确金额、方向和上述交易方式本身不能支持行业关联。相关定向测试22项通过；第二批50笔待用户侧执行。
- 2026-07-28：任如冰第二批已成功返回50/50笔，候选池由2050降至367，但50笔全部为无法判断；其中“金乡县明清酒水经营部”仍因“商户消费”被模型忽略，证明v2提示过度收紧。已升至 `business-relevance-mvp-v3`：整笔联合判断，通用交易类型只作上下文，不能抵消企业/门店名称、用途、备注、商品和商户类别的行业语义；仅有通用类型的交易才不送AI。
- 2026-07-28：新增本地AI字段覆盖审计和按来源平衡抽样。曹国民7192笔中155笔单位/行业确定性精确命中，另有1180笔具备AI语义字段：个人银行219、公户400、微信424、支付宝137；主要证据字段为企业对手989笔、备注238笔、商品说明137笔。该覆盖不等于行业相关。曹国民已替代任如冰作为正向AI质量案例；下一批50笔将四个来源各抽取约12至13笔。相关定向测试26项通过；未运行全量测试或统一回归，未提交、未推送。详见 `chg-20260728-01`。
- 2026-07-28：曹国民v3首批成功返回50/50笔，来源平衡为个人银行13、公户12、微信13、支付宝12；1笔直接相关、19笔可能相关、28笔未发现依据、2笔无法判断。环境治理项目材料费、建材、园林、塑木、护栏、环保科技等正向依据有效；仍发现实业公司+大额、贸易公司+货款、泛工业科技等过宽候选，以及可能相关理由写成直接相关。提示升至v4：金额、货款和泛化公司类型不能单独支持正向分类；正向结果必须引用语义字段，直接相关必须引用摘要/备注/用途/商品/类别之一。定向测试28项通过；待同一50笔A/B复验。
- 2026-07-28：曹国民v4同样本A/B复验成功：50/50笔结构合格，1笔直接相关、16笔可能相关、33笔未发现依据、0笔无法判断。贸易+货款和实业+大额已正确降级，环境治理材料费及建材/园林/塑木/护栏/环保等具体语义保留。用户确认泛化实业/贸易/工业科技等不能作为强相关，但可作为弱提示；完整调用前应新增结构化证据强度，而不是删除此类候选。
- 2026-07-28：曹国民1180个AI候选仅有337种唯一语义签名，843笔为重复企业/用途/商品组合；个人银行219笔仅27种、公户400笔147种、微信424笔92种、支付宝137笔73种。逐笔发送需24批，语义去重后约7批；若7192笔全量发送则约144批。结论为本地保留全部解析字段，AI仅使用行业语义白名单及必要上下文，并在完整调用前实现签名复用。当前未执行完整1180笔调用，未运行全量测试或统一回归，未提交、未推送。
- 2026-07-28：按用户确认“泛化企业信息可作提示但不能作强相关”，标准JSON升至schema 1.12，提示升至 `business-relevance-mvp-v5`。AI输入只保留企业/商户、非通用摘要、备注、用途、商品和类别；金额、日期、方向、银行名、交易方式等只留本地。可能相关结构化为 `medium/weak`，直接相关固定 `strong` 且须引用摘要/备注/用途/商品/类别。适配器已按语义签名只调用一次并展开回全部交易ID；小批入口也只抽取唯一语义签名。相关定向测试57项通过。待最后50种唯一语义验收，未执行完整调用、全量测试、提交或推送。

# 2026-07-28 流水核查MVP提交与续接快照

- 本轮以提交信息 `feat: close bankflow verification mvp` 统一收口：案件上下文、关键词/行业覆盖、下定前资金、大额与余额结息、Top 5、申报对照、Markdown验收、DeepSeek安全适配、证据强度和语义签名复用均进入同一提交；标准JSON为schema 1.12。
- 曹国民v4真实小批已通过结构验收：50/50笔有效，1笔直接相关、16笔可能相关、33笔未发现依据；v5代码已完成但尚未进行真实模型验收。
- v5下一步只运行50种唯一语义，不运行1180笔完整候选。命令见 `chg-20260728-02`；完成后先复核 `strong/medium/weak/none`、字段引用和重复签名展开，再决定是否执行约7批完整语义调用。
- 提交前语法编译通过；MVP、AI、结果导出、工行/微信新增字段相关定向测试89项通过。按用户边界未运行全量单元测试或统一回归。
- v1C、v1D、微信银行扣款关联和支付宝关联继续冻结；未推送。外部《阶段进展与续接记录》现行原件不在当前可写范围内，未改备份冒充现行记录；新任务须以本节、`INTEGRATION_CONTRACT.md`、`docs/流水核查MVP业务与验收矩阵.md`、`chg-20260728-02` 和实际Git状态共同核验。

# 2026-07-28 AI经营关联v5验收修正

- 曹国民v5真实小批50/50笔结构有效、四来源分布均衡，但“建材/园林景观设计 + 货款”被错误降为弱提示；同时审查发现通用摘要可能随同合格企业名称发送，适配器实际按完整字段字典而非统一规范化语义签名复用。v5因此未通过，未执行337种完整语义调用。
- 已将提示升至 `business-relevance-mvp-v6`：具体产品或服务优先为 `medium`，`货款`不得覆盖或削弱具体语义；只有缺少具体产品、服务、项目或用途时，泛化企业类型或货款才可为 `weak`。
- 出站AI字段现只保留实际语义证据，通用摘要不会因同笔存在企业名称而发送；抽样、覆盖统计和DeepSeek适配器统一使用同一规范化语义签名，相同语义只发送一个代表结果并展开回全部交易ID。
- 曹国民v6真实小批已通过：337个唯一语义中均衡抽取50个，四来源13/12/13/12，50/50结构有效；1个 `strong`、14个 `medium`、6个 `weak`，其余结果均为 `none`。此前两条“具体行业语义 + 货款”均修正为 `medium`，字段引用和直接证据门槛合格。
- 新增跨银行AI字段映射专项测试：不同银行同义表头映射到同一标准字段，AI只接收标准语义字段，未知混合列保持不可用。贷款能力不扩展，继续只使用现有受控关键词提示。
- 用户已单独确认进入337种完整语义的约7批调用。新增 `tools/run_ai_full_acceptance.py`：强制真实数据与完整调用双确认，输出337个唯一语义、预计批次、1180笔展开结果和逐笔交易ID/字段/证据；任一批失败或展开数不一致时失败关闭。
- 语法编译、97项MVP/AI/结果导出及相关字段定向测试、`git diff --check`均通过；纯本地曹国民覆盖审计仍为7192笔、1180个AI候选、337个唯一语义签名、843笔可复用重复候选，虚构传输模拟确认1180笔按337签名分7批并展开回1180个ID。未运行全量单元测试或统一回归，未提交、未推送；完整真实调用待用户在保留Key的原PowerShell执行。

# 2026-07-28 AI完整语义首次执行失败关闭与诊断补充

- 曹国民完整入口已实际执行结束：7192笔解析交易、1180个AI候选、337种唯一语义、预计7批；最终 `status=failed_closed`、`reason=ai_response_invalid`、`accepted_ai_results=0`。报告已落盘，但本次结果不得采用；`expected_provider_batches=7` 只表示预计批次，不能证明7批均成功。
- 原实现只保留总失败原因，未保存提供方原始响应，现有失败报告无法反推出具体批次或校验规则。继续禁止记录API Key、完整请求、原始响应或流水原文。
- 已将结构和业务契约校验前移到每个提供方批次：首个无效批次立即停止，不再继续消耗后续调用；终端与报告新增仅含批次号、批内序号和稳定规则名的 `failure_detail`。
- 新增回归覆盖无效JSON安全诊断、第二批分类枚举无效时立即停止及报告诊断展示。
- 用户确认后的诊断重试返回 `batch_2:item_1:direct_evidence_field_missing`，有效结果仍为0；本地重建第51个语义代表只含商户名称“王先生生鲜 超市”，没有直接证据字段，说明模型违反名称不能单独判direct的硬门槛，程序拒绝正确。
- 提示升至 `business-relevance-mvp-v7`：每笔附加仅由字段名派生的 `classification_constraints`；没有摘要、备注、用途、商品说明或商户类别时，`directly_related_allowed=false`。不新增客户字段，不改变输入白名单或其他MVP范围。
- 100项MVP/AI/结果导出及相关字段定向测试通过，语法编译和 `git diff --check` 通过；除用户侧已确认的诊断重试外，Codex未调用外部模型。未运行全量单元测试或统一回归，未提交、未推送。
- 下一步须由用户再次单独确认是否以v7重试。若仍失败，只依据新 `failure_detail` 定点修正；不得盲目连续重跑。
- v7完整重试结构成功：337种唯一语义全部完成并展开为1180/1180个唯一交易ID；103笔direct/strong、490笔medium、206笔weak、381笔none，字段白名单、分类强度、直接证据字段和重复签名一致性机械检查均无错误。
- 语义验收未通过：83种唯一direct语义中出现餐饮、便利店、话费充值、银行年费、医疗和打车等明显非申报行业内容。案件上下文实际传入的 `declared_industries=[]`，只有单位“河南省润恒环保工程有限公司”；模型把“存在具体产品/服务”误当成“与申报行业直接相关”。
- 本地资料另有明确原文“客户主要是做建筑材料批发投资的，还有其他生意”，但当前案件上下文未提取该行业描述。是否将“工作介绍及收入情况”中的明确业务描述作为带来源、待核实的申报行业上下文，需先确认口径；确认前不修改提取规则、不再次调用模型。
- 用户确认“工作介绍及收入情况”中的明确工作内容可纳入申报行业上下文，同时只处理工作内容，忽略“其他生意”、信用卡用途和其他备注。案件上下文现从明确“主要是做/从事/经营”句式提取工作内容，并保持 `customer_manager_description` / `unverified` 来源属性；曹国民得到“建筑材料批发投资”。
- 提示升至 `business-relevance-mvp-v8`：所有正向分类必须与申报行业或工作单位明确体现的行业语义相关；餐饮、便利店、话费、银行年费、医疗、打车等无关生活或通用服务应为 `no_relation_evidence/none`。生活轨迹与单位/住址匹配另行冻结到后续，本轮不实现。
- 真实案件纯本地复核仍为7192笔、1180个AI候选、337种唯一语义，说明上下文补充未改变发送交易范围。101项相关定向测试、语法编译和 `git diff --check` 通过；未调用外部模型，未运行全量单元测试或统一回归，未提交、未推送。
- 下一步只进行v8的50种来源平衡小批复验；通过前不再次运行337种完整语义。
- v8来源平衡小批已成功返回50/50，四来源13/12/13/12；生活类分离目标达到，医院、餐饮、便利店、话费、银行年费、打车、保险、汽车等均为 `no_relation_evidence/none`，环境治理项目材料费和栏杆款为direct/strong。
- v8语义仍未通过：`江山市杰成建材有限公司+货款`、`园林景观设计有限公司+货款`再次被降为weak，违反已确认的具体产品/服务medium优先级；无具体课题对象的“横向课题技术咨询费”被判medium也偏宽。
- 提示升至 `business-relevance-mvp-v9`：对建筑材料/环保工程上下文，建材、护栏、栏杆、围栏、塑木、园林景观设计固定作为具体相关产品/服务，货款不得降级；泛化咨询费、材料费、采购款缺少具体对象时不得仅凭可能性判medium。
- 101项相关定向测试、语法编译和 `git diff --check` 通过；v9尚未调用外部模型。下一步仍只做50种小批复验，不直接运行完整337种。
- v9真实小批已通过：337种唯一语义中来源平衡抽取50种，四来源13/12/13/12且50/50结构有效；1笔direct/strong、12笔medium、2笔weak、35笔none，字段白名单、分类强度、理由和证据定位机械检查零问题。
- “建材+货款”“园林景观设计+货款”均为medium；“新材料+货款”和泛化工业科技为weak；无具体课题对象的技术咨询费为undetermined/none；医院、餐饮、便利店、话费、银行年费、打车等继续为no evidence/none。v9小批语义验收通过。
- 下一步如需取得完整v9结果，必须由用户另行确认337种唯一语义调用；不得沿用此前确认自动重跑。
- 用户确认的v9完整调用在第5批第4项失败关闭：`batch_5:item_4:direct_evidence_field_missing`，有效结果0，后续批次未继续。纯本地重建该代表为“贵州荣盛（集团）建材有限公司 + remark=M0EEHDNH”。
- `M0EEHDNH`为纯字母数字代码，不含行业语义，却使字段级direct许可错误开启；模型随后只引用企业名称判direct，外层校验正确拒绝。v10在AI语义入口排除此类纯字母数字备注代码，原交易原文不变；包含“货款”等真实文字的备注不受影响。
- 纯本地覆盖审计更新为7192笔、155笔确定性精确命中、1157个AI候选、325种唯一语义、832笔可复用重复候选；预计仍为7批。公户AI候选由400降至377、唯一语义由147降至135，减少的均为没有其他语义字段的代码备注记录。
- 102项相关定向测试、语法编译和 `git diff --check` 通过；v10未调用外部模型。下一次完整调用须用户重新确认。

# 2026-07-28 AI完整语义结构性调整（chg-20260728-05）

- 已暂停v10完整真实语义调用并升至 `business-relevance-mvp-v11`。根因不是单个代码备注，而是硬边界分散在提示词和适配器、完整验收首项失败即终止、没有原始响应缓存，以及小批样本未固定。
- 曹国民4份流水的旧验收范围仍为7192笔、155笔确定性精确命中、1180笔候选和337种唯一语义。纯本地审计中38种改为确定性处理；余下299种仍有模型边界，代码清洗后合并为1115笔、286种实际送模语义。旧337种的最高允许强度为strong 107、medium 167、weak 25、none 38；direct允许107、禁止230。
- 候选层统一识别纯数字、纯字母数字、订单号、参考号和哈希类内容，原交易及证据不变；每项由代码生成 `directly_related_allowed` 和 `maximum_allowed_strength`。只有企业名称不能direct，纯代码不构成用途证据，明确餐饮、便利店、话费、银行年费、医疗和打车等生活类直接归none且不进入模型。
- 模型职责收缩为对剩余标准字段语义返回 `strong / medium / weak / none / undetermined`；本地代码派生分类并执行硬约束。业务项不合格时拒绝该项、继续全部批次、最终聚合失败类型和代表样本；整轮仍失败且不采用任何模型结果。网络、鉴权、超时等系统错误仍可立即终止。
- 新增按提供方、模型、提示版本、语义签名和实际输入指纹保存的无凭据缓存，包含请求正文、原始响应和校验结果；校验/展开变化先离线重放，只有实质输入变化才重新调用受影响语义。固定 `development` 50种与独立 `reserved_acceptance` 50种已建立，小批入口不再临时换样本。
- 经营判断仅消费统一字段 `counterparty_name / summary / remark / purpose / product_description / merchant_name / merchant_category`；交易类型、交易方式和支付方式只可参与本地保守排除，不构成行业证据。逻辑不读取银行名、原始表头、PDF列位置或曹国民固定字段组合；缺失字段不回退全文扫描，不拼接制造原件不存在的表达。
- 跨来源本地契约测试覆盖银行个人、银行对公、微信来源标签，以及同一语义在摘要、备注、用途、商品字段、只有企业名称、纯代码、生活类、字段缺失/重复/冲突等组合。支付宝仅沿用已确认标准字段，本轮不补解析器。
- 历史v7已渲染结果离线重放1180条，当前聚合90项失败：65项已本地排除、10项引用已过滤字段、15项超过最高允许强度；1090项单项合格但整轮采用0。旧报告不含原始提供方响应，真正原始响应级缓存重放从下一次获准调用开始。
- 当前只能区分表述为：曹国民4份流水已有历次真实语义结果；统一字段层已完成跨来源纯本地契约验证；其他客户、其他银行和其他版式尚未完成代表真实样本验收，不能称为所有银行均已支持。
- 本轮未调用任何外部模型，未运行固定50种真实小批或完整真实调用，未提交、未推送。下一次真实调用必须在全量本地测试、离线重放及格式检查通过后，由用户另行确认固定50种小批；小批通过且缓存可离线复算后，完整范围调用还需再次单独确认。
- v11检查点前敏感数据预检确认原19个改动文件不包含缓存或真实客户验收产物；默认 `output/ai-response-cache` 已处于Git忽略范围，并补充忽略 `outputs/`、`.bankflow_ai_cache/` 和 `ai-response-cache/`。缓存不保存请求头或API Key，只保存经营任务允许发送的标准语义输入、经营上下文、原始响应和校验结果。
- 缓存及AI载荷显式增加 `task_type=business_relevance`，并纳入缓存命名空间和输入指纹。后续生活轨迹使用独立 `task_type=life_trajectory`；当前仅在 `docs/流水核查MVP后续路线图.md` 记录后续里程碑，没有实现生活轨迹规则、提示词、模型调用、测试样本或结果schema。
- 检查点最终验证：语法编译通过，全量单元测试185项通过，统一回归71 PASS / 0 FAIL / 21 SKIP，`git diff --check`通过；新增的第185项专门验证不同 `task_type` 不会复用同一签名缓存。
