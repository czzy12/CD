# PDF流水项目当前状态

更新时间：2026-06-26

## 当前仓库

源码仓库：

```text
D:\Codex data\CD
```

当前分支：

```text
work/2026-05-31-flow-adjustment
```

远端跟踪：

```text
origin/work/2026-05-31-flow-adjustment
```

当前最新本地提交：

```text
当前提交见 git log -1
```

最近重要变更：

- `PROJECT_CONTEXT.md`：轻量项目入口。
- `PROJECT_STATUS.md`：当前状态概要，只保留必要过程和结果。
- `INTEGRATION_CONTRACT.md`：与车贷报告自动化总项目的集成契约。
- `技术变更记录.md`：按日期记录详细问题、原因、解决方式和验证结果。
- 2026-06-03：PDF GUI 的“佐证填写”成功路径不再弹出“已导出/已自动打开”提示框；只有无法自动打开 Word 收入佐证填表时才提示。
- 2026-06-06：修复建设银行对公 `账户明细信息` 17 列表被误判为 `活期存款明细账` 分支，导致多页流水解析为 0 或只解析到少量交易的问题。
- 2026-06-06：新增中信银行对公 `账户交易明细` 分列表格解析，并纳入回归。
- 2026-06-06：GUI 可信度不再显示百分比，只显示 `极高 / 高 / 中 / 低 / 未识别`；完全识别且无异常显示 `极高`；内部评分提高收支余额闭合校验权重，并增加最近两个月月度闭合且无复核加分。
- 2026-06-06：GUI 移除顶部 `导出 Excel` 按钮，收窄并压缩右侧 `流水调整` 面板；PDF/Excel 解析库改为按需加载，缩短窗口冷启动等待。
- 2026-06-07：城商行目录首批确认 5 家独立适配：九江银行、佛山农村商业银行、兰州银行、宁波银行、南京银行对公；天津银行、莱阳农商银行、河北农信样本保留为待单独适配。
- 2026-06-08：补充农行 `自动抹账` 按余额差修正方向、交通银行借贷方向与金额正负号兼容、总收支余额闭合复核提示，并进一步压缩 GUI 窗口和流水调整面板。
- 2026-06-09：新增招商银行对公 `账务明细清单 / Statement Of Account` 独立适配，`D:\Codex data\CD_assets\PDF流水\账务明细清单` 目录 6 份样本纳入回归。
- 2026-06-12：Excel 导入新增 HTML 伪 `.xlsx` 兜底，支持中国银行 PDF 转 Excel 文件 `记账日期 / 记账时间 / 金额 / 余额` 版式。
- 2026-06-12：新增中原银行 `账户交易流水` 独立适配，支持 `金额 + 收支状态 + 余额` 表格版式。
- 2026-06-13：新增成都农村商业银行对公 `客户明细` 独立适配，支持表格列内换行的 `借方金额 / 贷方金额 / 余额` 版式。
- 2026-06-14：完成 `流水0613` 专项 PDF 适配：交通银行对公 15 份、上海银行 4 份、华夏银行 1 份、平安银行 4 份、兴业银行 1 份均纳入回归；排除 Excel 和图片型 OCR/PCR 后，目录内 28 个 PDF 均可识别并解析。
- 2026-06-14：PDF GUI 新增 `工资佐证` 入口，导出 `.salary_income_proof.json` 供收入佐证 Word 项目填写工资类模板；工资类流入只统计工资/薪资/薪酬/奖金/奖/代发工资等入账，并排除补助、补贴、补增资、报销、退款；流出仍按个人账户全量支出统计。
- 2026-06-18：新增桂林银行对公 `企业客户交易清单` 独立适配，支持坐标抽取收入/支出/余额分列，并保留负数支出冲正符号。
- 2026-06-18：经营佐证/工资佐证导出接入 GUI 当前流水调整参数；启用收支平衡调整后，佐证 JSON 的月度收入和支出金额使用调整后口径。
- 2026-06-21：微信流水模拟展示改为按原始支出形态分布支出，并保证累计模拟余额保持小额正数；手动增加收入后沿用同一口径。
- 2026-06-21：新增重庆银行个人 `账户交易明细` 独立适配，支持 `交易日期 / 交易金额 / 活期账面余额` 表格版式。
- 2026-06-21：收入佐证导出兼容 PDF 转 Excel 流水与正常 PDF 流水混合导入；Excel 来源账号不再写成 `Excel文件银行`，改由 Word 佐证按未识别银行兜底生成。
- 2026-06-23：修复工商银行对公同秒多笔交易顺序恢复和负数借方冲正汇总口径；样本 `工行对公版式其他.pdf` 解析 2020 笔，0 异常。
- 2026-06-24：新增农村商业银行对公 `客户账户明细对账单` 8 列表格版式适配；样本 `公户_0.pdf` 解析 322 笔，0 异常；PDF 文本层未显示银行名，银行名由用户确认。
- 2026-06-25：修复中国银行 PDF 图片转 Excel 收入筛选流水口径；按页 `贷方发生数` 校正 OCR 少识别金额，并将筛选流水余额标记为参考余额，样本 `流水中行(1).xlsx` 解析 63 笔收入，0 异常。
- 2026-06-26：新增浙江网商银行对公 `企业账户交易明细` 独立适配；样本 `网商.pdf` 解析 198 笔，0 异常。
- 2026-06-26：GUI 固定浅色 Qt 调色板和表格/禁用态前景色，避免 Windows 深色模式下表格选中文字、禁用控件文字显示异常。
- 2026-06-26：GUI 默认日期范围改为每月 15 日及以后包含当月的近 6 个月；例如 6 月 26 日默认使用 1 月至 6 月。
- 2026-06-26：新增天津农村商业银行对公 `交易明细查询` 独立适配；样本 `ef6816a9e9f14d99950b6f2cd12610b91782392878046.pdf` 解析 383 笔，0 异常。

详细记录索引：

- `技术变更记录.md`：重要变更的问题、原因、解决方式和验证结果。
- `银行适配手册.md`：银行格式规则、解析入口和适配注意事项。

## 当前能力概览

当前项目已经具备：

- PDF 银行流水识别。
- Excel 流水导入。
- HTML 伪 `.xlsx` 流水导入兜底。
- 微信流水识别。
- 多银行专用解析器。
- 通用 PDF 兜底识别。
- 余额连续性校验。
- 月度统计。
- 文件汇总。
- 异常提示。
- GUI 可信度提示。
- 流水调整/测算层。
- GUI。
- Excel 导出。
- 收入佐证 JSON 导出。
- 工资类收入佐证 JSON 导出。
- 统一回归测试。
- PyInstaller 打包。

## 当前关键模块

| 模块 | 文件 |
|---|---|
| 标准交易模型 | `bankflow_v2/models.py` |
| 银行识别 | `bankflow_v2/auto_detect.py` |
| 解析入口 | `bankflow_v2/pipeline.py` |
| 汇总统计 | `bankflow_v2/summary.py` |
| 流水调整 | `bankflow_v2/adjustment.py` |
| Excel 导入 | `bankflow_v2/excel_input.py` |
| GUI | `gui_v2.py` |
| 统一回归 | `tools/regression.py` |
| 回归样本 | `tools/regression_cases.json` |

## 当前支持银行与格式

以 `银行适配手册.md` 为详细准则。

当前主要覆盖：

- 国有行和股份行常见个人/对公 PDF：工商、建设、农业、中国、交通、邮储、招商、兴业、平安、民生、中信、浦发、上海、华夏。
- 城商行/农商行专项格式：齐鲁、成都农商、九江、佛山农商、兰州、宁波、南京、中原、桂林银行、重庆银行等。
- 微信流水、Excel 导入、HTML 伪 `.xlsx` 导入和通用 PDF 兜底。

## 最新功能状态

### 流水调整/测算层

已实现：

- `bankflow_v2/adjustment.py`
- GUI 右侧流水调整面板。
- 收入调整（微信）。
- 收支平衡调整（个/公）。
- 固定分配/确定性随机分配。
- 调整后月度统计。
- Excel 导出保留原始和调整后统计。

调整层不修改原始流水明细，不污染银行解析器。

### GUI 现代化

已实现：

- 顶部主次操作栏。
- 日期筛选模块。
- 左主内容区 + 右调整面板。
- 摘要仪表板。
- 调整预览结果卡。
- 下划线风格 tabs。
- 顶部可信度字段，显示 `极高/高/中/低/未识别`，不显示具体百分比。
- 完全识别且没有异常、没有复核提示时显示 `极高`。
- 顶部工具栏保留 `佐证填写`，不再显示 `导出 Excel` 按钮。
- 月度统计 GUI 隐藏期初/期末余额两列。
- 调整启用后，月度统计仍保留按流水类型分组显示，下面再追加 `全部` 调整统计。
- GUI 不再显示 `流水明细` tab；Excel 导出仍保留明细。
- `日期范围内没有流水` 不参与可信度评分。
- 收支余额闭合校验：`收入合计 - 支出合计 = 期末余额 - 期初余额` 作为强可信加分，不闭合则扣分。
- 汇总层补充总收支余额闭合复核提示：当总净额和期末/期初余额变动不一致时，复核清单提示 `收支余额不闭合`。
- 最近两个月都有流水且月度收支余额闭合、无复核时额外加分；没有最近两个月流水仍不扣分。
- 微信流水不要求期初/期末余额，天然无余额列不影响可信度等级。
- 未识别且通用识别也没有流水时，可信度显示为 `未识别`，不再显示 `低 0%` 或弱可信基础分。

### 收入佐证 JSON 接入

已实现：

- GUI 可导出收入佐证 JSON。
- 默认按近 6 个完整月份筛选。
- 个人、微信、对公流水分组。
- 民生对公可识别为 `cmbc_corp`，明确标签后的 8-22 位账号可识别。
- 同一类型同一账号的多份 PDF 在 `accounts[]` 中只保留一组。
- 账户去重不影响交易明细和月度汇总。
- 源码 GUI 按钮已改为“佐证填写”，自动保存 `.income_proof.json` 到桌面 `银行流水解析结果` 文件夹，并打开 Word 填写表预填 JSON。
- 期末余额按同一流水类型同一账号取最新交易日期的余额，避免同账号多份 PDF 的期末余额叠加。
- 微信流水默认输出按模拟余额口径展示和导出：从 0 余额开始，按原始支出形态分布支出，保证累计模拟余额保持小额正数；收入调整后沿用同一口径。
- 交通银行个人流水账号识别支持中英混排账号标签。
- 交通银行借贷方向兼容金额正负号，避免方向列和金额符号同时表达时收入/支出反向。
- 农业银行 `自动抹账` 明细按余额差识别冲正方向，避免余额连续但方向相反时误判。
- 建设银行对公 `中国建设银行账户明细信息` 17 列表恢复完整解析。
- 中信银行对公 `账户交易明细` 支持 `借方发生额 / 贷方发生额 / 余额` 分列表格。
- 招商银行对公 `账务明细清单` 支持 `YYYYMMDD + 业务类型 + 带正负号金额 + 余额` 文本行解析。
- 中国银行 PDF 转 Excel 的 HTML 伪 `.xlsx` 可通过 Excel 导入解析，支持 `记账日期 / 记账时间 / 币别 / 金额 / 余额` 列。
- 中原银行 `账户交易流水` 支持表格层 `交易日期 / 交易时间 / 金额 / 收支状态 / 余额` 解析。
- 成都农村商业银行对公 `客户明细` 支持表格列内换行的 `借方金额 / 贷方金额 / 余额` 分列解析。
- 桂林银行对公 `企业客户交易清单` 支持坐标抽取 `交易日期 / 对方账号 / 对方户名 / 收入 / 支出 / 余额`，保留负数支出冲正符号。
- 重庆银行个人 `账户交易明细` 支持表格层 `交易日期 / 交易金额 / 活期账面余额 / 交易类型/摘要` 解析。

注意：当前未重新打包 EXE；`启动GUI.bat` 通过 `python gui_v2.py` 启动源码 GUI，因此本地启动 GUI 会使用最新源码。`dist\BankFlowGUI\BankFlowGUI.exe` 不代表当前最新源码状态。

## 当前验证结果

最近执行：

```powershell
python -m py_compile bankflow_v2\chongqing.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py bankflow_v2\adjustment.py bankflow_v2\income_proof_export.py gui_v2.py tools\regression.py
python tools\regression.py --all --allow-missing
```

结果：

```text
通过
```

最近执行：

```powershell
python tools\regression.py --all --allow-missing
```

结果：

```text
54 PASS / 0 FAIL / 15 SKIP
```

最近专项验证：

- `流水0613`：交通银行对公 15 份、上海银行 4 份、华夏银行 1 份、平安银行 4 份、兴业银行 1 份均通过；排除 Excel 和图片型 OCR/PCR 后，28 个 PDF 均已识别并解析。
- 桂林银行对公 `企业客户交易清单`：1 份样本通过，200 笔，0 异常。
- 重庆银行个人 `账户交易明细`：1 份样本通过，192 笔，0 异常。
- 当前剩余排除项：`流水0613\公户_0(2).pdf` 为图片型 PDF，需要 OCR，按本阶段要求不解析。

说明：15 个 SKIP 均为本机缺少桌面路径样本文件，不是解析逻辑失败。同类情况可按：

```powershell
python tools\regression.py --all --allow-missing
```

视为样本缺失跳过，目标结果应为：

```text
54 PASS / 0 FAIL / 15 SKIP
```

## 当前不支持或不作为稳定核心

- 通用扫描/图片 PDF 直接 OCR；当前仅对固定结构的“中国工商银行账户明细清单”九列对公版式提供专项离线 OCR。
- 工资类完整原字段专项导出。
- 完整车贷报告生成。
- 征信说明。
- 企业信息说明。

图片 PDF 当前建议：

```text
先用外部工具转 Excel -> 再导入本项目
```

## 与车贷报告自动化总项目的关系

本项目后续是总项目中的流水子模块。

推荐链路：

```text
PDF / Excel / 微信流水
  -> Transaction
  -> Summary / Adjustment
  -> 标准 JSON
  -> 收入佐证 Word
  -> 车贷报告自动化总项目读取
```

总项目不应重新实现流水识别。

## 下一步优先级

### 已完成

- 修复未识别且通用识别也没有流水时的可信度口径：`D:\Codex data\CD_assets\PDF流水\打包测试\城商行\兰州银行.pdf` 验证为 `低 0%`。只改 GUI 可信度显示，不影响导出。

### 第一优先级

新增标准 JSON 导出：

```text
bankflow_v2/result_export.py
```

建议接口：

```python
build_bankflow_result(transactions, adjustment_result, metadata) -> dict
write_bankflow_json(result, path) -> None
```

### 第二优先级

新增收入佐证 Word 填写：

```text
bankflow_v2/word_fill.py
templates/收入佐证模板.docx
```

Word 填写只读取标准结果，不直接解析 PDF。

### 第三优先级

GUI 增加：

```text
导出收入佐证 Word
导出标准 JSON
```

Excel 导出保持不变。

### 第四优先级

为 JSON 和 Word 输出补充测试。

建议测试：

```text
固定输入交易/调整结果 -> 输出 JSON -> 校验关键字段
固定 JSON -> 填写 Word -> 校验关键占位符替换
```

## 更新文档规则

后续任何重要更新：

1. 更新本文件。
2. 更新 `技术变更记录.md`。
3. 涉及银行适配时更新 `银行适配手册.md`。
4. 涉及总项目接口时更新 `INTEGRATION_CONTRACT.md`。

## 2026-06-29 多格式流水适配收尾

- 新增/修复格式：
  - `明细账查询` 通用表格：按坐标列解析无时间列流水。
  - 中国银行个人：`中国银行交易流水明细清单`。
  - 桂林银行个人：桂林银行 7 列收支明细表格。
- 同步修正 GUI/收入佐证流类型口径：个人 bank id 但户名明显为企业/商户时，收入佐证按对公归类。
- 已做单样本解析、月度汇总和余额连续性校验；暂未跑完整回归。

## 2026-06-29 问题流水0623 批量修复

- 修复目录：`D:\Codex data\PDF流水\问题流水0623`，并解压纳入 `流水(4).zip` 内样本。
- 本轮新增/修复：
  - 工商银行对公 `账户明细清单`：按 `转入金额/转出金额/余额` 表格解析，并优先识别为 `icbc_corp`。
  - 农业银行个人 `对私客户账户明细`：支持 7 列表格。
  - 农业银行对公无表格线账户明细：增加坐标列解析兜底。
  - 交通银行对公 `明细对账单`：省分行名称泛化识别；负借方/负贷方按冲正方向处理。
  - 工商银行个人历史明细：过滤混入人民币流水的外币行。
- 验证：
  - `python tools\batch_validate.py "D:\Codex data\PDF流水\问题流水0623" --out "D:\Codex data\PDF流水\问题流水0623\batch_validate_final.xlsx"`：31 个 PDF 中 30 个通过、1 个图片 PDF 忽略，异常明细 0。
  - `python tools\regression.py --all --allow-missing`：pass=54, fail=0, skip=18。
- 注意：`公户_0(2).pdf` 是杭州银行纯图片 PDF，无文字层；当前未接 OCR，保持图片 PDF 忽略口径。

## 2026-06-30 农业银行个人抹账补充

- 样本：`ca456ce48f8c45599de15e51b92cc1231782713074441.pdf`。
- 问题：`抹账` 行金额显示为负数，但余额链实际回增；旧逻辑只覆盖 `自动抹账`，导致该行误计支出并触发余额不连续。
- 修复：`bankflow_v2/abc.py` 将农业银行个人冲正方向修正扩展到 `抹账/自动抹账`。
- 验证：
  - 目标 PDF：753 笔，异常 0，收支净额与期初/期末余额闭合。
  - `python -m py_compile gui_v2.py tools\regression.py bankflow_v2\abc.py`：通过。
  - `python tools\regression.py --tag abc --allow-missing`：pass=1, fail=0, skip=0。
  - `python tools\regression.py --all --allow-missing`：两次运行分别在 120 秒和 300 秒超时，未返回失败结果。

## 2026-06-30 光大银行个人/对公适配

- 样本目录：`C:\Users\lenovo\Desktop\王晓晨`。
- 新增解析器：`bankflow_v2/everbright.py`。
- 新增 bank id：
  - `everbright`：中国光大银行个人账户明细查询清单。
  - `everbright_corp`：中国光大银行对公账户对账单。
- 验证：
  - `流水.pdf`：252 笔，收入 35,047,481.94，支出 35,497,036.26，异常 0。
  - `流水2.pdf`：51 笔，收入 2,700,211.68，支出 2,601,712.00，异常 0。
  - 导出文件：`C:\Users\lenovo\Desktop\王晓晨\王晓晨_光大流水解析结果.xlsx`。
  - `python -m py_compile gui_v2.py tools\regression.py bankflow_v2\everbright.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py`：通过。

## 2026-06-30 兴业强水印 PDF 与手工 Excel 明细

- PDF 样本：`C:\Users\lenovo\Desktop\芦桂春\eb1ce44defde40518cbe409a52da74d31782805563325.pdf`。
- PDF 自动识别：新增兴业银行强水印交易明细识别规则；受水印污染影响，PDF 直解析仍需复核。
- 手工明细：`C:\Users\lenovo\Desktop\芦桂春\eb1ce44defde40518cbe409a52da74d31782805563325.xlsx`。
- Excel 导入修正：支持 `支/收`、`收/支` 表头；方向列明确时交易金额按绝对值拆收入/支出。
- 手工明细验证：286 笔，收入 618,653.80（136 笔），支出 609,423.87（150 笔），净额 9,229.93，期初 0.00，期末 9,229.93，异常 0。
- 导出文件：`C:\Users\lenovo\Desktop\芦桂春\芦桂春_兴业流水解析结果_手工明细通过.xlsx`。

## 2026-07-03 对公客户账户明细与 Excel OCR 余额修复

- 新增 `customer_detail_corp`：识别并解析 `对公客户账户明细`，字段为 `交易日期 / 交易发生金额 / 账户余额 / 对方账号 / 对方户名 / 摘要 / 备注`。
- 样本：`C:\Users\lenovo\Desktop\问题流水\账户交易明细pdf打印20260702145216ve2l.pdf`、`账户交易明细pdf打印202607021452339fc8(1).pdf`。
- 验证：两份合计 133 笔，收入 2,604,239.34，支出 2,439,338.63，期初 254,422.48，期末 419,323.19，异常 0。
- Excel 导入修复：对普通四列表 Excel 复用余额链，修正 `58B,129.43`、`4,D81,288.27` 等 OCR 余额，并按余额变动反推缺失收支方向。
- Excel 样本：`C:\Users\lenovo\Desktop\张清20260702131511\基础资料\银行流水_0.xlsx`，验证 161 笔，收入 8,691,971.30，支出 8,508,562.75，异常 0。

## 2026-07-03 GUI 月均利润测算

- 流水调整面板隐藏调整月份选择和 `固定分配` 控件；调整月份改为跟随顶部筛选日期，未启用筛选日期时覆盖全部解析月份。
- 新增 `月均利润测算`：手工输入开具月收入（万元）和利润率（%），显示流水月均收入、利润率后月收入，并在利润率后月收入低于开具月收入时标红提示。
- 利润率校验区改为紧凑行式展示，右侧面板增加滚动容器，避免窗口未最大化时文字被裁切。
- 顶部工具栏不再显示 `选择文件` 按钮，日期筛选文案改为 `筛选` 并去掉 `从/到` 标签。
- 验证：`python -m py_compile gui_v2.py` 通过；离屏实例化 `MainWindow` 通过。

## 2026-07-04 建设银行个人筛选流水适配

- 样本：`C:\Users\lenovo\Desktop\李红梅\3a08cdc3597c48bc8560cd8d218a6f6e1783050410937.pdf`。
- 版式：`中国建设银行个人活期账户交易明细`，页眉包含 `【筛选】 交易金额:10000.00及以上`，页脚说明不保证交易连续。
- 识别仍使用 bank id `ccb`；新增识别关键词 `中国建设银行个人活期账户交易明细`。
- 解析时将该筛选流水余额列标记为参考余额，跳过相邻余额连续性和总闭合校验，避免把筛选造成的不连续误报为异常。
- 验证：244 笔，收入 13,393,755.82，支出 11,263,623.25，异常 0；已加入回归样例 `建设银行个人-活期账户交易明细筛选流水-李红梅`。

## 2026-07-05 问题流水0701 批量解析

- 目录：`D:\Codex data\PDF流水\问题流水0701`。
- 新增版式：
  - `alipay`：支付宝支付科技有限公司交易流水证明，续页无表头时按 8 列固定结构继续解析，余额列缺失，跳过余额闭合。
  - `shengjing`：盛京银行个人交易流水。
  - `xingtai`：邢台银行账户交易明细。
- 修复版式：
  - 建行对公 `中国建设银行账户明细信息` 优先识别为 `ccb_corp`，避免被对方开户行里的民生关键词误判。
  - 平安银行个人清单识别放宽，支持 `B/A/P` 水印干扰。
  - 招商银行个人流水允许缺失交易摘要的行，避免漏抽导致余额不连续。
  - 兴业银行强水印交易明细识别为 `cib`；该版式余额受水印污染，标记为参考余额，跳过余额连续性和总闭合校验。
- Excel 转表导入：支持 `YYYY-MM-\nDDHH:MM:SS` 断行日期；无余额列 Excel 自动标记余额可选；方向列存在但为空时按不计收支处理。
- 验证：`python tools\batch_validate.py "D:\Codex data\PDF流水\问题流水0701" --out "D:\Codex data\PDF流水\问题流水0701\batch_validate_final.xlsx"`：28 个 PDF 中 24 个通过、4 个图片 PDF 忽略，异常明细 0。`简化版产品大纲.pdf` 按用户要求不处理。

## 2026-07-06 汉口银行对公 Excel 转表导入

- Excel 导入新增汉口银行对公 `账户交易明细查询` 专项分支，支持页眉含 `交易笔数 / 贷方交易合计金额 / 借方交易合计金额` 的图片 PDF 转 Excel 表。
- 解析按 `序号 / 交易日期 / 交易时间 / 收入金额 / 支出金额 / 余额` 定位列，重复页表头自动跳过，页眉合计用于校验。
- 对少量小数点丢失的余额，仅在余额链期望值与原始数字完全匹配时校正。
- 验证：同账户 6 份 Excel 合计 634 笔，月度及合并余额闭合，异常 0。
- GUI 混合导入 Excel 与 PDF 时，Excel 转表会按解析标签识别对公/个人类型；`流水明细` tab 恢复显示，方便逐笔核对。

## 2026-07-06 农村商业银行个人交易明细

- 新增 `rural_commercial`，支持 `卡号/账号 / 客户名称 / 总收入 / 总支出` 页眉和 `序号 / 摘要 / 币别 / 钞汇 / 交易日期 / 交易金额 / 账户余额` 文本行版式。
- 当前样本 `银行流水_0(8).pdf` 实际 30 页，页脚显示总页数 45 页；解析现有 570 笔余额连续，但保留缺页及页眉合计不一致提示 5 条。

## 2026-07-09 GUI 收入测算提示和表格外壳

- `月均利润` 不足提示增强：当占股/利润率后收入低于系统月收入时，除显示低于金额外，额外提示需增加的月均流水收入和建议填写的调整金额。
- `调整后净额` 文案改为 `调整后收支差额`，更贴近流水调整用途。
- 表格四角问题确认：`QTableWidget` 内部 viewport、header、corner 等子控件不会被父级 `border-radius` 裁剪，单纯修改 stylesheet 圆角无法稳定解决四角小块。
- GUI 已将 `月度统计 / 文件汇总 / 流水明细 / 异常提示` 四个表格包入外层 `QFrame#FlowTableShell`，由外壳负责圆角、边框、底色，内部 `QTableWidget` 取消外框和圆角，只负责表格内容。
- 后续建议：报告工作台嵌入 PDF 流水窗口时，不应长期在 `D:\report` 内覆盖大量表格和控件细节样式；应在本项目提供统一主题入口，由报告工作台只传颜色 token。若外壳结构后仍有极小角块，再考虑 Qt mask/自绘裁剪。
- 验证：`python -m py_compile gui_v2.py`、`git diff --check -- gui_v2.py` 通过；PyQt offscreen 冒烟确认页签内容为 `FlowTableShell`，`monthly/details` 表格 parent 均为 `FlowTableShell`。

## 2026-07-09 嵌入主题入口下沉

- `gui_v2.py` 新增 `MainWindow.apply_embedded_theme(colors, dark=False)`，由 PDF 流水项目统一生成嵌入态 stylesheet。
- 嵌入主题入口覆盖表格外壳、表格内部 viewport/header/corner、横向滚动条、状态栏、收入测算卡片、可选按钮，以及报告工作台嵌入到流水窗口侧栏中的收入佐证控件样式。
- 报告工作台不再直接覆盖 PDF 流水窗口的大段子控件 stylesheet；只传浅色/深色和颜色 token。
- 影响范围仅为 GUI 主题职责迁移，不修改流水解析、调整计算、收入佐证 JSON 或 Excel/Word 输出。
- 验证：`python -m py_compile gui_v2.py`、`git diff --check -- gui_v2.py` 通过；PyQt offscreen 冒烟确认 `apply_embedded_theme` 可调用，主题 token 写入窗口，`monthly` 表格仍由 `FlowTableShell` 包裹。

## 2026-07-09 刘振红样本月度类型和重复提示修正

- 问题：`hebei_corp_detail` 不以 `_corp` 结尾且未写入 `CORP_BANK_IDS`，导致河北银行对公 `其他资料_*.pdf` 在 GUI 月度统计中被归为个人流水。
- 修复：`bankflow_v2/income_proof_export.py` 将 `hebei_corp_detail` 加入对公 bank id 映射；报告工作台和收入佐证导出均按对公处理。
- 问题：`其他资料_1.pdf` 是 `其他资料_0.pdf` 的重复子集，GUI 合并明细逐笔提示 397 条 `疑似重复流水`，导致异常数量虚高。
- 修复：`gui_v2.py` 保留逐笔去重，但按 `当前文件 + 首次来源` 聚合重复提示；该样本从 397 条压缩为 1 条 `疑似重复流水 397 笔`。
- 验证：`C:\Users\lenovo\Desktop\刘振红20260709105503\基础资料` 17 个 PDF 批量验证全部通过；月度统计直接构造确认出现 `个人 / 微信 / 对公 / 全部`，其中对公总计 458 笔；`python -m py_compile gui_v2.py bankflow_v2\income_proof_export.py` 通过。

## 2026-07-09 与车贷报告工作台的协作边界

- 本项目是 PDF 流水识别主线；新增银行解析器、批量验证、重复流水去重、收支闭合和月度汇总口径，优先在本项目窗口处理并验证。
- 车贷报告工作台只嵌入调用本项目，不应在 `D:\report` 内维护解析器细节；工作台侧只传主题颜色 token、处理嵌入加载和收入佐证/报告侧联动。
- 因两边都有 `gui_v2.py` / `bankflow_v2` 同名模块，若工作台表现与本项目独立运行不一致，优先核查工作台实际加载路径和 Python 模块缓存。

## 2026-07-09 嵌入 GUI 细节收口

- 流水表格外壳 `FlowTableShell` 保持直角，避免 `QTableWidget` 内部 header、viewport、corner 子控件在圆角处露出直角块；真圆角需 Qt mask/自绘，当前不扩大改动。
- 嵌入状态栏去掉 `border-top`，避免报告工作台识别页底部出现多余横线。
- 报告页 `系统 / 企业` 粘贴区的外层布局在 `D:\report` 中处理，本项目只提供嵌入流水窗口样式能力。
- `D:\report workflow\pdf_nuitka_src\gui_v2.py` 已撤回流水明细圆角相关更改，本轮不向该副本同步。

## 2026-07-10 嵌入 GUI 控件与真圆角收口

- 收入佐证勾选项改为透明底的普通复选框样式；操作按钮新增主次样式和按下反馈，`生成 Word` 使用主题强调色。
- `系统月收入 / 占股比例 / 利润率` 标签字重由 600 调整为 400。
- 流水表此前只依赖 QSS `border-radius`，但 `QTableWidget` 的 header、viewport、corner 子控件不会随父框裁切，因此四角仍会露出直角底色。
- 最终新增 `RoundedTableShell`，在 resize 时通过 `QRegion` mask 裁切整个表格容器；`FlowTableShell` 使用 12px 主题圆角边框，viewport 改为透明背景。PyQt offscreen 实际渲染确认四角均正常。
- 本轮只改 `gui_v2.py` 的 GUI 与主题样式，不改解析、去重、闭合校验、收入佐证数据或导出逻辑；未同步 `D:\report workflow` 副本。
- 验证：`python -m py_compile gui_v2.py`、`git diff --check` 通过；报告工作台侧 26 项单元测试及编译通过。

## 2026-07-10 Windows 真实窗口圆角最终修正

- 上一版 `RoundedTableShell` 使用 `QRegion` mask。该方案在 offscreen 渲染中四角正常，但报告工作台真实 Windows 窗口、150% DPI 下仍会出现表头直角底色。
- 根因不是 mask 路径或半径算错：控件层级检查确认外壳 mask 存在且 `(0, 0)` 不在 mask 区域，但 `QHeaderView` 等 `QTableWidget` 子控件在 Windows 实际合成时仍可能越过父级 mask 绘制。只检查 stylesheet、mask 属性或 offscreen 截图会得到假阳性；直接继续给 table/header/viewport 叠 mask 也不能稳定消除泄漏。
- 最终方案：移除无效的父级 mask，新增鼠标穿透的 `RoundedCornerOverlay`，置于 `FlowTableShell` 最上层。覆盖层仅用父卡片主题色填充圆角路径外的四个角，并用主题边框色重画 1px 抗锯齿圆角边框；表格主体、表头、viewport、滚动条和交互均不改。
- 浅色使用 `card=#fffdf8 / border=#dccfc1`，深色使用 `card=#1c1c1c / border=#333333`。顶边路径向内收至 1.5px，解决最外沿边线在高 DPI 下看似缺失的问题。
- 流水调整控件、月均利润三个输入框、收入佐证三个操作按钮统一为 34px 高。
- 影响范围仅为 `gui_v2.py` GUI 绘制和控件几何；不改解析、去重、闭合校验、收入佐证数据或导出逻辑，未同步 `D:\report workflow`。
- 验证：`python -m py_compile gui_v2.py`、`git diff --check -- gui_v2.py`、报告项目编译和 26 项测试通过；Windows 150% DPI 浅色/深色真实渲染四角均为外层卡片色，顶边命中各主题边框色；offscreen 下四个 `FlowTableShell` 层级、覆盖层几何和三组 34px 控件高度检查通过。

## 2026-07-10 嵌入主题交互收口

- GUI 概述：嵌入工作台的流水调整选中态、卡片悬停描边和收入佐证主按钮统一使用浅色/深色主题 token。
- 仅修改 `gui_v2.py` 样式，不改解析、调整计算或导出逻辑；`py_compile` 和 `git diff --check` 通过。

## 2026-07-11 固定六个月月均与嵌入界面校准

- 经营收入测算、月度统计、调整后月度统计和 Excel 的月均口径统一为选中流水收入合计 ÷ 6；实际只识别到 4 或 5 个月时仍固定除以 6。
- 收入测算卡不再并列显示实际月份月均和佐证月均，只显示 `月均流水收入（固定÷6）`，与收入佐证 JSON/Word 保持一致。
- 月度统计与 Excel 的月均行标为 `月均(÷6)`；调整后行说明实际识别月数和固定六个月口径，保留人工核对信息。
- 嵌入工作台的收入测算标题层级、卡片圆角和收入佐证卡片圆角同步校准；系统月收入输入框去掉重复占位文字。
- 验证：新增 3 项固定 ÷6 回归（含 5 个月合计 30 万显示 5.00 万、调整后行、收入佐证导出），`python -m py_compile gui_v2.py bankflow_v2\income_proof_export.py` 与 `git diff --check` 通过。

## 2026-07-11 微信调整佐证一致性与月均说明

- 修复纯微信流水增加收入调整后，界面已重平衡支出但收入佐证 JSON 仍使用原始支出的问题；佐证导出现在复用 `apply_adjustments` 的调整后月度收入、支出和净额。
- 月度表界面行名由 `月均(÷6)` 简化为 `月均`，顶部增加 `月均口径：收入、支出合计固定 ÷6`；Excel 行名继续保留 `月均(÷6)`。
- 新增微信增加 800 万回归；固定六个月与微信调整共 4 项测试、`py_compile` 和 `git diff --check` 通过，并已纳入三合一安装版。

## 2026-08-14 徽商银行对公倒序与跨行金额修正

- 徽商银行对公 `账户交易明细` 按相邻日期行中点切分交易块，兼容附言换行导致金额位于日期文字上方的版式。
- 同一秒多笔交易按余额链在页面顺序和反向顺序中择优，恢复时间倒序清单的实际入账顺序。
- 新样本解析 532 笔，收支笔数和金额与 PDF 汇总一致，余额异常 0。
- 报告辅助工作台源码入口确认实际加载 `D:\Codex data\CD\bankflow_v2\huishang_corp.py`，同样解析 532 笔、异常 0。

## 2026-08-19 中国工商银行账户明细清单图片 OCR 子版式

- 新文件与历史同名样本 SHA-256 均不同；业务版式此前已支持，但该文件无文字层，属于此前未覆盖的图片/文字轮廓化子版式。
- 新增 `bankflow_v2/icbc_corp_ocr.py`，只对横版 A4 九列表格候选启用，并由离线 OCR 二次确认标题“中国工商银行账户明细清单”。
- 按横线定位数据行、按九等分列读取交易时间/转出/转入/余额；电子章遮挡的最新余额仅在余额链和总闭合均可验证时重建。
- 工作台对普通图片型 PDF 不再误弹密码尝试提示；OCR 交易可回填企业户名和账号。
- 专项样本保存为 `CD_assets\PDF流水\专项解析\中国工商银行账户明细清单\中国工商银行账户明细清单_图片OCR.pdf`，并加入 `icbc_corp / image-ocr` 回归。
- 验证：29 笔，收入 4,130,844.84，支出 3,597,407.07，期初 901,026.64，期末 1,434,464.41，异常 0。
- 连续段 `公户1.pdf` 新增为专项回归：180 笔，收入 22,934,699.72，支出 22,034,297.61，期初 624.53，期末 901,026.64，异常 0；与 `公户2.pdf` 的期初 901,026.64 精确衔接。
- 图片 OCR 改为裁边后的单行批量识别，只对边界行、余额缺失或低置信度行补识别收支；按照清单倒序打印位置恢复同日交易顺序，并以相邻余额差校正 OCR 金额方向。
- 同一进程实测两段合计 209 笔由约 3 分钟缩短为约 2 分 8 秒；其中长段约 102 秒、短段约 25 秒，结果均为 0 异常。

## 2026-08-19 中国银行公户版式2账户历史明细

- 按用户指定暂定名称新增 `boc_corp_v2`，显示为“中国银行公户版式2”，不改变原 `boc_corp` 中国银行对公版式。
- 识别组合为 `账户历史明细 / 账户明细列表 / 交易日期 / 支出(借) / 收入(贷) / 余额 / 交易对手`，使用稳定九列表格直接解析，无需 OCR。
- 样本共 16 页、597 笔；收入 69 笔 / 5,213,658.59，支出 528 笔 / 5,175,081.35，期初 321,582.25，期末 360,159.49，异常 0。
- 6 月 21 日结息行的显示时间晚于其后打印交易，内部按余额连续的页面顺序校正，原始时间保留在 `raw_time`。
- 专项样本保存至 `CD_assets\PDF流水\专项解析\中国银行公户版式2\账户历史明细_川东_20260201-20260819.pdf` 并加入 `boc_corp_v2` 回归。
- 报告辅助工作台实际入口验证为 597 笔、“对公”、账户名称和 23 位账号均正确回填。

## 2026-08-22 农业银行对公账户明细文本/坐标互补修正

- 新样本已由现有 `abc_corp` 识别，但无表格线版式同时存在两种日期断行；旧逻辑在文本块解析得到部分结果后不再执行坐标解析，只抽取 51/222 笔，导致大量余额断链。
- 文本块解析的 51 笔与坐标解析的 171 笔互不重复；现改为按 `交易时间 + 金额 + 余额` 去重合并，完整恢复 PDF 页脚标示的 222 笔。
- 验证：收入 123 笔 / 6,116,034.93，支出 99 笔 / 5,482,279.80，期初 40,245.84，期末 674,000.97，异常 0。
- 工作台实际入口验证为“农业银行对公 / 对公”，户名“诺力（天津）货架制造有限公司”和账号 `02-071301040008217` 正确回填。
- 专项样本保存至 `CD_assets\PDF流水\专项解析\农业银行对公账户明细\账户明细_诺力天津_20260201-20260822.pdf` 并加入 `abc_corp` 回归。

## 2026-08-27 问题流水专项解析与佐证表去重一致性

- 专项问题按用户指定名称记录为：`农行重复流水计算合计错误，收入-支出与余额不符`。根因是界面合并明细已去掉两份农行个人 PDF 的重叠交易，但收入佐证导出仍累计原始文件结果；现佐证表在月度统计、调整分配和工资佐证前统一执行同账户跨文件去重。
- 农行重叠样本由 179 笔去重为 177 笔；收入 10,718,730.66，支出 7,621,782.50，期初 10,791.23，调查日余额 3,107,739.39。佐证表最近六个月收入/支出同步为 10,546,730.66 / 7,451,782.50，调查日余额继续取同账号最新交易的 3,107,739.39。
- 农业银行对公 `公户_1.pdf` 修复高位金额小数被列边界截断、日期日号落在下一行时漏读的问题：972 笔，收入 70,100,812.91，支出 69,820,922.79，期初 6,899.37，期末 286,789.49，异常 0。
- 新增华夏银行个人十列表格、湖北农商银行个人手机银行明细、长沙银行个人账户明细对账单（密码运行时提供）三个文字版式；分别验证 679 / 214 / 1,338 笔，均为 0 异常。
- 云南威信农村商业银行三段共 77 页、1,456 笔；单段保留缺页/页眉总额提示，工作台按账号、页码范围和页眉签名合并完整账单后使用完整页眉汇总。
- `浦发银行  个人.pdf` 为 43 页纯图片 PDF，按用户要求本轮暂不解析、不新增 OCR。
- 长沙银行 2026-07-23 存在真实的 `-500.00 / +500.00 冲正 / -500.00` 三笔连续交易，前后两笔日期、金额、余额和摘要完全相同且跨第 41–42 页；取消该版式的同源强制合并键后，GUI 保留全部 1,338 笔，不再提示疑似重复。
- 农行个人两份 PDF 的 2026-08-20 重叠区间包含同一笔 `+150,000.00` 和同一笔 `-300,000.00`，自动去重结果正确；聚合提示由“需复核/疑似重复”改为“低风险/重叠流水已自动去重”，保留来源追溯但不再作为需复核异常。
