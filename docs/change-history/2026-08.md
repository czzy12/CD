# PDF 流水技术变更归档：2026-08

> 本文件保存问题现象、原因、最终解决方式和验证证据。默认不要全文读取；先在仓库根目录《技术变更记录.md》搜索变更 ID，再读取对应章节。

# <a id="chg-20260807-25"></a>chg-20260807-25｜Gate D.2：Human Candidate Quality Review 工具与批次准备

## 需求

- 对冻结集 `real-ai-review-set-v1`（61 条）开展人工质量审核的准备阶段；
- 只做 `Frozen Candidate → Human Review Decision → Quality Metrics` 基础设施；
- 不写任何 human decision、不调用 AI、不改 KB/production；
- 生成适合逐批人工审核的批次文件与状态产物。

## 调整

1. **`bankflow_v2/knowledge/human_review.py`（新增）**：
   - `validate_decision_record()`：校验 decision 枚举（approve/modify/reject/insufficient）、
     reviewed_by=human、reviewed_at/reason 必填、modify 必须带 final_concept_id/name 或 final_relevance、
     reject/insufficient 保留 original_candidate、非 approve 必须带 error_category、
     promotion_status=not_promoted、review_set_version 一致；
   - `relation_dependency_status()` / `validate_relation_dependency()`：上游 Concept 为
     reject/insufficient 时 Relation 只能 reject/insufficient（dependent_concept_not_approved）；
     modify 时按 final concept 重新理解；approve 仅在上游 approve 时允许；
   - `compute_quality_metrics()`：concept / existing recovery / new proposal / relation / overall
     分组统计（total/reviewed/pending/exact_approve/modify/reject/insufficient/rates）、
     confidence calibration 分桶、error taxonomy 闭合校验、legacy pending 排除；
   - `error_taxonomy_totals()`：modify+reject+insufficient 严格闭合到解释桶，unexplained=0。
2. **`tools/knowledge/prepare_human_review_batches.py`（新增）**：
   - 冻结完整性校验（review_set_version、checksum、concept/relation/total、membership、decisions=0、
     legacy excluded=12）；
   - 从 canonical KB 计算 nearest plausible alternative concepts（本地词项重叠，仅审核辅助）；
   - 生成批次 batch_c01..c05（12/12/12/12/11）与 batch_r01（2），
     每批含 safe evidence（掩码）、AI proposal、canonical definition、aliases/examples、
     constraint check、Decision 表单与 error category 选项；
   - 生成 human_review_summary / concept_human_review / relation_human_review /
     error_taxonomy / systemic_findings / remediation_candidates / gate_d2_report
     （全部 status=not_yet_computable 或 awaiting_human_labels）。

## 验证

- 冻结校验：checksum=`35bc6d24a3e48e3abb75766c36637f9c3d2eee5c16ea021989863ac74ace679f` 一致、
  membership 61=59+2 一致、decisions=0、legacy excluded=12；
- 批次：5 concept batch + 1 relation batch，共 61 条；reviewed=0 / pending=61；
- 新增 `tests/test_human_review.py` 16 项（覆盖计划第 43 节 1–14：frozen membership 不可变、
  decision 枚举、reviewed_by=human、modify final_value、reject 保留 original、
  insufficient 保持 unresolved、metrics 计算、existing recovery accuracy、usable-after-modification、
  relation dependency、error taxonomy 闭合、manifest checksum 与 decisions 无关、legacy 排除、
  promotion 保持 not_promoted）；
- Python 全量 512 项通过（2 SKIP）；compileall 与 git diff --check 通过；未 push。

## 边界

- 未写入任何 human decision；未调用真实 AI；未改 candidate/canonical/production/GUI；
- 12 条 legacy relation pending 未触碰；未 push。

# <a id="chg-20260807-24"></a>chg-20260807-24｜Gate D.1C-2：冻结 Real-AI Candidate Review Set（real-ai-review-set-v1）

## 需求

- 将 Gate D / D.1C 真实 AI fallback 产生的 pending candidate 冻结为单一、可审计的
  Real-AI Candidate Review Set，供后续人工审核使用；
- 冻结集只含真实 provider candidate；12 条 legacy relation pending 与人工/测试候选完全排除；
- freeze 后成员不再因重复执行而改变；后续新候选进入新 review set version。

## 调整

- 新增 `bankflow_v2/knowledge/review_set.py`：
  - `review_set_identity()`：基于 review_set_version + knowledge_version + 排序后的 candidate ids
    计算确定性 checksum（不含时间戳）；
  - `build_review_set_manifest()`：输出 total/concept/relation 计数、stage_counts、
    legacy excluded、human decisions、manifest identity/checksum 与 provenance 字段清单。
- `tools/knowledge/build_human_review_package.py` 扩展：
  - `--d1c-candidate-ids` 标记 Gate D.1C 来源，`--d1c-provider-runs-json` 修正 D.1C 批次溯源；
  - 审核表为 D.1C 候选追加 `Source Stage: Gate D.1C` 与
    `Privacy History: previously blocked false-positive, safely remediated` 标记；
  - 输出 `real_ai_review_set_manifest.json`；`candidate_review_decisions.json` 保留已有 decisions
    （当前为空），不覆盖人工记录。

## 验证

- 冻结集：`real-ai-review-set-v1`，61 条（concept 59 / relation 2），
  stage_counts：Gate D=59、Gate D.1C=2；legacy excluded=12；human decisions=0；
  manifest identity/checksum=`35bc6d24a3e48e3abb75766c36637f9c3d2eee5c16ea021989863ac74ace679f`。
- 新增 `tests/test_review_set.py` 3 项：identity 确定性（同输入同值）、成员变化必变、
  manifest 计数/stage/legacy/decisions 正确。
- Python 全量 496 项通过（2 SKIP）；compileall 与 git diff --check 通过；未 push。

## 边界

- 未生成 human verdict；未修改 candidate 存储/canonical/production；12 legacy pending 未触碰；
  未调用真实 AI；未 push。

# <a id="chg-20260807-23"></a>chg-20260807-23｜Gate D.1C-1：2 条 Privacy Released Case 最小真实 AI 重验收

## 需求

- 对 D.1A 判定 false_positive 并已安全放行的 2 条 signature 完成首次真实 provider 调用，
  证明 `released signature → privacy preflight PASS → provider → structured output →
  KnowledgeCandidate(pending)` 完整链路成立；
- 只调用这 2 条；不得重跑原 57 条成功 signature；Relation AI 只允许自然触发；
- 验证 candidate persistence 幂等（不再次调用 provider）。

## 调整

- `build_validation_items()` 新增 `only_signatures` 过滤（非目标签名计 excluded_by_filter，不参与）；
- `tools/run_knowledge_ai_validation.py` 新增：
  - `--only-signatures`：最小化验证样本；
  - `--idempotency-check`：真实调用后从候选库按相同 task+signature+version 本地重提同一候选，
    验证 repository 去重（不产生 provider 调用）；
  - `--revalidation-dir` / `--review-set-dir`：生成 D.1C 专用产物
    （summary / privacy_preflight_revalidation / provider_runs / released_concept_results /
    released_relation_results / candidate_delta / revalidation_report）。

## 验证

- 最新版 privacy preflight 对 2 条均 PASS（blocked=0），unauthorized_sensitive_outbound=0；
- 真实调用 1 次（semantic-concept-v1，1 批 × 2 条）成功，0 重试 / 0 失败；
  - `ecab16e47ced65f0d4cd9b11` → existing concept `convenience_store`（confidence=high）
  - `8ec1dd4c746d74aee36fd90e` → existing concept `retail`（confidence=medium）
- 生成 2 个 pending concept candidate，0 new concept → relation fallback 未自然触发
  （`relation fallback not naturally triggered`，非失败）；
- 幂等检查：本地重复 persistence 全部被拒（idempotency_duplicate_prevented=2），
  未重复调用 provider；候选库新增 2 条 pending，无自动 approve；
- 新增 only_signatures 过滤测试 1 项；Python 全量 496 项通过（2 SKIP）；
  compileall 与 git diff --check 通过；未 push。

## 边界

- 未重跑原 57 条；未改 canonical/production/GUI；12 legacy pending 未触碰；未 push。

# <a id="chg-20260807-22"></a>chg-20260807-22｜Gate D.1B：59 条真实 AI Candidate 人工审核包

## 需求

- 为 Gate D 产生的 59 条真实 AI candidate（57 concept + 2 relation）构建独立、可审计的人工审核集；
- 与 12 条 legacy relation pending 完全隔离；不自动 approve / reject / modify；
- 保留完整 provenance（task、semantic signature、candidate id、knowledge version、provider run、source、pending 状态）；
- 审核结果数据结构必须支持 approve / reject / modify / insufficient，且只能由人工填写。

## 调整

- 新增 `tools/knowledge/build_human_review_package.py`：
  - 从 KnowledgeCandidate 存储（SQLite）按 task 过滤 59 条真实 AI 候选（semantic-concept-v1 57、
    industry-concept-relevance-v1 2），legacy pending（business-relevance-mvp-v11）只计数不纳入；
  - 按 semantic signature 回查 legacy 326 / unseen manifest 原始安全字段，逐条输出归一化语义文本、
    来源、proposal kind、confidence、used fields、模型 rationale、约束检查、privacy 状态；
  - existing concept 附带 canonical 定义、aliases/examples 安全摘要与文本重叠提示；
  - relation candidate 保留来源链（concept candidate ref），展示 industry/industry_name、
    proposed relevance / model raw / guard_adjusted / constraint_max、provider rationale；
  - 输出 8 个审核文件（见验证）；`candidate_review_decisions.json` 保持空，明确 reviewed_by=human；
  - `candidate_quality_metrics.json` 建立指标结构，人工裁决前为 not_yet_computable。

## 验证

- 审核包：`D:\Investigator PDF\outputs\knowledge-v1\candidate-human-review-20260807\`
  - review_summary.json（59 = 57 + 2；legacy excluded = 12；new concept = property_management 2 条；
    同 concept 多候选提示如 generic 22 / retail 8 / dining 5 等已列出）
  - concept_review_queue.json（57）、relation_review_queue.json（2）
  - candidate_review_sheet.md（逐条审核块 + Decision 勾选 + Human final value/reason）
  - candidate_review_decisions.json（decisions=[]，reviewed_by=human，promotion_status=not_promoted）
  - candidate_quality_metrics.json（status=not_yet_computable）
- 全部 59 条 review_status=pending；审核表对敏感字段掩码展示；relation 来源链指向
  property_management concept candidate；无自动裁决写入。
- Python 全量 492 项通过（2 SKIP）；compileall 与 git diff --check 通过；未 push。

## 边界

- 未生成任何 human verdict；未修改 candidate 存储、canonical 或 production；
- 12 条 legacy relation pending 未触碰；未调用真实 AI；未 push。

# <a id="chg-20260807-21"></a>chg-20260807-21｜Gate D.1A：Privacy Block Audit 与 PII Guard 保守例外

## 需求

- 审查 Gate D 中 2 条被 privacy guard 阻断的 case，逐条给出 true_positive / false_positive / ambiguous 分类；
- 不得为让 AI 成功而放宽默认 PII 保护；若确认 false positive 且能安全修复才改 guard。

## 调整

- 2 条 blocked case（`ecab16e47ced65f0d4cd9b11`、`8ec1dd4c746d74aee36fd90e`）审计结论：
  **false_positive**。均为商户小票类 `product_description`（美宜佳门店、百果园门店）中的
  16 位订单/流水号，Luhn 校验失败且嵌入中文业务文本，不是银行卡号。
- `bankflow_v2/knowledge/privacy.py` 保守例外（不弱化默认保护）：
  - 默认仍阻断任何 13–19 位数字串；
  - 仅 `product_description` / `merchant_category` 两个 typed 业务字段可放行
    **Luhn 校验失败**且嵌入非数字业务文本（≥2 个非数字字符）且无卡号/账号提示词的数字串；
  - Luhn 有效的银行卡号永远阻断（含嵌在中文文本中）；裸数字串永远阻断；其他自由文本字段永远阻断；
  - 顺带修复既有边界缺陷：数字串紧贴中文时原 `\b` 正则不匹配，现改为前后非数字边界的 run 匹配，
    避免“转账6222…银行卡号”漏检。
- 新增 `classify_bank_card_block()`（与 guard 共用同一判定）供审计工具与测试使用；
- 新增 `tools/knowledge/audit_privacy_blocks.py` 生成审计报告（掩码值、长度、字符类，不泄露完整敏感值）。

## 验证

- 新增 5 项 privacy 测试：typed 字段放行 Luhn 失败业务标识、Luhn 有效卡仍阻断（含 typed 字段）、
  卡号提示词阻断、自由文本字段严格阻断、classify 分类（true_positive / false_positive / ambiguous）。
- 审计汇总：total_blocked=2、true_positive=0、false_positive=2、ambiguous=0、
  safely_remediated=2、remains_blocked=0、released_cases_ai_validated=0（本轮未重新调用 AI）、
  unauthorized_sensitive_outbound=0、false_positive_rate=1.0；
  产物 `D:\Investigator PDF\outputs\knowledge-v1\candidate-human-review-20260807\privacy_block_audit.json/.md`。
- Python 全量 492 项通过（2 SKIP）；compileall 与 git diff --check 通过；未 push。

## 边界

- 未重新调用 57 条已成功 AI 签名；2 条已放行 case 留待后续最小化验收；
- 未改 candidate 存储、canonical 或 production；未 push。

# <a id="chg-20260807-20"></a>chg-20260807-20｜Gate D：第一次真实 AI Fallback Validation（knowledge tasks，candidate-only）

## 需求

- 完成首次真实闭环前半段：Local Unknown → AI → Candidate → Pending；
- 真实 DeepSeek 调用只经过 `semantic-concept-v1` / `industry-concept-relevance-v1` 任务契约与严格出站白名单；
- 调用前必须生成只读 privacy preflight 与逐项 PII guard；blocked 项不得发送；
- AI 只能生成 KnowledgeCandidate(pending)，不得自动 approve / 写 canonical / 切 production；
- 受控 retry、签名去重、候选幂等；不 push。

## 调整

1. **`bankflow_v2/knowledge/privacy.py`（新增）**：
   - `guard_item()`：拦截身份证模式、13-19 位数字银行卡、手机号、Windows 路径、身份/账号/电话/路径字段键与非白名单键；命中即 block，不自动“删值后继续”；
   - `build_privacy_preflight()`：只读报告，含 task / prompt_version / provider / model / signature_count / payload_keys / blocked_fields / privacy_blocked_count，不输出任何完整真实字段值。
2. **`bankflow_v2/knowledge/ai_validation.py`（新增）**：
   - `safe_validation_fields()`：AI 字段白名单 + 仅企业/商户名称（组织标记）+ 既有语义归一化；
   - `build_validation_items()`：按规范化语义签名去重，本地已解析（含 approved semantic cache 命中）直接跳过，只有 unresolved 进入 AI 候选；
   - `split_guarded()`：PII guard 分流，blocked 条目记录签名哈希与原因；
   - `call_with_retry()`：受控重试（默认 2 次），记录 attempt_count / final_status；
   - `run_concept_validation()`：调用 semantic-concept-v1，结果经结构校验后生成 pending KnowledgeCandidate；existing / new concept 分开计数；insufficient / invalid 保持 unresolved；同 task+签名+版本幂等（repository 去重）；
   - `run_relation_validation()`：仅当 AI 产生现有 KB 无关系的新通用概念时自然触发 industry-concept-relevance-v1；模型 relevance 经 `cap_strength` 本地护栏封顶，记录 model_raw_relevance / guarded_relevance / guard_adjusted；
   - `write_validation_package()`：输出 summary / privacy_preflight / provider_runs / concept_candidates / relation_candidates / candidate_review.md（全部无原始敏感字段）。
3. **既有小修**：`KnowledgeReviewService.propose` 在重复候选时返回 None（此前总是返回候选对象，去重计数失真）；`DeepSeekKnowledgeAdapter` 增加 `model` 属性；`IndustryTaxonomy` 增加 `nodes()`。
4. **CLI（新增）**：
   - `tools/run_knowledge_ai_validation.py`：真实调用唯一入口；`--dry-run` 零网络生成 preflight；真实调用必须 `--confirm-real-data` 且环境已授权；
   - `tools/knowledge/freeze_unseen_manifest.py`：从真实标准结果冻结 unseen 签名（不在 326 校准集、本地 unresolved、guard 通过），产物仓库外。

## 验证

- 新增 `tests/test_knowledge_ai_validation.py` 15 项：PII guard 拦截与放行、preflight 无敏感值、签名去重与本地已解析跳过、approved cache 阻止 AI、guard 分流、pending candidate 创建与幂等、insufficient 保持 unresolved、new concept 候选、失败保持 unresolved 无候选、重试成功/受控终止、relation 本地护栏封顶（strong→weak）、disabled 适配器零网络。
- Python 全量 487 项通过（2 SKIP）；统一回归 71 PASS / 0 FAIL / 20 MISSING / 1 SKIP 退出码 0；compileall 与 git diff --check 通过。
- 真实调用（2026-08-07，deepseek-v4-flash，先 `tools\load_deepseek_ai.ps1`）：
  - eligible 59 个唯一签名（326 主集 40 + unseen 20，跨集合重叠 1）；
  - privacy_blocked=2（product_description 长数字串被银行卡模式保守拦截，转人工检查，未发送）；
  - 发送 57 条：2 个概念批（50+7）+ 1 个关系批（2）共 3 次调用全部 ok，0 失败、0 重试；
  - concept_candidates=57（existing 55 / new 2，均为通用概念 property_management 物业管理）；relation_candidates=2（47 strong / 06 weak，guard_adjusted=0）；
  - insufficient=0、invalid=0、duplicate_candidate_prevented=0、items_remaining_unresolved=0、unauthorized_sensitive_outbound=0；
  - auto_approved=0、auto_rejected=0；候选全部 pending；approved semantic/relation cache 0 写入；
  - 审核包：`D:\Investigator PDF\outputs\knowledge-v1\ai-validation-20260807\`（summary.json、privacy_preflight.json、provider_runs.json、concept_candidates.json、relation_candidates.json、candidate_review.md、unseen_manifest_hanpf.json）。

## 边界

- production_resolver 仍为 legacy_v11；knowledge_v1 仍为 shadow；
- 历史 12 条 legacy relation pending 保持原样（现候选库 71 pending = 12 legacy + 59 本轮真实 AI）；
- 2 条 privacy_blocked 与 59 条 pending candidate 均待人工审核；未 push。

# <a id="chg-20260807-19"></a>chg-20260807-19｜C2.1 契约收口：unresolved 持久化、96 mismatch 全量分类与 relation snapshot identity

## 需求

- Gate C2.1 三个契约问题：
  1. unresolved 是否长期持久化（区分“从未运行 knowledge_v1”与“已运行但本地无法解析”）；
  2. 96 mismatch 中剩余 20 条到底是什么（此前只有 undetermined 40 / escalation 36，剩余未归类）；
  3. relation_id 身份语义正式冻结为 Canonical Relation Snapshot Identity。
- 不 push；真实 AI = 0；不改 GUI；不改 legacy_v11 生产链。

## 调整

1. **unresolved 持久化（schema_117.py）**：
   - 本地无法解析的签名桶写入最小 resolution（此前只进 diagnostics，跨会话无法区分 A/B）；
   - Concept unresolved：`concept_id=""`、`concept_resolution_source="unresolved"`、
     `industry_id=""`、`relation_id=""`、`relation_resolution_source="unresolved"`、
     `relevance="undetermined"`、`review_status="unresolved"`、`candidate_ref=""`；
   - Concept resolved + Relation unresolved：保留真实 concept_id / concept_name_snapshot /
     concept_resolution_source；仅当尝试关系评估的行业唯一时记录 industry_id（多行业或空画像时为空），
     relation 层全部 unresolved；
   - unresolved 使用同一 deterministic resolution_id（signature + industry + concept + version），
     不伪造 concept/relation、不自动写 none、不把 unresolved 当 approved；
   - diagnostics 新增 `unresolved_count`，`resolved_count` 只统计 approved resolution。
2. **mismatch 分类（shadow.py + classify_mismatches.py）**：
   - 新增封闭分类 `classify_mismatch()`：knowledge_undetermined / strength_escalation /
     strength_downgrade / legacy_undetermined_resolved / same_strength_different_state / other；
   - 每行 disagreement 增加 `mismatch_type`、`mismatch_reason`、`industry_id`；
   - 报告新增 `mismatch_classification` 计数与 `strength_downgrade_count`；
   - 新增 `tools/knowledge/classify_mismatches.py` 重算 326 条 per-entry 并输出审计报告，
     校验 mismatch == 所有桶之和、无 unexplained。
3. **relation_id 语义冻结（契约文档 + 测试）**：
   - relation_id = Canonical Relation Snapshot Identity（某一知识版本下不可变的行业×概念关系快照），
     不是永久关系主键；长期逻辑关联键 = industry_id + concept_id（+ knowledge version）；
   - relevance 变化必然产生新 ID；review_status 参与哈希，approved → deprecated 也产生新 ID；
   - 非语义 metadata 不参与哈希。

## 验证

- 新增 `tests/test_schema_117_c21.py` 10 项：never-parsed 与 parsed-unresolved 可区分、
  unresolved deterministic resolution_id（同输入稳定 + 顺序无关）、Concept unresolved 最小结构、
  Concept resolved + Relation unresolved（保留 concept、唯一行业落位）、approved 与 unresolved 并存、
  mismatch 封闭分类单测、合成 compare 分类加总无 unexplained、review_status 变化产生新 relation_id。
- 真实 326 条 per-entry 重算：mismatch 96 = knowledge_undetermined 40 + strength_escalation 36 +
  strength_downgrade 20 + legacy_undetermined_resolved 0 + same_strength_different_state 0 + other 0；
  此前的“剩余 20 条”即 strength_downgrade（knowledge 弱于 legacy），无 unexplained 桶；
  报告 `D:\Investigator PDF\outputs\knowledge-v1\mismatch-audit-20260807\mismatch_classification_v1.json/.md`。
- Python 全量 472 项通过（2 SKIP）；统一回归 71 PASS / 0 FAIL / 20 MISSING / 1 SKIP 退出码 0；
  compileall 与 git diff --check 通过。

## 边界

- 未改 schema 1.16 语义、legacy_v11、GUI、批准 JSON 与客户资料；真实 AI = 0；
  12 条历史 relation pending 保持不变；未 push。

# <a id="chg-20260807-18"></a>chg-20260807-18｜Gate B.1 + C1 + C2：schema 1.17 契约冻结、实施修订与 knowledge shadow writer

## 需求

- Foundation GREEN 基线之上恢复 schema 1.17 + knowledge_v1 shadow integration；
- Gate B.1：冻结最终契约（review_status/candidate_ref、deterministic resolution_id、内容可审计 relation_id、migration_status 落位、resolver source 直出、per-entry industry context、兼容方向明确）；
- Gate C1：契约/migration/compatibility 代码与测试；
- Gate C2：knowledge_v1 shadow resolution 写入 schema 1.17（production 仍为 legacy_v11）；
- 真实 AI = 0、零网络、不 push。

## 调整

1. `SemanticResolution` / `RelationResolution` 新增 `concept_resolution_source` / `relation_resolution_source`，由 resolver（SemanticResolver / IndustryRelationResolver / RelationKB）直接输出；schema writer 只序列化，不再根据 source/reason 二次猜测。
2. `resolution_id` 由序号改为确定性哈希：`res-<sha256(signature_ref|industry_id|concept_id|relation_id|resolver_version|knowledge_version)[:16]>`；重跑稳定、顺序无关、不含姓名/案件/卡号/路径/index。
3. `relation_id` 由 `industry:concept:version` 改为 canonical payload 内容哈希：`rel-<sha256(industry_id|concept_id|最终 relevance|confidence_tier|review_status|relation_kb_version)[:16]>`；relevance 变化必然改变 ID，`created_by/reviewed_at/reason_template/JSON 格式/key 顺序` 不参与哈希。
4. `migration_status` 正式落位到 `business_semantics_resolutions.value.migration_status`（parsed / not_parsed）；`resolutions=[] + not_parsed` 表示未运行 resolver，不解释为无经营语义。
5. resolution 新增可选 `candidate_ref`（当前为 ""）；`review_status` 正式枚举 approved / unresolved / candidate；rejected 属于 Candidate lifecycle，不进 resolution 主契约。
6. `build_business_semantics_resolutions` 新增 `per_entry_profiles` 参数：按交易行业画像拆分语义桶，避免单案件统一画像覆盖多行业交易；无 per-entry 时用 case_context 真实模型画像。
7. 兼容性文档按方向明确：new reader 读 1.16/1.17 均支持；old reader 不支持 1.17（不宣称双向兼容）；GUI/DTO 对未知 observation 忽略不崩。

## 验证

- 新增 `tests/test_schema_117_gate_c.py` 15 项：Foundation 字段保留（source_diagnostics / metadata availability / raw evidence refs / transaction IDs unchanged）、legacy production observation unchanged、unknown observation tolerant、deterministic resolution_id（同输入稳定 + 顺序无关）、relation_id relevance mutation + 非语义 metadata 排除、migration_status 位置、candidate_ref/review_status、resolver source 直出、per-entry profiles 拆分。
- 326 legacy replay（per-entry，合并 v11-full 286 + 任如冰 20 + 李娟 20）：agreement 70.55%、mismatch 96、undetermined 40、strength escalation 36、life_positive 0、unknown concept 40、relation fallback 0、AI fallback 40、exact_alias 286；报告 `outputs/knowledge-v1/shadow-20260807/per_entry_profile_v5.json/.md`。
- Gold Set（development/calibration）继续通过；schema writer 不改变 Gold Set resolution。
- Python 全量 462 项通过（2 SKIP）；统一回归 71 PASS / 0 FAIL / 20 MISSING / 1 SKIP；compileall 与 git diff --check 通过。
- Foundation corpus 核验：parser 层未改动；MVP-input 14 文件重跑 count/ID/invariant 与修复后基线一致；CD_assets 使用修复后 invariant artifact（61,740 笔）与统一回归作为回归证据。

## 边界

- `production_resolver = legacy_v11`；`knowledge_v1 = shadow`；schema 1.17 仅承载长期可审计 resolution。
- 12 条历史 relation pending 保持 pending（knowledge production promotion blocker，非 schema blocker）；未自动裁决。
- 未改 GUI、未改 parser、未真实 AI、未 push。

# <a id="chg-20260807-17"></a>chg-20260807-17｜Foundation Remediation：华夏 6 列回归、对公 raw evidence、方向契约、source diagnostics 与 metadata 可用性

## 需求

- 基于 Foundation Audit 已确认缺口实施修复，不做无关重构：
  1. 华夏银行 6 列电子版回归（旧基线 57 笔 → 当前 0 笔）；
  2. ccb_corp / psbc / icbc_corp format B 共 19 文件 4,316 笔缺失 raw_text/raw_fields/raw_headers；
  3. skipped / unparsed 行可观测性；
  4. icbc_corp 负 expense（7 笔）方向契约修复；
  5. 账户 metadata 缺失显式 unavailable；
  6. 修复后 164 文本 PDF + MVP-input 全量回归。
- 禁止：改 knowledge_v1 / legacy_v11 / schema 1.17 / GUI / 业务报告逻辑；真实 AI；OCR/密码范围扩展；push。

## 调整

1. **华夏 6/9 列版式（huaxia.py）**：列边界改为优先读取 pdfplumber `find_tables()` 表头 bbox（6 列或 9 列自适应），无表格时回退既有固定 9 列坐标；6 列语义重映射到 9 列标准位置（附言→列 8）。修复前金额/余额在 6 列版式落入错误列导致全部静默跳过。
2. **对公 raw evidence**：
   - `ccb_corp` 活期账户明细下载路径补 raw_fields/raw_headers/raw_text；
   - `psbc` 邮储历史明细（非对公）路径补 raw_fields/raw_headers/raw_text；
   - `icbc_corp` format B 补 raw_fields/raw_headers/raw_text（与 A/C/D 一致）。
   - 均为 parser 实际读取的源行内容，不从 canonical 字段反向伪造。
3. **方向契约（icbc_corp format A/B/C/D）**：借贷/转入转出列出现负值时按余额语义反向归入 income/expense（负数借项=收入、负数贷项=支出），不再产生负 income/expense；金额数值不变、方向不反转、余额连续性不恶化。
4. **source diagnostics**：`TransactionList` 新增 `diagnostics`；华夏/ccb_corp/psbc/icbc_corp 输出 `source_row_count / parsed_transaction_count / skipped_row_count / unparsed_row_count / ignored_non_transaction_row_count / review_row_count / unsupported_row_count`；`AnalysisService.run` 合并进 `source_diagnostics`；`result_export._source_files` 原样保留诊断字段。表头/空行/汇总行计 ignored，候选交易解析失败计 skipped/unparsed（fail-visible，不阻塞整文件）。
5. **metadata 可用性**：`source_files[]` 每条新增 `metadata_owner_available / metadata_account_available / metadata_period_available`；顶层 `statement_metadata` 新增 `account_name_available / account_number_available / statement_period_available`。缺失一律显式 unavailable，不做推断填充；华夏/psbc 补显式抬头提取（掩码账号只进 `raw_fields["masked_account_number"]`，不进完整 account_number）。

## 验证

- 华夏 6 列（董立波 2 文件）：57 笔恢复，income 30 / expense 27，0 issues；9 列（辛雨）1010 笔不回归；新增 6/9 列检测、关键字段映射、unparsed diagnostics 测试。
- raw evidence：19 文件 4,316 笔 raw_fields/raw_headers/raw_text 100% 完整；transaction_id 稳定（其他 77,316 笔不变；吴信鹏既有 1.16 结果 4,170 笔 ID 与修复后完全一致）。修复必然改变这 4,316 笔 ID（payload 含 raw 字段），但这些交易修复前从未写入任何正式标准结果/验收缓存，无已发布引用。
- canonical invariant：CD_assets 61,740 笔 + MVP 20,006 笔 = 81,746 笔；negative_income=0、negative_expense=0、double_nonzero=0、duplicate_id=0；余额连续性 CD 40,723 检查 40,305 ok / 418 broken（全部为既有已知断链，修复未恶化）、MVP 3,639/3,639 ok。
- differential（172 同 sha256）：unchanged 147、legacy_bug_fixed 13、intentional_fix 1（流水0613 负值方向修正，net 不变）、representation_or_explain 9（微信 7 为 neutral 口径差、佛山为已知差异、莱阳未适配）、unexplained 1（ccb_corp 0530 银行流水_5 收入/支出各 +1,004.00，证据不足保留）、regression 0。
- 测试：新增 `tests/test_foundation_remediation.py` 10 项；Python 全量 447 项通过（2 SKIP）；统一回归 71 PASS / 0 FAIL / 20 MISSING / 1 SKIP；compileall 与 git diff --check 通过。

## 边界与剩余发现

- 新发现（未扩大修复）：abc / abc_corp / generic_pdf 3 个文件 591 笔缺 raw_fields/raw_headers（raw_text 完整），记为 MEDIUM，后续 parser 迁移统一补齐。
- 保留 finding：兴业强水印文本噪声（MEDIUM）、账户元数据仍 59/79/127 文件缺（unavailable 显式化，不推断）、45 笔 0 金额非 neutral（LOW）、余额断链 418（既有）、佛山差异（已知）、ccb_corp 0530 +1,004（unexplained）。
- 未改 knowledge_v1 / legacy_v11 / schema 1.17 / GUI；真实 AI 0；未 push。

# <a id="chg-20260803-02"></a>chg-20260803-02｜WebView2基础审核工作台正式源码迁移

- 问题：12B-0A/0B 只完成单模块技术切片，缺少正式入口、统一模块目录、会话过期保护、来源复核面板和可持续扩展的前端结构。
- 原因：既有前端和 API 围绕购车切片设计，模块数量与导航不能由 Python 统一声明，切案异步响应也没有会话 ID 契约。
- 调整：新增 `gui_webview2_app.py` 和正式启动器；引入只读 `ModuleRegistry`、统一模块摘要/分页/筛选 DTO、来源复核 DTO、`case_session_id`/revision 和 `STALE_CASE`；前端拆分 app/bridge/components/styles，保持 V5 Linear 视觉并加入键盘、来源面板和过期响应丢弃。
- 业务边界：只读取 schema 1.16 已有观察；未扫描原交易生成候选，未重算业务规则，未调用 AI/外部 API，未修改旧 Qt GUI、schema、客户 JSON 或原资料。
- 验证：两个批准 JSON 的聚合级模块/证据/切案验证通过；正式 WebView2 进程内 QA、外网阻断、125%/150% 缩放通过；完整测试与构建结果见 `docs/Web_GUI_12B1_基础审核工作台迁移报告.md`。
- 发布：本轮不打包、不提交、不推送，等待用户检查正式源码窗口并决定是否进入 12B-2。

# <a id="chg-20260806-01"></a>chg-20260806-01｜12B-2 测试入口 bat 编码问题修复与 bat 文件规范

## 问题

- 双击 `运行12B2完整流程实测.bat` 无法启动，控制台报 `'OT' 不是内部或外部命令`、`'it' 不是内部或外部命令`、`系统找不到指定的路径`，随后直接进入 `请按任意键继续`。

## 原因

- bat 内容含中文提示和 `chcp 65001`，文件以 UTF-8 保存；cmd 双击时按系统 GBK 代码页解析 UTF-8 字节，中文与全角标点的字节序列破坏命令解析（乱码字符吞掉换行），叠加 LF 行尾后整条批处理被拆成错误命令，并非 Python 程序问题。

## 调整

- 将 `运行12B2完整流程实测.bat` 改为纯 ASCII 内容、CRLF 行尾、无 BOM；移除 `chcp 65001`；中文提示改为英文。
- 保留 `%~dp0` 相对定位 Python 环境与输入目录，避免硬编码带空格绝对路径；若未来需要中文提示，改用 PowerShell 脚本或按系统 GBK 编码保存 bat。

## 验证

- 使用与正式 bat 相同结构（`%~dp0`、CRLF、纯 ASCII）的探针 bat 经 `cmd /c` 实际运行：解析正常、完整实测跑完、退出码 0；12B-2 协作取消与失败恢复场景均通过（失败堆栈为 QA 注入的预期日志）。
- Python 代码未改动；测试基线沿用 2026-08-06 实测结果。

## 影响与后续规则

- 影响范围仅限测试入口 bat，不涉及 Python 程序与业务逻辑。
- 后续新建或修改 `.bat` 必须：纯 ASCII（或按系统 GBK 编码保存）、CRLF 行尾、无 BOM；不使用 `chcp 65001` 与 UTF-8 中文混搭；需要中文提示时优先写入 `.ps1` 或项目文档。

# <a id="chg-20260806-02"></a>chg-20260806-02｜12B-2 分析完成未自动进入工作台修复

## 问题

- 正式 WebView2 工作台新建案件完整分析完成后，界面停留在“分析完成”进度页并显示“原案件仍保留，可返回审核工作台”，没有自动进入模块工作台。

## 原因

- 前端轮询回调先执行 `setAnalysis(completed)` 再异步获取案件头准备切换；`setAnalysis` 触发 effect 清理把 `stopped` 置为 true，`await getCaseHeader()` 返回后完成分支被 `stopped` 检查提前返回，切换逻辑没有执行。

## 调整

- 将 `completed + result_ready` 的完成处理移到 `setAnalysis` 之前：先获取案件头、写入完成提示、清空 preflight/analysis，再 `activateCase` 进入工作台并 dismiss 任务；非完成状态仍按原逻辑 `setAnalysis` 刷新进度。
- 实测脚本新增第三个场景：真实分析完整跑完后自动进入工作台（`.view-bar` 出现、模块数大于 0、案件会话切换为新案件）。

## 验证

- 前端 23 PASS；TypeScript 与 Vite 构建通过；真实 WebView2 窗口完整实测三个场景全部通过：协作取消、失败恢复、成功完成自动进入工作台（MVP-input 14 个来源全量完成，20006 笔交易，模块 9 个，前端错误 0）。
- 结果文件：`outputs/web-gui-12b2/flow-qa-20260806.json`。

# <a id="chg-20260806-03"></a>chg-20260806-03｜12B-2 工作台 UI 修正：滚轮翻页、原始字段分隔符过滤、筛选行换行与小窗口显示

## 问题

- 中间明细列表无法用滚轮翻动或切换数据，只能看到当前页数量。
- Inspector 展开“完整允许内容”时，`raw_fields` 原样展示，包含来源文本拆分产生的 `/` 分隔符残留（如 `raw_fields[7] = "/"`），显示较复杂。
- 窗口非最大化时，筛选行出现横向滚动提示框体，不美观。
- 窗口非最大化时，程序窗体最下方内容显示不全。

## 原因

- 列表区只做内部滚动，没有滚轮翻页行为。
- 前端直接展示后端 `full_original_fields/masked_original_fields`，没有过滤空值与 `/` 分隔符。
- `.filter-controls` 使用 `overflow-x: auto`，窄窗口下产生横向滚动条。
- `.workflow-page` 使用 `overflow: hidden`，小窗口下底部操作被裁切。

## 调整

- 列表区滚轮滚动到顶部/底部边界时自动切换上一页/下一页；页内仍有内容时先正常滚动。
- Inspector 展示原始字段时过滤空字符串与 `/` 分隔符（仅影响展示，不修改后端原始字段）。
- 筛选行改为可换行布局，去掉横向滚动条，行高自适应。
- 工作流页（预检/分析进度）改为可纵向滚动，保证小窗口下底部按钮完整可见。

## 验证

- 前端 23 PASS；TypeScript 与 Vite 构建通过。
- 真实 WebView2 窗口完整实测三个场景全部通过：协作取消、失败恢复、成功完成自动进入工作台；前端错误 0。
- 结果文件：`outputs/web-gui-12b2/flow-qa-20260806.json`。

# <a id="chg-20260807-01"></a>chg-20260807-01｜12B-2 工作台底部显示修复：工作区滚动与分页脚固定

## 问题

- 带入客户资料后的“来源预检”页，底部“开始分析”一行被裁掉一半。
- 明细数据多时，底部“第 X / Y 页 · 上一页 / 下一页”分页脚不可见；数据少时可见。

## 原因

- `.work-area` 网格内容超出窗口高度时被外层 `overflow: hidden` 裁切，页面自身滚动无法覆盖全部溢出。
- `.list-region` 整块作为滚动容器，分页脚依赖 sticky 定位；数据多时不在可视区。

## 调整

- `.work-area` 增加 `overflow-y: auto`，任何模式（预检/分析/工作台）内容超高时整体可滚动，底部操作完整可见。
- `.list-region` 改为纵向 flex：`.issue-list` 内部滚动，`.page-footer` 固定在列表区底部，不随数据量变化被遮挡。

## 验证

- 实测脚本新增布局断言：预检页“开始分析”按钮滚动后完整可见；成功进入工作台后分页脚完整可见。
- 默认窗口（内容高 780）与小窗口（内容高 663）两轮真实 WebView2 完整实测全部通过，三个场景（取消/失败恢复/成功进入工作台）无回归，前端错误 0。
- 结果文件：`outputs/web-gui-12b2/flow-qa-20260806.json`、`outputs/web-gui-12b2/flow-qa-20260807-small.json`。

# <a id="chg-20260807-02"></a>chg-20260807-02｜历史案件页、经营上下文闭环与报告导出

## 需求

- 按用户确认顺序完成三项：①历史案件页；②经营上下文闭环（仅在“新建案件 → 来源预检”录入，确认人/时间不强制，修改后手动点“重新构建上下文观察”生效）；③报告导出。

## 调整

- 历史案件：新增 `list_recent_cases / open_recent_case / remove_recent_case` 白名单 API；自动记录最近打开的标准结果与案件目录摘要（不含绝对路径与完整结果）；前端新增“历史案件”页，支持打开与删除。
- 经营上下文：新增 `get_manual_case_context / save_manual_case_context / rebuild_context_observations`；预检页提供可选表单（工作单位、主要经营内容、产品/服务、补充说明、确认状态），保存到仓库外工作区 `outputs/web-gui-12b2/workspaces/<案件>-<哈希>/manual_case_context.json`，不写入客户目录；分析启动自动使用已保存上下文；工作台新增“重新构建上下文观察”按钮，仅重建经营候选、申报对照、人工问题与证据审计，AI 保持关闭、不重新解析。
- 报告导出：新增 `export_report`，复用 `render_mvp_markdown` 将当前案件渲染为 Markdown 验收报告并保存到用户选择位置。
- 新增 `bankflow_web/case_workspace.py`（无 PyQt 依赖），复用既有 `RecentCaseStore`。

## 验证

- Python 定向 63 项（61 PASS / 2 SKIP）；全量 363 项（361 PASS / 2 SKIP）；前端 26 PASS；TypeScript 与 Vite 构建通过。
- 真实 WebView2 窗口完整实测新增场景全部通过：预检保存/读取经营上下文、历史案件索引与页面打开、重新构建上下文观察（约 1.17 秒，会话替换）、报告导出（约 91 KB）；既有三个场景（取消/失败恢复/成功进入工作台）无回归。结果：`outputs/web-gui-12b2/flow-qa-20260807-full.json`。
- 边界：DTO 不返回客户绝对路径；经营上下文文件位于 git 忽略的 `outputs/` 下；AI/外部 API 保持关闭；schema 1.16、解析器、批准 JSON 与客户资料未修改。

# <a id="chg-20260807-03"></a>chg-20260807-03｜修复列表明细显示、历史案件打开与预检 TXT 上下文展示

## 问题

- 经营痕迹、申报对照、下定购车列表只显示英文分类或“此前收入”，具体内容需要展开 Inspector 才可见。
- 历史案件可见但打开失败：案件目录存在但未保存标准结果，提示“未找到该历史案件或案件位置不可用”。
- 更换 UI 后预检页不再展示 TXT 客户资料自动提取内容（工作单位、工作/居住地等）；TXT 在预检中显示“将跳过”，造成“读取遗失”的误解。

## 调整

- 列表显示：经营痕迹分类映射中文（直接相关/可能相关/无关联证据等）并把对手/摘要并入主文本；申报对照显示申报值、中文状态与类型；下定购车把摘要/用途并入命中文本，此前收入行显示具体摘要。
- 历史案件：分析成功后自动把标准结果保存到仓库外工作区 `outputs/web-gui-12b2/workspaces/<案件>-<哈希>/bankflow_verification_result.json`，历史记录同时保存案件目录与结果路径；打开时若目录内无 JSON，自动回退到工作区结果；确无结果时给出明确提示“请重新分析后再打开”。
- 预检页新增“案件上下文（来自 TXT 自动提取）”只读区（来源文件、工作单位、申报工作描述、单位/工作地/居住地候选），人工表单保留为辅助；TXT 在来源列表状态显示为“上下文资料”。

## 验证

- Python 全量 366 项（364 PASS / 2 SKIP）；前端 26 PASS；TypeScript 与 Vite 构建通过。
- 真实 WebView2 完整实测通过：预检“开始分析”按钮滚动后完整可见、分析后工作区结果已保存、历史案件页打开、上下文重建与报告导出正常；前端错误 0。结果：`outputs/web-gui-12b2/flow-qa-20260807-fixes.json`。

# <a id="chg-20260807-04"></a>chg-20260807-04｜AI 经营关联验收链重跑起点与运行配置持久化

## 背景

- 2026-07-28 在检查点 `68e0ac8` 完成经营关联 v11 验收（development/reserved/337 完整验收均通过），但当前重构仓库在验收后有 `6e149d6`、`7b08441` 经营上下文准入改动。
- 用当前代码对 v11-full 缓存做零网络离线重放：286 项全部缓存未命中（`cache_hit=0`），旧验收证据不能沿用，需要用当前代码重跑验收链。

## 调整

- 新增 `tools/save_deepseek_ai_config.ps1`（一次性保存，隐藏输入，DPAPI 加密到 `%LOCALAPPDATA%\BankFlowReview\ai_runtime.json`）与 `tools/load_deepseek_ai.ps1`（每个新会话加载环境变量）；Key 不进仓库、不进日志、不进标准结果。
- 用户授权重跑验收链；固定样本清单沿用 `outputs/mvp-acceptance-2026-07-28/v10-architecture/曹国民_AI固定开发与保留样本.json`（development 50 / reserved 50）。

## 已执行

- development 50 真实调用：`status=ok`，50/50 采用，1 次提供方调用，0 无效缓存。
- 产物：`outputs/mvp-acceptance-2026-08-07/dev50.md`、`outputs/mvp-acceptance-2026-08-07/ai-response-cache/`（仓库外）。

## 待执行（用户暂停，需指令继续）

1. development 缓存离线重放（零网络）。
2. reserved 50 真实调用 + 离线重放（用户确认节点）。
3. 337 完整验收（286 语义 / 1115 笔展开）+ 离线重放 + 输入审计（用户确认节点）。
4. 收口记录与冻结结论。

## 立项讨论结论（仅沟通，未执行）

- AI 生活轨迹：单独立项（独立 `task_type=life_trajectory`、提示词、schema、缓存命名空间、验收样本）。
- 用车痕迹：不单独 AI 立项，并入生活轨迹的行为/地点候选层，工作台单独展示模块。
- 消费水平：单独立项但走确定性统计，不建 AI；与生活轨迹共享消费类别字典。
- 三者进入标准结果均需 schema 解冻或 sidecar 方案，由用户另行确认。

# <a id="chg-20260807-05"></a>chg-20260807-05｜AI 经营关联验收链完成与工作台设置页经营上下文增强

## 需求

- 用户指令：解除 AI 默认关闭（仅验收链运行进程内启用），执行 AI 验收链（可连网）、回归与验收类；完成后做工作台设置页经营上下文小增强；依次完成并验收。

## 调整

- `tools/run_ai_sample_acceptance.py` 与 `tools/replay_ai_response_cache.py` 新增 `--split`（`development` / `reserved_acceptance`，默认 `development`），用于执行固定样本中的保留验收分片；批准样本 JSON 未修改。
- 工作台新增“设置”页（侧栏入口），展示当前案件经营上下文：TXT 自动提取只读区 + 人工确认可编辑区；侧栏设置按钮在已打开时关闭、未打开时加载上下文。
- 后端新增白名单方法 `get_current_manual_case_context` / `save_current_manual_case_context`：基于当前案件目录（`_current_case_dir`）读取/保存经营上下文，无关联目录时返回 `CURRENT_CASE_CONTEXT_UNAVAILABLE`；复用既有 `ManualContextDTO` 与工作区保存逻辑。
- 真实窗口 QA 新增设置页场景：打开设置页 → 校验字段渲染 → 编辑“主要经营内容” → 保存 → 后端回读 → 关闭。

## 执行结果（AI 验收链，产物在仓库外 `D:\Investigator PDF\outputs\mvp-acceptance-2026-08-07\`）

- development 50 离线重放（`dev50-replay.json`）：`cache_hits=50`、`cache_misses=0`、`provider_calls=0`、`replay_mismatches=0`、`replay_consistent=true`、50/50 采用。
- reserved 50 真实调用（`reserved50.md`）：`status=ok`、50/50 采用、1 次提供方调用、0 校验失败；离线重放（`reserved50-replay.json`）50/50 命中、零调用、零失配。
- 完整验收（`full286.md`）：286 种唯一语义、6 批、展开 1115 笔全部采用，0 校验失败；离线重放（`full286-replay.json`）286 命中、零调用、零失配；输入审计（`full286-audit.json/md`）为 7192 笔、155 笔确定性精确命中、337 旧口径语义、286 送模语义、1115 笔送模候选、42 笔本地归非经营。

## 回归与验收

- Python 全量单元测试：368 项通过（2 SKIP，含新增 2 项设置页后端测试）。
- 统一回归：71 PASS / 0 真实失败 / 20 项既有路径缺失 / 1 项佛山主动暂缓（与既有基线一致）。
- 前端：27 PASS；TypeScript 检查与 Vite 构建通过。
- `git diff --check` 通过。
- 真实 WebView2 完整实测（`outputs/web-gui-12b2/flow-qa-20260807-settings.json`）：`ok=true`，取消/失败恢复/成功进入工作台（7192 笔、9 模块）/经营上下文预检/设置页/历史案件/上下文重建/报告导出 8 个场景全部通过，前端错误 0。

## 边界

- 产品 AI 默认关闭语义未改变；验收链仅在运行进程内通过 `tools/load_deepseek_ai.ps1` 显式授权。
- 未修改 schema 1.16、批准 JSON 与客户资料；Key 未写入仓库、日志或标准结果。
- 本轮已本地收口提交，未 push；push 等待用户指令。

# <a id="chg-20260807-06"></a>chg-20260807-06｜工作台模块展示修复与证据页精简

## 需求

- 用户反馈：下定与购车、经营痕迹、申报对照、人工核实四个模块的“交易与命中内容、金额、分类”显示仍有问题；交易证据页的 `raw_fields[N]` 逐项显示对分析没有帮助，应去掉；修复后跑一轮回归验证。

## 调整（仅展示层 DTO 映射，未改业务规则与标准结果内容）

- 经营痕迹：确定性/AI 候选按 `transaction_id` 从 `original_transactions` 补全交易上下文（方向、金额、日期、来源、对手、摘要）；分类列显示中文分类（直接相关/可能相关/无关联证据/无法判断），不再显示来源标签。
- 下定与购车：命中文本去重（不再出现“购车款；购车款.”）；此前收入行避免“此前收入/此前收入”重复，无摘要时副文本显示“仅作时间并列”边界说明。
- 申报对照：分类列显示对照项类型（工作单位、工作地点、居住地点、车辆上牌地点、经销商、下定描述、下定相关支出等），命中状态/原因放副文本。
- 人工核实：分类列英文代码映射为中文（交易结构/资金流向/文字背景/申报核实）；金额、方向、日期、来源取首条证据交易；不再把布尔 `non_conclusion` 显示成“True”。
- 交易证据页：移除 `raw_fields[N]` 逐项行，保留 `raw_text` 原始整行；脱敏视图同步精简。

## 验证

- Python 全量单元测试：371 项通过（2 SKIP，含 4 项新增展示断言：经营痕迹上下文补全、人工核实中文分类与金额、证据页无 `raw_fields[` 标签）。
- 前端：27 PASS；TypeScript 检查与 Vite 构建通过；`git diff --check` 通过。
- 统一回归：71 PASS / 0 真实失败 / 20 项既有路径缺失 / 1 项佛山主动暂缓（与基线一致）。
- 真实 WebView2 完整实测（`outputs/web-gui-12b2/flow-qa-20260807-display-fixes.json`）：`ok=true`，8 个场景全部通过，前端错误 0。
- 韩鹏飞真实标准结果展示复核：经营痕迹行金额/方向/对手/中文分类正确；购车命中文本无重复；申报对照分类列为对照项；人工核实分类为中文且金额来自证据交易；证据页仅剩 `raw_text`。

## 边界

- 仅修改工作台展示层映射，未改 schema 1.16、解析器、业务观察、批准 JSON 与客户资料；本轮已本地收口提交，未 push。

# <a id="chg-20260807-07"></a>chg-20260807-07｜取消经营上下文“确认状态”，填写保存即视为已确认

## 需求

- 用户确认：经营上下文的“未确认/已确认”两档对单人审核流程没有意义，取消确认动作；填入信息即视为已确认。

## 调整

- 前端移除预检页与设置页经营上下文表单中的“确认状态”下拉；提示文案改为“填写并保存即视为已确认”。
- `ManualContextInput` 类型移除 `confirmation_status`；后端保存时固定写 `confirmation_status=confirmed`（忽略旧调用传入值），工作区记录与既有 AI 准入逻辑保持兼容。
- 真实窗口 QA 同步移除确认状态下拉断言，并修复历史案件页加载时序（等待 loading 结束再统计行数，消除偶发竞态）。

## 验证

- Python 全量 371 项通过（2 SKIP）；前端 27 项、TypeScript 与 Vite 构建通过；`git diff --check` 通过。
- 真实 WebView2 完整实测（`outputs/web-gui-12b2/flow-qa-20260807-confirm-removed.json`）：`ok=true`，8 场景通过，前端错误 0；设置页表单不再出现确认状态下拉，保存回读 `confirmation_status=confirmed`。

## 边界

- 仅改工作区人工上下文的保存语义与展示，未改 schema 1.16、业务观察、AI 规则与批准 JSON；本轮已本地收口提交，未 push。

# <a id="chg-20260807-08"></a>chg-20260807-08｜工作台 AI 默认关闭取消、字段级抽查与去向矩阵、工作台功能/显示增强、回归缺失分离

## 需求

- 用户确认：工作台取消按契约默认关闭 AI（进程内已持久化配置即可启用）；暂不处理河北银行对公、天津农商对公、中原银行等暂缓来源。
- 补一轮真实样本字段级抽查（个人/对公/微信/支付宝各一份，人工核对对手、摘要、备注），并维护“来源 × 字段 → 统一字段”矩阵（按统一标准字段口径，不按单个流水字段名）。
- 执行功能类、显示类与流程验收类改进（去重后共一批）。

## 调整

### 1. 工作台 AI 默认关闭取消

- `AnalysisService` 不再强制 `ai_config={}`，改为透传；`build_bankflow_result` 在未显式传参时从进程环境加载 DeepSeek 运行时。
- `WebView2Api.start_case_analysis` 每次分析前加载运行时配置：有 Key 且授权齐备时传入 `ai_config/ai_evaluator` 并放行外部网络；无配置时保持离线降级（确定性结果完整）。
- `AnalysisTaskManager` 透传 `ai_config/ai_evaluator/allow_external_network`。
- 启动器 `启动WebView2流水核查工作台.bat` 改为经 `tools/start_webview2_workbench.ps1` 启动：同一进程先加载持久化 AI 配置再启动工作台；没有配置时照常启动（AI 降级）。

### 2. 字段级抽查与去向矩阵

- 新增 `tools/field_spot_check.py`：按来源均匀抽样真实交易，并排展示原始字段与统一标准字段、字段来源与原文可溯源标记。
- 曹国民四来源抽查（`outputs/mvp-acceptance-2026-08-07/字段级抽查_曹国民.md/json`）：个人银行/对公/微信/支付宝各 10 笔，61 个非空标准字段全部可溯源，0 待核对。
- 新增 `docs/字段去向矩阵_来源到统一字段.md`：统一字段口径、真实去向（`counterparty_name←对手姓名/对手名称/交易对方` 等）、已确认版式清单与暂缓来源边界；要求新增解析器同步更新。

### 3. 功能类

- 人工核实：行内显示“涉及 N 笔证据”，并保留首条证据交易的金额/方向/日期/来源。
- 经营痕迹：新增“来源类型”徽标与筛选（确定性 / AI），分类列保持中文分类；确定性行副文本优先显示“命中：<锚点>”。
- 申报对照：“仅展示”项新增显式标签与中性样式（不再与“直接命中”同色），分类列显示对照项。
- 历史案件：无结果记录新增“重新分析”入口（后端 `reanalyze_recent_case` 注册不透明案件句柄，不泄露路径）。
- 设置页：新增“清空经营上下文”（后端 `clear_current_manual_case_context` 写入空记录，比删除工作区文件安全）。

### 4. 显示类

- 列表金额千分位显示（证据面板保留完整值）。
- 提取状态中文化（`unavailable` → 未提取到明确工作内容；`declared_unverified` → 已提取（未核实））。
- 经营痕迹确定性行命中锚点优先（并入功能类第 2 项）。

### 5. 流程验收类

- 统一回归 `--allow-missing`：缺失样本显式计入 `missing`（SKIP 只保留主动暂缓项），摘要为 `pass/fail/missing/skip/total`，退出码只按真实失败；`tests/test_regression.py` 新增缺失样本两条测试。
- 完全不同行业 20 种固定抽检：任如冰（人工确认经营内容：烟酒零售、超市经营；工行个人 10 种 + 微信 10 种）。真实调用 20/20、1 次提供方调用；零网络离线重放 20 命中、0 调用、0 失配。分布 `medium` 1（金乡龙贵超市）、`none` 19。产物在 `outputs/mvp-acceptance-2026-08-07/renrubing-20/`。

## 验证

- Python 全量：379 项通过（2 SKIP）。
- 前端：27 PASS；TypeScript 与 Vite 构建通过；`git diff --check` 通过。
- 统一回归：`--all --allow-missing` → `SUMMARY: pass=71, fail=0, missing=20, skip=1, total=92`，退出码 0。
- 真实 WebView2 完整实测（`outputs/web-gui-12b2/flow-qa-20260807-goal-batch.json`）：`ok=true`，8 场景通过，前端错误 0。
- 新启动器冒烟：加载 AI 配置后工作台就绪并自关，退出码 0。

## 边界

- 河北银行对公、天津农商对公、中原银行等暂缓来源未做任何解析/字段改动。
- 字段抽查报告需人工回原件最终核对（机械溯源 0 不代表人工结论）。
- 未改 schema 1.16、批准 JSON 与客户资料；AI Key 仍只存于本机加密配置；未 commit、未 push（等待用户指令）。

# <a id="chg-20260807-09"></a>chg-20260807-09｜工作台分析卡在“构建标准结果”修复：AI 改为验收缓存离线重放

## 问题

- 用户反馈：导入韩鹏飞客户资料后分析卡在“正在构建标准结果”，来源 3/3 已完成但耗时持续增长（1 分 57 秒仍在计时）。

## 原因

- `chg-20260807-08` 取消工作台 AI 默认关闭后，启动器会在同一进程加载持久化 AI 配置；韩鹏飞工作区存在“已确认”的人工经营上下文（建筑工程，煤炭），分析时 AI 门禁通过，工作台于是**在线**执行全量经营语义判断（653 种唯一语义，约 14 批模型调用），导致“构建标准结果”阶段长时间无进度且产生真实调用成本。

## 调整

- 工作台 AI 改为**验收缓存离线重放**：`WebView2Api.start_case_analysis` 使用 `load_deepseek_runtime(replay_only=True)`，分析永不联网；未命中验收缓存的语义直接跳过，AI 观察降级，确定性结果完整，分析立即完成。
- `tools/load_deepseek_ai.ps1` 新增 `BANKFLOW_AI_CACHE_DIR=%LOCALAPPDATA%\BankFlowReview\ai-response-cache` 并自动创建。
- 已把验收缓存合并到运行时缓存：曹国民完整 286 语义、任如冰 20、李娟 20（合计 334 个缓存文件）；已验收案例在工作台分析时可直接显示 AI 候选。
- 真实模型调用仍只发生在显式验收链；新案件/新语义需先经验收链生成缓存后，工作台才会出现对应 AI 候选。

## 验证

- 韩鹏飞全流程计时（加载 AI 配置、零网络）：真实未确认上下文场景 18.4 秒完成；模拟“已确认”触发 AI 门禁 + 离线重放场景 19.6 秒完成，均不再卡在构建阶段。
- Python 全量 379 项通过（2 SKIP）；前端 27 项；统一回归 `--allow-missing` 71/0/20/1 退出码 0；真实 WebView2 完整实测 `ok=true`、前端错误 0；启动器冒烟（加载配置 + GUI 自关）退出码 0。

## 边界

- 工作台不再发起在线 AI 调用；AI 结果显示上限 = 已验收缓存覆盖范围。
- 未改 schema 1.16、批准 JSON 与客户资料；未 commit、未 push。

# <a id="chg-20260807-10"></a>chg-20260807-10｜工作台可执行优化批：AI 状态可见、不可用文案、回归缺失清单与清空语义修复

## 需求

- 收口上一批后，继续分析剩余可执行优化（非重大改动）并自动实施、验收。

## 调整

1. 设置页新增“AI 经营判断状态”面板：运行时是否加载、模式（验收缓存离线重放、零网络）、缓存条目数、模型；不向前端暴露本地路径。
2. 经营痕迹不可用时侧栏显示“不可用”（不再误标“未实施”），模块边界说明显示中文原因（经营上下文不足需人工确认 / AI 未授权 / 验收缓存未覆盖等），并说明确定性候选仍可查看。
3. 分析进度页“正在构建标准结果”文案补充“AI 仅离线重放，不联网”，避免再被误认为卡住。
4. 预检页经营上下文提示“AI 经营判断仅使用已验收缓存，分析不联网”。
5. 统一回归 `--allow-missing` 输出 `MISSING: <名称> -> <路径>` 缺失清单。
6. 真实窗口 QA 增加“清空经营上下文”验证（清空后回读为空且状态为未确认）。
7. 修复清空语义：清空后记录改为“未确认 + 空内容”（此前仍写“已确认 + 空内容”，会导致后续“重新构建上下文观察”或再次分析报 `confirmed_primary_business 在确认状态下不能为空`）；新增回归断言：清空后重建上下文不报错。

## 验证

- Python 全量 381 项通过（2 SKIP）；前端 27 项；TypeScript 与 Vite 构建通过；`git diff --check` 通过。
- 统一回归 `--allow-missing`：71/0/20/1，退出码 0，并输出 20 条 `MISSING` 路径清单。
- 真实 WebView2 完整实测（`outputs/web-gui-12b2/flow-qa-20260807-optimizations.json`）：`ok=true`，前端错误 0，设置页保存/清空/重建均通过。

## 边界

- 未做重大改动：生活轨迹/用车痕迹/消费水平立项、在线 AI 显式开关、打包复验等仍待用户确认。
- 未改 schema 1.16、批准 JSON 与客户资料；本轮已本地提交，未 push。

# <a id="chg-20260807-11"></a>chg-20260807-11｜设置页点击无反应修复与证据第三层默认完整、逐行中文排版

## 问题

- 用户在预检页/分析页点击左下方“设置”无反应：渲染分支中 `preflight`/`analysis` 优先级高于 `settingsOpen`，设置页被前面分支挡住。
- 证据第三层（点击明细后右侧页面）仍默认显示脱敏摘要并可“展开完整允许内容”，用户要求默认完整显示并取消折叠；原始整行 `raw_text：…` 混在一行且带英文标签，不直观。

## 调整

1. 设置页提升为最高渲染优先级：任意页面（预检、分析、历史、工作台）点击“设置”都会打开；关闭后返回原页面；设置页案件名回退到预检/分析中的案件名。
2. 证据第三层：删除“展开/收起完整允许内容”切换，默认直接显示完整允许内容。
3. 原始内容排版：把 `raw_text：` 前缀取消，改为中文标题“原始内容（每行一项）”，并按 `|` 分隔逐行展示（一个信息一行），空段与 `/` 占位过滤。

说明：第 2 点覆盖 `chg-20260801-01` 的“默认脱敏、主动展开”边界（仅限工作台第三层），按用户最新要求默认完整显示。

## 验证

- 前端 27 项通过（含新增断言：设置分支优先于预检、证据第三层默认完整且无展开按钮）；TypeScript 与 Vite 构建通过。
- Python 全量 381 项通过（2 SKIP）；`git diff --check` 通过。
- 真实 WebView2 完整实测（`outputs/web-gui-12b2/flow-qa-20260807-settings-fix.json`）：`ok=true`，前端错误 0，设置页打开/清空/重建通过。

## 边界

- 未改 schema 1.16、批准 JSON 与客户资料；未 push。

# <a id="chg-20260807-12"></a>chg-20260807-12｜下定购车“此前收入”显示改名为“下定前收入”

## 需求

- 用户确认：下定命中前的收入提示由“此前收入”改为“下定前收入”。

## 调整

- 工作台下定与购车模块及 Markdown 验收视图中的显示标签统一改为“下定前收入”：
  - `PURCHASE_BOUNDARY_NOTE`：下定前收入只作时间并列，不表示资金来源。
  - 列表行分类/命中文本/对手回退：下定前收入。
  - 模块描述、报告表头“下定前收入候选”与“未发现满足当前规则的下定前收入”。
- 内部筛选键与 schema 观察值不变（仅展示层文案）。

## 验证

- 相关定向 53 项、Python 全量 381 项通过（2 SKIP）。
- 说明：旧 Qt GUI（`gui_verification.py`）属保留的历史界面，未同步改名。

## 边界

- 未改 schema 1.16、业务观察内容、批准 JSON 与客户资料；未 push。

# <a id="chg-20260807-13"></a>chg-20260807-13｜设置页预检阶段读取经营上下文与同名工作区回退

## 问题

- 用户在来源预检页填写并保存经营上下文后打开“设置”，提示“当前案件没有关联目录，无法读取经营上下文”：
  1. 设置页此前只读“当前案件目录”（`_current_case_dir`），而预检阶段目录只登记为不透明句柄，尚未绑定，导致读不到；
  2. 韩鹏飞早前保存的“建筑工程，煤炭”位于旧路径归一化产生的同名工作区（`韩鹏飞-5ae…`），与当前目录规范工作区（`韩鹏飞-3c19…`）哈希不一致，按规范路径读取不到。

## 调整

1. 设置页支持预检句柄：预检阶段打开设置时，用 `preflight.case_handle` 走 `get/save/clear_manual_case_context`；分析后走既有 current-case 接口；无案件目录时仍明确提示。
2. 新增后端 `clear_manual_case_context(case_handle)`（与 current 清空共用同一实现：写入“未确认 + 空内容”）。
3. 工作区读取回退：`load_manual_case_context` 在规范路径不存在时，按“同名案件目录前缀”在 `workspaces/` 下找最新的 `manual_case_context.json`，兼容旧路径归一化产生的工作区；保存仍写规范路径。

## 验证

- 韩鹏飞实测：`load_manual_case_context(D:\…\MVP-input\韩鹏飞)` 经回退读到 `confirmed_primary_business=建筑工程，煤炭`。
- Python 全量 383 项通过（2 SKIP，含句柄清空与工作区回退新增测试）；前端 27 项、TypeScript/Vite 构建通过。
- 真实 WebView2 完整实测（`outputs/web-gui-12b2/flow-qa-20260807-preflight-settings.json`）：`ok=true`，前端错误 0，预检阶段设置页显示已保存上下文、保存/清空/重建通过。

## 边界

- 仅展示与工作区读取兼容；未改 schema 1.16、批准 JSON 与客户资料；未 push。

# <a id="chg-20260807-14"></a>chg-20260807-14｜经营语义知识库架构 v1 独立阶段（shadow 实现 + 人工验收扩充）

## 需求

- 新独立阶段：将“客户上下文 + AI 缓存”升级为“标准行业体系 + 通用经营语义库 + 行业/语义关系库 + AI 增量补洞”。
- schema 1.16 冻结：本轮不改 schema；knowledge_v1 先内部实现与 shadow 验证，验证后单独提 schema 1.17 设计。
- 不删除 legacy_v11；旧结果作为回归基线；候选不自动 approved；客户身份不得进入缓存 Key。

## 调整

1. 新增 `bankflow_v2/knowledge/`：models / versioning / normalization / industry_taxonomy /
   semantic_concepts / relations / repository / resolver / ai_fallback / review / validator / shadow。
2. 新增四层正式知识库（`bankflow_v2/knowledge/canonical/`）：taxonomy（GB/T 4754-2017 官方核心节点 +
   内部细分）、55→70 个语义概念、123→235 个通用别名、261→286 条行业×概念模板关系。
3. 双轨缓存：Semantic Cache（signature_version + hash）与 Relation Cache
   （taxonomy + industry + concept + rules），客户身份/案件 ID/姓名完全退出 Key；运行缓存 SQLite 位于
   `outputs/knowledge-v1-cache/`（git 忽略），canonical 只保存脱敏通用内容。
4. AI 双任务契约：`semantic-concept-v1`（semantic_concept_resolution）与
   `industry-concept-relevance-v1`（industry_concept_relevance），DeepSeek 仅为实现之一；
   出站字段白名单过滤与本地校验有测试覆盖；本轮未执行真实模型调用。
5. 候选审核：`KnowledgeReviewService` + `review_knowledge_candidates.py`；
   AI/legacy 结果先 pending，人工 approve 才 promote 进 canonical。
6. CLI：`tools/knowledge/` 下 build/validate/extract/generate/review/compare/stats 共 8 个工具。
7. 人工验收（2026-08-07）：复核 170 条 pending 候选，158 条作废或拒绝（111 已被扩充知识库覆盖，
   47 为品牌/地点/交易类噪声），12 条关系冲突保留 pending；通用词扩充已并入 canonical。

## 验证

- 新增定向测试 38 项通过；Python 全量 421 项通过（2 SKIP），无回归；`compileall` 与 `git diff --check` 通过。
- 真实 legacy 缓存离线 shadow（326 条，零网络）：第一轮一致 117（35.89%）/ 不一致 209 / 新增 undetermined 163；
  验收扩充后第二轮一致 205（62.88%）/ 不一致 121 / 新增 undetermined 47；生活类正向两轮均为 0；
  强度上调 21 → 40 条已逐条列入 violations 报告待人工复核。
- canonical validator：0 error / 0 warning。
- 产物在仓库外 `D:\Investigator PDF\outputs\knowledge-v1\`（shadow 报告、迁移摘要、复核明细）。

## 边界

- 未修改 schema 1.16、批准 JSON、客户资料与确定性业务规则；生产主链仍由 legacy_v11 运行，knowledge_v1 保持 shadow mode。
- 未执行真实 AI 调用；未接入高德/客户信息 API；真实客户商户名未进入 Git 知识库。
- 本轮已本地 commit（含记录），未 push。

# <a id="chg-20260807-15"></a>chg-20260807-15｜Gate A：121 条 mismatch 人工裁决、Gold Set 与第三轮 Shadow

## 需求

- 对 121 条 legacy vs knowledge_v1 mismatch 做人工裁决，建立 Gold Set，并据裁决校准知识库；
- 不追求与 legacy 一致率机械对齐；主指标切换为 knowledge_v1 vs Gold Set accuracy；
- schema 1.16 / legacy_v11 生产链不改；零网络、零真实 AI；不 push。

## 调整

1. per-entry profile 修正：326 条缓存按 legacy 业务上下文分派建材/烟酒超市/家具家电画像，
   消除统一画像造成的错配。
2. 知识校准：具体概念优先于泛化词（护栏/建材/木塑/环保等覆盖材料款/工程公司）；
   仅名称命中且非专项概念时弱关联封顶 weak；水泥误匹配阻断（山泉水泥沙）；
   单字噪声归一化丢弃（豫）；新增 17 个通用别名与 mining / real_estate 两个概念；
   consulting 对建材/环保关系 medium → weak。
3. 新增 `tools/knowledge/export_mismatch_review.py`（脱敏导出）与
   `tools/knowledge/adjudicate_mismatches.py`（规则裁决 + Gold Set 生成）；
   `compare_legacy_v11.py` 增加第三轮扩展指标（理论 AI fallback / unknown concept / unknown relation）。
4. Gold Set：`tests/fixtures/knowledge_v1/gold_set.json`（63 条泛化条目，无客户敏感信息）
   与 `tests/test_knowledge_gold_set.py`。

## 验证

- 第三轮 shadow（326 条，零网络，per-entry profile）：agreement 226（69.33%）、
  mismatch 100、undetermined 40、upgrade 40、downgrade 19 + other 1、生活类正向 0；
  unknown Concept 40、unknown Relation 0、理论 AI fallback 40（语义 40 / 关系 0）。
- 裁决 100 条：knowledge_v1 正确 99（99%），两者均需修正 1（退货款 strong 应 medium）；
  issue 分布：legacy_context_specific 57 / transaction_noise 18 / customer_specific 16 /
  insufficient 6 / alias_missing 1 / other 2。
- Gold Set 测试 4 项通过；Python 全量 425 项通过（2 SKIP）；compileall、git diff --check 通过。
- 裁决明细在仓库外 `D:\Investigator PDF\outputs\knowledge-v1\mismatch-review-20260807\`。

## 边界

- 未修改 schema 1.16、批准 JSON、客户资料与确定性业务规则；生产主链仍为 legacy_v11。
- 未执行真实 AI 调用；未接入网络；客户专属表达未进入 canonical 与 Gold Set。
- 已本地 commit（含记录），未 push。

# <a id="chg-20260807-16"></a>chg-20260807-16｜schema 1.17 实施：business_semantics_resolutions（方案 B）

## 需求

- 按 Gate B 正式设计实施 schema 1.17：独立 `business_semantics_resolutions` 观察；
- schema 与 knowledge version 解耦；migration 不伪造历史；legacy shadow 只进 diagnostics；
- 不改变 legacy_v11 生产裁决权；不改 GUI；不 push。

## 调整

1. `SCHEMA_VERSION` 1.16 → 1.17；`build_bankflow_result` 默认输出 1.17，
   `include_knowledge_shadow=False` 可关闭（旧行为不含新观察）。
2. 新增 `bankflow_v2/knowledge/schema_117.py`：
   - `build_business_semantics_resolutions`：按语义签名去重解析，输出
     concept/industry/relation 的 ID 与快照名、`concept_resolution_source` /
     `relation_resolution_source` 枚举、`relation_id`（industry:concept:rules 哈希）、
     `inherited` / `inherited_from_industry_id`、`review_status=approved`；
   - 未解析概念/关系只计入 diagnostics（unknown_concept / unknown_relation /
     理论 AI fallback），不写入 resolutions；
   - legacy comparison（legacy_relevance / agreement）只放顶层
     `diagnostics.knowledge_v1.legacy_comparison`，不进正式观察。
3. `migrate_schema_116_to_117(result)` + `tools/migrate_schema_116_to_117.py`：
   旧案件只升级版本并追加空 resolutions + `migration_status=not_parsed`，不伪造历史；幂等。
4. 兼容：`standard_result_view` 接受 1.16 / 1.17（1.15 仍拒绝）；
   `schema_versions_supported=["1.16","1.17"]`；GUI 未适配新观察时不崩。

## 验证

- 新增 schema 契约/迁移/兼容测试 11 项；Python 全量 437 项通过（2 SKIP）；
  compileall 与 git diff --check 通过。
- 生产 legacy 观察不变（知识层只新增 shadow 观察与 diagnostics）；
  replay_only、AI disabled 行为保持；真实 AI 调用 0；schema 1.16 文件继续可读。
- 本阶段两个独立 commit：`4893b47`（契约/迁移/兼容）与后续 shadow 写入 commit；未 push。

## 边界

- 未改 schema 1.16 语义；未改 GUI；未让 knowledge_v1 接管 production resolver。

# <a id="chg-20260803-01"></a>chg-20260803-01｜WebView2真实结果切片与来源复核链路收口

## 必要节点

- 独立 React + TypeScript 前端、Python CaseSession/DTO、pywebview + Edge WebView2 外壳和只读 schema 1.16 导入链路完成；Qt WebEngine 失败切片保留为历史验证记录。
- 新增独立 schema 1.16 客户结果导出工具；AI与网络关闭，不修改客户原资料或正式GUI。

## 问题与修正

- Worker 已识别来源“需复核”，但状态未进入标准结果；现由 `source_files[].status/review_reason` 正式承载，经 CaseSession 轻量 DTO 展示，前端不猜测或重算。
- WebView2 文件过滤器与带空格启动路径问题已修正；真实结果切案后清空旧列表、选中项和 Inspector。

## 影响与验证

- 影响限定在独立 Web/WebView2 技术切片及标准结果来源诊断传递，不替换正式GUI，不改变交易、购车或证据业务判断。
- case-025 源码窗口显示11来源、其中1来源需复核；切换case-129无状态残留，再切回可稳定恢复。
- 来源状态定向3 PASS，WebView2定向28 PASS / 1 SKIP，全量334 PASS / 2 SKIP；TypeScript检查、Vite构建和差异检查通过。
- 下一阶段：工作台正式迁移；最新功能打包复验在正式迁移阶段统一收口。
- 功能提交：`28a70e4`；此前未推送节点及本轮提交已按授权同步到远端同名分支。

# <a id="chg-20260801-01"></a>chg-20260801-01｜GUI步骤10至12用户确认与提交收口

## 用户确认

- 步骤10证据边界按当前实现收口：候选列表和默认证据摘要继续脱敏；用户主动点击“展开完整证据”后直接显示未脱敏的原始字段。
- 不增加共享屏幕提示、二次确认或展开后的额外脱敏。
- 步骤11的150ms卡片悬停和220ms页面切换速度经用户确认，暂按现状保留。

## 收口与验证基线

- 功能范围为GUI步骤5至12，包含兴业8列强水印解析修复、吴信鹏购车口径、证据中心、轻量动效和视觉收口；详细实现与真实案例数据见`chg-20260730-09`、`chg-20260730-10`、`chg-20260731-01`和`chg-20260731-02`。
- 验证基线：GUI定向43 PASS，全量单元279 PASS，兴业专项2 PASS / 0 FAIL / 0 SKIP，统一回归71 PASS / 0 FAIL / 21 SKIP，语法、离屏启动、运行态动效和`git diff --check`通过。
- 用户已授权本地提交和Obsidian同步；未授权推送。

## 持续边界

- 不修改schema 1.16字段含义，不恢复经营AI调用，不启动生活轨迹、用车轨迹或高德。
- 展开完整证据是用户主动操作；默认列表脱敏边界没有被取消。
- 不修改客户原始资料，未调用外部模型，未推送。
