# PDF 流水技术变更归档：2026-08

> 本文件保存问题现象、原因、最终解决方式和验证证据。默认不要全文读取；先在仓库根目录《技术变更记录.md》搜索变更 ID，再读取对应章节。

# <a id="chg-20260803-02"></a>chg-20260803-02｜WebView2基础审核工作台正式源码迁移

- 问题：12B-0A/0B 只完成单模块技术切片，缺少正式入口、统一模块目录、会话过期保护、来源复核面板和可持续扩展的前端结构。
- 原因：既有前端和 API 围绕购车切片设计，模块数量与导航不能由 Python 统一声明，切案异步响应也没有会话 ID 契约。
- 调整：新增 `gui_webview2_app.py` 和正式启动器；引入只读 `ModuleRegistry`、统一模块摘要/分页/筛选 DTO、来源复核 DTO、`case_session_id`/revision 和 `STALE_CASE`；前端拆分 app/bridge/components/styles，保持 V5 Linear 视觉并加入键盘、来源面板和过期响应丢弃。
- 业务边界：只读取 schema 1.16 已有观察；未扫描原交易生成候选，未重算业务规则，未调用 AI/外部 API，未修改旧 Qt GUI、schema、客户 JSON 或原资料。
- 验证：两个批准 JSON 的聚合级模块/证据/切案验证通过；正式 WebView2 进程内 QA、外网阻断、125%/150% 缩放通过；完整测试与构建结果见 `docs/Web_GUI_12B1_基础审核工作台迁移报告.md`。
- 发布：本轮不打包、不提交、不推送，等待用户检查正式源码窗口并决定是否进入 12B-2。

# <a id="chg-20260806-01"></a>chg-20260806-01｜12B-2 测试入口 bat 编码问题修复与 bat 文件规范

## 问题

- 双击 `运行12B2完整流程实测.bat` 无法启动，控制台报 `'OT' 不是内部或外部命令`、`'it' 不是内部或外部命令`、`系统找不到指定的路径`，随后直接进入 `请按任意键继续`。

## 原因

- bat 内容含中文提示和 `chcp 65001`，文件以 UTF-8 保存；cmd 双击时按系统 GBK 代码页解析 UTF-8 字节，中文与全角标点的字节序列破坏命令解析（乱码字符吞掉换行），叠加 LF 行尾后整条批处理被拆成错误命令，并非 Python 程序问题。

## 调整

- 将 `运行12B2完整流程实测.bat` 改为纯 ASCII 内容、CRLF 行尾、无 BOM；移除 `chcp 65001`；中文提示改为英文。
- 保留 `%~dp0` 相对定位 Python 环境与输入目录，避免硬编码带空格绝对路径；若未来需要中文提示，改用 PowerShell 脚本或按系统 GBK 编码保存 bat。

## 验证

- 使用与正式 bat 相同结构（`%~dp0`、CRLF、纯 ASCII）的探针 bat 经 `cmd /c` 实际运行：解析正常、完整实测跑完、退出码 0；12B-2 协作取消与失败恢复场景均通过（失败堆栈为 QA 注入的预期日志）。
- Python 代码未改动；测试基线沿用 2026-08-06 实测结果。

## 影响与后续规则

- 影响范围仅限测试入口 bat，不涉及 Python 程序与业务逻辑。
- 后续新建或修改 `.bat` 必须：纯 ASCII（或按系统 GBK 编码保存）、CRLF 行尾、无 BOM；不使用 `chcp 65001` 与 UTF-8 中文混搭；需要中文提示时优先写入 `.ps1` 或项目文档。

# <a id="chg-20260806-02"></a>chg-20260806-02｜12B-2 分析完成未自动进入工作台修复

## 问题

- 正式 WebView2 工作台新建案件完整分析完成后，界面停留在“分析完成”进度页并显示“原案件仍保留，可返回审核工作台”，没有自动进入模块工作台。

## 原因

- 前端轮询回调先执行 `setAnalysis(completed)` 再异步获取案件头准备切换；`setAnalysis` 触发 effect 清理把 `stopped` 置为 true，`await getCaseHeader()` 返回后完成分支被 `stopped` 检查提前返回，切换逻辑没有执行。

## 调整

- 将 `completed + result_ready` 的完成处理移到 `setAnalysis` 之前：先获取案件头、写入完成提示、清空 preflight/analysis，再 `activateCase` 进入工作台并 dismiss 任务；非完成状态仍按原逻辑 `setAnalysis` 刷新进度。
- 实测脚本新增第三个场景：真实分析完整跑完后自动进入工作台（`.view-bar` 出现、模块数大于 0、案件会话切换为新案件）。

## 验证

- 前端 23 PASS；TypeScript 与 Vite 构建通过；真实 WebView2 窗口完整实测三个场景全部通过：协作取消、失败恢复、成功完成自动进入工作台（MVP-input 14 个来源全量完成，20006 笔交易，模块 9 个，前端错误 0）。
- 结果文件：`outputs/web-gui-12b2/flow-qa-20260806.json`。

# <a id="chg-20260806-03"></a>chg-20260806-03｜12B-2 工作台 UI 修正：滚轮翻页、原始字段分隔符过滤、筛选行换行与小窗口显示

## 问题

- 中间明细列表无法用滚轮翻动或切换数据，只能看到当前页数量。
- Inspector 展开“完整允许内容”时，`raw_fields` 原样展示，包含来源文本拆分产生的 `/` 分隔符残留（如 `raw_fields[7] = "/"`），显示较复杂。
- 窗口非最大化时，筛选行出现横向滚动提示框体，不美观。
- 窗口非最大化时，程序窗体最下方内容显示不全。

## 原因

- 列表区只做内部滚动，没有滚轮翻页行为。
- 前端直接展示后端 `full_original_fields/masked_original_fields`，没有过滤空值与 `/` 分隔符。
- `.filter-controls` 使用 `overflow-x: auto`，窄窗口下产生横向滚动条。
- `.workflow-page` 使用 `overflow: hidden`，小窗口下底部操作被裁切。

## 调整

- 列表区滚轮滚动到顶部/底部边界时自动切换上一页/下一页；页内仍有内容时先正常滚动。
- Inspector 展示原始字段时过滤空字符串与 `/` 分隔符（仅影响展示，不修改后端原始字段）。
- 筛选行改为可换行布局，去掉横向滚动条，行高自适应。
- 工作流页（预检/分析进度）改为可纵向滚动，保证小窗口下底部按钮完整可见。

## 验证

- 前端 23 PASS；TypeScript 与 Vite 构建通过。
- 真实 WebView2 窗口完整实测三个场景全部通过：协作取消、失败恢复、成功完成自动进入工作台；前端错误 0。
- 结果文件：`outputs/web-gui-12b2/flow-qa-20260806.json`。

# <a id="chg-20260807-01"></a>chg-20260807-01｜12B-2 工作台底部显示修复：工作区滚动与分页脚固定

## 问题

- 带入客户资料后的“来源预检”页，底部“开始分析”一行被裁掉一半。
- 明细数据多时，底部“第 X / Y 页 · 上一页 / 下一页”分页脚不可见；数据少时可见。

## 原因

- `.work-area` 网格内容超出窗口高度时被外层 `overflow: hidden` 裁切，页面自身滚动无法覆盖全部溢出。
- `.list-region` 整块作为滚动容器，分页脚依赖 sticky 定位；数据多时不在可视区。

## 调整

- `.work-area` 增加 `overflow-y: auto`，任何模式（预检/分析/工作台）内容超高时整体可滚动，底部操作完整可见。
- `.list-region` 改为纵向 flex：`.issue-list` 内部滚动，`.page-footer` 固定在列表区底部，不随数据量变化被遮挡。

## 验证

- 实测脚本新增布局断言：预检页“开始分析”按钮滚动后完整可见；成功进入工作台后分页脚完整可见。
- 默认窗口（内容高 780）与小窗口（内容高 663）两轮真实 WebView2 完整实测全部通过，三个场景（取消/失败恢复/成功进入工作台）无回归，前端错误 0。
- 结果文件：`outputs/web-gui-12b2/flow-qa-20260806.json`、`outputs/web-gui-12b2/flow-qa-20260807-small.json`。

# <a id="chg-20260807-02"></a>chg-20260807-02｜历史案件页、经营上下文闭环与报告导出

## 需求

- 按用户确认顺序完成三项：①历史案件页；②经营上下文闭环（仅在“新建案件 → 来源预检”录入，确认人/时间不强制，修改后手动点“重新构建上下文观察”生效）；③报告导出。

## 调整

- 历史案件：新增 `list_recent_cases / open_recent_case / remove_recent_case` 白名单 API；自动记录最近打开的标准结果与案件目录摘要（不含绝对路径与完整结果）；前端新增“历史案件”页，支持打开与删除。
- 经营上下文：新增 `get_manual_case_context / save_manual_case_context / rebuild_context_observations`；预检页提供可选表单（工作单位、主要经营内容、产品/服务、补充说明、确认状态），保存到仓库外工作区 `outputs/web-gui-12b2/workspaces/<案件>-<哈希>/manual_case_context.json`，不写入客户目录；分析启动自动使用已保存上下文；工作台新增“重新构建上下文观察”按钮，仅重建经营候选、申报对照、人工问题与证据审计，AI 保持关闭、不重新解析。
- 报告导出：新增 `export_report`，复用 `render_mvp_markdown` 将当前案件渲染为 Markdown 验收报告并保存到用户选择位置。
- 新增 `bankflow_web/case_workspace.py`（无 PyQt 依赖），复用既有 `RecentCaseStore`。

## 验证

- Python 定向 63 项（61 PASS / 2 SKIP）；全量 363 项（361 PASS / 2 SKIP）；前端 26 PASS；TypeScript 与 Vite 构建通过。
- 真实 WebView2 窗口完整实测新增场景全部通过：预检保存/读取经营上下文、历史案件索引与页面打开、重新构建上下文观察（约 1.17 秒，会话替换）、报告导出（约 91 KB）；既有三个场景（取消/失败恢复/成功进入工作台）无回归。结果：`outputs/web-gui-12b2/flow-qa-20260807-full.json`。
- 边界：DTO 不返回客户绝对路径；经营上下文文件位于 git 忽略的 `outputs/` 下；AI/外部 API 保持关闭；schema 1.16、解析器、批准 JSON 与客户资料未修改。

# <a id="chg-20260807-03"></a>chg-20260807-03｜修复列表明细显示、历史案件打开与预检 TXT 上下文展示

## 问题

- 经营痕迹、申报对照、下定购车列表只显示英文分类或“此前收入”，具体内容需要展开 Inspector 才可见。
- 历史案件可见但打开失败：案件目录存在但未保存标准结果，提示“未找到该历史案件或案件位置不可用”。
- 更换 UI 后预检页不再展示 TXT 客户资料自动提取内容（工作单位、工作/居住地等）；TXT 在预检中显示“将跳过”，造成“读取遗失”的误解。

## 调整

- 列表显示：经营痕迹分类映射中文（直接相关/可能相关/无关联证据等）并把对手/摘要并入主文本；申报对照显示申报值、中文状态与类型；下定购车把摘要/用途并入命中文本，此前收入行显示具体摘要。
- 历史案件：分析成功后自动把标准结果保存到仓库外工作区 `outputs/web-gui-12b2/workspaces/<案件>-<哈希>/bankflow_verification_result.json`，历史记录同时保存案件目录与结果路径；打开时若目录内无 JSON，自动回退到工作区结果；确无结果时给出明确提示“请重新分析后再打开”。
- 预检页新增“案件上下文（来自 TXT 自动提取）”只读区（来源文件、工作单位、申报工作描述、单位/工作地/居住地候选），人工表单保留为辅助；TXT 在来源列表状态显示为“上下文资料”。

## 验证

- Python 全量 366 项（364 PASS / 2 SKIP）；前端 26 PASS；TypeScript 与 Vite 构建通过。
- 真实 WebView2 完整实测通过：预检“开始分析”按钮滚动后完整可见、分析后工作区结果已保存、历史案件页打开、上下文重建与报告导出正常；前端错误 0。结果：`outputs/web-gui-12b2/flow-qa-20260807-fixes.json`。

# <a id="chg-20260807-04"></a>chg-20260807-04｜AI 经营关联验收链重跑起点与运行配置持久化

## 背景

- 2026-07-28 在检查点 `68e0ac8` 完成经营关联 v11 验收（development/reserved/337 完整验收均通过），但当前重构仓库在验收后有 `6e149d6`、`7b08441` 经营上下文准入改动。
- 用当前代码对 v11-full 缓存做零网络离线重放：286 项全部缓存未命中（`cache_hit=0`），旧验收证据不能沿用，需要用当前代码重跑验收链。

## 调整

- 新增 `tools/save_deepseek_ai_config.ps1`（一次性保存，隐藏输入，DPAPI 加密到 `%LOCALAPPDATA%\BankFlowReview\ai_runtime.json`）与 `tools/load_deepseek_ai.ps1`（每个新会话加载环境变量）；Key 不进仓库、不进日志、不进标准结果。
- 用户授权重跑验收链；固定样本清单沿用 `outputs/mvp-acceptance-2026-07-28/v10-architecture/曹国民_AI固定开发与保留样本.json`（development 50 / reserved 50）。

## 已执行

- development 50 真实调用：`status=ok`，50/50 采用，1 次提供方调用，0 无效缓存。
- 产物：`outputs/mvp-acceptance-2026-08-07/dev50.md`、`outputs/mvp-acceptance-2026-08-07/ai-response-cache/`（仓库外）。

## 待执行（用户暂停，需指令继续）

1. development 缓存离线重放（零网络）。
2. reserved 50 真实调用 + 离线重放（用户确认节点）。
3. 337 完整验收（286 语义 / 1115 笔展开）+ 离线重放 + 输入审计（用户确认节点）。
4. 收口记录与冻结结论。

## 立项讨论结论（仅沟通，未执行）

- AI 生活轨迹：单独立项（独立 `task_type=life_trajectory`、提示词、schema、缓存命名空间、验收样本）。
- 用车痕迹：不单独 AI 立项，并入生活轨迹的行为/地点候选层，工作台单独展示模块。
- 消费水平：单独立项但走确定性统计，不建 AI；与生活轨迹共享消费类别字典。
- 三者进入标准结果均需 schema 解冻或 sidecar 方案，由用户另行确认。

# <a id="chg-20260807-05"></a>chg-20260807-05｜AI 经营关联验收链完成与工作台设置页经营上下文增强

## 需求

- 用户指令：解除 AI 默认关闭（仅验收链运行进程内启用），执行 AI 验收链（可连网）、回归与验收类；完成后做工作台设置页经营上下文小增强；依次完成并验收。

## 调整

- `tools/run_ai_sample_acceptance.py` 与 `tools/replay_ai_response_cache.py` 新增 `--split`（`development` / `reserved_acceptance`，默认 `development`），用于执行固定样本中的保留验收分片；批准样本 JSON 未修改。
- 工作台新增“设置”页（侧栏入口），展示当前案件经营上下文：TXT 自动提取只读区 + 人工确认可编辑区；侧栏设置按钮在已打开时关闭、未打开时加载上下文。
- 后端新增白名单方法 `get_current_manual_case_context` / `save_current_manual_case_context`：基于当前案件目录（`_current_case_dir`）读取/保存经营上下文，无关联目录时返回 `CURRENT_CASE_CONTEXT_UNAVAILABLE`；复用既有 `ManualContextDTO` 与工作区保存逻辑。
- 真实窗口 QA 新增设置页场景：打开设置页 → 校验字段渲染 → 编辑“主要经营内容” → 保存 → 后端回读 → 关闭。

## 执行结果（AI 验收链，产物在仓库外 `D:\Investigator PDF\outputs\mvp-acceptance-2026-08-07\`）

- development 50 离线重放（`dev50-replay.json`）：`cache_hits=50`、`cache_misses=0`、`provider_calls=0`、`replay_mismatches=0`、`replay_consistent=true`、50/50 采用。
- reserved 50 真实调用（`reserved50.md`）：`status=ok`、50/50 采用、1 次提供方调用、0 校验失败；离线重放（`reserved50-replay.json`）50/50 命中、零调用、零失配。
- 完整验收（`full286.md`）：286 种唯一语义、6 批、展开 1115 笔全部采用，0 校验失败；离线重放（`full286-replay.json`）286 命中、零调用、零失配；输入审计（`full286-audit.json/md`）为 7192 笔、155 笔确定性精确命中、337 旧口径语义、286 送模语义、1115 笔送模候选、42 笔本地归非经营。

## 回归与验收

- Python 全量单元测试：368 项通过（2 SKIP，含新增 2 项设置页后端测试）。
- 统一回归：71 PASS / 0 真实失败 / 20 项既有路径缺失 / 1 项佛山主动暂缓（与既有基线一致）。
- 前端：27 PASS；TypeScript 检查与 Vite 构建通过。
- `git diff --check` 通过。
- 真实 WebView2 完整实测（`outputs/web-gui-12b2/flow-qa-20260807-settings.json`）：`ok=true`，取消/失败恢复/成功进入工作台（7192 笔、9 模块）/经营上下文预检/设置页/历史案件/上下文重建/报告导出 8 个场景全部通过，前端错误 0。

## 边界

- 产品 AI 默认关闭语义未改变；验收链仅在运行进程内通过 `tools/load_deepseek_ai.ps1` 显式授权。
- 未修改 schema 1.16、批准 JSON 与客户资料；Key 未写入仓库、日志或标准结果。
- 本轮已本地收口提交，未 push；push 等待用户指令。

# <a id="chg-20260807-06"></a>chg-20260807-06｜工作台模块展示修复与证据页精简

## 需求

- 用户反馈：下定与购车、经营痕迹、申报对照、人工核实四个模块的“交易与命中内容、金额、分类”显示仍有问题；交易证据页的 `raw_fields[N]` 逐项显示对分析没有帮助，应去掉；修复后跑一轮回归验证。

## 调整（仅展示层 DTO 映射，未改业务规则与标准结果内容）

- 经营痕迹：确定性/AI 候选按 `transaction_id` 从 `original_transactions` 补全交易上下文（方向、金额、日期、来源、对手、摘要）；分类列显示中文分类（直接相关/可能相关/无关联证据/无法判断），不再显示来源标签。
- 下定与购车：命中文本去重（不再出现“购车款；购车款.”）；此前收入行避免“此前收入/此前收入”重复，无摘要时副文本显示“仅作时间并列”边界说明。
- 申报对照：分类列显示对照项类型（工作单位、工作地点、居住地点、车辆上牌地点、经销商、下定描述、下定相关支出等），命中状态/原因放副文本。
- 人工核实：分类列英文代码映射为中文（交易结构/资金流向/文字背景/申报核实）；金额、方向、日期、来源取首条证据交易；不再把布尔 `non_conclusion` 显示成“True”。
- 交易证据页：移除 `raw_fields[N]` 逐项行，保留 `raw_text` 原始整行；脱敏视图同步精简。

## 验证

- Python 全量单元测试：371 项通过（2 SKIP，含 4 项新增展示断言：经营痕迹上下文补全、人工核实中文分类与金额、证据页无 `raw_fields[` 标签）。
- 前端：27 PASS；TypeScript 检查与 Vite 构建通过；`git diff --check` 通过。
- 统一回归：71 PASS / 0 真实失败 / 20 项既有路径缺失 / 1 项佛山主动暂缓（与基线一致）。
- 真实 WebView2 完整实测（`outputs/web-gui-12b2/flow-qa-20260807-display-fixes.json`）：`ok=true`，8 个场景全部通过，前端错误 0。
- 韩鹏飞真实标准结果展示复核：经营痕迹行金额/方向/对手/中文分类正确；购车命中文本无重复；申报对照分类列为对照项；人工核实分类为中文且金额来自证据交易；证据页仅剩 `raw_text`。

## 边界

- 仅修改工作台展示层映射，未改 schema 1.16、解析器、业务观察、批准 JSON 与客户资料；本轮已本地收口提交，未 push。

# <a id="chg-20260807-07"></a>chg-20260807-07｜取消经营上下文“确认状态”，填写保存即视为已确认

## 需求

- 用户确认：经营上下文的“未确认/已确认”两档对单人审核流程没有意义，取消确认动作；填入信息即视为已确认。

## 调整

- 前端移除预检页与设置页经营上下文表单中的“确认状态”下拉；提示文案改为“填写并保存即视为已确认”。
- `ManualContextInput` 类型移除 `confirmation_status`；后端保存时固定写 `confirmation_status=confirmed`（忽略旧调用传入值），工作区记录与既有 AI 准入逻辑保持兼容。
- 真实窗口 QA 同步移除确认状态下拉断言，并修复历史案件页加载时序（等待 loading 结束再统计行数，消除偶发竞态）。

## 验证

- Python 全量 371 项通过（2 SKIP）；前端 27 项、TypeScript 与 Vite 构建通过；`git diff --check` 通过。
- 真实 WebView2 完整实测（`outputs/web-gui-12b2/flow-qa-20260807-confirm-removed.json`）：`ok=true`，8 场景通过，前端错误 0；设置页表单不再出现确认状态下拉，保存回读 `confirmation_status=confirmed`。

## 边界

- 仅改工作区人工上下文的保存语义与展示，未改 schema 1.16、业务观察、AI 规则与批准 JSON；本轮已本地收口提交，未 push。

# <a id="chg-20260803-01"></a>chg-20260803-01｜WebView2真实结果切片与来源复核链路收口

## 必要节点

- 独立 React + TypeScript 前端、Python CaseSession/DTO、pywebview + Edge WebView2 外壳和只读 schema 1.16 导入链路完成；Qt WebEngine 失败切片保留为历史验证记录。
- 新增独立 schema 1.16 客户结果导出工具；AI与网络关闭，不修改客户原资料或正式GUI。

## 问题与修正

- Worker 已识别来源“需复核”，但状态未进入标准结果；现由 `source_files[].status/review_reason` 正式承载，经 CaseSession 轻量 DTO 展示，前端不猜测或重算。
- WebView2 文件过滤器与带空格启动路径问题已修正；真实结果切案后清空旧列表、选中项和 Inspector。

## 影响与验证

- 影响限定在独立 Web/WebView2 技术切片及标准结果来源诊断传递，不替换正式GUI，不改变交易、购车或证据业务判断。
- case-025 源码窗口显示11来源、其中1来源需复核；切换case-129无状态残留，再切回可稳定恢复。
- 来源状态定向3 PASS，WebView2定向28 PASS / 1 SKIP，全量334 PASS / 2 SKIP；TypeScript检查、Vite构建和差异检查通过。
- 下一阶段：工作台正式迁移；最新功能打包复验在正式迁移阶段统一收口。
- 功能提交：`28a70e4`；此前未推送节点及本轮提交已按授权同步到远端同名分支。

# <a id="chg-20260801-01"></a>chg-20260801-01｜GUI步骤10至12用户确认与提交收口

## 用户确认

- 步骤10证据边界按当前实现收口：候选列表和默认证据摘要继续脱敏；用户主动点击“展开完整证据”后直接显示未脱敏的原始字段。
- 不增加共享屏幕提示、二次确认或展开后的额外脱敏。
- 步骤11的150ms卡片悬停和220ms页面切换速度经用户确认，暂按现状保留。

## 收口与验证基线

- 功能范围为GUI步骤5至12，包含兴业8列强水印解析修复、吴信鹏购车口径、证据中心、轻量动效和视觉收口；详细实现与真实案例数据见`chg-20260730-09`、`chg-20260730-10`、`chg-20260731-01`和`chg-20260731-02`。
- 验证基线：GUI定向43 PASS，全量单元279 PASS，兴业专项2 PASS / 0 FAIL / 0 SKIP，统一回归71 PASS / 0 FAIL / 21 SKIP，语法、离屏启动、运行态动效和`git diff --check`通过。
- 用户已授权本地提交和Obsidian同步；未授权推送。

## 持续边界

- 不修改schema 1.16字段含义，不恢复经营AI调用，不启动生活轨迹、用车轨迹或高德。
- 展开完整证据是用户主动操作；默认列表脱敏边界没有被取消。
- 不修改客户原始资料，未调用外部模型，未推送。
