# 对手账号字段能力矩阵 v1

> 更新时间：2026-07-26
> 范围：各解析器向 `Transaction.counterparty_account` 提供可靠完整账号的能力。
> 判定边界：进入 v1C 还必须满足账号规范化后为 12 至 32 位数字、`field_confidence.counterparty_account == 1.0`、交易 ID 非空且交易非中性、非零收支。

## 状态定义

| 状态 | 含义 | v1C |
| --- | --- | --- |
| 可靠支持 | 来自独立表头列，或已有明确且已确认的固定分隔规则 | 满足完整账号门槛时可进入 |
| 仅 raw 保留 | 原文存在账号或混合对手信息，但不写入标准账号字段 | 不进入 |
| 明确排除 | 人工确认规则要求排除账号，或解析器显式清空标准账号 | 不进入 |
| 未确认 | 当前版式没有可靠拆列证据，不能从混合文本或相似版式推断 | 不进入 |
| 主动暂缓 | 已有争议或用户决定暂停，自动验证不执行 | 不进入 |

## 解析器级矩阵

| 解析器 / 版式 | 状态 | 当前依据与边界 |
| --- | --- | --- |
| `abc_corp` 八列有横线版、九列表格版 | 可靠支持 | `对方账号 / 对方户名 / 对方开户行`为独立列；按精确表头或列线边界映射。单元格内换行只合并本列，不跨列猜测。 |
| `boc` 十二列个人版 | 可靠支持 | `对方卡号/账号`为独立列。 |
| `bocom` 十一列时间版 | 可靠支持 | `对方账号`为独立列；六列日期版没有该字段，属于未确认。 |
| `bocom_corp` 十六列版 | 可靠支持 | `对方账号`为独立列。 |
| `citic` 个人固定尾段、`citic_corp` 九列表格 | 可靠支持 | 个人版只在固定尾部三段完整形成时拆出账号；对公版为独立账号列。 |
| `cmb` 个人固定尾段 | 可靠支持 | 仅末尾形成明确完整账号时拆分。 |
| `cmbc_corp` 对方户名/账号版 | 可靠支持 | 仅单个 `/` 的已确认固定规则，斜杠后为账号。 |
| `fudian`、`guilin`、`guilin_corp` | 可靠支持 | 均为独立账号列或固定坐标列。 |
| `hebei_corp_detail`、`huaxia_corp`、`luzhou`、`nanjing_corp`、`psbc`、`zhongyuan` | 可靠支持 | 当前确认版式均有独立账号表头并按同列映射。 |
| `foshan_rural` | 主动暂缓 | 表头虽独立，但 535 vs 533 疑点仍冻结；不得作为 v1C 已验证来源或表述为通过。 |
| `ccb` 建行个人固定七列版 | 可靠支持 | `对方账号与户名`完整原文保留；仅唯一 `/`、左侧规范化后为 12 至 32 位完整数字账号且右侧户名非空时拆出标准账号和户名。掩码、非数字、长度不足或边界不唯一不进入。 |
| `icbc` 个人、`icbc_corp` 账户明细清单 | 明确排除 | 解析器显式清空标准对手账号；对公只保留已确认名称、摘要、用途等字段。 |
| `customer_detail_corp`、`hebei_personal`、`bazhou_shunfeng_corp` | 明确排除 | 账号只保留为 `counterparty_account_raw` 来源证据。 |
| `lanzhou`、`tianjin_rural` 个人、`xingtai` | 明确排除 | 人工确认规则明确排除账号列。 |
| `spdb` 个人 | 明确排除 | 当前确认版显式清空标准账号。 |
| `abc` 个人、`cib`、`shengjing` | 仅 raw 保留 | 分别保留 `对手信息`、`对方账户/对方银行`或`对手信息`混合原文，不拆账号。 |
| `ccb_corp`、`chengdu_rural_corp`、`shanghai` 当前确认版 | 未确认 | 当前稳定字段不包含可靠标准对手账号；不得因原文出现数字而补写。 |
| `generic_pdf` | 未确认 | 继续只作数字字段兜底；禁止从混合 `raw_text` 猜账号。 |
| 其他未列出或新增版式 | 未确认 | 必须先取得原件、确认独立表头/固定边界并做字段专项验证。 |

## 案件账户发现与对手账号能力的区别

- `StatementMetadata.account_number` 是流水文件抬头中的本方账号，用于案件账户自动发现；它不等于交易侧 `counterparty_account`。
- `bankflow_v2.case_accounts.discover_case_accounts()` 只接受抬头户名和账号均非空、置信度均为 1.0、账号为完整 12 至 32 位数字的结果。
- 文件夹只定义案件边界，不自动认定文件属于同一自然人；每个发现账户必须通过 `account_ref` 做一次角色确认。
- `verification_context_from_manifest()` 只把已确认角色的账户送入 v1C，用户不需要重新录入完整账号。
- `verification_context_from_manifest()` 同时保留已确认账户可靠的 `source_file_ids`，仅供 v1D 将交易来源文件精确归属到账户；缺少该映射时双边候选明确不可用。
