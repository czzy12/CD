# 续接说明：2026-08-07 新窗口

## 当前执行定位（2026-08-07 收口，新窗口先读本段）

- 仓库：`D:\Investigator PDF\CD-bankflow-refactor`；分支：`work/deepseek-12b2-followup`。
- HEAD：`c88ee8f`（docs: sync handoff with schema 1.17 implementation status）。
- 推送状态：`5fb4685` 及之前已推送至 `origin/work/2026-07-18-bankflow-verification`（远端 = `29063cb`）；
  本轮 `c38c597 / ae8f8c1 / 4893b47 / 7e4f999 / c88ee8f` 仅本地提交，未 push。
- 工作区：唯一未跟踪文件为 `CD-bankflow-refactor.bundle`（保留，不提交）。
- schema 1.17 已实施（`chg-20260807-16`）：独立 `business_semantics_resolutions` 观察 +
  diagnostics legacy comparison + `migrate_schema_116_to_117`（不伪造历史）；schema 与 knowledge
  version 解耦；1.16/1.17 双向兼容；生产 resolver 仍为 legacy_v11；GUI 未改。
- Gate A 已完成（`chg-20260807-15`）：per-entry 行业画像修正、100 条 mismatch 人工裁决、
  63 条泛化 Gold Set（knowledge_v1 accuracy 99%）、第三轮 shadow agreement 69.33% /
  理论 AI fallback 40。产物在仓库外 `D:\Investigator PDF\outputs\knowledge-v1\`。
- 经营语义知识库架构 v1（`chg-20260807-14`）已完成：四层知识库（72 概念 / 252 别名 / 292 关系）、
  双轨缓存、AI 双任务契约、候选审核、validator、shadow 对比与迁移工具。
- 已完成（均已提交并推送）：AI 验收链与缓存优先重放（`chg-05/09`）、字段级抽查与来源×字段矩阵（`chg-08`）、工作台功能/显示优化与回归缺失分离（`chg-08/10`）、设置/证据/上下文读取修复（`chg-11/13`）、下定前收入改名（`chg-12`）。
- 当前停点（待用户指令）：
  1. 是否推送本地提交（`c38c597` 起）到 `origin/work/2026-07-18-bankflow-verification`；
  2. GUI 只读展示 `business_semantics_resolutions` 是否立项；
  3. 12 条行业关系冲突候选待人工裁决；40 条理论语义 fallback 的后续策略；
  4. 生活轨迹/用车痕迹/消费水平立项、字段级抽查人工回填、正式打包复验；
  5. 行业桶共享缓存方案（①跨客户共享 AI 验收缓存；②按 4 案例建行业桶）仍待确认。
- 新窗口第一轮动作：只读核验 `git branch --show-current`、`git rev-parse HEAD`、
  `git status --short`、`git log --oneline -6`；读 `PROJECT_STATUS.md`、本文件、
  `docs/change-history/2026-08.md`（chg-14/15/16）、`docs/schema_1.17_经营语义知识库设计草案.md`、
  `INTEGRATION_CONTRACT.md`；等待用户指令。
- 边界：不自动 commit/push（除非用户明确要求）；不改 schema 1.16 既有语义、批准 JSON 与客户资料；
  schema 1.17 仅承载 knowledge shadow，生产 resolver 仍为 legacy_v11；AI Key 只存本机加密配置；
  工作台 AI 仅离线重放已验收缓存；不接高德/客户信息 API；不删旧 Qt GUI 与 WebView2 技术切片。

## 一、仓库与 Git 状态

- 仓库：`D:\Investigator PDF\CD-bankflow-refactor`（同机同目录，无需复制文件）。
- 分支：`work/deepseek-12b2-followup`。
- HEAD：`c88ee8f`；`5fb4685` 及之前已推送，`c38c597` 起为本地未推送提交（远端仍为 `29063cb`）。
- 工作区：唯一未跟踪文件为仓库根目录 `CD-bankflow-refactor.bundle`（保留原样，不提交）。
- 已推送：`origin/work/2026-07-18-bankflow-verification` 与本地 `work/2026-07-18-bankflow-verification` 一致；提交链见 `git log --oneline -15`。
- 先读：`PROJECT_STATUS.md`、`docs/Web_GUI_12B2_完整分析流程接入报告.md`、`技术变更记录.md` 最新条目、本文件。

## 二、已完成并提交的功能

- 12B-1/12B-2 工作台主体；协作取消、失败恢复、成功进入工作台三场景实测。
- UI 修正：滚轮翻页、原始字段分隔符过滤、筛选行换行、底部显示完整。
- 历史案件页（自动记录摘要、打开/删除、工作区结果回退）。
- 经营上下文闭环（预检录入、手动重建观察、AI 关闭）。
- 报告导出（Markdown 验收视图）。
- 预检 TXT 上下文展示、列表中文显示。
- 设置页经营上下文查看/编辑（`chg-20260807-05`）。
- 取消经营上下文确认状态，填写保存即视为已确认（`chg-20260807-07`）。
- 工作台取消 AI 默认关闭（启动器加载配置后启用）；字段级抽查与来源×字段矩阵；人工核实证据笔数、经营痕迹确定性/AI 徽标与筛选、申报对照仅展示标签、历史案件重新分析、设置页清空上下文、金额千分位、提取状态中文化；统一回归缺失样本分离；任如冰不同行业固定 20 种抽检（`chg-20260807-08`）。
- 工作台 AI 改为验收缓存离线重放（`chg-20260807-09`）：分析零网络、不再卡在构建标准结果；真实 AI 调用只在显式验收链发生。
- 可执行优化批（`chg-20260807-10`）：设置页 AI 状态面板、经营痕迹不可用中文原因、构建阶段文案、回归缺失清单、清空上下文语义修复。
- 设置页点击无反应修复与证据第三层默认完整、逐行中文排版（`chg-20260807-11`）。
- 下定购车“此前收入”显示改名为“下定前收入”（`chg-20260807-12`）。
- 设置页预检阶段读取经营上下文与同名工作区回退（`chg-20260807-13`）。

## 三、AI 经营关联验收链（当前停点）

- 旧验收（2026-07-28，检查点 `68e0ac8`）不能沿用：当前代码有 `6e149d6/7b08441` 改动，旧缓存离线重放 286 项全部未命中。
- 已执行（2026-08-07，`chg-20260807-05`）：
  - development 50 真实验收（`status=ok`，50/50 采用，1 次调用）与离线重放（50/50 命中、零调用、零失配）。
  - reserved 50 真实调用（`status=ok`，50/50 采用，1 次调用）与离线重放（50/50 命中、零调用、零失配）。
  - 完整验收：286 语义、6 批、1115 笔展开全部采用；离线重放 286 命中、零调用、零失配；输入审计 337 旧口径 → 286 送模语义 / 1115 笔。
  - 报告：`outputs/mvp-acceptance-2026-08-07/dev50.md`
  - 报告：`outputs/mvp-acceptance-2026-08-07/dev50-replay.json`、`reserved50.md`、`reserved50-replay.json`、`full286.md`、`full286-replay.json`、`full286-audit.json/md`
  - 缓存：`outputs/mvp-acceptance-2026-08-07/ai-response-cache/`
- 已收口：`chg-20260807-05` 记录验收结果与冻结结论；回归（Python 368、统一回归基线、前端 27）与真实 WebView2 完整实测（含设置页场景）通过。
- 本轮收口已全部推送至 `origin/work/2026-07-18-bankflow-verification`（`13fa94b`）。
- 固定样本清单：`outputs/mvp-acceptance-2026-07-28/v10-architecture/曹国民_AI固定开发与保留样本.json`。
- 案件目录：`D:\Investigator PDF\MVP-input\曹国民`（7192 笔，286 语义）。

## 四、AI 运行配置（Key 持久化）

- 已保存：`%LOCALAPPDATA%\BankFlowReview\ai_runtime.json`（DPAPI 加密，不进仓库）。
- 每个新会话先运行：`tools\load_deepseek_ai.ps1`。
- 保存/更换：`tools\save_deepseek_ai_config.ps1`（隐藏输入，一次即可）。
- 连接测试：`tools\test_ai_connection.py`（合成数据）。
- Key 不得写入仓库、日志、标准结果；工作台分析零网络（AI 仅离线重放已验收缓存）；真实模型调用只在显式验收链发生。

## 五、后续立项讨论结论（仅沟通，未执行）

- AI 生活轨迹：单独立项（独立 `task_type=life_trajectory`、提示词、schema、缓存、验收样本）；用 TXT 中工作地/住家地址与流水生活轨迹交叉核对。
- 用车痕迹：不单独 AI 立项，并入生活轨迹行为/地点候选层，工作台单独展示模块。
- 消费水平：单独立项但走确定性统计，不建 AI；与生活轨迹共享消费类别字典。
- 三者进入标准结果需 schema 解冻或 sidecar 方案，由用户确认。

## 六、新窗口第一轮动作

1. 只读核验：`git branch --show-current`、`git rev-parse HEAD`、`git status --short`、`git log --oneline -3`。
2. 读 `PROJECT_STATUS.md`、本文件、`docs/change-history/2026-08.md`（chg-05..13）与 `INTEGRATION_CONTRACT.md`。
3. 如确需真实 AI 调用，先运行 `tools\load_deepseek_ai.ps1`（Key 在本机 DPAPI 配置）。
4. 等待用户指令：确认 AI 行业桶共享方案 / 立项生活轨迹等 / 打包复验。
5. 未经用户确认不得：schema 改动、打包、自动 commit/push。

## 七、安全边界

- schema 1.16、冻结业务规则、批准 JSON、客户资料不得修改；工作台 AI 零网络离线重放、真实调用需显式验收链；不接入高德/客户信息 API。
- 不输出客户完整姓名/账号/证件/完整交易内容；历史与工作区数据保持在 git 忽略的 `outputs/` 下。
- 不自动 commit/push；不删除旧 Qt GUI 与 WebView2 技术切片。
