# 续接说明：2026-08-08 Gate D.3.1 收口（下一窗口先读本段）

## 当前执行定位

- 仓库：`D:\Investigator PDF\CD-bankflow-refactor`；分支：`work/deepseek-12b2-followup`。
- HEAD：以 `git rev-parse HEAD` 为准；Gate D.3.1 已本地 commit，未 push。
- 工作区：唯一未跟踪文件为 `CD-bankflow-refactor.bundle`（保留，不提交）。
- Gate D.3.1：**PASS WITH FOLLOW-UP**（`chg-20260808-31`）：
  - payment rail 剥离 + business evidence 再平衡；generic/life/settlement 恢复；
  - home_appliance/retail/food/电商/政务边界与“汽车小镇”实体名 overreach 修复；
  - knowledge/semantic/alias/resolver/prompt → v3；schema 仍 1.17；
  - 真实 AI（semantic-concept-v3）：28 条唯一签名、1 批、18 pending candidate、0 失败/0 敏感外发；
  - Calibration：exact=45 / acceptable=14 / wrong=1 / unresolved_when_sufficient=1；
    sufficient recall=0.96；Human insufficient 11/11 保持 unresolved；
  - 24 条 false-insufficient 恢复 23 条；payment rail 0 regression；PII=0；
    property_management 本地复用稳定；47 strong / 06 conditional 不变；
  - Python 全量 543 项通过（2 SKIP）；compileall / git diff --check 通过；未 push。

## 当前主停点（待用户指令）

1. D.3.1 已知 follow-up：
   - `e05010d712a6` 纯扫码付款仍 unresolved（payment-rail-only 边界，按设计保守）；
   - `0fe0c6d1e03b` 与 `3895753ab684` 同一“消费退货”文本 Human Gold 分别为 generic/settlement，
     本地按 settlement，记录为 Human Gold 边界歧义，不改 Human Gold；
2. 条件关系架构：06×property_management 保持 undetermined + conditional_relation_candidate，
   future architecture work required；
3. D.3 / D.3.1 AI 产生的新 pending candidate（21 + 18 条）→ 后续独立 review set（v2）材料；
4. 原 12 条 legacy relation pending（未触碰）；
5. Production Holdout 与 Production Promotion Gate（未开始）；
6. GUI 只读展示立项、生活轨迹/用车痕迹/消费水平立项、行业桶共享缓存方案、正式打包复验。

## 新窗口第一轮动作

1. 只读核验 `git branch --show-current`、`git rev-parse HEAD`、`git status --short`、
   `git log --oneline -6`、`git rev-parse origin/work/2026-07-18-bankflow-verification`；
2. 读 `PROJECT_STATUS.md`、本文件、`docs/change-history/2026-08.md`（chg-31..30 及后续）、
   `docs/schema_1.17_经营语义知识库设计草案.md`（Frozen Contract）、`INTEGRATION_CONTRACT.md`；
3. 如确需真实 AI 调用先运行 `tools\load_deepseek_ai.ps1`；
4. 等待用户指令，不自动 commit/push。

## 边界

- 不改 schema 1.16/1.17 语义、批准 JSON 与客户资料；生产 resolver 仍为 legacy_v11；
  knowledge_v1 未转正；AI Key 只存本机加密配置；工作台 AI 仅离线重放已验收缓存；
  不接高德/客户信息 API；不删旧 Qt GUI 与 WebView2 技术切片。
