# 续接说明：2026-08-08 Gate F3B-PREP 后（新窗口先读本段）

## 当前执行定位

- 仓库：`D:\Investigator PDF\CD-bankflow-refactor`
- 分支：`work/deepseek-12b2-followup`
- 本地 HEAD：以 `git rev-parse HEAD` 为准（F3B-PREP 提交后）
- 已推送：用户授权 push 至 `origin/work/deepseek-12b2-followup`
  （push 后以 `git rev-parse origin/work/deepseek-12b2-followup` 核验）
- bundle：`CD-bankflow-refactor.bundle` 保留，不提交

## Gate 状态

- Gate F0 / F1 / F1.1 / F1.1b / F2：PASS（production-candidate-v2 已冻结）
- Gate F1.2 / F1.3 / F1.3.1：PASS / PASS WITH FOLLOW-UP
- Gate F3A：PASS WITH FOLLOW-UP（初版 80/4，已 supersede）
- Gate F3A.1 Resume：PASS WITH FOLLOW-UP（0808 接入：
  Transaction 100 / 8 案，Case 5 案，池独立，reserve 1）
- Gate F3B：**PREP 完成，等待用户人工填写 Human Gold**

## 冻结系统（不可改）

- schema = 1.17；knowledge = business-semantic-kb-v3；
  concepts = semantic-concepts-v3；aliases = semantic-aliases-v3；
  relations = industry-relations-v2；resolver = knowledge-v1-resolver-3
- production-candidate-v2 manifest checksum =
  `4e80dfdfd19de7d844c7509d7e02631e844c695bf9855fe6c4ddb547bf2681ea`
  （24 个 prediction-affecting files，只读）
- legacy_v11 = Production；knowledge_v1 = Shadow
- Human Gold Review Standard = `human_gold_review_standard_v1`（冻结）

## Holdout 产物（正式版，F3A.1 Resume）

目录：`D:\Investigator PDF\outputs\knowledge-v1\gate-f3a-1-resume-holdout-20260808\`

- Transaction：100 条 / 8 案
  `production_transaction_evidence_holdout_v1_items.jsonl`
- Case：5 案
  `production_case_holdout_v1_manifest.json` +
  `case_review_packets\`
- reserve：1 案（`reserve_cases.json`）
- checksums / lineage：`holdout_checksums.json` / `holdout_lineage.json`

## F3B 人工 Review 工作文件

目录：`D:\Investigator PDF\outputs\knowledge-v1\gate-f3b-human-gold-20260808\`

- `transaction_human_review_v1.csv`：100 行，逐笔填写
  human_industry_direct_relation / human_business_evidence_role /
  human_business_trace_strength / human_expected_route /
  human_sufficient_information / human_confidence /
  supporting_evidence_refs / reviewer_reasoning / reviewer_id / reviewed_at
- `case_human_review_v1.csv`：5 行，逐案填写
  business_activity_presence / declared_industry_consistency /
  human_assessment_sufficiency / refs / uncertainty / reasoning
- `transaction_qc_rereview_v1.csv`：10 条隐藏顺序复核清单
  （完成后独立再评一次，仅用于 QC，不计 accuracy）
- `case_review_meta.json`：案件事实参考
- `f3b_prep_report.json`：PREP 报告（gold=blank、prediction=0、provider=0）

## 新窗口第一轮动作（只读）

1. 读本文件；
2. `git fetch origin` 后核验
   `git branch --show-current` / `git rev-parse HEAD` /
   `git rev-parse origin/work/deepseek-12b2-followup` / `git status --short`；
3. 运行 `python -m unittest tests.test_gate_f2_v2 tests.test_gate_f3a_1_resume tests.test_gate_f3b`
   确认 Candidate v2 完整性、holdout 与 F3B-PREP 测试通过；
4. 确认 Human Gold 仍 blank；
5. 等用户完成人工填写 review CSV；不要自动填 Gold。

## 用户填写完成后的 F3B-FREEZE 动作

1. 读取用户填好的 `transaction_human_review_v1.csv` 与
   `case_human_review_v1.csv`；
2. 用 `tools/knowledge/gate_f3b_prep.py` 的
   `validate_transaction_gold` / `validate_case_gold` 校验：
   100/100、5/5、枚举合法、refs 合法、reasoning/reviewer 完整；
3. 处理冲突/漏填，全部通过后调用 `freeze_gold` 生成：
   `production_transaction_evidence_human_gold_v1.jsonl`、
   `production_case_human_gold_v1.json`、
   `human_gold_freeze_manifest.json`（含 checksum、QC、零调用计数）；
4. 再次核验 Candidate v2 checksum 未变；
5. commit（不 push，除非用户再次授权）；停止。

## 边界

- 不运行 Candidate Blind Run（Gate F4 需单独下发）；
- AI/程序不参与 Gold 判定；无法确定时人工填 undetermined/unknown；
- 不改 production-candidate-v2 24 个 prediction files；
- 不处理 reserve=1 的 shortage；不补 OCR；不找新案例；
- Review Standard 修改必须走 amendment + version 升级，不能静默改标准。
