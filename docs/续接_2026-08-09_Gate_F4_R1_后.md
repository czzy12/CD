# 续接说明：2026-08-09 Gate F4-R1 后（新窗口先读本段）

## 当前执行定位

- 仓库：`D:\Investigator PDF\CD-bankflow-refactor`
- 分支：`work/deepseek-12b2-followup`
- 本地 HEAD：以 `git rev-parse HEAD` 为准（本记录提交后）
- 已推送：用户授权 push 至 `origin/work/deepseek-12b2-followup`
  （push 后以 `git rev-parse origin/work/deepseek-12b2-followup` 核验）
- bundle：`CD-bankflow-refactor.bundle` 保留，不提交

## Gate 状态

- Gate F3B-FREEZE：PASS（Human Gold v1：Transaction 100/100、Case 5/5）
- Gate F3B.1：PASS（Human Gold v1.1 amendment：43 条 none/sufficient
  人工修正，unresolved semantic conflict = 0）
- Gate F4 Phase A：**BLOCKED**（provider runtime contract output
  truncation：batch_size=50 + max_tokens=4096 → 50 条结构化 JSON 截断；
  未生成预测、未评分、未访问 Gold；诊断已归档）
- Gate F4-R1：**PASS**（runtime contract revision：外部冻结配置
  batch_size=20；synthetic 20/20；semantic diff = 0）

## 冻结基线与 checksum

- Candidate v2（semantic predecessor）checksum：
  `4e80dfdfd19de7d844c7509d7e02631e844c695bf9855fe6c4ddb547bf2681ea`
- v2.1-runtime aggregate checksum：
  `d5a65e0fe9a4734e3717200c864e2d88b2f97218819438484a25105a0e6090bb`
  - 24 个 repo prediction-affecting files **全部未变**（与 v2 manifest 一致）；
  - 外部 runtime config（batch_size=20）已纳入 prediction-affecting surface，
    总文件数 25；
  - repo 内 `DEFAULT_BATCH_SIZE` 仍为 50（未改）；有效 batch_size=20 来自
    冻结的外部 runtime config。
- Human Gold v1.1（全部只读）：
  - Transaction Gold = `a02456225c7f9c596c8aef347abf70c36dbe5a50bd2a5838b95fe3def963a3f2`
  - Case Gold = `7713408a6f73013ff476cd2ce36f02a9030a2a3eece5a6dbb03d835a1664aeb7`
  - Freeze aggregate = `bfa5a0c1034307f4299586f792e2e14991d605459f81754648d85e9e2511a0dd`
- Holdout：Transaction 100 / Case 5 / pool overlap 0 / contamination clean

## 关键路径

- Human Gold（v1 / v1.1 / amendment / audit / xlsx）：
  `D:\Investigator PDF\outputs\knowledge-v1\gate-f3b-human-gold-20260809\`
- Holdout：
  `D:\Investigator PDF\outputs\knowledge-v1\gate-f3a-1-resume-holdout-20260808\`
- Candidate v2 freeze：
  `D:\Investigator PDF\outputs\knowledge-v1\production-candidate-v2-freeze-20260808\`
- v2.1-runtime freeze：
  `D:\Investigator PDF\outputs\knowledge-v1\production-candidate-v2.1-runtime-freeze-20260809\`
  （manifest / checksums / revision diff / synthetic preflight 均只读）
- F4 blocked 诊断（不得复用）：
  `D:\Investigator PDF\outputs\knowledge-v1\production-candidate-v2-blind-run-20260809\blocked_run_diagnostics\`
- AI runtime config（外部冻结配置，batch_size=20）：
  `%LOCALAPPDATA%\BankFlowReview\ai_runtime.json`
  （加载方式：`tools\load_deepseek_ai.ps1`；不输出密钥）
- Phase A 脚本（F4 Restart 可复用，需先更新 identity）：
  `D:\Codex Deepseek\Flow\_f4_phase_a.py`

## 新窗口第一轮动作（只读）

1. 读本文件；
2. `git fetch origin`；
   `git -C "D:\Investigator PDF\CD-bankflow-refactor" checkout work/deepseek-12b2-followup`；
   核验 `git rev-parse HEAD` 与 `git rev-parse origin/work/deepseek-12b2-followup`；
3. 运行测试（116 项）：
   `python -m unittest tests.test_deepseek_adapter tests.test_gate_f2_v2 tests.test_gate_f3a_1_resume tests.test_gate_f3b tests.test_local_ai_routing tests.test_business_evidence tests.test_case_trace tests.test_gate_f1_3_1`
4. 核验 checksum：Candidate v2=`4e80...`、v2.1-runtime=`d5a65e0f...`、
   Gold v1.1 aggregate=`bfa5a0c1...`；
5. 等待 ChatGPT 下发 `Gate F4 — Blind Run Restart on
   production-candidate-v2.1-runtime`。

## F4 Restart 注意事项

- 必须从 100 条 Transaction Holdout input 全量重新开始；
  **不得复用 blocked_run_diagnostics 中任何 local/partial/diagnostic 输出**；
- Phase A 不得读取 Human Gold label；只允许读 freeze manifest 的 checksum；
- 使用 v2.1-runtime identity：有效 batch_size=20（外部冻结 config）；
  脚本中 `CANDIDATE_CHECKSUM` 需改为 `d5a65e0f...`、manifest 记录
  candidate_id=`production-candidate-v2.1-runtime`；
- AI 调用前先确认 `tools\load_deepseek_ai.ps1` 加载成功且 batch_size=20；
- 若 batch=20 仍出现 `finish_reason=length`：立即 BLOCKED 交回 ChatGPT，
  不得自行提高 max_tokens / 改 batch / 改 prompt / 改 schema。

## 边界

- 不运行 Blind Run（除非 ChatGPT 下发 F4 Restart）；
- 不改 semantic / KB / prompt / schema / threshold；不重新调优；
- Human Gold 保持只读；不 push（除非用户另行授权）。
