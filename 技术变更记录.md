# PDF流水技术变更记录

用途：记录每次重要更新前遇到的问题、原因分析、解决方式、验证结果和影响范围。  
说明：本文件用于当前阶段测试和复盘，不代表最终交付范围。

## 2026-05-29 中信个人、民生对公、Excel 小数误差与 GUI 月度列顺序

### 更新前问题

- 中信银行个人账户交易明细提示未适配。
- 民生对公 `单位账户对账单` 会被更宽泛的民生关键字提前识别，导致 GUI 没有解析出流水。
- 交通银行图片 PDF 先转 Excel 后导入时，`借贷状态` 列未被识别，收入/支出口径和人工透视表不一致。
- Excel/通用识别场景存在 0.01 级余额差异，影响不大，但会把文件标为需复核。
- GUI `月度统计` 中 `流水笔数` 在前面，用户要求移动到 `期末余额(万元)` 后面。

### 解决方式

- 新增中信个人解析器，按上一笔余额判断单金额列是收入还是支出。
- 民生对公识别优先匹配 `单位账户对账单`。
- Excel 导入支持 `借贷状态`，并保留原始借/贷列口径；例如借方负数冲正仍计入支出笔数和支出合计。
- Excel 导入和通用 PDF 识别加入小额余额容差，0.99 元以内的余额差异标为 `低风险`，不再把文件状态打成 `需复核`。
- GUI 和导出 Excel 的 `月度统计` 列顺序调整为：月份、收入笔数、收入(万元)、支出笔数、支出(万元)、净额(万元)、期初余额(万元)、期末余额(万元)、流水笔数。

### 修改位置

- `bankflow_v2/citic.py`
- `bankflow_v2/auto_detect.py`
- `bankflow_v2/pipeline.py`
- `bankflow_v2/excel_input.py`
- `bankflow_v2/generic_pdf.py`
- `bankflow_v2/summary.py`
- `gui_v2.py`
- `tools/regression_cases.json`

### 验证结果

统一回归：

- 通过：10
- 失败：0
- 跳过：0

交通银行图片转 Excel 对照人工透视表：

- 流水：167 笔
- 收入：110 笔 / 1,841,181.92
- 支出：57 笔 / 1,744,549.50
- 余额差异：13 条，均为 `低风险`
- 需复核：0 条

### 影响范围

- 新增中信个人支持。
- 民生对公识别更稳定。
- Excel 导入和通用 PDF 对小额余额误差更宽容。
- 专用银行解析器的余额连续性仍保持原复核标准。

---

## 2026-05-25 统一回归入口与配置化路线

### 更新前问题

项目已经有零散验证工具，例如批量验证、工行专项回归、浦发专项回归，但缺少一个统一入口。

实际风险是：后续新增或修改某个银行解析逻辑时，如果只测当前样本，可能忘记复测之前已经通过的银行和特殊版式。

### 解决方式

新增统一回归入口：

```powershell
python tools\regression.py --all
```

新增代表样本清单：

- `tools/regression_cases.json`

当前第一批纳入：

- 工行个人强水印
- 工行个人历史明细
- 农行无具体时间排序
- 交通银行 6 列日期版
- 交通银行 11 列时间版
- 邮储银行历史明细
- 招商银行个人交易流水

输出只保留关键状态，例如：

```text
PASS: 工行个人-强水印 (918笔, 0异常)
FAIL: 某银行样本 (余额断点 3处)
```

失败时进程退出码为 1，方便后续作为打包前强制检查。

### 配置化路线

当前不做大重构，不把现有解析器立即改成配置化。

配置化的触发时机：

- `tools/regression.py --all` 覆盖主要银行、关键版式和最近踩过坑的样本。
- 某类银行规则开始重复，例如固定表格列只差日期列、收入列、支出列、余额列的位置。
- 每次打包前统一回归已经成为固定步骤。

推荐推进顺序：

1. 继续补充 `tools/regression_cases.json`。
2. 先从固定列表格类银行抽配置。
3. 复杂银行保留专用解析器，例如工行强水印、微信无余额、Excel 导入、图片 PDF 转 Excel 口径。

### 修改位置

- `tools/regression.py`
- `tools/regression_cases.json`
- `技术变更记录.md`

### 验证结果

已运行：

```powershell
python -m py_compile tools\regression.py
python tools\regression.py --all
```

当前结果：

- 通过：7
- 失败：0
- 跳过：0

### 影响范围

这是旁路验证工具，只在手动执行时运行。

不影响：

- GUI 日常使用
- EXE 打包运行
- 现有银行解析逻辑
- Excel 导出逻辑

---

## 2026-05-26 工行活期与定期账户混排复核口径确认

### 更新前问题

`工商银行历史明细（申请单号：26050110572953314933）.pdf` 在 0526 批量测试中：

- 流水笔数正确
- 收入/支出金额正确
- 期初余额正确
- 期末余额按交易日期取到 `8,827.46`，该口径是正确的
- 异常提示 13 条

PDF 页面最后一行余额为 `0.00`，但该笔交易日期不是整份流水的最新日期，因此不能作为期末余额。

### 原因分析

该 PDF 不是单一活期账户流水。最后一页先列出活期账户 2026-04-30 的末段交易，随后又列出多个定期账户的开立和销户记录。

程序把整份工行历史明细按交易日期排序后当作一条余额链校验。由于该 PDF 同时出现活期账户和定期账户，余额链会出现不连续提示。

该提示不能静默删除，因为它对使用者意味着“这里存在跨账户/储种混排，需要人工复核余额口径”。

### 解决方式

本次确认口径：

- 不按页面最后一行作为期末余额。
- 文件汇总的期初/期末余额仍按交易日期顺序取首尾。
- 保留余额不连续异常提示。
- 将该样本加入统一回归，要求统计金额正确、期初期末正确，同时异常数保持为 13。

### 修改位置

- `tools/regression_cases.json`
- `技术变更记录.md`

### 验证结果

该样本修复后：

- 流水：114 笔
- 收入：39 笔 / 6,326,210.92
- 支出：75 笔 / 6,336,938.07
- 净额：-10,727.15
- 期初余额：19,554.61
- 期末余额：8,827.46
- 异常：13

统一回归：

- 通过：8
- 失败：0

### 影响范围

不修改工行解析逻辑，只确认回归口径与复核提示口径。

---

## 2026-05-25 工行强水印余额连续性修复

### 更新前问题

`刘少伟已解密.pdf` 为工行个人历史明细，GUI 解析后出现大量余额不连续提示，统计结果与手工核对差异较大。

GUI 旧结果：

- 流水：918 笔
- 收入：61 笔 / 137,053.03
- 支出：857 笔 / 134,650.66
- 净额：2,402.37
- 期初余额：437.47
- 期末余额：3,478.84
- 异常：80 条

### 原因分析

该 PDF 有强水印、印章、二维码等干扰。水印字符不只是视觉层干扰，还会混入 PDF 表格文本抽取结果。

典型污染值：

```text
392+4,935.00 -> 应取 +4,935.00
D14,722.40 -> 应取 4,722.40
C9CD4,712.50 -> 应取 4,712.50
AF04,352.50 -> 应取 4,352.50
22-960.00: -> 应取 -960.00
```

旧逻辑是逐行余额连续性校验。它只根据“上一笔已选余额 + 当前金额 = 当前余额”来选择当前行候选。

这对普通水印污染有效，但这份样本中存在“局部也能连续的错误候选”。一旦前面某一行选错，后续会沿着错误余额路径继续计算，导致整段偏移。

### 解决方式

工行个人解析改为全局余额连续性候选路径选择。

核心思路：

- 先为每一行生成多个金额候选和余额候选。
- 不再逐行立即拍板。
- 保留多条可能路径。
- 用整段流水的余额连续性选择最合理路径。

### 修改位置

- `bankflow_v2/icbc.py`
- `bankflow_v2/number_parser.py`

### 验证结果

`刘少伟已解密.pdf` 新结果：

- 流水：918 笔
- 收入：61 笔 / 145,123.03
- 支出：857 笔 / 145,381.66
- 净额：-258.63
- 期初余额：437.47
- 期末余额：178.84
- 异常：0 条

工行历史明细专项回归：

- 工行历史明细 PDF：15 份
- 可读样本：10 份
- 通过：10 份
- 需复核：0 份
- 密码/读取失败忽略：5 份
- 失败：0 份

### 影响范围

只影响工行个人历史明细 `icbc`。

不直接影响：

- 工行对公 `icbc_corp`
- 其他银行
- Excel 导入
- 微信流水

---

## 2026-05-23 交通银行两种格式与排序修复

### 更新前问题

部分交通银行 PDF 可以识别为交通银行，但解析不到流水。

另有部分只有日期、没有时分秒的流水，会被补成 `00:00:00`，排序到当天最前面，导致余额连续性误报。

### 原因分析

交通银行存在两种不同版式：

```text
旧版：序号 / 交易日期 / 交易时间 / 交易类型 / 借贷标志 / 交易金额 / 账户余额 / ...
新版：交易日期 / 交易地点 / 交易方式 / 借贷状态 / 交易金额 / 余额
```

原解析器只支持旧版 11 列带交易时间格式。

排序问题来自：无时分秒交易被当作真实 `00:00:00` 参与排序。

### 解决方式

- `bankflow_v2/bocom.py` 新增交通银行 6 列仅日期版解析。
- `bankflow_v2/summary.py` 新增统一排序规则：
  - 同一天全部有明确时分秒时，按时分秒排序。
  - 同一天混有无具体时间交易时，按 PDF 原始页码/行号顺序。
- `gui_v2.py` 的明细展示和 Excel 导出统一使用该排序。

### 验证结果

交通银行打包测试目录 6 份全部通过：

- 4 份 11 列带交易时间版
- 2 份 6 列仅交易日期版
- 异常均为 0

农业银行样本 `26051514250707878157.pdf` 排序修复后：

- 流水：225 笔
- 异常：0

### 影响范围

- 交通银行 `bocom`
- 通用排序逻辑
- GUI 明细展示和 Excel 导出顺序

---

## 2026-05-23 GUI 导出入口整理

### 更新前问题

GUI 左上角菜单中有一个“文件 -> 导出 Excel”，工具栏右侧也有一个“导出 Excel”，入口重复。

### 解决方式

删除左上角菜单导出入口，只保留工具栏右侧按钮。

### 修改位置

- `gui_v2.py`

### 影响范围

只影响 GUI 入口展示，不影响导出逻辑。

---

## 2026-05-21 至 2026-05-22 工行、浦发与批量验证

### 更新前问题

新增打包测试目录后，需要批量确认银行支持范围。工行个人历史明细受水印、红章、二维码干扰，金额和余额会混入噪声。

### 解决方式

- 新增批量验证工具 `tools/batch_validate.py`。
- 新增工行专项回归 `tools/icbc_regression.py`。
- 增强金额和余额候选解析。
- 新增浦发个人、浦发对公解析器。

### 修改位置

- `tools/batch_validate.py`
- `tools/icbc_regression.py`
- `tools/spdb_regression.py`
- `bankflow_v2/number_parser.py`
- `bankflow_v2/auto_detect.py`
- `bankflow_v2/spdb.py`
- `bankflow_v2/pipeline.py`

### 验证结果

工行：

- 工行历史明细 PDF：15 份
- 密码/读取失败忽略：5 份
- 可读样本：10 份
- 可读样本通过：10 份
- 需复核：0

浦发：

- `浦发 (2).pdf`：个人，异常 0
- `浦发.pdf`：对公，异常 0
- `浦发2.pdf`：个人，异常 0
- `浦发3.pdf`：个人，异常 0

### 影响范围

- 工行个人历史明细
- 浦发个人
- 浦发对公
- 批量验证报告

---

## 2026-05-17 GUI、Excel 导入与图片 PDF 处理口径

### 更新前问题

部分银行 PDF 是图片型或扫描型，直接 PDF 文本抽取为空。OCR 尝试准确率不足，容易造成余额连续性异常。

### 解决方式

- GUI 支持导入 Excel（`.xlsx`、`.xlsm`）。
- 图片 PDF 当前不做 OCR，建议先用外部工具转 Excel，再导入 GUI。
- GUI 支持选择文件、选择文件夹、拖拽导入。
- 新增日期筛选，默认自然月近 6 个月。
- 月度统计按万元显示和导出，保留两位小数。
- 文件汇总和流水明细仍按元显示。

### 修改位置

- `gui_v2.py`
- `bankflow_v2/excel_input.py`
- `bankflow_v2/wechat.py`

### 影响范围

- GUI 输入方式
- Excel 导入
- 微信流水
- 图片 PDF 处理口径

### 当前不支持

- 扫描/图片 PDF 直接 OCR
- 密码/加密 PDF
- 工资类完整原字段导出
- 兴业银行 PDF 直解
