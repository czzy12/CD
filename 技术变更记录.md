# PDF流水技术变更记录

用途：记录每次重要更新前遇到的问题、原因分析、解决方式、验证结果和影响范围。  
说明：本文件用于当前阶段测试和复盘，不代表最终交付范围。

## 2026-06-26 天津农村商业银行对公交易明细查询适配

### 背景

用户提供 `C:\Users\lenovo\Desktop\杜\ef6816a9e9f14d99950b6f2cd12610b91782392878046.pdf`，询问该银行是否之前解析过。该样本当前自动识别为未识别。项目中已有农村商业银行相关适配，但不是该天津农村商业银行 `交易明细查询` 版式。

### 原因分析

- PDF 标题为 `交易明细查询`，页眉包含 `机构名称：天津农村商业银行股份有限公司...`。
- 表格层稳定，字段为 `交易日期 / 收入 / 支出 / 余额 / 对方户名 / 对方账号 / 对方开户行 / 摘要 / 备注`。
- 现有农村商业银行适配为另一类 `客户账户明细对账单`，识别关键词和表格结构均不相同。

### 解决方式

- 新增 `bankflow_v2/tianjin_rural_corp.py`，按表格层解析天津农村商业银行对公 `交易明细查询`。
- 接入 `auto_detect.py` 和 `pipeline.py`，bank id 为 `tianjin_rural_corp`。
- 新增回归样例 `天津农村商业银行对公-交易明细查询-杜`。

### 验证

样本解析结果：

- 交易笔数：383
- 收入：109 笔，`10,047,435.39`
- 支出：274 笔，`10,097,272.09`
- 净额：`-49,836.70`
- 期初余额：`50,079.29`
- 期末余额：`242.59`
- 异常提示：0

## 2026-06-26 Windows 深色模式下 GUI 显示修复

### 背景

用户将 Windows 系统颜色改为深色后，PDF/Excel 流水解析 GUI 中部分控件显示异常：表格选中单元格出现浅蓝底白字，右侧禁用项文字发白，影响可读性。

### 原因分析

- GUI 主体使用浅色 stylesheet，但 `QApplication` 未固定 Qt 调色板，Windows 深色模式会影响部分原生控件的前景色、选中前景色和禁用态颜色。
- 表格只设置了 `selection-background-color`，没有显式设置选中前景色。

### 解决方式

- `gui_v2.py` 启动时调用 `apply_light_palette(app)`，固定 `Fusion` 样式和浅色 `QPalette`。
- 补齐 `QTableWidget::item:selected`、`QLineEdit/QDateEdit:disabled`、`QCheckBox:disabled`、`QPushButton:disabled` 等前景色和背景色。
- 新增 `assets/check.svg`，保证自定义 checkbox 在固定样式下仍显示勾选标记。

## 2026-06-26 浙江网商银行企业账户交易明细适配

### 背景

用户提供 `C:\Users\lenovo\Desktop\张燕\网商.pdf`，标题为 `浙江网商银行企业账户交易明细`，现有自动识别未命中。样本共 20 页，页眉统计为借方 85 笔、贷方 113 笔。

### 原因分析

- PDF 有可抽取文本层，但 `pdfplumber.extract_tables()` 将主体明细合并为大单元格，不能直接按表格行读取。
- 明细列坐标稳定，可以按词块坐标拆分序号、时间、交易名称、收入、支出、余额等字段。
- 表头为 `借方金额（收） / 贷方金额（支）`，列名方向与常见银行流水相反；借方列应作为收入，贷方列应作为支出。
- PDF 按新到旧打印；同一秒多笔交易需要按较大的序号优先恢复余额链。

### 解决方式

- 新增 `bankflow_v2/mybank_corp.py`，按词块坐标解析浙江网商银行对公 `企业账户交易明细`。
- 接入 `auto_detect.py` 和 `pipeline.py`，bank id 为 `mybank_corp`。
- 同秒多笔通过微秒排序恢复：序号越大，在同一秒内越早参与余额链。
- 新增回归样例 `浙江网商银行对公-企业账户交易明细-张燕`。

### 验证

样本解析结果：

- 交易笔数：198
- 收入：85 笔，`9,528,442.33`
- 支出：113 笔，`9,474,278.22`
- 净额：`54,164.11`
- 期初余额：`56,475.66`
- 期末余额：`110,639.77`
- 异常提示：0

## 2026-06-25 中国银行 PDF 图片转 Excel 收入筛选流水修复

### 背景

用户提供 `C:\Users\lenovo\Desktop\王梁\流水中行(1).xlsx`，为中国银行 PDF 图片转 Excel 后的流水。导入后只看到收入，且收入合计明显偏小，并出现大量余额不连续提示。

### 原因分析

- Excel 页眉明确显示 `按收支筛选：收入`、`借方发生数：0.00`，源文件不是完整收支流水，支出明细没有出现在表格中。
- 余额列仍是账户真实余额，会受到未展示支出影响；因此不能对收入筛选流水执行完整的相邻余额链校验。
- 第 2 页一笔工资金额被 OCR 为 `998.48`，但该页 `贷方发生数` 与明细合计相差 `8,990.00`，结合原始数字可校正为 `9,988.48`。

### 解决方式

- `bankflow_v2/excel_input.py` 的中行 PDF 转 Excel 分支识别 `按收支筛选：收入/支出`。
- 对筛选流水标记余额为参考余额，跳过完整流水才适用的相邻余额连续性和总闭合校验。
- 收入筛选流水按页读取 `贷方发生数`，当页明细合计与页眉合计存在唯一可由 OCR 漏位解释的差额时，自动校正该笔金额。
- `bankflow_v2/summary.py` 对 `balance_optional` 交易跳过相邻余额连续性校验。

### 验证

样本解析结果：

- 交易笔数：63
- 收入：63 笔，`7,940,602.97`
- 支出：0 笔，`0.00`
- 异常提示：0
- 自动校正：`2026-01-15 13:12:50` 金额 `998.48 -> 9,988.48`

说明：该样本源文件为收入筛选流水，无法从 Excel 中恢复未展示的支出明细。

## 2026-06-24 农村商业银行对公客户账户明细对账单适配

### 背景

新样本 `C:\Users\lenovo\Desktop\幸小利\公户_0.pdf` 为农村商业银行对公流水，标题为 `客户账户明细对账单`，现有自动识别未命中，GUI 无法直接解析。PDF 文本层未显示银行名，银行名由用户确认为农村商业银行。

### 原因分析

- PDF 文本层和表格层完整，但页眉没有可识别的银行名称。
- 表格为稳定 8 列：`交易日期 / 摘要 / 借方金额 / 贷方金额 / 余额 / 币种 / 对方账号/户名 / 用途`。
- 交易日期只有年月日，没有具体时分秒；同日顺序需要按 PDF 页码和行号保留。

### 解决方式

- 新增 `bankflow_v2/customer_account_corp.py`，按表格层解析 8 列客户账户明细。
- 新增 bank id `customer_account_corp`，显示名称为 `农村商业银行对公`；自动识别条件限定为标题、账号/卡号、8 列表头和借方总笔数字段同时命中。
- 借方金额作为支出，贷方金额作为收入，保留分列原始符号参与余额校验和汇总。
- 接入 `auto_detect.py`、`pipeline.py`，并新增回归样例。

### 验证

样本解析结果：

- 交易笔数：322
- 收入：78 笔，`6,493,129.95`
- 支出：244 笔，`6,102,568.19`
- 净额：`390,561.76`
- 期初余额：`2,660.18`
- 期末余额：`393,221.94`
- 异常提示：0

```powershell
python -m py_compile bankflow_v2\customer_account_corp.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py tools\regression.py
python tools\regression.py --case 公户客户账户明细-浙江瑞拓智能电气 --allow-missing
python tools\regression.py --all --allow-missing
```

## 2026-06-23 工商银行对公同秒多笔与负数冲正修复

### 背景

新样本 `C:\Users\lenovo\Desktop\问题流水0623\工行对公版式其他.pdf` 可识别为工商银行对公流水，但 GUI 异常提示出现多条黄色 `余额不连续`。问题集中在同一秒内有多笔交易的记录，另有总收支与首末余额闭合差异。

### 原因分析

- 该工行对公版式同一秒多笔交易在 PDF 表格中的打印顺序可能与余额链顺序相反，直接按页码、行号排序会把相邻交易颠倒。
- 部分借方发生额为负数，属于支出冲正；旧版 A/B 工行对公解析未显式保留分列金额符号，汇总时负数支出被按正支出口径处理，导致总闭合差 85,000 元。

### 解决方式

- `bankflow_v2/icbc_corp.py` 对完全相同交易时间的交易组按余额链评分，必要时反向恢复组内顺序，并用微秒保持稳定排序。
- 工行对公旧版 A/B 与新版 C 一致，保留收入/支出列原始符号参与汇总，负数借方按冲正减少支出。

### 验证

样本解析结果：

- 交易笔数：2020
- 收入：301 笔，`46,797,927.24`
- 支出：1719 笔，`48,277,986.54`
- 净额：`-1,480,059.30`
- 期初余额：`1,877,639.21`
- 期末余额：`397,579.91`
- 异常提示：0

```powershell
python -m py_compile bankflow_v2\icbc_corp.py tools\regression.py
python tools\regression.py --all --allow-missing
```

全量回归结果：`54 PASS / 0 FAIL / 15 SKIP`。15 个 SKIP 均为本机缺少桌面路径样本文件，不是解析逻辑失败。

## 2026-06-21 PDF 转 Excel 与正常 PDF 混合导入佐证兼容

### 背景

同时导入 PDF 转 Excel 的流水和正常 PDF 识别流水后，点击收入佐证生成时存在失败风险。该场景下 Excel 导入结果的 `bank_id` 为 `excel`，银行标签为 `Excel导入`，不是实际银行名称。

### 原因分析

- 佐证 JSON 导出会把每个文件结果转换为账号行。
- 旧逻辑会将 `Excel导入` 归一成类似 `Excel文件` 的银行名，后续 Word 账号行可能生成 `Excel文件 银行...` 这类非实际银行格式。
- Excel 来源本身不能可靠代表银行名称，应交给收入佐证模板按“未识别银行”兜底，同时保留账号、来源文件和月度汇总。

### 解决方式

- `bankflow_v2/income_proof_export.py` 中 `bank_id == "excel"` 的账号银行名导出为空。
- 所有以 `Excel` 开头的导入标签不再作为银行名写入佐证 JSON。
- 金额、月份、账号号、来源文件仍正常导出；Word 生成时使用既有“未识别银行”容错文案。

### 验证

```powershell
python -m py_compile bankflow_v2\income_proof_export.py gui_v2.py tools\regression.py
```

补充内存样例：

- Excel 来源账号 + 正常 PDF 账号可合并导出经营佐证 JSON。
- 导出的 Excel 来源账号 `bank` 为空，`account_no` 和 `source_file` 保留。
- 将混合账号形态放入 Word 收入佐证准备 JSON 后，`validate_income_proof_input.py --ready` PASS，`fill_self_employed_word_com.py` 可实际生成 Word。

## 2026-06-21 微信流水模拟余额口径与重庆银行账户交易明细适配

### 背景

微信流水无期初/期末余额，原 GUI 和佐证导出采用收入与支出近似相等的展示口径。用户复核时发现每个月收入、支出几乎一致，视觉上不自然；同时收入调整后若直接使用微信原始支出，又会出现调整后收入和支出严重不一致。另有新样本 `C:\Users\lenovo\Desktop\新建文件夹\60d724e51fd041d4ade9318f9489e71c1781943258888.pdf`，标题为重庆银行 `账户交易明细`，现有自动识别未命中，通用 PDF 兜底解析不到流水。

### 原因分析

- 微信原始流水没有余额列，程序不能用真实余额闭合校验；此前只把支出贴近收入，能保持净额为正，但每月形态过于机械。
- 微信收入调整层最初直接基于原始月度支出汇总，和 GUI 上方微信平衡展示口径不一致。
- 重庆银行样本表格字段为 `序号 / 交易日期 / 交易金额 / 活期账面余额 / 交易类型/摘要 / 交易网点/交易渠道/附言 / 交易对手信息`，其中交易日期在表格中表现为 `2025\n0621`，不符合既有通用 PDF 日期解析规则。

### 解决方式

- 微信模拟口径改为从 0 模拟余额开始，按原始支出金额占比分布模拟支出。
- 每个月约束 `上月模拟余额 + 本月收入 - 本月支出 > 0`，并将累计模拟余额控制为小额正数，避免余额过大。
- 微信收入调整后先增加收入，再继续按原始支出形态和累计模拟余额约束重算支出；原始明细不被修改。
- 经营佐证和工资佐证导出继续接收 GUI 当前调整参数，导出的月度金额与 GUI 调整后口径一致。
- 新增 `bankflow_v2/chongqing.py`，按表格层解析重庆银行个人 `账户交易明细`；接入 `auto_detect.py` 和 `pipeline.py`。

### 验证

重庆银行样本自动识别为 `chongqing`，全量解析 192 笔，0 异常：

- 收入 160 笔，`1,395,094.65`
- 支出 32 笔，`1,397,813.05`
- 净额 `-2,718.40`
- 期初余额 `200,890.21`
- 期末余额 `198,171.81`

微信样本 `C:\Users\lenovo\Desktop\魏香娟\微信.pdf` 按 `2025-12` 至 `2026-05`、收入调整 40 万复算后，调整后月度支出保留原始支出走势，累计模拟余额始终为小额正数，最终累计约 302 元。

```powershell
python -m py_compile bankflow_v2\chongqing.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py bankflow_v2\adjustment.py bankflow_v2\income_proof_export.py gui_v2.py tools\regression.py
python tools\regression.py --all --allow-missing
```

全量回归结果：`54 PASS / 0 FAIL / 15 SKIP`。15 个 SKIP 均为本机缺少桌面路径样本文件，不是解析逻辑失败。

### 影响范围

- 微信流水的月度展示、调整后统计和佐证导出使用新的模拟余额口径。
- 重庆银行个人账户交易明细新增自动识别和专用解析。
- 不修改各银行原始交易明细；不把临时桌面重庆银行样本加入统一回归清单。

## 2026-06-18 收支平衡调整接入收入佐证导出

### 背景

流水识别 GUI 勾选 `启用收支平衡调整（个/公）` 并填写调整金额后，主界面月度统计会显示调整后金额，但点击 `经营佐证` 或 `工资佐证` 导出的佐证 JSON 仍保留原始流水金额，导致后续 Word 佐证表未体现调整金额。

### 原因分析

- `gui_v2.py` 的 Excel 导出已传入 `adjustment_configs()`。
- 佐证导出仍调用 `write_income_proof_input(path, self.results)` / `write_salary_income_proof_input(path, self.results)`，没有传递当前调整参数。
- `bankflow_v2/income_proof_export.py` 只按原始交易重新汇总月度金额，没有使用调整层的分摊结果。

### 解决方式

- `gui_v2.py` 的经营佐证和工资佐证导出均传入当前 `adjustment_configs()`。
- `income_proof_export.py` 支持接收调整参数，并将启用的调整金额分摊到导出的月度金额中。
- 个人和对公流水同时存在时，调整金额只作为一笔总调整处理，按当月两侧原始流水占比分摊，避免个人/对公两边重复加同一笔调整。
- 启用收支平衡调整时，收入和支出金额同步增加；未启用或调整金额为空时保持原始导出口径。

### 验证

```powershell
python -m py_compile gui_v2.py bankflow_v2\income_proof_export.py bankflow_v2\adjustment.py tools\regression.py
```

补充内存样例：

- 个人单侧原收入 3 万、支出 1.2 万，收支平衡调整 6 万后，佐证 JSON 收入为 9 万、支出为 7.2 万。
- 个人和对公同时存在时，6 万调整只加一次，合并收入从 3 万变为 9 万，合并支出从 0 万变为 6 万。

影响范围：仅调整 `经营佐证` / `工资佐证` 导出的金额口径，不修改银行解析器和原始交易明细。

## 2026-06-18 桂林银行企业客户交易清单适配

### 背景

用户提供 `C:\Users\lenovo\Desktop\张罗洋桂林银行.pdf`，当前自动识别为未适配银行。该 PDF 为桂林银行对公 `企业客户交易清单`，有稳定文本层和清晰版面，不属于图片 OCR。

### 原因分析

- 表头为 `交易日期 / 对方账号 / 对方户名 / 收入 / 支出 / 余额`，不命中现有城商行或通用 PDF 稳定规则。
- `pdfplumber.extract_tables()` 只能稳定抽取页眉信息，交易明细需要按文本坐标抽取。
- 部分支出列存在负数冲正；普通 `extract_text()` 会丢失个别负号，必须用 `extract_words()` 的坐标结果保留金额符号。
- 个别对方户名换行，但交易日期、对方账号、收入、支出、余额仍在同一行，需要允许户名为空后继续解析。

### 解决方式

- 新增 `bankflow_v2/guilin_corp.py`：
  - 按 PDF 单词坐标分行抽取交易。
  - 保留收入和支出列原始符号，支出负数用于冲正。
  - 按新到旧打印顺序反向恢复同日余额链。
- `auto_detect.py` 新增 `guilin_corp` 标签和关键词识别。
- `pipeline.py` 接入 `extract_guilin_corp`。
- `tools/regression_cases.json` 新增桂林银行样本回归。

### 验证

```powershell
python -m py_compile bankflow_v2\guilin_corp.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py tools\regression.py
python tools\regression.py --tag guilin_corp --allow-missing
python tools\regression.py --all --allow-missing
```

专项结果：

```text
桂林银行对公企业客户交易清单：1 PASS / 0 FAIL / 0 SKIP，200 笔，0 异常。
全量回归：56 PASS / 0 FAIL / 13 SKIP。
```

### 影响范围

- 新增桂林银行对公流水独立识别与解析入口。
- 不改变其他银行解析器、Excel 导入、GUI 展示和导出结构。

## 2026-06-14 流水0613 专项 PDF 适配

### 背景

用户提供 `D:\Codex data\CD_assets\PDF流水\流水0613` 问题样本目录，并明确要求只处理当前目录 PDF：Excel 只作核对参考，OCR/图片型 PDF 先排除。该批中交通银行对公、上海银行、华夏银行、平安银行和兴业银行存在未识别或识别后解析为 0 笔的问题。

### 原因分析

- 交通银行对公 `四川省分行明细对账单` 不是已支持的交通银行个人 6 列/11 列交易清单，需按 `会计日期 / 交易日期 / 借方发生额 / 贷方发生额 / 余额` 表格单独解析。
- 上海银行包含对公 `账户明细对账单 / 账户明细查询` 和个人 `上海银行交易明细`，对公有借贷分列表格，个人为文本行；部分流水按新到旧打印，同秒多笔需恢复余额链顺序。
- 华夏银行 `个人账户交易流水(电子版)` 有稳定文本层，交易行稳定包含 `YYYY-MM-DD + 摘要 + 交易金额 + 余额`，按新到旧打印。
- 平安银行 `个人账户交易明细清单`：
  - PDF 有文本层和表格层，不属于图片 OCR。
  - 4 份 PDF 是同一份 31 页清单拆分：`银行流水_2.pdf` 为 1-10 页，`银行流水_0.pdf` 为 11-20 页，`银行流水_3.pdf` 为 21-30 页，`银行流水_1.pdf` 为 31 页。
  - 水印 `BAP` 混入序号、日期、金额、余额和部分文本字段。
  - 续页没有完整银行标题，只能依赖清单编号、英文表头和交易字段组合识别。
- 兴业银行 `交易流水` 已识别为 `cib`，但旧逻辑走通用 PDF 兜底后解析为 0 笔；PDF 表格层可抽取，水印字符和数字会混入日期与余额，需用余额连续性选择正确值。

### 解决方式

- 新增 `bankflow_v2/bocom_corp.py`，按表格列解析交通银行四川省分行对公明细对账单。
- 新增 `bankflow_v2/shanghai.py`，解析上海银行个人和对公账户明细。
- 新增 `bankflow_v2/huaxia.py`，解析华夏银行个人账户交易流水电子版文本层。
- 新增 `bankflow_v2/pingan.py`：
  - 解析 8 列表格层：`序号 / 交易日期 / 交易金额 / 余额 / ...`。
  - 清洗金额与余额中的 `BAP` 水印。
  - 使用页脚 `x/31` 作为原始页码，序号作为行号，保证多 PDF 合并顺序稳定。
- 新增 `bankflow_v2/cib.py`：
  - 解析兴业银行 9 列表格层。
  - 优先用 `记账日期` 恢复日期。
  - 对余额生成候选值，优先选择满足余额链的候选。
- `auto_detect.py`、`pipeline.py` 接入上述银行识别和解析入口。
- `tools/regression_cases.json` 新增该批样本回归。
- `银行适配手册.md` 记录格式规则和解析入口。

### 验证

```powershell
python -m py_compile bankflow_v2\bocom_corp.py bankflow_v2\shanghai.py bankflow_v2\huaxia.py bankflow_v2\pingan.py bankflow_v2\cib.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py tools\regression.py
python tools\regression.py --tag bocom_corp --allow-missing
python tools\regression.py --tag shanghai --allow-missing
python tools\regression.py --tag shanghai_corp --allow-missing
python tools\regression.py --tag huaxia --allow-missing
python tools\regression.py --tag pingan --allow-missing
python tools\regression.py --tag cib --allow-missing
python tools\regression.py --all --allow-missing
```

专项结果：

```text
交通银行对公：15 PASS / 0 FAIL / 0 SKIP，合计 395 笔，逐文件 0 异常。
上海银行：4 PASS / 0 FAIL / 0 SKIP，逐文件 0 异常。
华夏银行：1 PASS / 0 FAIL / 0 SKIP，1010 笔，0 异常。
平安银行：4 PASS / 0 FAIL / 0 SKIP，合计 519 笔，逐文件 0 异常。
兴业银行：1 PASS / 0 FAIL / 0 SKIP，44 笔，0 异常。
全量回归：55 PASS / 0 FAIL / 13 SKIP。
```

目录复扫结果：

```text
D:\Codex data\CD_assets\PDF流水\流水0613
排除 Excel 和图片型 OCR/PCR 后：28 个 PDF 均已识别并解析。
剩余排除项：公户_0(2).pdf 为图片型 PDF，需要 OCR。
```

### 影响范围

- 新增交通银行对公、上海银行、华夏银行、平安银行、兴业银行专项解析入口。
- 不解析同目录手工 Excel，Excel 仍只作核对参考。
- 图片型 OCR/PCR PDF 保持排除，不改变 OCR 支持范围。

## 2026-06-14 工资类收入佐证导出入口

### 背景

收入佐证 Word 项目新增工资类模板，工资客户的流水表口径与自雇/个体模板不同：个人账户“流入”只填写客户工资类入账，“流出”仍填写个人账户全量支出。因此 PDF 流水项目需要单独提供工资类佐证 JSON，避免复用普通 `佐证填写` 入口时把全部收入写入工资流入栏。

### 解决方式

- `bankflow_v2/income_proof_export.py` 新增工资类导出：
  - `build_salary_income_proof_input`
  - `write_salary_income_proof_input`
  - `salary_flow_block`
  - `is_salary_transaction`
- 工资流入识别口径：
  - 仅统计收入交易。
  - 命中 `工资 / 薪资 / 薪酬 / 奖金 / 奖 / 代发工资`。
  - 排除 `补助 / 补贴 / 补增资 / 报销 / 退款`。
- 工资表月份优先取最近 6 个有工资入账的月份；流出金额按这些月份内个人账户全量支出统计。
- `gui_v2.py` 新增按钮 `工资佐证`，导出 `.salary_income_proof.json` 并调用收入佐证填表程序。

### 集成关系

- PDF 流水项目只负责导出工资类 JSON。
- Word 收入佐证项目负责读取 `proof_type: salary`，使用 `data/流水佐证工资.docx` 和 `fill_salary_word_com.py` 生成 Word。
- 普通 `佐证填写` 入口仍导出 `.income_proof.json`，用于自雇/个体/普通收入佐证；不会自动走工资模板。
- 两个项目分别提交和推送：
  - PDF 项目：`D:\Codex data\CD`，分支 `work/2026-05-31-flow-adjustment`。
  - Word 项目：`D:\report workflow`，分支 `codex/word-income-proof`。

### 验证

```powershell
python -m py_compile bankflow_v2\income_proof_export.py gui_v2.py
```

使用 `C:\Users\lenovo\Desktop\石丁庚\中原银行.pdf` 验证工资统计：

```text
2025.12: 流入 1 笔 / 0.42 万，流出 40 笔 / 1.68 万
2026.01: 流入 1 笔 / 0.42 万，流出 18 笔 / 1.13 万
2026.02: 流入 1 笔 / 0.42 万，流出 2 笔 / 0.72 万
2026.03: 流入 1 笔 / 0.42 万，流出 2 笔 / 0.34 万
2026.04: 流入 1 笔 / 0.42 万，流出 1 笔 / 0.33 万
2026.05: 流入 3 笔 / 1.79 万，流出 1 笔 / 0.33 万
合计: 工资流入 8 笔 / 3.90 万，流出 64 笔 / 4.52 万
月均: 工资流入 0.65 万，流出 0.75 万
```

### 影响范围

- 新增 GUI 入口和工资类 JSON 导出，不改变普通 `佐证填写` 入口。
- 工资识别依赖解析器保留交易摘要或原始字段；若某银行解析器没有保留工资摘要，需要后续给该解析器补 `raw_text/raw_fields`。

## 2026-06-13 成都农村商业银行对公客户明细适配

### 背景

用户提供 `C:\Users\lenovo\Desktop\张建蓉\张建蓉20260613131759\基础资料\公户_0.pdf`。该 PDF 当前自动识别为未适配银行，文本显示为成都农村商业银行对公 `客户明细`。

### 原因分析

- PDF 每页表格标题为 `客户明细`，开户银行为成都农村商业银行。
- 表头为 `交易日期 / 摘要 / 借方金额 / 贷方金额 / 余额 / 附言 / 对方账号 / 对方户名`。
- `pdfplumber.extract_tables()` 返回的不是逐笔行，而是整页每列一个换行单元格，需要按列内换行拆分。
- 流水按新到旧倒序打印，同一天可能跨页，必须按全局打印序号反向排序才能恢复余额链。

### 解决方式

- 新增 `bankflow_v2/chengdu_rural_corp.py`，独立解析成都农村商业银行对公客户明细。
- 只使用稳定对齐的核心列：序号、子序号、交易日期、摘要、币种、借方金额、贷方金额、余额；不强行拼接稀疏的对方账号/户名列。
- `auto_detect.py` 新增 `chengdu_rural_corp` 标签和关键词识别。
- `pipeline.py` 接入 `extract_chengdu_rural_corp`。
- `tools/regression_cases.json` 新增专项回归样本。

### 验证

```powershell
python -m py_compile bankflow_v2\chengdu_rural_corp.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py tools\regression.py
python tools\regression.py --tag chengdu_rural_corp --allow-missing
```

结果：

```text
公户_0.pdf：103 笔，收入 18 笔 / 4,542,903.55，支出 85 笔 / 4,104,442.61，期初余额 8,766.01，期末余额 447,226.95，0 异常。
专项回归：1 PASS / 0 FAIL / 0 SKIP
```

### 影响范围

- 新增成都农村商业银行对公 `客户明细` 独立识别与解析入口。
- 不改变其他农商行、农村信用社、通用 PDF、Excel 导入、GUI 展示和导出结构。

## 2026-06-12 中原银行账户交易流水适配

### 背景

用户提供 `C:\Users\lenovo\Desktop\石丁庚\中原银行.pdf`。该 PDF 当前自动识别为未适配银行，截图/文本层显示为中原银行 `账户交易流水` 表格版式。

### 原因分析

- 该版式为 12 列表格：`交易日期 / 交易时间 / 金额 / 收支状态 / 余额 / 对方行名 / 对方户名 / 对方账号 / 交易渠道 / 交易类型 / 币种 / 附言`。
- 金额列本身没有正负号，必须依赖 `收支状态` 判断收入或支出，通用 PDF 解析不能安全处理。
- 流水按新到旧倒序打印，同秒多笔交易需要反转页内顺序才能恢复余额链。

### 解决方式

- 新增 `bankflow_v2/zhongyuan.py` 独立解析器，直接读取 PDF 表格层。
- `auto_detect.py` 新增 `zhongyuan` 标签和关键词识别：`账户交易流水 + 中原银行 + 收支状态`。
- `pipeline.py` 接入 `extract_zhongyuan`。
- `tools/regression_cases.json` 新增中原银行样本回归。

### 验证

```powershell
python -m py_compile bankflow_v2\zhongyuan.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py tools\regression.py
python tools\regression.py --tag zhongyuan --allow-missing
```

结果：

```text
中原银行.pdf：202 笔，收入 42 笔 / 422,348.09，支出 160 笔 / 388,424.90，期初余额 4,041.37，期末余额 37,964.56，0 异常。
专项回归：1 PASS / 0 FAIL / 0 SKIP
```

### 影响范围

- 新增中原银行个人 `账户交易流水` 独立识别与解析入口。
- 不改变通用 PDF、Excel 导入、GUI 展示和导出结构。

## 2026-06-12 中国银行 PDF 转 Excel 的 HTML xlsx 导入

### 背景

用户提供 `C:\Users\lenovo\Desktop\石丁庚\中国银行.xlsx`，文件扩展名为 `.xlsx`，但实际内容是 WPS/Excel HTML 工作簿，不是标准 OOXML 压缩包。现有 Excel 导入入口直接调用 `openpyxl.load_workbook`，因此会报 `BadZipFile: File is not a zip file`。

### 原因分析

- PDF 转 Excel 工具可能把 HTML 表格保存为 `.xlsx` 扩展名，Excel/WPS 能打开，但 `openpyxl` 无法读取。
- `excel_input.py` 已有中国银行转换表结构识别逻辑，支持 `记账日期 / 记账时间 / 币别 / 金额 / 余额`，但入口没有 HTML Excel 兜底。

### 解决方式

- `bankflow_v2/excel_input.py` 新增标准库 HTML 表格解析兜底。
- 当 `openpyxl` 遇到 `BadZipFile` 或 `InvalidFileException` 时，转为读取 HTML `<table>`，再复用现有 `_extract_sheet` 和中国银行转换表解析逻辑。
- 不改变标准 `.xlsx/.xlsm` 的读取路径。

### 验证

```powershell
python -m py_compile bankflow_v2\excel_input.py gui_v2.py tools\regression.py
python tools\regression.py --all --allow-missing
```

结果：

```text
中国银行.xlsx：513 笔，收入 98 笔 / 360,753.50，支出 415 笔 / 360,234.81，期初余额 10.01，期末余额 528.70，0 异常。
全量回归：29 PASS / 0 FAIL / 12 SKIP
```

### 影响范围

- 增强 Excel 导入对“HTML 伪 xlsx”文件的兼容性。
- 不改变 PDF 银行识别、标准 xlsx 导入、GUI 展示和 Excel 导出结构。

## 2026-06-09 招商银行对公账务明细清单适配

### 背景

`D:\Codex data\CD_assets\PDF流水\账务明细清单` 目录新增 6 份未解析 PDF。截图和文本层显示版式为招商银行对公 `账务明细清单 / Statement Of Account`，现有项目只有招商银行个人交易流水解析器。

### 原因分析

- 现有 `cmb.py` 只支持招商个人 `日期 + CNY + 金额 + 余额 + 类型` 行格式。
- 新版式为对公账务明细清单，交易行以 `YYYYMMDD` 开头，业务类型紧跟日期，金额列自带正负号，余额和对手户名在金额后方。
- 税单号、摘要和对手户名可能跨行，不能把续行当成新交易。

### 解决方式

- 新增 `bankflow_v2/cmb_corp.py`，独立解析招商对公账务明细清单，避免影响招商个人解析。
- `auto_detect.py` 新增 `cmb_corp` 标签和关键词识别：`账务明细清单 + Statement Of Account + 招商银行`。
- `pipeline.py` 接入 `extract_cmb_corp`。
- `tools/regression_cases.json` 新增 6 个 `cmb_corp` 回归样本。
- `银行适配手册.md` 记录新版式规则。

### 验证

```powershell
python tools\regression.py --tag cmb_corp --allow-missing
```

结果：

```text
6 PASS / 0 FAIL / 0 SKIP
```

### 影响范围

- 新增招商银行对公 `账务明细清单` 独立识别与解析入口。
- 不改变招商个人解析器、GUI 展示层、Excel 导出结构和收入佐证 JSON 结构。

## 2026-06-08 农行/交行口径修正、总闭合校验与 GUI 压缩

### 背景

继续回归小银行和历史样本时，发现个别银行流水存在特殊口径：农行 `自动抹账` 明细显示的交易方向和余额实际变化方向相反；交通银行部分流水同时带借贷方向和金额正负号，若只按方向列固定转换，可能把收入/支出反向。GUI 侧在小屏幕或远程环境下仍希望更紧凑。

### 原因分析

- 农行 `自动抹账` 属于冲正类交易，余额链比交易类型文字更可靠。
- 交通银行借贷方向列和金额正负号可能同时表达方向，需要兼容两者。
- 汇总层此前更关注逐笔余额连续性，缺少总口径的 `收入 - 支出 = 期末余额 - 期初余额` 复核提示。
- GUI 已压缩过一次，但摘要栏、日期筛选和右侧调整面板仍有继续收窄空间。

### 解决方式

- `bankflow_v2/abc.py`：新增农行金额/余额解析函数，遇到 `自动抹账` 且余额差等于金额相反数时，按余额差统计。
- `bankflow_v2/bocom.py`：交通银行借贷方向解析兼容金额正负号，方向和符号同时存在时按综合结果判断收入/支出。
- `bankflow_v2/summary.py`：增加总收支余额闭合校验，不适用于微信流水和余额可选流水；不闭合时追加 `收支余额不闭合` 复核提示。
- `gui_v2.py`：窗口默认尺寸和最小尺寸下调，日期输入、摘要栏、右侧流水调整面板和字号进一步压缩；可信度评分同步提高总闭合、最近两个月闭合的权重。
- `tools/regression_cases.json`：更新相关样本期望金额，匹配修正后的统计口径。

### 验证

```powershell
python -m py_compile bankflow_v2\abc.py bankflow_v2\bocom.py bankflow_v2\summary.py gui_v2.py tools\regression.py
python tools\regression.py --all --allow-missing
```

目标结果仍按当前 `PROJECT_STATUS.md` 记录执行；本次修改不改变 Excel 导出结构、收入佐证 JSON 结构或既有银行入口。

### 影响范围

- 提高农行冲正类交易和交通银行方向判断稳定性。
- 增加总口径闭合复核提示，便于人工发现整体统计异常。
- GUI 更适合小屏幕和远程窗口使用。

## 2026-06-07 城商行/农商行首批确认 5 家适配

### 背景

`D:\Codex data\CD_assets\PDF流水\打包测试\城商行` 目录下有多家小银行样本。初始批量验证时，全部未命中现有银行适配器，通用 PDF 兜底也没有抽取到流水。

用户明确要求：先确认一批解析较好的银行，剩余样本单独处理，不要把全部小银行共同归入一个解析结果。

### 原因分析

这些 PDF 都有文本层，不属于纯扫描件；失败原因主要是版式未适配。首批样本中，九江银行、佛山农村商业银行、兰州银行、宁波银行、南京银行对公可通过稳定的文本行或表格列抽取核心字段，并能通过余额闭合校验。

天津银行、莱阳农商银行、河北农信虽然能部分抽取，但存在水印打断日期、同日无时间流水顺序不稳定、或余额链仍需复核等问题，因此不纳入首批确认范围。

### 解决方式

- 新增 `bankflow_v2/city_commercial.py` 作为内部共享的文本/表格抽取工具。
- 在 `pipeline.py` 中拆成独立 bank id：`jiujiang`、`foshan_rural`、`lanzhou`、`ningbo`、`nanjing_corp`。
- 在 `auto_detect.py` 中只让 5 个已确认格式命中对应独立 bank id。
- 将 5 个样本加入 `tools/regression_cases.json`，tag 为 `city-commercial`。
- 天津银行、莱阳农商银行、河北农信保持未识别，后续按单银行逐个适配。

### 验证

```powershell
python -m py_compile bankflow_v2\city_commercial.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py tools\regression.py
python tools\regression.py --tag city-commercial --allow-missing
python tools\batch_validate.py "D:\Codex data\CD_assets\PDF流水\打包测试\城商行" --out "D:\Codex data\CD_assets\测试输出\城商行_批量验证_20260607_已确认5家.xlsx"
```

结果：

- 专项回归：`5 PASS / 0 FAIL / 0 SKIP`。
- 目录批量验证：`通过: 5`，`未识别银行: 3`。

### 影响范围

- 新增 5 家小银行独立识别与解析入口。
- 不改变 GUI、Excel 导出、调整层和既有银行适配器。
- 未确认的天津银行、莱阳农商银行、河北农信不被首批解析器自动处理。

## 2026-06-06 补充 AGENTS.md 并精简状态文档

### 背景

项目已经有 `PROJECT_STATUS.md`、`技术变更记录.md`、`银行适配手册.md` 等文档，但缺少面向 coding agent 的统一协作规则。`PROJECT_STATUS.md` 也开始混入逐文件金额、客户本地路径和样本细账，后续每次读取会消耗较多上下文。

### 解决方式

- 新增 `AGENTS.md`，记录项目定位、工作原则、关键入口、银行适配要求、验证要求和文档记录要求。
- 轻量整理 `PROJECT_STATUS.md`：保留当前状态、主要过程、遇到的问题、解决方式、验证结果和详细记录索引。
- 移除 `PROJECT_STATUS.md` 中不适合长期保留的逐文件金额、笔数、客户本地路径和过时提交列表。
- 更新 `银行适配手册.md`：同步 2026-06-06 当前回归结果、建行对公 17 列账户明细信息、中信对公账户交易明细，并压缩旧版配置化流程说明。
- 明确详细排查过程继续放在 `技术变更记录.md`，银行格式规则放在 `银行适配手册.md`。

### 影响范围

- 仅文档更新，不影响解析、GUI、导出或回归逻辑。

## 2026-06-06 GUI 调整面板压缩并优化启动

### 背景

右侧 `流水调整` 面板高度撑满窗口，底部空白较大；顶部仍显示 `导出 Excel` 按钮。近期 GUI 打开时偶尔需要等待几秒，主要与窗口显示前提前加载 PDF/Excel 解析链路有关。

### 解决方式

- 移除顶部 `导出 Excel` 按钮入口，保留导出函数以便后续需要时复用。
- 右侧 `流水调整` 面板宽度从 330 降为 290，并改为按内容高度顶部对齐，不再撑满整列。
- 将 `pdfplumber`、`openpyxl` 和银行解析器从 GUI 顶层导入改为按需导入，点击处理或导出时再加载。

### 验证

- `python -m py_compile gui_v2.py` 通过。
- UI 实例检查：按钮列表不包含 `导出 Excel`；右侧面板宽度为 290。
- 启动测量：`import gui_v2` 约 0.27 秒；创建 `MainWindow` 约 0.58 秒。
- `python tools\regression.py --all --allow-missing` 通过，结果 `25 PASS / 0 FAIL / 5 SKIP`。

## 2026-06-06 GUI 可信度新增“极高”等级

### 背景

完全识别且没有任何异常、没有复核提示时，原来仍显示为 `高`。业务上希望这类结果和普通高可信结果区分开。

### 解决方式

- GUI 可信度等级新增 `极高`。
- 参与评分的结果全部为专用解析、均解析到流水，且没有全局异常、没有文件内异常、没有复核消息时，显示 `极高`。
- 通用识别、Excel 导入、存在任意异常或提示时不会显示 `极高`。

## 2026-06-06 GUI 可信度改为等级显示并增强闭合校验

### 更新前问题

GUI 顶部可信度显示为 `高 96%`、`中 82%`、`低 58%` 这类百分比形式。该分数本质是启发式评分，不是精确概率，容易让用户误解为可量化准确率。

同时，原可信度已经考虑余额连续性和最近两个月是否有流水，但没有单独提高下面这个财务闭合关系的权重：

```text
收入合计 - 支出合计 = 期末余额 - 期初余额
```

### 解决方式

- GUI 顶部可信度只显示等级：`极高 / 高 / 中 / 低 / 未识别`。
- 底部摘要文案同步去掉具体百分比。
- 内部仍保留分数用于等级判断，不写入导出。
- 未识别且没有解析到流水时，可信度等级显示为 `未识别`。
- 当流水需要余额且期初/期末余额存在时：
  - 若 `净额 == 期末余额 - 期初余额`，提高加分权重。
  - 若不闭合，扣分。
  - 若闭合且无 `余额不连续` 需复核，视为强可信信号。
- 最近两个月判断从单纯“都有流水”细化为：
  - 最近两个月都有流水：小幅加分。
  - 最近两个月各自月度收支余额闭合且无 `需复核`：额外加分。
  - 最近两个月没有流水：仍不扣分。
- 微信流水和余额可选流水继续跳过余额闭合要求。

### 修改位置

- `gui_v2.py`
- `PROJECT_CONTEXT.md`
- `PROJECT_STATUS.md`
- `技术变更记录.md`

### 验证结果

已验证：

- `python -m py_compile gui_v2.py` 通过。
- 中信对公专用解析无异常时，可信度等级按最新口径显示为 `极高`。
- 合成未识别无流水结果可信度等级为 `未识别`。
- `python tools\regression.py --all --allow-missing` 通过，结果 `25 PASS / 0 FAIL / 5 SKIP`。
- 本次不打包 EXE。

## 2026-06-06 新增中信银行对公账户交易明细解析

### 更新前问题

`C:\Users\lenovo\Desktop\江丽娟\中信.pdf` 是中信银行对公账户交易明细，GUI 自动识别未命中，通用 PDF 兜底也解析为 0 笔。

### 是否已有同类解析

项目此前已支持 `中信银行个人账户交易明细`，并纳入统一回归：

```text
D:\Codex data\CD_assets\PDF流水\打包测试\0528流水测试\中信.pdf
```

但旧样本是个人版式，文本行为：

```text
交易日期 RMB 金额 RMB 余额 摘要
```

当前样本是对公分列表格版式，字段为：

```text
交易日期 / 柜员交易号 / 摘要信息 / 对方账号 / 对方账户名称 / 动账资金分簿 / 借方发生额 / 贷方发生额 / 余额
```

两者不是同一解析口径。

### 解决方式

- 在 `bankflow_v2/citic.py` 中新增 `extract_citic_corp()`。
- 自动识别新增 `citic_corp`，通过 `账户交易明细`、`柜员交易号`、`动账资金分簿`、`借方发生额`、`贷方发生额`、`余额` 组合表头命中。
- 解析器按 PDF 表格 9 列读取，借方计支出，贷方计收入，余额用于连续性校验。
- 保留旧 `citic` 个人解析器不变。
- 新增统一回归样本：`中信银行对公-账户交易明细-江丽娟`。

### 修改位置

- `bankflow_v2/citic.py`
- `bankflow_v2/auto_detect.py`
- `bankflow_v2/pipeline.py`
- `tools/regression_cases.json`
- `PROJECT_STATUS.md`
- `银行适配手册.md`
- `技术变更记录.md`

### 验证结果

中信对公样本自动识别为 `citic_corp`。

专项结果：

| 文件 | 流水 | 收入 | 支出 | 净额 | 期初余额 | 期末余额 | 异常 |
|---|---:|---:|---:|---:|---:|---:|---:|
| `中信.pdf` | 245 | 1,057,985.59 | 887,384.26 | 170,601.33 | 41,933.33 | 212,534.66 | 0 |

验证命令：

```powershell
python -m py_compile bankflow_v2\citic.py bankflow_v2\pipeline.py bankflow_v2\auto_detect.py tools\regression.py
python tools\regression.py --tag citic_corp --allow-missing
python tools\regression.py --tag citic --allow-missing
python tools\regression.py --all --allow-missing
```

结果：

- 中信对公专项回归：1 PASS / 0 FAIL。
- 原中信个人专项回归：1 PASS / 0 FAIL。
- 全量回归：25 PASS / 0 FAIL / 5 SKIP。
- 5 个 SKIP 均为本机缺少历史样本文件，不是解析失败。

## 2026-06-06 修复建设银行对公账户明细信息误分支

### 更新前问题

`C:\Users\lenovo\Desktop\张圣星\张圣星20260606104111\基础资料` 下的建设银行对公样本中，`银行流水_2.pdf` 和 `银行流水_4.pdf` 在 GUI 异常提示中显示 `未解析到流水`；`银行流水_3.pdf`、`银行流水_5.pdf` 因日期筛选提示 `日期范围内没有流水`。进一步排查发现，`银行流水_1.pdf`、`3.pdf`、`5.pdf` 虽然能解析到少量交易，但并未完整解析整份 PDF。

### 原因分析

这些 PDF 标题为 `中国建设银行账户明细信息`，表格为 17 列结构：

```text
账号 / 交易时间 / 借方发生额 / 贷方发生额 / 余额 / 币种 / 对方户名 / ...
```

`bankflow_v2/ccb_corp.py` 中用于识别 `活期存款明细账` 的 `_is_deposit_detail_table()` 条件过宽，只要表头包含 `日期`、`发生额`、`借方`、`贷方`、`余额` 就进入列内换行拆分分支。`账户明细信息` 表头中也包含这些字样，但列号完全不同，导致该分支按错误列位读取，返回 0 笔或漏掉大部分交易。

### 解决方式

- 收窄 `_is_deposit_detail_table()` 判断条件。
- 如果表头包含 `账号` 或 `交易时间`，不再视为 `活期存款明细账` 分支。
- `中国建设银行账户明细信息` 继续走原 17 列逐行解析逻辑。
- 不修改自动识别规则、金额口径、余额统计口径或 GUI 导出逻辑。

### 修改位置

- `bankflow_v2/ccb_corp.py`
- `PROJECT_STATUS.md`
- `银行适配手册.md`
- `技术变更记录.md`

### 验证结果

张圣星 5 份建设银行对公样本均识别为 `ccb_corp`，解析结果如下：

| 文件 | 流水 | 收入 | 支出 | 期初余额 | 期末余额 | 异常 |
|---|---:|---:|---:|---:|---:|---:|
| `银行流水_1.pdf` | 347 | 17,967,133.77 | 18,216,917.41 | 466,736.95 | 216,953.31 | 0 |
| `银行流水_2.pdf` | 144 | 5,308,987.70 | 5,424,181.78 | 216,953.31 | 101,759.23 | 0 |
| `银行流水_3.pdf` | 193 | 8,565,708.14 | 8,224,333.43 | 119,340.29 | 460,715.00 | 0 |
| `银行流水_4.pdf` | 243 | 6,080,338.82 | 6,125,316.87 | 460,715.00 | 466,736.95 | 0 |
| `银行流水_5.pdf` | 46 | 1,008,018.26 | 1,247,608.93 | 358,930.96 | 119,340.29 | 0 |

验证命令：

```powershell
python -m py_compile bankflow_v2\ccb_corp.py bankflow_v2\pipeline.py bankflow_v2\auto_detect.py
python tools\regression.py --tag ccb_corp --allow-missing
python tools\regression.py --all --allow-missing
```

结果：

- 建行对公专项回归：4 PASS / 0 FAIL。
- 全量回归：24 PASS / 0 FAIL / 5 SKIP。
- 5 个 SKIP 均为本机缺少历史样本文件，不是解析失败。

## 2026-06-05 新增农村信用社表格流水解析

### 更新前问题

`C:\Users\lenovo\Desktop\罗文峰\流水.pdf` 是农村信用社个人流水表格，现有自动识别未命中，通用 PDF 兜底解析为 0 笔。

### 原因分析

该 PDF 表格字段清晰，包括 `交易流水号`、`交易日期`、`收入/支出`、`交易金额`、`实时余额`。但流水按新到旧倒序打印，且同一秒内可能有多笔交易；如果直接按页面顺序或普通时间排序，同秒记录会出现余额链断点。

### 解决方式

- 新增 `bankflow_v2/rural_credit.py`。
- 自动识别新增 `rural_credit`，关键词为 `交易流水号交易日期交易网点收入/支出交易金额实时余额`。
- 解析器按表格列读取方向、金额和实时余额。
- 对同一秒内的记录增加不可见微秒排序，恢复余额链顺序；`raw_time` 仍保留 PDF 原始秒级时间。
- 新增统一回归样本：`农村信用社-个人交易流水表格`。

### 修改位置

- `bankflow_v2/rural_credit.py`
- `bankflow_v2/auto_detect.py`
- `bankflow_v2/pipeline.py`
- `tools/regression_cases.json`
- `PROJECT_CONTEXT.md`
- `PROJECT_STATUS.md`
- `银行适配手册.md`
- `技术变更记录.md`

### 验证结果

- 农村信用社样本自动识别为 `rural_credit`。
- 专项回归通过：211 笔，收入 `5,812,859.79`，支出 `5,670,778.56`，期初余额 `70,528.21`，期末余额 `212,609.44`，0 异常。
- `python -m py_compile bankflow_v2\rural_credit.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py tools\regression.py` 通过。
- `python -m py_compile bankflow_v2\summary.py bankflow_v2\pipeline.py bankflow_v2\adjustment.py bankflow_v2\qilu_corp.py bankflow_v2\rural_credit.py gui_v2.py tools\regression.py` 通过。
- `python tools\regression.py --all --allow-missing` 首次 180 秒超时，延长超时后通过，结果 `24 PASS / 0 FAIL / 5 SKIP`。

## 2026-06-04 新增齐鲁银行对公交易明细解析

### 更新前问题

`C:\Users\lenovo\Desktop\杨和平\齐鲁银行对公.pdf` 是新银行样本，标题为 `单位活期存款账户交易明细`。现有自动识别未命中，通用 PDF 兜底也解析不到流水。

### 原因分析

该 PDF 虽然有文本层和表格线，但存在齐鲁银行水印，水印字符会混入日期、金额和余额文本。直接 `extract_text()` 会把页面文字打散；简单按行或整行正则解析会漏行或把余额、支出首位数字读错。

### 解决方式

- 新增 `bankflow_v2/qilu_corp.py`。
- 自动识别新增 `qilu_corp`，关键词为 `单位活期存款账户交易明细`。
- 解析器按 PDF 表格行边界定位流水行，再按固定列坐标抽取收入、支出和账户余额。
- 对每个金额列按字符基线生成候选，过滤水印插入的零碎数字。
- 使用余额连续性在收入、支出、余额候选中选择正确组合。
- 支出列负数按原列口径保留，设置 `preserve_signed_columns=True`，余额校验仍按 `收入 - 支出`。
- 新增统一回归样本：`齐鲁银行对公-单位活期存款账户交易明细`。

### 修改位置

- `bankflow_v2/qilu_corp.py`
- `bankflow_v2/auto_detect.py`
- `bankflow_v2/pipeline.py`
- `tools/regression_cases.json`
- `PROJECT_CONTEXT.md`
- `PROJECT_STATUS.md`
- `银行适配手册.md`
- `技术变更记录.md`

### 验证结果

- 齐鲁银行样本自动识别为 `qilu_corp`。
- 齐鲁银行专项回归通过：1521 笔，收入 `60,215,496.73`，支出 `59,347,060.15`，期初余额 `3,930,196.45`，期末余额 `4,798,633.03`，0 异常。
- `python -m py_compile bankflow_v2\qilu_corp.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py tools\regression.py` 通过。
- `python -m py_compile bankflow_v2\summary.py bankflow_v2\pipeline.py bankflow_v2\adjustment.py bankflow_v2\qilu_corp.py gui_v2.py tools\regression.py` 通过。
- `python tools\regression.py --all --allow-missing` 通过，结果 `23 PASS / 0 FAIL / 5 SKIP`。

## 2026-06-04 未识别无流水样本可信度归零

### 更新前问题

`D:\Codex data\CD_assets\PDF流水\打包测试\城商行\兰州银行.pdf` 未命中已适配银行格式，通用 PDF 识别也没有解析到流水。GUI 顶部可信度仍会给出 `21%` 这类弱可信分，容易让用户误以为存在可参考的解析结果。

### 原因分析

`gui_v2.py` 的 `result_confidence_score()` 对 `summary.count <= 0` 的结果仍给了 25 分基础分，后续再按异常提示扣分，因此单个无流水样本会显示为低百分比，而不是 0。

### 解决方式

- 将 `summary.count <= 0` 的可信度基础分改为 `0`。
- 保留 `日期范围内没有流水` 的特殊处理逻辑，仍只作为异常提示，不参与可信度评分。
- 不修改银行识别、通用 PDF 识别、Excel 导出或解析结果结构。

### 修改位置

- `gui_v2.py`
- `PROJECT_CONTEXT.md`
- `PROJECT_STATUS.md`
- `技术变更记录.md`

### 验证结果

- 兰州银行样本验证：流水 `0`，可信度 `低 0%`，原因 `未识别到流水`。
- `python -m py_compile bankflow_v2\summary.py bankflow_v2\pipeline.py bankflow_v2\adjustment.py gui_v2.py tools\regression.py` 通过。
- `python tools\regression.py --all --allow-missing` 通过，结果 `22 PASS / 0 FAIL / 5 SKIP`。
- 未打包 EXE。

## 2026-06-03 GUI 增加流水可信度提示

### 更新前问题

部分未识别银行会走通用识别；部分已测试银行虽然整体解析正确，也可能因为少量可复核提示让用户难以判断整份流水是否可靠。原 GUI 顶部只有文件、流水、收入、支出、异常，不能表达“异常虽有但整体可信”或“通用识别需重点复核”的差异。

### 解决方式

- 在 GUI 顶部汇总栏新增 `可信度` 字段，放在 `异常` 右侧。
- 显示短格式：`高 96%`、`中 82%`、`低 58%`。
- 颜色按等级区分：高为绿色，中为橙色，低为红色。
- 可信度只在 GUI 显示，不写入 Excel 导出，不影响解析逻辑。
- 计算逻辑综合考虑：
  - 专用银行解析或通用识别的基础分。
  - 银行识别置信度。
  - 异常提示数量与级别。
  - 期初余额、期末余额是否存在，且余额链是否连续。
  - 当前日期筛选范围最后两个月是否都识别到流水；如果关闭日期筛选，则按识别到的最新流水月份向前取两个月。
- 2026-06-03 追加调整：`日期范围内没有流水` 只保留在原异常栏提示，不参与可信度评分；最后两个月都有流水作为加分项，没有则不扣分。
- 2026-06-03 追加调整：月度统计 GUI 显示去掉 `期初余额(万元)`、`期末余额(万元)` 两列；启用 `收入调整（微信）` 或 `收支平衡调整（个/公）` 后，月度表仍先保留按流水类型分组的原始统计，下面再显示 `全部` 调整统计。Excel 导出保持原字段。
- 2026-06-03 追加调整：GUI 不再显示 `流水明细` tab，但导出 Excel 仍保留流水明细；可信度对微信流水不再要求期初/期末余额，避免天然无余额列的微信样本被压到 98%。
- 2026-06-04 追加调整：`D:\Codex data\CD_assets\PDF流水\打包测试\城商行\兰州银行.pdf` 未识别且通用识别也没有流水时，可信度显示为 `低 0%`。

### 修改位置

- `gui_v2.py`

### 验证结果

- `python -m py_compile gui_v2.py` 通过。
- `python tools\regression.py --all --allow-missing` 通过，结果 `22 PASS / 0 FAIL / 5 SKIP`。
- 合成验证：正常专用解析文件 + `日期范围内没有流水` 提示时，可信度保持 `100% confidenceHigh`。
- GUI 月度表结构验证：调整启用时可生成 `个人`、`微信` 分组行，并在下方追加 `全部` 行；GUI 表头不再包含期初/期末余额两列。
- 样本验证：`公户_8.pdf` 单文件可信度 `100`，`微信流水_1.pdf` 单文件可信度 `100`，两者合并可信度 `高 100%`。
- 样本验证：`兰州银行.pdf` 的无流水可信度为 `低 0%`。
- 未打包 EXE。

## 2026-05-31 项目上下文轻量整理

### 更新前问题

`PROJECT_CONTEXT.md` 长期累积了大量历史进度、旧版实现记录和样本细节。后续在新窗口继续项目时，如果每次完整读取该文件，会占用较多上下文，并且容易把旧状态和当前最新状态混在一起。

同时，PDF 流水项目后续会作为车贷报告自动化总项目的流水子模块，需要更清晰地区分：

- 当前入口说明。
- 当前最新状态。
- 长期集成契约。
- 历史技术变更。
- 银行适配细节。

### 解决方式

- 将 `PROJECT_CONTEXT.md` 轻量整理为项目入口文件，只保留必读顺序、当前定位、核心架构、统计口径、集成契约和下一步方向。
- 新增 `PROJECT_STATUS.md`，专门记录当前分支、能力概览、支持范围、验证结果、已知限制和下一步优先级。
- 保留 `INTEGRATION_CONTRACT.md` 作为后续接入车贷报告自动化总项目的开发契约。
- 历史问题和详细变更继续保留在 `技术变更记录.md`。
- 银行支持范围和版式细节继续以 `银行适配手册.md` 为准。

### 修改位置

- `PROJECT_CONTEXT.md`
- `PROJECT_STATUS.md`
- `INTEGRATION_CONTRACT.md`
- `技术变更记录.md`

### 验证结果

本次只调整文档，不修改解析器、GUI、回归脚本或导出逻辑。

当前继续项目时建议优先读取：

```text
PROJECT_CONTEXT.md
PROJECT_STATUS.md
INTEGRATION_CONTRACT.md
```

需要追溯历史时再读取：

```text
技术变更记录.md
银行适配手册.md
```

### 影响范围

影响：

- 后续新窗口接续项目的读取顺序。
- 上下文占用和项目状态定位效率。

不影响：

- 银行解析器。
- 流水调整逻辑。
- GUI。
- Excel 导出。
- 回归测试结果。

## 2026-05-31 流水调整/测算层

### 更新前问题

GUI 只能展示原始识别统计。对于微信流水、个人流水、对公流水的收入补充测算，需要用户导出后再手动改 Excel，容易混淆原始流水和测算结果。

需求确认后，调整功能必须满足：

- 不修改原始识别逻辑。
- 不污染原始明细数据。
- 默认不启用调整。
- 启用后 GUI 主界面能立即看到调整后的月度统计。
- 导出 Excel 时保留原始结果和调整后结果。

### 解决方式

新增独立调整模块 `bankflow_v2/adjustment.py`，在统计结果之后做测算，不接触各银行解析器。

GUI 调整区改为两行：

```text
[ ] 启用收入调整（微信）    [ ] 启用收支平衡调整（个/公）
调整金额 ____ 万元，月份 ____ 至 ____
```

两种模式互斥：

- 收入调整（微信）：只增加收入，适合微信或收入补充测算。
- 收支平衡调整（个/公）：收入和支出同步增加，保持期末余额不变。
- 默认随机分配：不再机械平均到每月，而是在已有流水月份内做确定性随机分摊；同一金额、月份、模式下结果保持一致，且总调整金额不变。勾选 `固定分配` 时改为平均分摊。

月度统计显示规则：

- 未启用调整时，`月度统计` 显示原始统计。
- 启用调整时，标签自动变为 `调整月度统计`，同一张表显示调整后统计。
- 调整启用时，月度统计字段保持原字段顺序，并在末尾追加 `收入调整(万元)`、`支出调整(万元)`、余额变动/收支差额校验和 `说明`。
- `总计` 下新增 `月均` 行，收入和支出按已有流水月份数平均。

导出 Excel 增加：

- `原始月度统计`
- `调整后月度统计`
- `调整参数`
- `调整说明`
- `原始明细`

### 修改位置

- `bankflow_v2/adjustment.py`
- `gui_v2.py`

### 验证结果

已运行：

```powershell
python -m py_compile bankflow_v2\adjustment.py gui_v2.py
python tools\regression.py --all
```

结果：

- 统一回归：14 PASS / 0 FAIL
- 源码版 GUI 短启动成功
- 用户已做简单 GUI 测试，反馈基本正常

### 影响范围

影响：

- GUI 月度统计展示
- Excel 导出 Sheet

不影响：

- 银行 PDF/Excel/微信原始解析逻辑

## 2026-05-31 GUI 现代化样式调整

### 更新前问题

顶部按钮、日期筛选、摘要统计和调整区都在同一纵向布局里，信息层级不够清楚。摘要用长句展示，阅读效率低；调整预览也是文字块，不够直观。

### 解决方式

- 顶部操作栏调整为左侧主操作、右侧导出：`开始处理 / 选择文件 / 选择文件夹 / 清空 / 筛选日期 / 导出 Excel`。
- `开始处理` 使用蓝色主按钮，文件选择和清空保持次级按钮。
- 日期筛选做成独立模块，并统一按钮和日期框高度；日期下拉改为无阴影小箭头。
- 主区域改为左侧内容区和右侧流水调整面板。
- 摘要从长句改成仪表板指标：文件、流水、收入、支出、异常。
- 收入/支出指标做颜色区分，笔数加粗，金额降字号和字重。
- 调整预览改为结果卡，突出调整后净额和平衡校验。
- 表格 tabs 改为下划线风格，降低按钮块感。

### 修改位置

- `gui_v2.py`
- `assets/down_arrow.svg`

### 验证结果

已运行：

```powershell
python -m py_compile gui_v2.py bankflow_v2\adjustment.py
```

并从 `D:\Codex data\CD_assets\PDF流水` 中抽取 5 个现有样本测试：

- 中信银行个人
- 微信流水
- 工商银行个人
- 工商银行对公
- 建设银行对公

结果：5 个样本均可正常识别并输出统计。

### 影响范围

影响：

- GUI 布局和视觉样式
- 顶部操作栏、摘要指标、日期筛选和调整预览展示

不影响：

- 银行解析器
- 月度统计计算口径
- Excel 导出逻辑

---

## 2026-05-31 建行对公与中国银行对公识别扩展

### 更新前问题

`0530流水1建设对公` 中的建设银行对公样本不是原先已覆盖的建行格式。PDF 表格抽取后不是逐行交易，而是整页表格中每列包含多行文本，需要按列内换行重新拆分。

`中银对公流水2` 中的中国银行对公 PDF 也不是表格抽取型，`pdfplumber.extract_tables()` 返回空，但页面文本中存在稳定的 `|` 分隔流水行。原自动识别未命中，GUI 会提示未适配。

### 解决方式

- 扩展 `bankflow_v2/ccb_corp.py`：
  - 识别 `中国建设银行股份有限公司活期存款明细账`；
  - 对列内多行文本按索引拆分为逐笔交易；
  - 借方/贷方按原列保留，避免负数借方被归为收入。
- 新增 `bankflow_v2/boc_corp.py`：
  - 识别中国银行对公活期明细；
  - 从 `|` 分隔文本行解析记账日、借方发生额、贷方发生额、余额；
  - 读取 `承前页余额` 作为文件期初核对依据。
- 更新 `bankflow_v2/auto_detect.py` 和 `bankflow_v2/pipeline.py`，接入 `boc_corp`。
- 更新 `tools/regression_cases.json`，加入建行对公 4 份、中国银行对公 6 份回归样本。

### 验证结果

```powershell
python -m py_compile gui_v2.py bankflow_v2\boc_corp.py bankflow_v2\ccb_corp.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py
python tools\regression.py --tag ccb_corp
python tools\regression.py --tag boc_corp
python tools\regression.py --all --allow-missing
python -m PyInstaller --noconfirm --clean BankFlowGUI.spec
```

- 建行对公活期存款明细账：4 份样本全部 PASS，0 异常。
- 中国银行对公活期明细：6 份样本全部 PASS，0 异常。
- 全量回归：19 PASS / 0 FAIL / 5 SKIP。
- 新 EXE 已生成：`D:\Codex data\CD\dist\BankFlowGUI\BankFlowGUI.exe`。

### 影响范围

影响：

- 建设银行对公活期存款明细账格式
- 中国银行对公活期明细文本格式
- 自动识别与统一回归样本

不影响：

- 工行、交通、邮储、招商、中信、浦发等既有解析器
- GUI 调整测算逻辑
- Excel 导出口径
- 原始流水明细
- 已有银行回归结果

---

## 2026-05-30 工行对公企业存款对账单版式

### 更新前问题

`C:\Users\lenovo\Desktop\李翠\新建文件夹` 下 4 份工行流水被识别为工商银行对公，但专用解析未得到流水，GUI 退回通用识别后出现大量余额不连续提示。

GUI 旧结果的典型表现：

- 银行显示为 `工商银行对公（通用识别）`
- 文件状态为 `需复核`
- 收入/支出方向明显错位
- 余额列大量被解析为 `0.00`

### 原因分析

这批 PDF 标题为 `中国工商银行企业存款对账单`，表头为：

```text
交易时间 / 电子回单编号 / 交易类型 / 凭证种类 / 凭证号 / 对方账号 / 对方户名 / 摘要 / 借方发生额 / 贷方发生额 / 余额
```

它与之前支持的工行对公版式不同：

- 旧版 A：`借贷标志 + 发生额 + 余额`
- 旧版 B：`借/贷 + 借方发生额 + 贷方发生额 + 余额`
- 本次新版 C：只有 `借方发生额 / 贷方发生额 / 余额`，没有单独的 `借/贷` 或 `借贷标志` 列

原 `icbc_corp` 解析器只覆盖旧版 A/B，因此专用解析返回 0 笔，GUI 才回退到通用识别。

### 解决方式

- 在自动识别中新增 `中国工商银行企业存款对账单` 高优先级关键词。
- 在 `bankflow_v2/icbc_corp.py` 中新增 format C 分支。
- format C 按 `借方发生额` 计支出、`贷方发生额` 计收入、`余额` 做连续性校验。
- 将 4 份李翠样本加入统一回归。

### 修改位置

- `bankflow_v2/auto_detect.py`
- `bankflow_v2/icbc_corp.py`
- `tools/regression_cases.json`

### 验证结果

4 份样本修复后：

| 样本 | 流水 | 收入 | 支出 | 期初余额 | 期末余额 | 异常 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `1ec551...040.pdf` | 25 | 4笔 / 501,360.28 | 21笔 / 614,033.00 | 334,749.69 | 222,076.97 | 0 |
| `7ebada...125.pdf` | 42 | 2笔 / 460,000.00 | 40笔 / 353,536.02 | 22,076.97 | 128,540.95 | 0 |
| `307ea...091.pdf` | 101 | 14笔 / 1,839,486.77 | 87笔 / 1,663,569.89 | 155,730.81 | 335,967.69 | 0 |
| `db205...530.pdf` | 127 | 18笔 / 1,864,486.77 | 109笔 / 1,902,798.17 | 354,732.09 | 320,740.69 | 0 |

统一回归：

- 通过：14
- 失败：0
- 跳过：0

### 影响范围

只影响工行对公 `icbc_corp`。

不影响：

- 工行个人 `icbc`
- 其他银行专用解析器
- Excel 导入
- 通用 PDF 识别

---

## 2026-05-29 中信个人、民生对公、Excel 小数误差与 GUI 月度列顺序

### 更新前问题

- 中信银行个人账户交易明细提示未适配。
- 民生对公 `单位账户对账单` 会被更宽泛的民生关键字提前识别，导致 GUI 没有解析出流水。
- 交通银行图片 PDF 先转 Excel 后导入时，`借贷状态` 列未被识别，收入/支出口径和人工透视表不一致。
- Excel/通用识别场景存在 0.01 级余额差异，影响不大，但会把文件标为需复核。
- GUI `月度统计` 中 `流水笔数` 在前面，用户要求移动到 `期末余额(万元)` 后面。

### 解决方式

- 新增中信个人解析器，按上一笔余额判断单金额列是收入还是支出。
- 民生对公识别优先匹配 `单位账户对账单`。
- Excel 导入支持 `借贷状态`，并保留原始借/贷列口径；例如借方负数冲正仍计入支出笔数和支出合计。
- Excel 导入和通用 PDF 识别加入小额余额容差，0.99 元以内的余额差异标为 `低风险`，不再把文件状态打成 `需复核`。
- GUI 和导出 Excel 的 `月度统计` 列顺序调整为：月份、收入笔数、收入(万元)、支出笔数、支出(万元)、净额(万元)、期初余额(万元)、期末余额(万元)、流水笔数。

### 修改位置

- `bankflow_v2/citic.py`
- `bankflow_v2/auto_detect.py`
- `bankflow_v2/pipeline.py`
- `bankflow_v2/excel_input.py`
- `bankflow_v2/generic_pdf.py`
- `bankflow_v2/summary.py`
- `gui_v2.py`
- `tools/regression_cases.json`

### 验证结果

统一回归：

- 通过：10
- 失败：0
- 跳过：0

交通银行图片转 Excel 对照人工透视表：

- 流水：167 笔
- 收入：110 笔 / 1,841,181.92
- 支出：57 笔 / 1,744,549.50
- 余额差异：13 条，均为 `低风险`
- 需复核：0 条

### 影响范围

- 新增中信个人支持。
- 民生对公识别更稳定。
- Excel 导入和通用 PDF 对小额余额误差更宽容。
- 专用银行解析器的余额连续性仍保持原复核标准。

---

## 2026-05-25 统一回归入口与配置化路线

### 更新前问题

项目已经有零散验证工具，例如批量验证、工行专项回归、浦发专项回归，但缺少一个统一入口。

实际风险是：后续新增或修改某个银行解析逻辑时，如果只测当前样本，可能忘记复测之前已经通过的银行和特殊版式。

### 解决方式

新增统一回归入口：

```powershell
python tools\regression.py --all
```

新增代表样本清单：

- `tools/regression_cases.json`

当前第一批纳入：

- 工行个人强水印
- 工行个人历史明细
- 农行无具体时间排序
- 交通银行 6 列日期版
- 交通银行 11 列时间版
- 邮储银行历史明细
- 招商银行个人交易流水

输出只保留关键状态，例如：

```text
PASS: 工行个人-强水印 (918笔, 0异常)
FAIL: 某银行样本 (余额断点 3处)
```

失败时进程退出码为 1，方便后续作为打包前强制检查。

### 配置化路线

当前不做大重构，不把现有解析器立即改成配置化。

配置化的触发时机：

- `tools/regression.py --all` 覆盖主要银行、关键版式和最近踩过坑的样本。
- 某类银行规则开始重复，例如固定表格列只差日期列、收入列、支出列、余额列的位置。
- 每次打包前统一回归已经成为固定步骤。

推荐推进顺序：

1. 继续补充 `tools/regression_cases.json`。
2. 先从固定列表格类银行抽配置。
3. 复杂银行保留专用解析器，例如工行强水印、微信无余额、Excel 导入、图片 PDF 转 Excel 口径。

### 修改位置

- `tools/regression.py`
- `tools/regression_cases.json`
- `技术变更记录.md`

### 验证结果

已运行：

```powershell
python -m py_compile tools\regression.py
python tools\regression.py --all
```

当前结果：

- 通过：7
- 失败：0
- 跳过：0

### 影响范围

这是旁路验证工具，只在手动执行时运行。

不影响：

- GUI 日常使用
- EXE 打包运行
- 现有银行解析逻辑
- Excel 导出逻辑

---

## 2026-05-26 工行活期与定期账户混排复核口径确认

### 更新前问题

`工商银行历史明细（申请单号：26050110572953314933）.pdf` 在 0526 批量测试中：

- 流水笔数正确
- 收入/支出金额正确
- 期初余额正确
- 期末余额按交易日期取到 `8,827.46`，该口径是正确的
- 异常提示 13 条

PDF 页面最后一行余额为 `0.00`，但该笔交易日期不是整份流水的最新日期，因此不能作为期末余额。

### 原因分析

该 PDF 不是单一活期账户流水。最后一页先列出活期账户 2026-04-30 的末段交易，随后又列出多个定期账户的开立和销户记录。

程序把整份工行历史明细按交易日期排序后当作一条余额链校验。由于该 PDF 同时出现活期账户和定期账户，余额链会出现不连续提示。

该提示不能静默删除，因为它对使用者意味着“这里存在跨账户/储种混排，需要人工复核余额口径”。

### 解决方式

本次确认口径：

- 不按页面最后一行作为期末余额。
- 文件汇总的期初/期末余额仍按交易日期顺序取首尾。
- 保留余额不连续异常提示。
- 将该样本加入统一回归，要求统计金额正确、期初期末正确，同时异常数保持为 13。

### 修改位置

- `tools/regression_cases.json`
- `技术变更记录.md`

### 验证结果

该样本修复后：

- 流水：114 笔
- 收入：39 笔 / 6,326,210.92
- 支出：75 笔 / 6,336,938.07
- 净额：-10,727.15
- 期初余额：19,554.61
- 期末余额：8,827.46
- 异常：13

统一回归：

- 通过：8
- 失败：0

### 影响范围

不修改工行解析逻辑，只确认回归口径与复核提示口径。

---

## 2026-05-25 工行强水印余额连续性修复

### 更新前问题

`刘少伟已解密.pdf` 为工行个人历史明细，GUI 解析后出现大量余额不连续提示，统计结果与手工核对差异较大。

GUI 旧结果：

- 流水：918 笔
- 收入：61 笔 / 137,053.03
- 支出：857 笔 / 134,650.66
- 净额：2,402.37
- 期初余额：437.47
- 期末余额：3,478.84
- 异常：80 条

### 原因分析

该 PDF 有强水印、印章、二维码等干扰。水印字符不只是视觉层干扰，还会混入 PDF 表格文本抽取结果。

典型污染值：

```text
392+4,935.00 -> 应取 +4,935.00
D14,722.40 -> 应取 4,722.40
C9CD4,712.50 -> 应取 4,712.50
AF04,352.50 -> 应取 4,352.50
22-960.00: -> 应取 -960.00
```

旧逻辑是逐行余额连续性校验。它只根据“上一笔已选余额 + 当前金额 = 当前余额”来选择当前行候选。

这对普通水印污染有效，但这份样本中存在“局部也能连续的错误候选”。一旦前面某一行选错，后续会沿着错误余额路径继续计算，导致整段偏移。

### 解决方式

工行个人解析改为全局余额连续性候选路径选择。

核心思路：

- 先为每一行生成多个金额候选和余额候选。
- 不再逐行立即拍板。
- 保留多条可能路径。
- 用整段流水的余额连续性选择最合理路径。

### 修改位置

- `bankflow_v2/icbc.py`
- `bankflow_v2/number_parser.py`

### 验证结果

`刘少伟已解密.pdf` 新结果：

- 流水：918 笔
- 收入：61 笔 / 145,123.03
- 支出：857 笔 / 145,381.66
- 净额：-258.63
- 期初余额：437.47
- 期末余额：178.84
- 异常：0 条

工行历史明细专项回归：

- 工行历史明细 PDF：15 份
- 可读样本：10 份
- 通过：10 份
- 需复核：0 份
- 密码/读取失败忽略：5 份
- 失败：0 份

### 影响范围

只影响工行个人历史明细 `icbc`。

不直接影响：

- 工行对公 `icbc_corp`
- 其他银行
- Excel 导入
- 微信流水

---

## 2026-05-23 交通银行两种格式与排序修复

### 更新前问题

部分交通银行 PDF 可以识别为交通银行，但解析不到流水。

另有部分只有日期、没有时分秒的流水，会被补成 `00:00:00`，排序到当天最前面，导致余额连续性误报。

### 原因分析

交通银行存在两种不同版式：

```text
旧版：序号 / 交易日期 / 交易时间 / 交易类型 / 借贷标志 / 交易金额 / 账户余额 / ...
新版：交易日期 / 交易地点 / 交易方式 / 借贷状态 / 交易金额 / 余额
```

原解析器只支持旧版 11 列带交易时间格式。

排序问题来自：无时分秒交易被当作真实 `00:00:00` 参与排序。

### 解决方式

- `bankflow_v2/bocom.py` 新增交通银行 6 列仅日期版解析。
- `bankflow_v2/summary.py` 新增统一排序规则：
  - 同一天全部有明确时分秒时，按时分秒排序。
  - 同一天混有无具体时间交易时，按 PDF 原始页码/行号顺序。
- `gui_v2.py` 的明细展示和 Excel 导出统一使用该排序。

### 验证结果

交通银行打包测试目录 6 份全部通过：

- 4 份 11 列带交易时间版
- 2 份 6 列仅交易日期版
- 异常均为 0

农业银行样本 `26051514250707878157.pdf` 排序修复后：

- 流水：225 笔
- 异常：0

### 影响范围

- 交通银行 `bocom`
- 通用排序逻辑
- GUI 明细展示和 Excel 导出顺序

---

## 2026-05-23 GUI 导出入口整理

### 更新前问题

GUI 左上角菜单中有一个“文件 -> 导出 Excel”，工具栏右侧也有一个“导出 Excel”，入口重复。

### 解决方式

删除左上角菜单导出入口，只保留工具栏右侧按钮。

### 修改位置

- `gui_v2.py`

### 影响范围

只影响 GUI 入口展示，不影响导出逻辑。

---

## 2026-05-21 至 2026-05-22 工行、浦发与批量验证

### 更新前问题

新增打包测试目录后，需要批量确认银行支持范围。工行个人历史明细受水印、红章、二维码干扰，金额和余额会混入噪声。

### 解决方式

- 新增批量验证工具 `tools/batch_validate.py`。
- 新增工行专项回归 `tools/icbc_regression.py`。
- 增强金额和余额候选解析。
- 新增浦发个人、浦发对公解析器。

### 修改位置

- `tools/batch_validate.py`
- `tools/icbc_regression.py`
- `tools/spdb_regression.py`
- `bankflow_v2/number_parser.py`
- `bankflow_v2/auto_detect.py`
- `bankflow_v2/spdb.py`
- `bankflow_v2/pipeline.py`

### 验证结果

工行：

- 工行历史明细 PDF：15 份
- 密码/读取失败忽略：5 份
- 可读样本：10 份
- 可读样本通过：10 份
- 需复核：0

浦发：

- `浦发 (2).pdf`：个人，异常 0
- `浦发.pdf`：对公，异常 0
- `浦发2.pdf`：个人，异常 0
- `浦发3.pdf`：个人，异常 0

### 影响范围

- 工行个人历史明细
- 浦发个人
- 浦发对公
- 批量验证报告

---

## 2026-05-17 GUI、Excel 导入与图片 PDF 处理口径

### 更新前问题

部分银行 PDF 是图片型或扫描型，直接 PDF 文本抽取为空。OCR 尝试准确率不足，容易造成余额连续性异常。

### 解决方式

- GUI 支持导入 Excel（`.xlsx`、`.xlsm`）。
- 图片 PDF 当前不做 OCR，建议先用外部工具转 Excel，再导入 GUI。
- GUI 支持选择文件、选择文件夹、拖拽导入。
- 新增日期筛选，默认自然月近 6 个月。
- 月度统计按万元显示和导出，保留两位小数。
- 文件汇总和流水明细仍按元显示。

### 修改位置

- `gui_v2.py`
- `bankflow_v2/excel_input.py`
- `bankflow_v2/wechat.py`

### 影响范围

- GUI 输入方式
- Excel 导入
- 微信流水
- 图片 PDF 处理口径

### 当前不支持

- 扫描/图片 PDF 直接 OCR
- 工资类完整原字段导出
- 兴业银行 PDF 直解

---

## 2026-06-03 佐证填写成功提示精简

本地提交：当前提交见 git log -1

### 背景

PDF 流水 GUI 点击“佐证填写”后会自动导出收入佐证 JSON 并打开 Word 收入佐证填表。原先成功路径会额外弹出“已导出/已自动打开”提示框，操作上多一步确认。

### 调整

- `gui_v2.py` 中 `export_income_proof_json()` 成功打开 Word 收入佐证填表时不再弹完成提示框。
- 如果未能自动打开 Word 收入佐证填表，仍保留提示，告知 JSON 已导出并需要手动打开。

### 边界

- 本次只改 PDF 流水 GUI 的提示行为。
- 不修改收入佐证 JSON 结构。
- 不修改 Word 收入佐证项目源码。

---

## 2026-06-29 明细账查询、中国银行个人、桂林银行个人流水适配

### 更新前问题

- `明细账查询` 样本未命中已适配银行，通用正则也无法解析；原因是 PDF 文本层顺序被水印打乱，表格没有交易时间列。
- `中国银行交易流水明细清单` 个人借记卡样本未识别；现有 `boc_corp` 只覆盖中国银行对公活期明细。
- 桂林银行 7 列收支明细表格样本最初被当作对公处理，用户确认为个人流水；原桂林按词行解析会漏多行户名/备注相关交易，余额不闭合。
- 收入佐证/GUI 的流类型仅按 bank id 判断，遇到个人识别格式但户名明显为企业/商户时，可能错误归入个人。

### 解决方式

- `bankflow_v2/generic_pdf.py` 增加 `明细账查询` 坐标列解析：按日期 y 坐标切行，读取支出、收入、余额、交易名称、对方账号、对方户名，并过滤页脚/水印。
- `bankflow_v2/boc.py` 新增中国银行个人解析器：按 `pdfplumber.extract_tables()` 读取 12 列表格，金额正负拆分收入/支出；同秒多笔按打印倒序还原校验顺序；`冲正` 负金额按收入处理。
- `bankflow_v2/guilin_corp.py` 将桂林 7 列表格解析抽为可复用逻辑，新增 `extract_guilin()` 输出 `桂林银行个人`，旧 `extract_guilin_corp()` 继续保留对公逻辑和兜底。
- `bankflow_v2/auto_detect.py` 新增 `generic_pdf`、`boc`、`guilin` 识别规则。
- `bankflow_v2/pipeline.py` 接入 `boc` 和 `guilin` 调度。
- `gui_v2.py`、`bankflow_v2/income_proof_export.py` 增加企业/商户户名关键词兜底，个人 bank id 但户名像企业时按对公流类型归类。

### 验证结果

- `553661a2a19345a8ba9a4de8c157e2f51782456824354.pdf`：识别 `generic_pdf`，156 笔，收入 1,569,617.87，支出 1,614,594.71，净额 -44,976.84，余额连续，0 复核。
- `中国银行.pdf`：识别 `boc`，877 笔，收入 4,202,381.33，支出 4,209,763.74，净额 -7,382.41，余额连续，0 复核。
- `对公.pdf`（用户确认为个人流水）：识别 `guilin`，514 笔，收入 628,866.13，支出 632,960.81，净额 -4,094.68，余额连续，0 复核。
- 编译检查：`python -m py_compile bankflow_v2\generic_pdf.py bankflow_v2\boc.py bankflow_v2\guilin_corp.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py gui_v2.py bankflow_v2\income_proof_export.py`。
- 按用户要求，本轮未跑完整回归。

### 影响范围

- 通用 PDF 兜底解析。
- 中国银行个人流水。
- 桂林银行个人/对公流水。
- GUI 展示和收入佐证导出中的个人/对公流类型归类。

---

## 2026-06-29 问题流水0623 批量解析修复

### 更新前问题

- `D:\Codex data\PDF流水\问题流水0623` 中多份工商银行 `账户明细清单` 是对公流水，但原识别会命中 `icbc` 或退到通用解析，导致对公被归为个人、支出列漏识别或余额不闭合。
- `工行.pdf` / `工行佐证中未识别到银行账号.pdf` 的工商银行个人历史明细中夹有美元行，参与人民币余额链后造成异常。
- `未识别.pdf` 是农业银行对公账户明细，PDF 无表格线且文本按列混排，既有文本块解析无法抽取。
- `农行1.pdf` / `农行2.pdf` 是农业银行 `对私客户账户明细` 7 列表格，旧 `abc` 正则只支持带交易摘要/交易时间的文本行。
- `流水(4).zip` 内交通银行辽宁省分行对公明细未识别；其中 `交通银行1.pdf` 被对方行名中的招商银行误识别为招商银行。
- `交通银行5 (2).pdf` 存在负借方抹账，余额链实际回增，但对公解析汇总时未按收入处理，导致总闭合差。

### 解决方式

- `bankflow_v2/auto_detect.py`
  - 增加工商银行对公 `账户明细清单` 识别，要求同时命中 `本方账号户名`、`转入金额`、`转出金额`、`余额`。
  - 将交通银行对公 `明细对账单` 识别从指定四川省分行扩展为任意交通银行分行明细对账单。
  - 增加农业银行 `对私客户账户明细` 个人流水识别。
- `bankflow_v2/icbc_corp.py`
  - 增加 `转入金额/转出金额/余额` 表格解析分支，按显式收支列输出对公交易。
- `bankflow_v2/icbc.py`
  - 过滤 `美元/港币/欧元/日元/英镑` 等外币行，避免混入人民币余额链。
- `bankflow_v2/abc.py`
  - 增加农业银行个人 7 列表格解析：`交易日期 / 交易发生额 / 账户余额 / 对方账号 / 对方户名 / 摘要 / 备注`。
- `bankflow_v2/abc_corp.py`
  - 增加坐标列解析兜底，按日期列 y 坐标切行，读取收入、支出、余额；兼容日期拆成 `YYYY-MM- + DD` 或 `YYYY- + MM-DD` 两种断行方式。
- `bankflow_v2/bocom_corp.py`
  - 借方负数按收入、贷方负数按支出处理，用于抹账/冲正类交易。

### 验证结果

- `python tools\batch_validate.py "D:\Codex data\PDF流水\问题流水0623" --out "D:\Codex data\PDF流水\问题流水0623\batch_validate_final.xlsx"`
  - PDF 总数：31（含 `流水(4).zip` 解压后的 18 个 PDF）。
  - 通过：30。
  - 忽略图片 PDF：1。
  - 异常明细：0。
- `python -m py_compile gui_v2.py tools\regression.py bankflow_v2\abc.py bankflow_v2\abc_corp.py bankflow_v2\auto_detect.py bankflow_v2\bocom_corp.py bankflow_v2\icbc.py bankflow_v2\icbc_corp.py`：通过。
- `python tools\regression.py --all --allow-missing`：pass=54, fail=0, skip=18。

### 未覆盖项

- `问题流水0622\问题流水0609\公户_0(2).pdf` 是杭州银行客户交易标准对账单扫描件，前两页文字层字符数为 0，当前没有 OCR 能力，保持图片 PDF 忽略口径。

### 影响范围

- 工商银行个人/对公流水。
- 农业银行个人/对公流水。
- 交通银行对公流水。
- 批量验证识别状态和余额闭合校验。

---

## 2026-06-30 农业银行个人抹账方向修正

### 更新前问题

- `ca456ce48f8c45599de15e51b92cc1231782713074441.pdf` 识别为农业银行个人流水，`2026-04-18 15:27:33` 的 `抹账` 行金额显示为 `-676,800.00`，但余额从 `9,301.09` 回增到 `686,101.09`。
- 旧逻辑只对 `自动抹账` 按余额差反向修正，未覆盖 `抹账`，导致该行被计为支出，出现余额不连续和总收支余额不闭合。

### 解决方式

- `bankflow_v2/abc.py` 将农业银行个人冲正类交易类型从 `自动抹账` 扩展为 `抹账/自动抹账`。
- 当余额变动刚好等于原金额反向数时，按余额差统计实际收入/支出方向。

### 验证结果

- 目标 PDF：753 笔，收入 11,860,213.27，支出 11,906,076.55，净额 -45,863.28，期初余额 48,136.01，期末余额 2,272.73，异常 0。
- `python -m py_compile gui_v2.py tools\regression.py bankflow_v2\abc.py`：通过。
- `python tools\regression.py --tag abc --allow-missing`：pass=1, fail=0, skip=0。
- `python tools\regression.py --all --allow-missing`：两次运行分别在 120 秒和 300 秒超时，未返回失败结果。

### 影响范围

- 农业银行个人流水中交易类型为 `抹账` 或 `自动抹账`，且余额链显示方向与金额符号相反的冲正明细。

---

## 2026-06-30 中国光大银行个人/对公流水适配

### 更新前问题

- `C:\Users\lenovo\Desktop\王晓晨\流水.pdf` 是中国光大银行个人账户明细查询清单，原自动识别未命中。
- `C:\Users\lenovo\Desktop\王晓晨\流水2.pdf` 是中国光大银行对公账户对账单，原自动识别未命中。
- 两份 PDF 均为倒序打印；对公样本存在同秒交易跨页，若按页码顺序校验会产生伪余额不连续。

### 解决方式

- 新增 `bankflow_v2/everbright.py`。
  - `extract_everbright()`：解析个人表格列 `交易日期 / 支出金额 / 存入金额 / 账户余额 / 对手信息 / 摘要`。
  - `extract_everbright_corp()`：解析对公文本行 `序号 / 交易日期 / 时间 / 借/贷 / 交易金额 / 账户余额`。
- `bankflow_v2/auto_detect.py` 新增 `everbright`、`everbright_corp` 识别规则。
- `bankflow_v2/pipeline.py` 接入光大个人/对公解析调度。
- 对倒序打印样本使用全局倒序行号恢复同日/同秒余额链。

### 验证结果

- `流水.pdf`：识别 `everbright`，252 笔，收入 35,047,481.94，支出 35,497,036.26，净额 -449,554.32，期初余额 450,142.51，期末余额 588.19，异常 0。
- `流水2.pdf`：识别 `everbright_corp`，51 笔，收入 2,700,211.68，支出 2,601,712.00，净额 98,499.68，期初余额 40,290.81，期末余额 138,790.49，异常 0。
- 已导出：`C:\Users\lenovo\Desktop\王晓晨\王晓晨_光大流水解析结果.xlsx`。
- `python -m py_compile gui_v2.py tools\regression.py bankflow_v2\everbright.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py`：通过。

### 影响范围

- 中国光大银行个人账户明细查询清单。
- 中国光大银行对公账户对账单。

---

## 2026-06-30 兴业强水印 PDF 手工 Excel 明细核对

### 更新前问题

- `eb1ce44defde40518cbe409a52da74d31782805563325.pdf` 是兴业银行个人交易明细，PDF 文字层带强水印，直接表格抽取会混入打印日期、水印数字和页脚数字。
- PDF 页眉合计为支出 609,423.87（150 笔）、收入 618,653.80（136 笔），但直接 PDF 解析结果无法稳定贴合该合计，仍需人工复核。
- 用户手工导出的 Excel 明细表头为 `支/收`，旧 Excel 导入没有识别该方向列，第一笔收入无法判定方向。

### 解决方式

- `bankflow_v2/auto_detect.py` 增加兴业银行强水印交易明细识别规则。
- `bankflow_v2/excel_input.py` 增加 `支/收`、`收/支`、`支出/收入`、`支出收入` 等方向表头识别。
- Excel 方向列明确时，交易金额按绝对值拆收入/支出，兼容支出金额以负数展示的手工明细。

### 验证结果

- 手工明细 `eb1ce44defde40518cbe409a52da74d31782805563325.xlsx`：286 笔，收入 618,653.80（136 笔），支出 609,423.87（150 笔），净额 9,229.93，期初 0.00，期末 9,229.93，余额连续，异常 0。
- 已导出：`C:\Users\lenovo\Desktop\芦桂春\芦桂春_兴业流水解析结果_手工明细通过.xlsx`。
- `python -m py_compile bankflow_v2\excel_input.py gui_v2.py tools\regression.py`：通过。
- `python tools\regression.py --tag cib --allow-missing`：通过。

### 影响范围

- Excel 手工明细导入中的 `支/收`、`收/支` 方向列识别。
- 兴业强水印 PDF 仅新增自动识别，未改变既有兴业解析器逻辑。


---

## 2026-07-03 对公客户账户明细与普通 Excel OCR 修复

### 更新前问题

- `C:\Users\lenovo\Desktop\问题流水` 下两份 `对公客户账户明细` PDF 未命中自动识别。
- `银行流水_0.xlsx` 为 OCR/转换后的四列表 Excel，余额中存在 `B/D` 等误识别字符，部分正数金额无显式收支方向，旧逻辑提示多条余额不连续和方向无法解析。

### 解决方式

- 新增 `bankflow_v2/customer_detail_corp.py`，按 `YYYYMMDD + 带符号发生额 + 账户余额` 文本行解析对公客户账户明细。
- `bankflow_v2/auto_detect.py` 新增 `customer_detail_corp` 识别规则。
- `bankflow_v2/pipeline.py` 接入 `customer_detail_corp` 调度。
- `bankflow_v2/excel_input.py` 在普通 Excel 缺失方向修复阶段，先用余额链修正 OCR 余额，再按余额变动反推收入/支出。

### 验证结果

- 对公客户账户明细两份 PDF 合计 133 笔，收入 2,604,239.34，支出 2,439,338.63，期初 254,422.48，期末 419,323.19，异常 0。
- `银行流水_0.xlsx`：161 笔，收入 8,691,971.30，支出 8,508,562.75，期初 87,411.18，期末 270,819.73，异常 0。
- `python -m py_compile bankflow_v2\customer_detail_corp.py bankflow_v2\auto_detect.py bankflow_v2\pipeline.py bankflow_v2\excel_input.py`：通过。

### 影响范围

- 对公客户账户明细 PDF。
- 普通 Excel 导入中余额 OCR 轻微污染、正数金额缺少方向但余额链可闭合的场景。

---

## 2026-07-03 GUI 月均利润测算

### 更新前问题

- 流水调整面板中的调整月份选择和 `固定分配` 控件占用空间，且和顶部日期筛选存在重复理解成本。
- 识别后只能看到流水月均收入，不能直接按利润率换算可支撑的月收入，也不能和拟开具月收入做即时比较。

### 解决方式

- `gui_v2.py` 中隐藏右侧流水调整面板的调整月份选择和 `固定分配` 控件。
- 调整期间改为跟随顶部筛选日期；顶部未启用日期筛选时，调整覆盖全部解析月份。
- 新增 `月均利润测算` 区域：
  - 手工输入开具月收入，单位为万元。
  - 手工输入利润率，单位为百分比数字，例如 `5` 表示 5%。
  - 根据当前流水月均收入计算 `利润率后月收入 = 流水月均收入 × 利润率`。
  - 当利润率后月收入低于开具月收入时，校验结果和生成金额标红提示。
- 利润率校验区改为紧凑行式展示，右侧面板增加滚动容器，避免窗口未最大化时文字被裁切。
- 顶部工具栏不再显示 `选择文件` 按钮，日期筛选文案改为 `筛选`，并去掉 `从/到` 标签。

### 验证结果

- `python -m py_compile gui_v2.py`：通过。
- 设置 `QT_QPA_PLATFORM=offscreen` 后实例化 `MainWindow`：通过。

### 影响范围

- 仅影响 GUI 展示和右侧面板计算提示。
- 不修改 PDF/Excel 解析器、交易明细、Excel 导出结构和收入佐证 JSON 结构。

---

## 2026-07-03 报告工作台嵌入适配 GUI 微调

### 背景

PDF 流水 GUI 已被 `D:\report` 车贷报告工作台直接嵌入到 `识别` 页。嵌入后，原独立工具的一些布局和样式在报告工作台内显得重复或不协调。

### 调整

- 顶部工具栏排序改为：

```text
开始处理 / 选择文件夹 / 清空 / 工资佐证 / 经营佐证 / 筛选日期
```

- 五个主要工具栏按钮统一为同宽同高框体。
- 取消 `文件导入` 卡片；主窗口和表格仍支持拖拽 PDF/Excel 导入。
- 表格取消外边框和内部网格线，改为隔行底色区分；单元格统一居中。
- 表格不再拉伸最后一列，避免 `流水笔数` 与其他列距离过大。
- 表格显式启用横向/纵向滚动条，调整后明细行数增加时可继续滚动查看。
- 圆角、结果卡和按钮颜色做轻量统一，便于嵌入报告工作台后视觉一致。

### 验证

```powershell
python -m py_compile gui_v2.py
```

### 影响范围

- 仅影响 PDF 流水 GUI 展示和嵌入体验。
- 不修改 PDF/Excel 解析器、交易模型、调整计算、Excel 导出和收入佐证 JSON 结构。

---

## 2026-07-03 报告工作台嵌入识别页细节微调

### 背景

PDF 流水 GUI 嵌入报告工作台后继续按截图校准：右侧筛选位置、调整/利润测算字号、表格黑色空条、横向滚动条显示时机和顶部汇总说明条影响整体观感。

### 调整

- 日期 `筛选` 控件从顶部工具栏移到右侧 `流水调整` 面板顶部，并收窄日期框。
- 顶部汇总说明条隐藏，让表格区域上移。
- `未启用` 和调整结果数值字号调大；`原始统计保持不变`、利润率提示文字改小且不加粗；`开具月收入` 小一号。
- 表格横向滚动条改为按需显示，列宽超出时仍可拖动，不需要时不显示。
- 补齐表格 viewport、corner 和 header 背景，避免嵌入深色模式下出现黑色空条。

### 验证

```powershell
python -m py_compile gui_v2.py
```

报告工作台侧同步验证：

```powershell
python -m compileall -q car_report_tool
python -m unittest discover -s tests
```

### 影响范围

- 仅影响 PDF 流水 GUI 及其在报告工作台 `识别` 页中的展示。
- 不修改 PDF/Excel 解析器、交易模型、调整计算、Excel 导出和收入佐证 JSON 结构。

---

## 2026-07-04 嵌入识别页筛选和右栏 GUI 修正

### 背景

报告工作台嵌入 PDF 流水 GUI 后，继续按截图处理右侧面板和筛选日期控件问题。该轮为纯 GUI 调整。

### 问题和解决

- 右侧 `流水调整` 面板偏窄，左侧明细区域空白偏多：右侧面板加宽，左侧明细和上方指标区相应收窄。
- `开具月收入` 与 `利润率` 纵向占空间：改为同一行并排显示，利润测算卡标签缩小并取消粗体。
- 筛选日期最后一位被裁剪：先用 PyQt 测量确认根因是日期文本宽度接近控件宽度，扣除 padding、边框和下拉箭头区后空间不足；随后调大日期框、筛选面板和右侧栏宽度，并单独设置日期框字号、padding、下拉区和箭头尺寸。

### 验证

```powershell
python -m py_compile gui_v2.py
```

报告工作台侧同步验证：

```powershell
python -m compileall -q car_report_tool
python -m unittest discover -s tests
```

### 影响范围

- 仅影响 PDF 流水 GUI 及其在报告工作台 `识别` 页的展示。
- 不修改 PDF/Excel 解析器、交易模型、调整计算、Excel 导出和收入佐证 JSON 结构。

---

## 2026-07-04 建设银行个人筛选流水适配

### 更新前问题

- `C:\Users\lenovo\Desktop\李红梅\3a08cdc3597c48bc8560cd8d218a6f6e1783050410937.pdf` 标题为 `中国建设银行个人活期账户交易明细`，不命中旧规则 `中国建设银行个人活期账户全部交易明细`，自动识别结果为未识别。
- 用既有 `ccb` 解析器强制解析可得到交易，但该 PDF 页眉包含 `【筛选】 交易金额:10000.00及以上`，页脚说明不保证交易连续；旧逻辑仍按完整流水做余额连续性校验，导致大量伪异常。

### 解决方式

- `bankflow_v2/auto_detect.py` 新增建设银行个人识别关键词 `中国建设银行个人活期账户交易明细`，仍返回 bank id `ccb`。
- `bankflow_v2/ccb.py` 识别 `【筛选】` 或 `不保证为连续交易` 后，将流水余额标记为参考余额：
  - 金额和余额仍正常抽取。
  - 不用上一笔余额反推当前金额/余额。
  - 为交易设置 `balance_optional = True`，跳过相邻余额连续性和总收支余额闭合校验。
- `tools/regression_cases.json` 新增回归样例 `建设银行个人-活期账户交易明细筛选流水-李红梅`。

### 验证结果

- 自动识别：`ccb / 建设银行个人`，置信度 96，命中关键词 `中国建设银行个人活期账户交易明细`。
- 样本解析：244 笔，收入 103 笔 / 13,393,755.82，支出 141 笔 / 11,263,623.25，净额 2,130,132.57，异常 0。
- 页眉合计核对：总收入 13,393,755.82，总支出 11,263,623.25，与解析结果一致。

### 影响范围

- 影响建设银行个人 `中国建设银行个人活期账户交易明细` 筛选流水。
- 不改变 `ccb_corp` 建设银行对公解析。
- 对旧版建设银行个人完整流水，仅新增识别关键词；无筛选标记时仍保留原余额连续性校验。

---

## 2026-07-05 问题流水0701 批量解析与新增版式

### 背景

处理目录 `D:\Codex data\PDF流水\问题流水0701`。该目录包含已适配流水、新版式流水、重复 PDF、手工 Excel 以及非流水/图片型 PDF。

### 问题和解决

- 支付宝交易流水证明未识别：新增 `alipay` bank id 和 `bankflow_v2/alipay.py`，按 `收/支 / 交易对方 / 商品说明 / 收/付款方式 / 金额 / 交易订单号 / 商家订单号 / 交易时间` 解析；续页没有表头时按 8 列固定结构继续解析。支付宝无余额列，交易设置 `balance_optional = True`。
- 盛京银行交易流水未识别：新增 `shengjing` bank id 和 `bankflow_v2/shengjing.py`，按文本行 `记账日期 / 币种 / 交易金额 / 账户余额 / 摘要` 解析。
- 邢台银行账户交易明细未识别：新增 `xingtai` bank id 和 `bankflow_v2/xingtai.py`，按 `交易时间 / 收入或支出 / 交易金额 / 余额` 解析。
- 建行对公账户明细信息被民生关键词抢先误判：在自动识别中提前命中 `中国建设银行账户明细信息` 与借贷余额表头，返回 `ccb_corp`。
- 平安银行个人清单受 `B/A/P` 水印干扰未识别：放宽平安识别条件，命中 `平安银行 + 清单编号 + 交易金额余额交易地点摘要备注`。
- 招商银行个人流水漏抽无摘要行：`bankflow_v2/cmb.py` 将余额后的交易摘要字段改为可选，修复 `2026-05-20 CNY -7,602.78 39,423.12` 漏抽造成的余额不连续。
- 兴业银行强水印交易明细被误识别为工行对公，且余额列受水印污染：识别规则改为命中 `兴业银行交易明细` 表格；解析时对 `兴业银行交易明细 + 核验` 强水印版式将余额标记为参考余额，跳过相邻余额连续性和总闭合硬校验。
- 图片 PDF 转 Excel 后的普通明细导入补充：
  - 支持 `YYYY-MM-\nDDHH:MM:SS` 这类断行日期。
  - Excel 无余额列时为交易设置 `balance_optional = True`，不再误报余额缺失。
  - 存在方向列但方向为空时按不计收支处理，避免空白方向行误入收入或支出。

### 验证结果

- 批量验证命令：

```powershell
python tools\batch_validate.py "D:\Codex data\PDF流水\问题流水0701" --out "D:\Codex data\PDF流水\问题流水0701\batch_validate_final.xlsx"
```

- 结果：28 个 PDF 中 24 个通过、4 个图片 PDF 忽略，异常明细 0。
- `简化版产品大纲.pdf` 不是流水，按用户要求不处理。
- 典型新增样本：
  - 支付宝：855 笔，收入 1,101,734.00，支出 611,223.97，异常 0。
  - 盛京银行：`银行流水_0(4).pdf` 31 笔，异常 0；`银行流水_1(5).pdf` 231 笔，异常 0。
  - 邢台银行：`银行流水_3(1).pdf` 38 笔，异常 0；`银行流水_4(1).pdf` 43 笔，异常 0。
  - 兴业强水印：864 笔，收入 8,005,956.16，支出 8,024,370.99，余额参考，异常 0。
  - `银行流水_0(1).xlsx`：无余额 Excel 明细 76 笔，异常 0。

### 影响范围

- 新增 `alipay`、`shengjing`、`xingtai` 三个 bank id。
- 修复 `ccb_corp`、`pingan`、`cmb`、`cib` 的特定版式识别/解析。
- 不改变图片型 PDF 处理口径；无文字层 PDF 仍需 OCR 后才能解析。

---

## 2026-07-06 汉口银行对公图片 PDF 转 Excel 明细导入

### 背景

用户提供同一对公账户 6 份汉口银行图片型 PDF，并已手动转换为 Excel。原 PDF 无可抽取文本层；转换后的 Excel 页眉含交易笔数、贷方交易合计金额、借方交易合计金额，可用于核对。

### 问题和解决

- 现有通用 Excel 导入对该表解析为 0 笔：表头同时存在 `交易日期` 和 `交易时间`，旧通用表头映射会优先把 `交易时间` 当日期列，导致日期解析失败。
- 该格式每页重复表头，且转换器可能插入空白列；新增汉口银行转表专项分支，直接按 `序号 / 交易日期 / 交易时间 / 收入金额 / 支出金额 / 摘要 / 用途 / 对方账号 / 对方户名 / 余额` 定位列。
- 个别余额丢失小数点，如 `73536124`。解析器只在上一笔余额加本笔收支得到的期望余额，其分值数字与原始余额数字完全一致时，才校正余额。
- 页眉交易笔数、贷方合计、借方合计被纳入解析校验；不一致时标记交易需复核。
- GUI 混合导入 Excel 与 PDF 时，Excel 文件的 `bank_id` 固定为 `excel`，旧展示逻辑只按 `bank_id` 推断流水类型，因此汉口银行对公 Excel 会被归到“个人”，并与个人 PDF 合并展示。现改为 Excel 导入后优先使用交易解析出的银行标签推断类型；命中 `对公` 标签时写入交易 `flow_type = 对公`。
- `流水明细` 表原本仍在刷新但没有挂到 tab 上，恢复显示 `流水明细` tab，用于混合导入后逐笔核对个人/对公分类。

### 验证结果

- 6 份 Excel 合计 634 笔，收入 7,694,474.19，支出 7,387,816.49。
- 2026-01 至 2026-06 月度余额连续；合并期初 47,976.45，期末 354,634.15，异常 0。
- 混合导入验证：`工行.pdf` 识别为个人 220 笔；6 份汉口银行 Excel 识别为对公 634 笔；月度统计生成 `个人 / 对公 / 全部` 三组。
- `python -m py_compile bankflow_v2\excel_input.py gui_v2.py tools\regression.py`：通过。

### 影响范围

- 仅影响命中汉口银行对公转表特征的 Excel 导入。
- 不改变 PDF OCR 口径；图片型 PDF 仍需先外部转 Excel 后导入。

---

## 2026-07-06 农村商业银行个人交易明细缺页样本

### 背景

用户提供 `银行流水_0(8).pdf`，自动识别结果为未识别。PDF 有文本层，页眉包含 `卡号/账号`、`客户名称`、`总收入`、`总支出`，明细表头为 `序号 / 摘要 / 币别 / 钞汇 / 交易日期 / 交易金额 / 账户余额`。页脚显示总页数 45 页，但当前文件实际只有 30 页。

### 问题和解决

- 旧通用 PDF 解析器仅支持 `YYYY-MM-DD HH:MM:SS` 等常见格式，该样本交易日期为 `YYYYMMDD`，交易金额带正负号，导致通用解析为 0 笔。
- 新增 `rural_commercial` bank id，复用 `bankflow_v2/rural_credit.py` 增加文本行解析分支。
- 解析时按序号倒序恢复新到旧打印流水的余额链；正金额计收入，负金额计支出。
- 页眉收入/支出笔数和金额用于校验；当前页数小于页脚总页数时，标记 PDF 缺页提示，避免把缺页文件误判为完整通过。

### 验证结果

- 当前文件实际解析 570 笔，收入 168 笔 / 33,285,438.91，支出 402 笔 / 33,101,395.33。
- 当前文件内余额连续，期初 238,308.72，期末 422,352.30。
- 因 PDF 只有 30 页而页脚显示总页数 45 页，缺 15 页；与完整页眉合计不一致，异常 5 条。
- 已加入回归样例 `农村商业银行个人-交易明细缺页提示-李骏军`，预期保留 5 条异常。

