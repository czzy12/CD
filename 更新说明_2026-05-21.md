# 更新说明：批量验证与工行个人历史明细适配（2026-05-21）

## 本次目标

- 对 `D:\Codex data\CD_assets\PDF流水\打包测试` 中新增的一批银行流水 PDF 做批量验证。
- 暂不处理扫描/图片型 PDF 的 OCR。
- 忽略需要密码的加密 PDF。
- 优先推进未识别银行中的工商银行，其次为华夏银行、浦发银行；其他银行先标记为暂不处理。

## 新增批量验证工具

新增文件：

- `tools/batch_validate.py`

主要能力：

- 递归扫描指定目录下的 PDF。
- 自动识别银行类型。
- 调用现有解析流水线。
- 未识别或专用解析失败时尝试通用 PDF 兜底解析。
- 输出中文 Excel 验证报告，包含：
  - `概览`
  - `汇总`
  - `异常明细`
- 支持 `--ignore-parse-errors`，用于把加密/密码 PDF 标记为 `忽略-加密PDF`。
- 支持 `--manual-pass`，用于把已人工核对通过的文件标记为 `已人工核对通过`。
- 扫描/图片型 PDF 统一标记为 `忽略-图片PDF`，当前不做 OCR。

最新批量报告：

- `D:\Codex data\CD_assets\测试输出\打包测试_批量验证_中文_工行v1.xlsx`

生成命令：

```powershell
python tools\batch_validate.py "D:\Codex data\CD_assets\PDF流水\打包测试" --out "D:\Codex data\CD_assets\测试输出\打包测试_批量验证_中文_工行v1.xlsx" --ignore-parse-errors --manual-pass "中国银行5.pdf" --manual-pass "工商银行历史明细（申请单号：26051516345654037193）.pdf"
```

当时报表状态：

- `通过`：51
- `未识别银行`：24
- `需要复核`：15
- `忽略-加密PDF`：6
- `未抽取到流水`：5
- `忽略-图片PDF`：5
- `已人工核对通过`：2
- `通用解析需复核`：1

## 已确认核对口径

### 民生公户个人

- 程序统计收入 42 笔，金额 `2,022,989.32`。
- 人工统计差异来自两笔 `结息`：
  - `2025-12-21 01:01:34`，收入 `0.58`
  - `2026-03-21 01:19:11`，收入 `2.08`
- 已确认：结息应保留为收入。
- 结论：程序口径正确，人工转换时可能遗漏结息。

### 淮坊银行

- 三金额列格式应按第一笔交易倒推期初。
- 例：第一笔收入 `190000`，余额 `190000`，则期初余额为 `0`。
- 用户确认 01-12 当天实际交易顺序为：
  - 收入 `190000`
  - 支出 `190000`
  - 收入 `190000`
  - 支出 `190000`

### 中国银行5

- 通用解析通过。
- 用户已人工核对无误。
- 人工核对数据位置：
  - `C:\Users\lenovo\Desktop\流水测试\打包测试\已核对`

## 通用 PDF 兜底解析更新

修改文件：

- `bankflow_v2/generic_pdf.py`

更新内容：

- 新增支持 `日期 时间 收入 支出 余额` 三金额列结构。
- 修复淮坊/潍坊类样本中期初余额误取第一笔余额的问题。
- 期初余额现在按第一笔交易金额与第一笔余额倒推。

## 工商银行个人历史明细适配

修改文件：

- `bankflow_v2/auto_detect.py`
- `bankflow_v2/number_parser.py`

### 自动识别

新增工行个人历史明细识别关键词：

- `中国工商银行借记账户历史明细`
- `交易日期账号储种序号币种钞汇摘要地区收入/支出金额余额`

### 样本特点

- 工行 PDF 视觉表格结构稳定。
- 文本层会受到斜向水印数字、二维码、红章影响。
- 当前不做 OCR。
- 适配策略是：文本表格列位置 + 金额候选生成 + 余额连续性校验。

### 金额解析增强

`bankflow_v2/number_parser.py` 已增强脏金额处理能力：

- 清理金额字符串中的非金额字符。
- 从异常逗号金额中生成候选，例如：
  - `-15,2000.00` 可候选为 `-15000.00`
  - `+1,0200.00` 可候选为 `+1000.00`
- 余额前缀或后缀缺失时，结合上笔余额做连续性匹配。
- `choose_amount_and_balance` 会在金额候选、余额候选、上笔余额之间选择最符合连续性的组合。

## 工行黄金样本结果

黄金样本 PDF：

- `D:\Codex data\CD_assets\PDF流水\打包测试\工商银行历史明细（申请单号：26051516345654037193）.pdf`

人工核对 Excel：

- 同名 `.xlsx`
- `Sheet1` 字段：`交易日期`、`收入/支出金额`、`余额`、`收支`

当前程序解析结果与人工核对表完全一致：

- 明细：1148 笔
- 收入：78 笔，金额 `1,616,841.29`
- 支出：1070 笔，金额 `1,571,813.04`
- 余额连续异常：0
- 逐笔差异：0

## 工行批量样本现状

- 打包测试目录中约 15 个工行历史明细 PDF。
- 其中 5 个为密码/加密文件，按当前规则忽略并剔除测试。
- 可读样本中，部分已做到余额连续异常为 0。
- 仍有少数工行样本存在连续性异常，需要继续查看具体异常行并调规则。

## 当前源码变更清单

- `PROJECT_CONTEXT.md`
- `bankflow_v2/auto_detect.py`
- `bankflow_v2/generic_pdf.py`
- `bankflow_v2/number_parser.py`
- `tools/batch_validate.py`
- `更新说明_2026-05-21.md`

## 后续建议

1. 继续收敛工行剩余有连续性异常的样本。
2. 工行可读样本稳定后，再处理华夏银行。
3. 华夏完成后处理浦发银行。
4. 扫描/图片型 PDF、密码 PDF、其他未识别银行暂不进入当前主线。
5. 用户测试确认后，再决定是否接入主结构；如果测试发现样本不稳定，继续在当前分支调整。

## 追加更新：工行回归通过（2026-05-22）

新增文件：

- `tools/icbc_regression.py`

用途：

- 专门回归 `打包测试` 目录中的工行历史明细 PDF。
- 输出每份 PDF 的解析笔数、收入/支出汇总和余额连续异常数。
- 将读取失败的工行 PDF 作为 `IGNORED`，用于忽略密码/加密文件。

本次修复：

- 修改 `bankflow_v2/number_parser.py`。
- `balance_candidates` 增加三位小数噪声候选。
- 解决工行水印数字混入余额小数位导致的连续性异常。
- 典型问题：
  - `20A200,429.019` 需要能候选为 `200,429.09`。
  - `200127,490.761` 需要能候选为 `127,490.71`。

最新工行专项回归结果：

- 工行 PDF 总数：15
- 密码/读取失败忽略：5
- 可读样本：10
- 通过：10
- 需复核：0
- 失败：0

结论：

- 按当前口径，工行个人历史明细测试已通过。
- 后续可以进入华夏银行适配。
