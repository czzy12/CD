# 更新说明：2026-05-21 至 2026-05-22

## 分支与基线

- 当前分支：`work/2026-05-21-batch-icbc`
- 基线提交：`075e6738e9ca219950790e5418b0889d7d8d686d`
- 本轮目标：批量验证新增流水样本，优先确认工行、浦发、中行可用口径，并修复 GUI 测试中暴露的通用解析、Excel 导入、合并去重问题。

## 已确认通过的银行/场景

### 工商银行

- 工行个人历史明细已确认。
- 工行对公补充样本已确认。
- 打包测试中工行历史明细 PDF 共 15 份：
  - 密码/读取失败忽略：5 份。
  - 可读样本：10 份。
  - 可读样本通过：10 份。
  - 需复核：0。
- 追加测试：
  - `新工行.pdf`：工行个人，1406 笔，异常 0。
  - `新工行2.pdf`：工行对公，406 笔，异常 0。

### 浦发银行

- 已新增浦发个人与对公解析器。
- 新增 bank_id：
  - `spdb`：浦发个人客户交易流水专用回单。
  - `spdb_corp`：浦发对公电子对账单。
- 当前 4 份浦发样本均通过：
  - `浦发 (2).pdf`：个人，26 笔，异常 0。
  - `浦发.pdf`：对公，457 笔，异常 0。
  - `浦发2.pdf`：个人，94 笔，异常 0。
  - `浦发3.pdf`：个人，26 笔，异常 0。
- 对公样本首页汇总借方、贷方、期末余额与程序统计一致。

### 中国银行

- `中国银行5.pdf`：通用 PDF 解析通过，81 笔，异常 0，用户已人工核对。
- `刘春个人流水_.pdf`：原 PDF 为图片型，不做 OCR；用户转 Excel 后导入通过。
- `刘春个人流水_.xlsx` 与 `刘春个人流水的原版_.xlsx`：
  - 70 笔。
  - 收入 `503,162.45`。
  - 支出 `468,226.21`。
  - 期初 `76,375.82`。
  - 期末 `111,312.06`。
  - 异常 0。
- 当前口径：中行图片 PDF 仍先转 Excel 再导入，不做 OCR。

### 淮坊银行

- 通用 PDF 三金额列解析已修复。
- 关键修复：收入/支出/余额列允许整数金额，例如 `0`。
- `淮坊银行.pdf` 当前结果：
  - 42 笔。
  - 收入 18 笔，`1,926,951.15`。
  - 支出 24 笔，`1,925,128.15`。
  - 期初 `0.00`。
  - 期末 `1,823.00`。
  - 异常 0。
- 重要规则：有具体时分秒时必须按时分秒排序；没有时间时才退回页码/行号顺序，不按金额或余额重排。

## GUI 与导出修复

### GUI 合并去重

- 修复招商样本误判重复的问题。
- 原问题：招商 PDF 只有日期，没有时分秒；同一天可能出现“支出、退款、再支出”，余额和金额会重复，旧 GUI 去重规则会误删真实流水。
- 新规则：
  - 同一个 PDF 内不再凭日期/金额/余额去重。
  - 不同文件之间仍做重复提示和去重。
  - 有 `merge_key` 的流水仍按唯一键去重。
- 验证：
  - 招商样本 `招商银行交易流水(申请时间2026年05月11日18时12分47秒).pdf`：去重前后均为 842 笔，重复提示 0。
  - 刘春两份公户：39 笔跨文件合并后去重为 38 笔，重复提示 1，余额连续正常。

### Excel 导入

- 修复 PDF 转 Excel 后页眉行被误识别为流水的问题。
- 规则：有日期但金额/收入/支出/余额全空的行跳过。
- 用于处理中行图片 PDF 转出的 Excel。

### 工资类导出

- 曾尝试新增 `工资明细` 导出表，但工资 PDF 的原字段在 PDF 抽取阶段会被水印/噪声污染。
- 为避免影响当前稳定解析，已暂时关闭工资类专表导出。
- 当前保留策略：工资类需要完整原始字段时，优先参考用户转换后的 Excel；后续如要做，需要单独设计解析字段保留方案。

## 新增/修改的主要文件

- `tools/batch_validate.py`：批量验证工具。
- `tools/icbc_regression.py`：工行专项回归。
- `tools/spdb_regression.py`：浦发专项回归。
- `bankflow_v2/number_parser.py`：增强脏金额/余额候选解析。
- `bankflow_v2/generic_pdf.py`：通用 PDF 三金额列、整数金额兼容。
- `bankflow_v2/auto_detect.py`：新增工行个人、浦发个人、浦发对公识别。
- `bankflow_v2/pipeline.py`：接入浦发解析器。
- `bankflow_v2/spdb.py`：新增浦发个人/对公解析器。
- `bankflow_v2/excel_input.py`：跳过 Excel 页眉空金额行。
- `gui_v2.py`：修复成功通用识别异常提示、合并去重规则；暂时关闭工资类专表导出。

## 当前注意事项

- 扫描/图片 PDF 暂不做 OCR。
- 密码/加密 PDF 忽略，不计入失败。
- 工资类“完整原字段导出”暂不启用，避免影响现有解析稳定性。
- 当前改动尚未合并 main，仍在测试分支上供继续验证。
