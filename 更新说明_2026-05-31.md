# 2026-05-31 更新说明

说明：本文件记录本次“流水调整/测算层”更新，用于当前阶段手动测试和回溯。

## 本次分支

- 分支：`work/2026-05-31-flow-adjustment`
- 基于主线：`main`
- 功能定位：识别后的可选调整/测算层

## 主要更新

### 1. 新增独立调整模块

新增：

- `bankflow_v2/adjustment.py`

调整逻辑独立于银行解析器，不修改：

- `icbc.py`
- `icbc_corp.py`
- `spdb.py`
- `citic.py`
- 其他银行专用解析器

### 2. GUI 增加流水调整区

调整区为两行：

```text
[ ] 启用收入调整（微信）    [ ] 启用收支平衡调整（个/公）
调整金额 ____ 万元，月份 ____ 至 ____    [ ] 固定分配
```

两种模式互斥，可以都不选。

### 3. 月度统计直接切换为调整后数据

- 默认不启用调整，`月度统计` 显示原始结果。
- 启用调整后，标签自动变为 `调整月度统计`。
- 同一张月度表直接显示调整后收入、支出、净额、期初、期末。
- 表尾追加：
  - `收入调整(万元)`
  - `支出调整(万元)`
  - `调整后余额变动(万元)`
  - `调整后收支差额(万元)`
  - `平衡校验`
  - `说明`

### 4. 增加月均行

`总计` 行下方增加 `月均` 行：

- 收入月均 = 收入合计 / 已有流水月份数
- 支出月均 = 支出合计 / 已有流水月份数

不对笔数、净额、期初、期末余额做平均。

### 5. Excel 导出保留原始与调整结果

导出 Excel 包含：

- `原始月度统计`
- `调整后月度统计`
- `调整参数`
- `调整说明`
- `原始明细`
- `汇总`
- `异常提示`

## 计算口径

### 收入调整（微信）

- 只增加收入。
- 按已有流水月份平均分摊。
- 不生成没有流水的月份。
- 微信“其他”仍按原规则默认排除。

### 收支平衡调整（个/公）

- 增加收入。
- 同时增加等额支出作为平衡项。
- 保持期末余额不变。
- 按已有流水月份平均分摊。

### 金额分摊

- 输入单位为万元。
- 默认在已有流水月份内做确定性随机分摊，看起来更自然。
- 随机分配不是每次刷新都变；同一金额、月份和调整模式会得到同一组结果，便于复核和留档。
- 勾选 `固定分配` 后改为平均分摊。
- 分摊到每个月时保留到分。
- 不能整除时，最后一个调整月份补差，保证总调整金额等于输入金额。

## 验证结果

已运行：

```powershell
python -m py_compile bankflow_v2\adjustment.py gui_v2.py
python tools\regression.py --all
```

结果：

- 统一回归：14 PASS / 0 FAIL
- 源码版 GUI 短启动成功
- 已完成简单 GUI 手动测试

## 注意事项

- 本功能是测算层，不代表银行原始流水。
- 原始明细不会被改写。
- 默认不启用调整。
- 若启用调整但未输入金额，调整金额按 0 处理，并在参数中记录提示。

## GUI 样式补充更新

- 顶部操作栏改为更清晰的主次结构：左侧突出 `开始处理`，右侧保留 `导出 Excel`。
- 日期筛选移动到顶部操作栏中，作为独立筛选模块展示。
- 新增横向仪表板摘要：文件、流水、收入、支出、异常。
- 收入和支出指标使用颜色区分；笔数加粗，金额减小字号以提升可读性。
- 右侧流水调整区改为独立面板，并用结果卡展示调整后净额和平衡校验。
- 月度统计等标签改为下划线风格，减少按钮块堆叠感。
- 日期/月度选择框下拉箭头改为轻量小箭头。
