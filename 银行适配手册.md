# 银行流水PDF提取 — 银行适配手册

## 当前实际支持与解析入口（2026-07-20）

当前代码不是纯 JSON 配置驱动，核心解析器位于 `bankflow_v2/*.py`，统一回归入口为：

```powershell
python tools\regression.py --all --allow-missing
```

本手册只记录当前生效的识别、字段和解析规则；最新统一回归结果见 `PROJECT_STATUS.md`，逐次验证证据见 `技术变更记录.md`。

| 银行/格式 | 类型 | 解析入口 | 当前状态 | 说明 |
|---|---|---|---|---|
| 工商银行 | 个人历史明细 | `icbc.py` | 已适配 | 支持强水印、金额/余额污染候选路径选择 |
| 工商银行 | 个人活期/定期混排 | `icbc.py` | 已纳入回归 | 统计正确，保留 13 条跨账户余额链复核提示 |
| 工商银行 | 对公旧版 A | `icbc_corp.py` | 已适配 | `借贷标志 + 发生额 + 余额`，同秒多笔按余额链恢复顺序 |
| 工商银行 | 对公旧版 B | `icbc_corp.py` | 已适配 | `借/贷 + 借方发生额 + 贷方发生额 + 余额`，保留负数借方冲正符号 |
| 工商银行 | 企业存款对账单新版 C | `icbc_corp.py` | 已适配 | `借方发生额 / 贷方发生额 / 余额`，无单独借贷方向列，保留分列符号 |
| 建设银行 | 个人活期账户交易明细筛选流水 | `ccb.py` / `ccb` | 已适配 | `交易金额 / 账户余额` 单金额列；`【筛选】` 流水余额仅作参考，不做连续性校验 |
| 建设银行 | 对公活期存款明细账 | `ccb_corp.py` | 已适配 | 表格列内多行换行文本 |
| 建设银行 | 对公账户明细信息 17 列版 | `ccb_corp.py` | 已适配 | `交易时间 / 借方发生额 / 贷方发生额 / 余额` 逐行表 |
| 交通银行 | 6 列日期版 | `bocom.py` | 已适配 | 无具体时分秒时按页面顺序保持日内顺序；借贷方向兼容金额正负号 |
| 交通银行 | 11 列时间版 | `bocom.py` | 已适配 | 带交易时间；借贷方向兼容金额正负号 |
| 交通银行 | 四川省分行对公明细对账单 | `bocom_corp.py` | 已适配 | `会计日期 / 交易日期 / 借方发生额 / 贷方发生额 / 余额` 表格，bank id 为 `bocom_corp` |
| 光大银行 | 个人账户明细查询清单 | `everbright.py` / `everbright` | 已适配 | `支出金额 / 存入金额 / 账户余额` 表格，PDF 倒序打印，同日按倒序还原 |
| 光大银行 | 对公账户对账单 | `everbright.py` / `everbright_corp` | 已适配 | `借/贷 + 交易金额 + 账户余额` 文本行，PDF 倒序打印，同秒按倒序还原 |
| 农业银行 | 个人 | `abc.py` | 已适配 | 无具体时间排序样本已纳入回归；`抹账/自动抹账` 按余额差修正方向 |
| 邮储银行 | 个人历史明细 | `psbc.py` | 已适配 | 已纳入回归 |
| 招商银行 | 个人交易流水 | `cmb.py` | 已适配 | 已纳入回归 |
| 招商银行 | 对公账务明细清单 | `cmb_corp.py` | 已适配 | `Statement Of Account`，金额列自带正负号 |
| 民生银行 | 对公单位账户对账单 | `cmbc_corp.py` | 已适配 | `单位账户对账单` 优先识别为对公 |
| 中信银行 | 个人账户交易明细 | `citic.py` | 已适配 | 单金额列按余额关系判断收入/支出 |
| 中信银行 | 对公账户交易明细 | `citic.py` | 已适配 | `借方发生额 / 贷方发生额 / 余额` 分列表格，bank id 为 `citic_corp` |
| 农村商业银行 | 对公客户账户明细对账单 | `customer_account_corp.py` / `customer_account_corp` | 已适配 | `交易日期 / 借方金额 / 贷方金额 / 余额` 8 列表格；PDF 文本层未显示银行名，银行名由用户确认 |
| 天津农村商业银行 | 对公交易明细查询 | `tianjin_rural_corp.py` / `tianjin_rural_corp` | 已适配 | `交易日期 / 收入 / 支出 / 余额` 表格层解析 |
| 浦发银行 | 个人 | `spdb.py` | 已适配 | 已做专项样本测试 |
| 浦发银行 | 对公 | `spdb.py` | 已适配 | 已做专项样本测试 |
| 齐鲁银行 | 对公单位活期存款账户交易明细 | `qilu_corp.py` | 已适配 | 强水印 PDF，按表格行边界和列坐标解析 |
| 农村信用社 | 个人交易流水表格 | `rural_credit.py` | 已适配 | PDF 表格抽取，倒序流水按同秒余额链排序 |
| 农村商业银行 | 个人交易明细 | `rural_credit.py` / `rural_commercial` | 已适配 | `卡号/账号 / 客户名称 / 总收入 / 总支出` 页眉，`序号 / 摘要 / 币别 / 钞汇 / 交易日期 / 交易金额 / 账户余额` 文本行 |
| 成都农村商业银行 | 对公客户明细 | `chengdu_rural_corp.py` | 已适配 | 表格列内换行，借方/贷方/余额分列 |
| 桂林银行 | 对公企业客户交易清单 | `guilin_corp.py` / `guilin_corp` | 已适配 | 坐标抽取收入/支出/余额分列；保留负数支出冲正符号 |
| 重庆银行 | 个人账户交易明细 | `chongqing.py` / `chongqing` | 已适配 | 表格层 `交易日期 / 交易金额 / 活期账面余额`，金额正负号表示收支 |
| 上海银行 | 对公账户明细对账单 / 账户明细查询 | `shanghai.py` / `shanghai_corp` | 已适配 | 借方/贷方/余额分列；`账户明细查询` 对同秒多笔按打印顺序反向恢复余额链 |
| 上海银行 | 个人交易明细 | `shanghai.py` / `shanghai` | 已适配 | 文本行 `YYYYMMDD + CNY + 交易金额 + 期末金额`，倒序打印 |
| 华夏银行 | 个人账户交易流水电子版 | `huaxia.py` / `huaxia` | 已适配 | 文本行 `YYYY-MM-DD + 摘要 + 交易金额 + 余额`，新到旧打印，按日内反向恢复余额链 |
| 平安银行 | 个人账户交易明细清单 | `pingan.py` / `pingan` | 已适配 | 表格层 8 列，带 `BAP` 水印；按清单页码和序号恢复跨文件顺序 |
| 兴业银行 | 个人交易流水 | `cib.py` / `cib` | 已适配 | 表格层 9 列，带水印；余额按候选值和连续性校验选择 |
| 九江银行 | 个人交易流水 | `city_commercial.py` / `jiujiang` | 已适配 | 文本行 `日期 + CNY + 金额 + 余额` |
| 佛山农村商业银行 | 个人交易流水 | `city_commercial.py` / `foshan_rural` | 已适配 | 表格列 `交易日期 / 收入/支出 / 余额`，过滤跨页重复流水 |
| 兰州银行 | 对公账户对账单 | `city_commercial.py` / `lanzhou` | 已适配 | 表格列 `交易日期 / 收支金额 / 余额`，支持冲正双负号 |
| 宁波银行 | 个人交易流水 | `city_commercial.py` / `ningbo` | 已适配 | 文本行 `日期 + 摘要 + 人民币 + 金额 + 余额` |
| 南京银行 | 对公账户交易明细 | `city_commercial.py` / `nanjing_corp` | 已适配 | 表格列 `收入 / 支出 / 账户余额`，与人工 Excel 汇总口径一致 |
| 中原银行 | 个人账户交易流水 | `zhongyuan.py` | 已适配 | 表格列 `金额 + 收支状态 + 余额`，倒序打印 |
| 浙江网商银行 | 对公企业账户交易明细 | `mybank_corp.py` / `mybank_corp` | 已适配 | 坐标词块解析；`借方金额（收）/贷方金额（支）`；同秒按序号倒序恢复余额链 |
| Excel 导入 | 外部转表流水 | `excel_input.py` | 已支持 | 支持 `借贷状态`；支持 HTML 伪 `.xlsx`；中行收入筛选流水按页合计修复 OCR 金额 |
| 通用 PDF 识别 | 未适配专用银行 | `generic_pdf.py` | 兜底支持 | 小额余额误差降为低风险，仍需人工核对关键指标 |

打包前建议固定执行：

```powershell
python -m py_compile bankflow_v2\summary.py bankflow_v2\auto_detect.py gui_v2.py tools\regression.py
python tools\regression.py --all --allow-missing
```

---

## 2026-06-26 天津农村商业银行对公交易明细查询

- 识别关键词组合：`交易明细查询`、`天津农村商业银行`、`交易日期 / 收入 / 支出 / 余额 / 对方户名 / 对方账号 / 对方开户行 / 摘要 / 备注`。
- 解析器：`bankflow_v2/tianjin_rural_corp.py`，bank id 为 `tianjin_rural_corp`。
- 格式特点：PDF 表格层稳定；收入、支出、余额三列分列；手续费行可能只有支出和余额，无对方信息。
- 处理方式：按表格行解析交易日期、收入、支出和余额；`--` 按 0 处理；保留对方户名、账号、开户行、摘要、备注等原始字段。

---

## 2026-06-26 浙江网商银行企业账户交易明细

- 识别关键词组合：`浙江网商银行企业账户交易明细`、`借方金额（收） / 贷方金额（支） / 余额`。
- 解析器：`bankflow_v2/mybank_corp.py`，bank id 为 `mybank_corp`。
- 格式特点：主体明细在表格抽取中会合并为大单元格；页面词块坐标稳定；PDF 按新到旧打印；同一秒可能有多笔交易。
- 处理方式：按词块坐标拆分序号、账务流水号、交易时间、交易名称、收入、支出、余额、对方信息；借方金额列按收入统计，贷方金额列按支出统计；同秒多笔按序号倒序恢复余额链。

---

## 2026-06-25 中国银行 PDF 图片转 Excel 收入筛选流水

- 识别关键词组合：`中国银行交易流水明细清单`、`记账日期 / 记账时间 / 币别 / 金额 / 余额 / 交易名称`。
- 解析器：`bankflow_v2/excel_input.py` 的中行 PDF 转 Excel 分支。
- 格式特点：页眉可能显示 `按收支筛选：收入`，此时表格不是完整收支流水，只有贷方收入明细；余额列仍为账户真实余额，会受未展示支出影响。
- 处理方式：收入筛选流水只统计已展示收入，不生成支出明细；余额标记为参考余额，不做完整余额链校验；每页按 `贷方发生数` 校正 OCR 少识别的金额。

---

## 2026-06-24 农村商业银行对公客户账户明细对账单

- 识别关键词组合：`客户账户明细对账单`、`账号/卡号`、`交易日期 / 摘要 / 借方金额 / 贷方金额 / 余额 / 币种 / 对方账号/户名 / 用途`、`借方总笔数`。
- 解析器：`bankflow_v2/customer_account_corp.py`，bank id 为 `customer_account_corp`。
- 格式特点：用户确认为农村商业银行对公流水；页眉未显示银行名；PDF 表格层稳定；日期只有 `YYYYMMDD`，没有时分秒；页脚有本页与总计行。
- 处理方式：只解析首列为 8 位日期且借方/贷方/余额金额有效的表格行；借方作为支出，贷方作为收入；按 PDF 页码和行号保留同日交易顺序。

---

## 2026-06-23 工商银行对公版式其他

- 识别关键词组合：工商银行对公流水表头，命中 `借贷标志 / 发生额 / 余额` 或 `借方发生额 / 贷方发生额 / 余额`。
- 解析器：`bankflow_v2/icbc_corp.py`，bank id 为 `icbc_corp`。
- 格式特点：同一秒可能存在多笔交易，PDF 打印顺序不一定等于余额链顺序；借方发生额可能出现负数冲正。
- 处理方式：相同交易时间的交易组按余额连续性评分恢复组内顺序；收入/支出分列保留原始符号参与汇总。

---

## 2026-06-21 重庆银行账户交易明细

- 识别关键词组合：`重庆银行账户交易明细`、`交易金额 / 活期账面余额 / 交易类型/摘要`。
- 解析器：`bankflow_v2/chongqing.py`，bank id 为 `chongqing`。
- 格式特点：PDF 有稳定表格层；交易日期列可能拆成 `2025\n0621`；交易金额自带正负号；余额列为交易后 `活期账面余额`。
- 处理方式：按表格行解析 `交易日期 / 交易金额 / 活期账面余额`；正数作为收入，负数取绝对值作为支出；保留摘要、渠道、交易对手等原始字段。

---

## 2026-06-18 桂林银行企业客户交易清单

- 识别关键词组合：`桂林银行企业客户交易清单`、`交易日期 / 对方账号 / 对方户名 / 收入 / 支出 / 余额`。
- 解析器：`bankflow_v2/guilin_corp.py`，bank id 为 `guilin_corp`。
- 格式特点：PDF 有文本层，但表格抽取只稳定返回页眉；交易明细需要按单词坐标分行抽取。
- 处理方式：保留收入、支出、余额三列原始符号；支出列负数按冲正参与余额链；对方户名换行时仍以日期、账号和三列金额识别交易。

---

## 2026-06-07 城商行/农商行首批确认样本

本批只确认 0 异常、余额闭合或与人工汇总一致的银行格式；未达确认条件的样本不混入同一个通用解析器，后续单独适配。

- 已确认并纳入回归：九江银行、佛山农村商业银行、兰州银行、宁波银行、南京银行对公。
- 待单独适配：天津银行、莱阳农商银行、河北农信 `账户历史明细清单`。
- 验证命令：`python tools\regression.py --tag city-commercial --allow-missing`

---

## 2026-06-08 农行/交行与汇总校验补充

- 农业银行个人流水：`抹账/自动抹账` 明细优先按余额差判断冲正方向，避免交易类型文字与余额变化方向相反时统计错误。
- 交通银行个人流水：借贷方向列兼容金额正负号，方向和符号同时存在时按综合结果判断收入/支出。
- 汇总层：除微信流水和余额可选流水外，增加总口径闭合复核提示；若 `收入合计 - 支出合计` 与 `期末余额 - 期初余额` 不一致，提示人工复核。

---

## 2026-06-14 交通银行对公明细对账单

- 识别关键词组合：`交通银行四川省分行明细对账单`、`会计日期 / 交易日期 / 交易名称`、`借方发生额 / 贷方发生额 / 余额`。
- 解析器：`bankflow_v2/bocom_corp.py`，bank id 为 `bocom_corp`。
- 格式特点：PDF 横向表格可由 `pdfplumber.extract_tables()` 稳定抽取为 16 列，流水金额为借方/贷方分列，余额为交易后余额；同月多份文件通过首笔交易反推期初余额。

---

## 2026-06-14 上海银行账户明细与交易明细

- 识别关键词组合：
  - 对公：`账户明细对账单` 或 `账户明细查询`，并命中借方/贷方/余额分列表头。
  - 个人：`上海银行交易明细`、`Transaction Details`、`交易金额 / 期末金额`。
- 解析器：`bankflow_v2/shanghai.py`，bank id 为 `shanghai_corp` / `shanghai`。
- 格式特点：
  - 对公 `账户明细对账单` 为 10 列表格，第一页有表头，后续页延续同一列结构。
  - 对公 `账户明细查询` 为 13 列表格，按新到旧打印；同秒多笔需要按打印顺序反向恢复余额链。
  - 个人 `交易明细` 无表格层，文本行按 `YYYYMMDD + 摘要 + CNY + 金额 + 期末金额` 抽取，按倒序打印恢复日内顺序。

---

## 2026-06-14 华夏银行个人账户交易流水电子版

- 识别关键词组合：`华夏银行个人账户交易流水`、`Huaxia Bank Personal Transaction Statement`、`交易金额 / 余额`。
- 解析器：`bankflow_v2/huaxia.py`，bank id 为 `huaxia`。
- 格式特点：PDF 有文本层；交易行按 `YYYY-MM-DD + 摘要 + 交易金额 + 余额` 抽取；交易按新到旧打印，同日内也需要反向恢复余额链。
- 处理方式：从文本层逐行抽取日期、交易金额和交易后余额；金额正数计收入、负数计支出；按日期升序和打印序号反向排序后补秒级顺序用于余额校验。

---

## 2026-06-14 平安银行个人账户交易明细清单

- 识别关键词组合：`平安银行个人账户交易明细清单` / `Transaction Details List of Personal Account of Pingan Bank`；续页使用 `清单编号 / List Number`、`交易金额 / 余额 / 交易地点 / 摘要 / 备注`、`Counterparty Information` 组合识别。
- 解析器：`bankflow_v2/pingan.py`，bank id 为 `pingan`。
- 格式特点：PDF 表格层为 8 列，核心列为 `序号 / 交易日期 / 交易金额 / 余额`；水印 `BAP` 会混入序号、日期、金额、余额和文本字段。
- 处理方式：从表格层逐行抽取交易；金额和余额清洗时只保留数字、小数点和正负号；用清单页脚 `x/31` 作为原始页码，序号作为行号，保证 4 个拆分页 PDF 合并后仍按原清单顺序校验。

---

## 2026-06-14 兴业银行个人交易流水

- 识别关键词组合：`兴业银行交易流水`。
- 解析器：`bankflow_v2/cib.py`，bank id 为 `cib`。
- 格式特点：PDF 表格层为 9 列，核心列为 `记账日期 / 支收 / 交易金额 / 账户余额`；水印字符和数字会混入日期与余额。
- 处理方式：优先使用 `记账日期` 列恢复交易日期；交易金额按符号判断收入/支出；余额生成多个候选值，并优先选择满足 `上笔余额 + 本笔金额 = 当前余额` 的候选。

---

## 2026-06-09 招商银行对公账务明细清单

- 识别关键词组合：`账务明细清单`、`Statement Of Account`、`招商银行`。
- 解析器：`bankflow_v2/cmb_corp.py`，bank id 为 `cmb_corp`。
- 格式特点：PDF 文本层每笔流水以 `YYYYMMDD` 开头，业务类型紧跟日期；`借方/贷方金额` 为单列带正负号金额，随后为余额和对手户名；税单号、摘要和对手户名可能跨行。
- 处理方式：按交易起始行正则抽取日期、业务类型、摘要/票据号、金额、余额和对手户名；非交易续行追加到上一笔 `raw_text/raw_fields`，不参与统计；按余额连续性追加复核提示。

---

## 2026-06-12 中国银行 PDF 转 Excel 的 HTML xlsx

- 适用文件：扩展名为 `.xlsx`，实际内容为 WPS/Excel HTML 表格的 PDF 转 Excel 文件。
- 关键列：`记账日期`、`记账时间`、`币别`、`金额`、`余额`。
- 处理方式：标准 `openpyxl` 读取失败后，使用 HTML `<table>` 兜底读取，再复用 Excel 导入与中国银行转换表解析逻辑。

---

## 2026-06-12 中原银行账户交易流水

- 识别关键词组合：`账户交易流水`、`中原银行`、`收支状态`。
- 解析器：`bankflow_v2/zhongyuan.py`，bank id 为 `zhongyuan`。
- 格式特点：PDF 表格层有 12 列，核心列为 `交易日期`、`交易时间`、`金额`、`收支状态`、`余额`；金额列无正负号，收入/支出由 `收支状态` 决定。
- 处理方式：读取表格层逐行解析；流水按新到旧倒序打印，同秒多笔交易反转顺序以恢复余额链。

---

## 2026-06-13 成都农村商业银行对公客户明细

- 识别关键词组合：`成都农村商业银行`、`客户明细`、`借方金额 / 贷方金额 / 余额`。
- 解析器：`bankflow_v2/chengdu_rural_corp.py`，bank id 为 `chengdu_rural_corp`。
- 格式特点：PDF 表格抽取后每页只有一条数据行，各列内部用换行承载多笔交易；核心列为 `交易日期`、`借方金额`、`贷方金额`、`余额`。
- 处理方式：按列内换行拆分核心字段；借方计支出，贷方计收入；流水按新到旧倒序打印，同日跨页时按全局打印序号反向恢复余额链。

---

## 2026-05-31 新增/扩展格式

### 农村信用社：个人交易流水表格

- 识别关键词：`交易流水号交易日期交易网点收入/支出交易金额实时余额`
- 解析器：`bankflow_v2/rural_credit.py`
- 格式特点：PDF 表格结构清晰，字段包括 `交易日期`、`收入/支出`、`交易金额`、`实时余额`；流水按新到旧倒序打印。
- 处理方式：按表格列解析方向、金额和余额；对同一秒内的多笔交易使用不可见微秒排序，恢复余额链顺序，导出原始时间仍保留秒级文本。

### 齐鲁银行对公：单位活期存款账户交易明细

- 识别关键词：`单位活期存款账户交易明细`
- 解析器：`bankflow_v2/qilu_corp.py`
- 格式特点：PDF 有完整文本层和表格线，但强水印会混入日期、金额和余额文本；支出列可能出现负数冲正。
- 处理方式：按 PDF 表格行边界定位 1521 条流水，再按固定列坐标抽取收入、支出和账户余额；每列按字符基线生成金额候选，并用余额连续性选择正确候选。
- 统计口径：保留收入/支出列的原始符号，`preserve_signed_columns=True`，余额校验仍按 `收入 - 支出`。

### 建设银行对公：活期存款明细账

- 识别关键词：`中国建设银行股份有限公司活期存款明细账`
- 解析器：`bankflow_v2/ccb_corp.py`
- 格式特点：PDF 表格可抽取，但交易不是逐行返回，而是同一列内多行换行文本。
- 处理方式：按列内换行索引拆分日期、借方、贷方、余额，再重组为逐笔交易。

### 中国银行对公：活期明细文本流水

- 识别关键词组合：`中国银行`、`单位人民币活期`、`承前页余额`、`借方发生额`、`贷方发生额`
- 解析器：`bankflow_v2/boc_corp.py`
- 格式特点：PDF 表格抽取为空，但文本层中每笔流水为 `|` 分隔行。
- 处理方式：解析记账日、借方发生额、贷方发生额、余额，并用 `承前页余额` 验证期初。

---

## 历史配置说明

早期 JSON 配置表、旧列号和旧样本统计已停止作为当前实现依据，不在本手册重复保留。需要追溯旧配置迁移、乱码处理、借贷方向和跨页表头等踩坑时，读取 `技术变更记录.md` 的对应日期条目和下方“核心修复记录”。

---
## 核心修复记录

### 2026-05-30 工行对公企业存款对账单新版式

**现象**: 4 份 `中国工商银行企业存款对账单` 被识别为工行对公，但专用解析返回 0 笔，GUI 回退到通用识别后出现大量余额不连续。

**原因**: 表头只有 `借方发生额 / 贷方发生额 / 余额`，没有旧版工行对公依赖的 `借/贷` 或 `借贷标志` 列。

**修复**:

- 自动识别新增关键词：`中国工商银行企业存款对账单`
- `icbc_corp.py` 新增 format C
- format C 按 `借方发生额=支出`、`贷方发生额=收入`、`余额=余额链校验`
- 4 份样本加入 `tools/regression_cases.json`

**验证**: 4 份李翠样本全部 0 异常，统一回归 `14 PASS / 0 FAIL`。

### 2026-05-29 中信、民生对公、Excel 导入与小额余额差异

**中信个人**:

- 新增 `citic.py`
- 支持 `账户交易明细 / Transaction details`
- PDF 文本层只有单金额列时，使用上一笔余额判断收入或支出

**民生对公**:

- `单位账户对账单` 优先识别为 `民生银行对公`
- 避免被通用民生关键字误识别为个人

**Excel 导入**:

- 支持 `借贷状态`
- 保留银行原始借/贷列口径
- 借方负数冲正仍按支出列口径统计，方便和人工透视表一致

**小额余额差异**:

- Excel 导入和通用 PDF 识别中，0.99 元以内余额连续性差异标为 `低风险`
- 低风险不再把文件状态打为 `需复核`
- 专用银行解析器仍保持严格复核标准

### 1. PDF乱码金额 — `clean_number` 三次迭代
**现象**: 工商银行PDF金额列混入随机字符（如 `04-59.90`、`40:5-29.70`）
**v1**: `re.search` 第一个匹配 → 乱码前缀被误认为金额
**v2**: 优先匹配 `[+-]\d+\.\d{2}` → 部分解决
**v3**: `re.findall` 取最后一个匹配 → 解决大部分
**v4**: 余额交叉验证 → 解决数字粘合（`1426.19` → `146.19`）

### 2. 余额交叉验证
**文件**: core/extractor.py `_validate_with_balance`
**原理**: `余额变动 = 当前余额 - 上笔余额`，与解析金额对比
**安全机制**: 比值限制0.05x~20x，过滤不可靠余额

### 3. 银行检测加权
**现象**: 短关键词（"工行"、"ABC"）在乱码中误匹配
**修复**: 改为关键词长度加权 `score = sum(len(kw) for kw in matched)`

### 4. 四舍五入误差
**现象**: 月度值先舍入再求和 → 合计行与余额行不一致
**修复**: 合计行从原始DataFrame计算，不累加月度值

### 5. 跨页表头缺失
**现象**: 浦发仅首页有表头
**修复**: `_cached_col_match` 缓存首页匹配

### 6. 跨行文本配对
**现象**: 民生对公PDF每笔交易跨两行（日期/时间分离）
**修复**: `text_line_pair: true` 配置项，相邻行合并

### 7. 借贷方向反转
**现象**: 个人户借方=支出，对公户借方=收入
**修复**: 配置项 `debit_is` 控制

### 8. 文本行乱码过滤
**现象**: 农业、民生PDF乱码行被正则误匹配
**修复**: 验证日期+时间前缀格式，不匹配则跳过

### 9. 时间排序
**现象**: 同日交易未按时间排序
**修复**: 捕获time字段，复合排序

### 10. 表格复制
**现象**: QTableWidget Ctrl+C只复制当前格
**修复**: 子类化CopyableTable，遍历选中区生成制表符文本

---

## 新增银行流程

1. 先用 `pdfplumber` 检查文本层、表格结构、字段顺序和页内排序。
2. 在 `bankflow_v2/auto_detect.py` 增加稳定识别规则，避免短关键词误判。
3. 新增或修改对应银行解析器，输出统一 `Transaction`。
4. 在 `bankflow_v2/pipeline.py` 接入 bank id。
5. 在 `tools/regression_cases.json` 增加样本期望。
6. 运行专项回归和全量回归。
7. 更新 `技术变更记录.md`；涉及格式规则时同步更新本手册。

---

## 当前技术栈与入口

- Python 3.12+
- `pdfplumber`：PDF 文本和表格抽取。
- `openpyxl`：Excel 导入和导出。
- `PyQt6`：源码 GUI，入口为 `gui_v2.py`。
- `bankflow_v2/*.py`：当前核心解析器、识别、汇总和调整逻辑。
- `tools/regression.py`：统一回归入口。

安装依赖以 `requirements.txt` 为准。

## 2026-06-06 建设银行对公账户明细信息

- 识别关键词：`中国建设银行账户明细信息`
- 解析器：`bankflow_v2/ccb_corp.py`
- 表格结构：17 列逐行表，关键列为 `交易时间`、`借方发生额`、`贷方发生额`、`余额`。
- 处理方式：按 17 列账户明细表逐行解析，借方计支出，贷方计收入，余额用于连续性校验。
- 注意事项：该格式表头也包含 `日期`、`发生额`、`借方`、`贷方`、`余额`，但不是 `活期存款明细账` 的列内换行格式。判断 `活期存款明细账` 时必须排除包含 `账号` 或 `交易时间` 的账户明细表，避免误进入列内拆分分支。

## 2026-06-06 中信银行对公账户交易明细

- 识别关键词组合：`账户交易明细`、`柜员交易号`、`动账资金分簿`、`借方发生额`、`贷方发生额`、`余额`。
- 解析器：`bankflow_v2/citic.py` 的 `extract_citic_corp()`。
- 表格结构：9 列逐行表，字段为 `交易日期 / 柜员交易号 / 摘要信息 / 对方账号 / 对方账户名称 / 动账资金分簿 / 借方发生额 / 贷方发生额 / 余额`。
- 处理方式：借方计支出，贷方计收入，余额用于连续性校验；交易日期无时分秒时按 PDF 页码和行号保持日内顺序。
- 与旧版区别：既有 `citic` 个人解析器处理 `RMB 金额 RMB 余额` 单金额列，本格式使用独立 bank id `citic_corp`，不复用个人版式。

## 2026-06-29 明细账查询通用表格

- 识别关键词组合：`明细账查询`、`交易日期`、`支出金额`、`收入金额`、`账户余额`、`交易名称`、`对方账号`、`对方户名`。
- bank id：`generic_pdf`。
- 解析器：`bankflow_v2/generic_pdf.py`。
- 表格结构：无交易时间列，列为 `交易日期 / 支出金额 / 收入金额 / 账户余额 / 交易名称 / 对方账号 / 对方户名`。
- 处理方式：文本层受水印影响不能按整行正则解析，改用字符/词坐标按日期 y 坐标切行，再按固定 x 区间取列；PDF 是倒序/顺序由页内坐标和余额校验确认。

## 2026-06-29 中国银行个人交易流水明细清单

- 识别关键词组合：`中国银行交易流水明细清单`、`记账日期记账时间币别金额余额交易名称`、`借记卡号`。
- bank id：`boc`。
- 解析器：`bankflow_v2/boc.py`。
- 表格结构：12 列表，字段为 `记账日期 / 记账时间 / 币别 / 金额 / 余额 / 交易名称 / 渠道 / 网点名称 / 附言 / 对方账户名 / 对方卡号/账号 / 对方开户行`。
- 处理方式：金额正数计收入，负数计支出；`冲正` 行虽然金额为负，但余额实际回增，按收入处理；同秒多笔交易按打印倒序还原，以保证余额连续。

## 2026-06-29 桂林银行个人收支明细表格

- 识别关键词组合：`桂林银行`、`交易日期对方账号对方户名收入（元）支出（元）账户余额（元）备注`。
- bank id：`guilin`。
- 解析器：`bankflow_v2/guilin_corp.py` 的 `extract_guilin()`。
- 表格结构：7 列表，字段为 `交易日期 / 对方账号 / 对方户名 / 收入（元） / 支出（元） / 账户余额（元） / 备注`。
- 处理方式：优先使用 `pdfplumber.extract_tables()`，可保留跨行户名/备注；收入列计收入，支出列计支出。该样本虽文件名写对公，用户确认是个人流水，所以输出 `桂林银行个人`。原 `桂林银行企业客户交易清单` 继续走 `guilin_corp`。

## 2026-06-29 工商银行对公账户明细清单

- 识别关键词组合：`中国工商银行账户明细清单`、`本方账号户名`、`转入金额`、`转出金额`、`余额`。
- bank id：`icbc_corp`。
- 解析器：`bankflow_v2/icbc_corp.py`。
- 表格结构：以 `交易时间` 为主键，存在多种列序，关键列为 `转入金额 / 转出金额 / 余额`。
- 处理方式：按显式转入列计收入、转出列计支出，余额用于连续性和总闭合校验；该规则优先于工商银行个人 `中国工商银行账户明细清单` 低置信关键词，避免对公识别为个人。

## 2026-06-29 农业银行对私客户账户明细

- 识别关键词：`对私客户账户明细`。
- bank id：`abc`。
- 解析器：`bankflow_v2/abc.py`。
- 表格结构：7 列，字段为 `交易日期 / 交易发生额 / 账户余额 / 对方账号 / 对方户名 / 摘要 / 备注`。
- 处理方式：交易日期为 `YYYYMMDD`，无具体时间；交易发生额带正负号，正数计收入、负数计支出，账户余额用于连续性和总闭合校验。

## 2026-06-29 农业银行对公无表格线账户明细

- 识别关键词组合：`交易时间`、`收入金额`、`支出金额`、`账户余额`。
- bank id：`abc_corp`。
- 解析器：`bankflow_v2/abc_corp.py`。
- 表格结构：PDF 文本按固定列坐标排布，但 `extract_tables()` 无法抽表，普通文本行会混入对方账号/户名/开户行。
- 处理方式：在表格解析和旧文本块解析都失败后，按坐标列解析兜底；支持日期拆成 `YYYY-MM- + DD` 或 `YYYY- + MM-DD` 两种断行方式，按收入、支出、余额列构造交易。

## 2026-06-29 交通银行对公明细对账单泛化

- 识别关键词组合：`交通银行`、`明细对账单`、`会计日期交易日期交易名称`、`借方发生额贷方发生额余额`。
- bank id：`bocom_corp`。
- 解析器：`bankflow_v2/bocom_corp.py`。
- 表格结构：16 列，字段为 `序号 / 会计日期 / 交易日期 / 交易名称 / 凭证种类 / 凭证号码 / 借方发生额 / 贷方发生额 / 余额 / 卡号 / 交易地点 / 对方账号 / 对方户名 / 对方行名 / 摘要 / 流水号`。
- 处理方式：借方正数计支出，贷方正数计收入；借方负数按收入、贷方负数按支出处理，用于抹账/冲正交易。识别规则不再限定具体省分行，避免辽宁省分行样本未识别或被对方行名误判为招商银行。

## 2026-06-30 中国光大银行个人/对公流水

- 个人识别关键词组合：`Transaction Statement of China Everbright Bank`、`Trans Amt Dr`、`Trans Amt Cr`、`Account Balance`。
- 个人 bank id：`everbright`。
- 对公识别关键词组合：`光大`、`对公`、`借方发生额`、`贷方发生额`、`序号 / 交易日期 / 时间 / 借/贷 / 交易金额 / 账户余额`。
- 对公 bank id：`everbright_corp`。
- 解析器：`bankflow_v2/everbright.py`。
- 个人表格结构：`交易日期 / 支出金额 / 存入金额 / 账户余额 / 对手信息 / 摘要`。
- 对公文本结构：`序号 / 交易日期 / 时间 / 借/贷 / 交易金额 / 账户余额 / 对方信息...`。
- 处理方式：个人按支出/存入分列计收支；对公按 `借方` 计支出、`贷方` 计收入。两类 PDF 都是倒序打印，解析时对同日或同秒交易使用全局倒序序号恢复余额链。

## 2026-07-03 对公客户账户明细

- 识别关键词组合：`对公客户账户明细`、`客户名称`、`交易日期交易发生金额账户余额对方账号对方户名摘要备注`、`借方合计笔数`、`贷方合计笔数`。
- bank id：`customer_detail_corp`。
- 解析器：`bankflow_v2/customer_detail_corp.py`。
- 表格结构：无表格线文本，字段为 `交易日期 / 交易发生金额 / 账户余额 / 对方账号 / 对方户名 / 摘要 / 备注`。
- 处理方式：交易日期为 `YYYYMMDD`；交易发生金额带正负号，正数计收入、负数计支出；账户余额用于连续性和总闭合校验；跨行备注追加到上一笔原始文本。

## 2026-07-05 支付宝交易流水证明

- 识别关键词组合：`支付宝支付科技有限公司交易流水证明`、`收/支交易对方商品说明收/付款方式金额`。
- bank id：`alipay`。
- 解析器：`bankflow_v2/alipay.py`。
- 表格结构：`收/支 / 交易对方 / 商品说明 / 收/付款方式 / 金额 / 交易订单号 / 商家订单号 / 交易时间`。
- 处理方式：`收入` 计收入，`支出` 计支出，`不计收支` 标为 neutral；支付宝无余额列，设置 `balance_optional = True`，不做余额连续性和总闭合校验。续页无表头时按 8 列结构继续解析。

## 2026-07-05 盛京银行个人交易流水

- 识别关键词组合：`盛京银行交易流水`、`Transaction Statement of Shengjing Bank`。
- bank id：`shengjing`。
- 解析器：`bankflow_v2/shengjing.py`。
- 表格结构：文本行包含 `记账日期 / 人民币 / 交易金额 / 账户余额 / 交易摘要 / 对手信息 / 附言`。
- 处理方式：交易金额正数计收入，负数计支出；账户余额用于连续性和总闭合校验。

## 2026-07-05 邢台银行账户交易明细

- 识别关键词组合：`邢台银行账户交易明细`、`收入/支出交易金额（元）余额（元）`。
- bank id：`xingtai`。
- 解析器：`bankflow_v2/xingtai.py`。
- 表格结构：文本行包含 `交易时间 / 收入或支出 / 交易金额（元） / 余额（元） / 对方账号 / 对方账户名称 / 交易户名 / 交易账号 / 交易渠道 / 交易摘要`。
- 处理方式：按方向列拆收入/支出；余额用于连续性和总闭合校验。

## 2026-07-05 兴业银行强水印交易明细

- 识别关键词组合：`兴业银行交易明细`、`交易金额`、`账户余额`。
- bank id：`cib`。
- 解析器：`bankflow_v2/cib.py`。
- 表格结构：`交易时间 / 记账日期 / 摘要 / 支收 / 交易金额 / 账户余额 / 交易用途 / 对方户名 / 对方账户及银行`。
- 处理方式：强水印版式金额和余额列会混入核验编号/水印字符；金额仍按表格列解析，余额标记为参考余额，跳过余额连续性和总闭合硬校验。

## 2026-07-05 Excel 转表无余额明细

- 适用入口：`bankflow_v2/excel_input.py`。
- 适用场景：图片 PDF 转出的 Excel 明细，字段通常只有 `交易日期/交易时间`、`交易金额` 或 `收入金额/支出金额`、`收支方向`，可能没有余额列。
- 处理方式：无余额列时设置 `balance_optional = True`，不做余额连续性和总闭合校验；方向列存在但为空时按不计收支处理；支持 `YYYY-MM-\nDDHH:MM:SS` 这类断行日期。

## 2026-07-06 汉口银行对公账户交易明细 Excel 转表

- 适用入口：`bankflow_v2/excel_input.py`。
- 识别关键词组合：`账户交易明细查询`、`交易笔数`、`贷方交易合计金额`、`借方交易合计金额`、`收入金额 / 支出金额 / 余额`。
- 表格结构：图片 PDF 转 Excel 后每页重复表头，字段为 `序号 / 交易日期 / 交易时间 / 收入金额 / 支出金额 / 摘要 / 用途 / 对方账号 / 对方户名 / 余额`，中间可能多出空白时间列。
- 处理方式：按 `收入金额 / 支出金额` 分列计收支；用页眉交易笔数、贷方合计、借方合计核对解析结果；余额按流水序号链校验，遇到 `73536124` 这类小数点丢失余额时，仅在原始数字等于期望余额分值时校正为 `735,361.24`。

## 2026-07-06 农村商业银行个人交易明细

- 识别关键词组合：`卡号/账号`、`客户名称`、`总收入`、`总支出`、`序号 / 摘要 / 币别 / 钞汇 / 交易日期 / 交易金额 / 账户余额`。
- bank id：`rural_commercial`。
- 解析器：`bankflow_v2/rural_credit.py`。
- 表格结构：文本层按行抽取，交易日期为 `YYYYMMDD`，交易金额带正负号，账户余额为本笔后余额；PDF 新到旧倒序打印，解析时按序号倒序恢复余额链。
- 处理方式：正交易金额计收入，负交易金额计支出；页眉总收入/总支出和笔数用于核对；页脚当前页小于总页数时标记缺页提示。

## 2026-07-09 河北银行个人/对公与霸州舜丰村镇银行对公

- 河北银行个人识别关键词组合：`账户历史交易明细清单`、`客户账号`、`账户名称`、`交易日期 / 借贷 / 交易金额 / 账户余额`。
- 河北银行个人 bank id：`hebei_personal`。
- 河北银行对公识别关键词组合：`交易明细记录`、`收支标识`、`交易日期 / 交易金额 / 借贷标志 / 交易后余额`。
- 河北银行对公 bank id：`hebei_corp_detail`。
- 霸州舜丰村镇银行对公识别关键词组合：`霸州舜丰村镇银行企业账户交易明细`、`汇出金额 / 汇入金额 / 余额 / 摘要 / 用途`。
- 霸州舜丰村镇银行对公 bank id：`bazhou_shunfeng_corp`。
- 解析器：`bankflow_v2/hebei_bazhou.py`。
- 处理方式：河北银行个人和对公均处理倒序打印、同日/同秒排序和红字冲账；霸州舜丰村镇银行按文本块抽取汇出、汇入、余额。

## 2026-07-09 中国银行个人加密 PDF 与外币交易

- 加密 PDF：GUI 通过 `bankflow_v2/pdf_password.py` 检测密码保护文件；输入正确密码后直接解析原 PDF 文字层，不需要手工另存为无密码图片 PDF。
- 识别关键词组合：`中国银行交易流水明细清单`、`记账日期 / 记账时间 / 币别 / 金额 / 余额 / 交易名称`。
- bank id：`boc`。
- 解析器：`bankflow_v2/boc.py`。
- 币种口径：`人民币` 交易按金额正负计收入/支出，并参与人民币余额连续性校验；非人民币交易保留明细但标记为中性，不计入人民币收支汇总，不参与人民币余额链校验。

## 2026-07-14 徽商银行对公账户交易明细

- 识别关键词组合：`徽商银行`、`账户交易明细`、`账户/户名`、`交易时间 / 收入金额 / 支出金额 / 账户余额 / 对方账号 / 对方户名 / 对方开户行 / 用途 / 流水号 / 附言 / 摘要`。
- bank id：`huishang_corp`。
- 解析器：`bankflow_v2/huishang_corp.py`。
- 表格结构：PDF 无表格线，按词块坐标定位收入、支出、余额三列；每笔交易以日期块开头，交易时间与金额在后续行。
- 处理方式：坐标列 `收入金额` 计收入，`支出金额` 计支出，`账户余额` 用于连续性和总闭合校验；跨行对方账号、户名、开户行、用途等保留在原始字段。

## 2026-07-17 泸州银行个人账户历史明细清单

- 识别关键词组合：`泸州银行账户历史明细清单`、`客户名称`、`总条数`、`序号 / 交易时间 / 币种 / 交易金额 / 账户余额 / 对方账号 / 对方户名 / 交易类型 / 摘要 / 交易渠道`。
- bank id：`luzhou`。
- 解析器：`bankflow_v2/luzhou.py`。
- 表格结构：每页为固定十列表格，交易时间、交易金额和账户余额可直接从表格单元格抽取；正金额计收入，负金额计支出。

## 2026-07-18 全量样本基线与适配回归规则

- 主要样本根目录：`D:\Codex data\CD_assets\PDF流水`、`D:\Codex data\PDF流水`。
- 项目回归配置、技术变更记录和本手册明确引用的其他现存路径可加入盘点；若文件已存在于主要根目录或内容完全相同，只记录路径别名，不重复解析。
- 完全重复优先按 SHA-256 识别；二进制不同但内容相同的 PDF，再按标准化文本或解析后的交易指纹识别。
- 同一银行、同一账户的不同月份文件不是重复样本：每个文件保留单文件回归，同时建立跨月组合回归，检查日期顺序、重叠交易、余额连续性和月度汇总。
- 部分月份重叠或重复导出的流水，组合时去重但不得改变单文件解析结果；去重必须保留来源文件、页码、行号和原始字段。
- 本阶段以原始 PDF 解析为主要基线。人工整理且只保留数字的 Excel 标记为辅助数字参考，不用于交易对手、摘要、备注等文字字段验收；图片 PDF 转 Excel 在来源和准确性未确认时列为待核实。
- 已纳入正式自动回归的 Excel 继续单独运行，确保 PDF 文字字段重构不破坏既有 Excel 导入能力。
- 样本目录保持只读；批量验证、清单、哈希、日志和报告写入独立输出目录。
- 新银行适配仍需同时更新 `auto_detect.py`、`pipeline.py`、解析器、回归案例、本手册和 `技术变更记录.md`。

### 首次基线历史状态

- 本小节只说明适配工作的基线来源，不能作为当前待办或当前能力判断依据。
- 当时的未识别、零行和待复核分布，以及完整路径、首个异常、识别说明和 FontBBox 警告归属，保留在 `D:\Investigator PDF\baseline-output\2026-07-18\文件级解析基线.md` 和 `技术变更记录.md`。
- 华夏对公、农行对公九列表格、富滇银行、河北农信、天津银行和长沙银行等后续处理结果，以本手册随后的当前规则章节、人工确认工作簿及续接记录顶部快照为准。

## 2026-07-18 华夏银行对公交易明细十五列表格

- 识别字段组合：`账号 / 账户名称 / 查询日期 / 交易方向：全部 / 排序方向：由近及远`，以及完整表头 `序号 / 交易日期 / 交易时间 / 支出金额 / 收入金额 / 余额 / 对方账号 / 对方户名 / 对方行名 / 核心流水号 / 交易描述 / 摘要 / 凭证号码 / 明细标注 / 记账日期`。
- bank id：`huaxia_corp`。
- 解析器：`bankflow_v2/huaxia_corp.py`。
- 账户类型：对公；样本账户名称为公司，首页右上角为华夏银行股份有限公司电子印章。
- 表格特点：PDF 按“由近及远”倒序打印，日期和时间在单元格内换行；收入、支出和余额为独立列，序号可能从中间值开始，表示同一查询区间的分段页。
- 处理方式：直接读取 15 列表格，按交易日期、时间和原序号恢复顺序；收入列计收入，支出列计支出，余额用于相邻连续性和总闭合校验；原始 15 列完整保留。
- 误识别边界：交易对方行名可能出现“中国工商银行”，不得仅因正文出现工行名称进入 `icbc_corp`；本格式的精确表头规则应优先命中。
- 与个人版区别：`huaxia` 个人电子版为 6 列 `记账日期 / 摘要 / 交易金额 / 余额 / 交易机构 / 附言`，有中英文华夏标题；对公 15 列格式使用独立解析器，不复用个人规则。

## 2026-07-18 农业银行对公账户历史明细九列表格

- 识别归属：bank id 继续使用 `abc_corp`；首页为“中国农业银行明细回单专用章”，账户户名为公司。
- 解析器：`bankflow_v2/abc_corp.py` 的九列表格分支。
- 精确表头：`交易时间 / 收入金额 / 支出金额 / 账户余额 / 对方账号 / 对方户名 / 对方开户行 / 摘要 / 交易用途`。
- 与既有格式区别：该格式没有“会计日期”列，交易时间单元格已同时包含日期和时分秒，总列数为 9；不能使用原有至少 10 列的对公表格条件。
- 处理方式：收入、支出分列计入汇总，余额用于相邻连续性和总闭合校验；对方账号、户名、开户行、摘要和交易用途保留在原始行文本中。
- 文件级影响：仅目标样本由 `NO_ROWS` 改善为 `PASS`，其余 216 份关键字段不变。

## 2026-07-18 富滇银行个人交易流水九列表格

- 识别关键词组合：`富滇银行交易流水`、`Fudian Bank Transaction Details`，以及中英文列名 `Serial Number / Trading Date / Trading Amount / Account Balance / Counterparty Account / Counterparty Name / Trading Description / Remark`。
- bank id：`fudian`。
- 解析器：`bankflow_v2/fudian.py`。
- 表格结构：9 列，字段为 `序号 / 交易日期 / 货币 / 交易金额 / 账户余额 / 对方账号 / 对方户名 / 摘要描述 / 备注`。
- 分页特点：仅第 1 页包含表头，第 2 至 16 页直接续表；解析器先验证首页富滇标题，再接受后续9列数据行。
- 处理方式：交易金额正数计收入、负数计支出；账户余额用于相邻连续性和总闭合校验；9列原始字段完整保留。
- 文件级影响：仅富滇样本由 `UNRECOGNIZED` 改善为 `PASS`，其余216份关键字段不变。

## 2026-07-18 河北省农村信用社联合社账户历史明细清单

- 识别关键词组合：`河北省农村信用社联合社账户历史明细清单`、`账号 / 户名 / 总条数`，以及表头 `序号 / 交易日期 / 交易金额 / 金额 / 对方户名 / 对方账号 / 摘要 / 网点 / 来源`。
- bank id：`rural_credit`。
- 解析器：`bankflow_v2/rural_credit.py` 的河北农信账户历史明细分支。
- 表格结构：57 页固定 9 列，第一页有标题、账户信息和总条数，后续页直接续表；`交易金额` 为带正负号单列，`金额` 为交易后余额。
- 排序特点：PDF 按新到旧打印，但同一交易日期内序号不一定等于真实余额链顺序。
- 处理方式：逐行抽取交易日期、交易金额和交易后余额；正金额计收入、负金额计支出；同日内按余额链恢复顺序，并用微秒保持统一汇总排序；原始页码、序号和 9 列字段完整保留。
- 文件级影响：仅目标样本由 `UNRECOGNIZED` 改善为 `PASS`，其余216份关键字段不变。

## 2026-07-18 天津银行个人账户交易明细清单

- 识别关键词组合：`天津银行个人账户交易明细清单`、`户名 / 账号`，以及表头 `序号 / 交易日期 / 交易金额 / 余额 / 交易摘要 / 附言`。
- bank id：`tianjin_bank`。
- 解析器：`bankflow_v2/tianjin_bank.py`。
- 表格结构：1 页无表格线文本，交易列按固定 x 坐标排列；字段为 `序号 / 交易日期 / 交易金额 / 余额 / 交易摘要 / 附言`。
- 格式特点：电子印章和户名水印会混入文字层，个别日期与摘要显示噪声；数字列坐标稳定。
- 处理方式：按词块序号确定交易行边界，再按坐标列抽取交易日期、交易金额和余额；日期和金额列过滤水印文字；交易金额正数计收入、负数计支出；余额用于连续性和总闭合校验。
- 文件级影响：仅目标样本由 `UNRECOGNIZED` 改善为 `PASS`，其余216份关键字段不变。

## 2026-07-18 长沙银行单位账户明细对账单

- 识别关键词组合：`单位账户明细对账单`、`账户名称 / 客户账号 / 账单期初余额 / 账单期末余额`，以及表头 `交易日期 / 交易金额 / 账户余额 / 摘要/备注 / 编号`。
- bank id：`changsha_bank_corp`。
- 解析器：`bankflow_v2/changsha_bank_corp.py`。
- 表格结构：4 页无表格线文本，字段为 `交易日期 / 交易金额 / 账户余额 / 摘要/备注 / 编号`；每页页脚给出累计页码/总条数，末页给出收入合计和支出合计。
- 格式特点：摘要/备注可能跨多行，日期列是稳定行锚点；交易金额和账户余额列坐标稳定。
- 处理方式：按日期词块确定每笔交易的纵向区间，再按固定 x 坐标抽取交易金额、账户余额、摘要/备注和编号；交易金额正数计收入、负数计支出；账户余额用于连续性、期初期末闭合和页脚合计校验。
- 文件级影响：仅目标样本由 `UNRECOGNIZED` 改善为 `PASS`，其余216份关键字段不变。

## 2026-07-18 统一标准文字字段映射第一步

- 入口：`bankflow_v2/models.py` 的 `Transaction` 与 `map_standard_text_fields()`。
- 标准字段：交易对手名称/账号/银行，摘要、备注、用途、交易类型，商户名称/类别/地点，以及字段来源、字段置信度和人工复核信息。
- 映射依据：只使用 `raw_headers` 与 `raw_fields` 的同列精确表头同义词；表头允许 Unicode 形式、空白和常见分隔符差异。
- 保守边界：不从 `raw_text` 猜测；不拆分 `摘要/备注`、`对方账号/户名` 等混合列；缺失、空值或不可靠字段保持空。
- 优先级：解析器显式传入的非空标准字段优先，自动映射不得覆盖；显式 `field_sources` 和 `field_confidence` 同样保留。
- 来源口径：自动映射来源记录为 `raw_headers[列号]:原表头`；置信度 `1.0` 仅表示精确表头同列映射，不代表原 PDF 内容已经人工核真。
- 兼容性：新增字段位于 `Transaction` 旧字段之后，既有位置参数、解析器、数字汇总和余额校验不变。

## 2026-07-19 微信、支付宝确认文字字段

- 统一模型新增：`transaction_direction`（收支方向原文）、`transaction_method`（交易方式）、`payment_method`（付款方式）、`product_description`（商品说明）。四者含义独立，不互相合并。
- 微信真实表头：`交易单号 / 交易时间 / 交易类型 / 收/支/其他 / 交易方式 / 金额(元) / 交易对方 / 商户单号`。
- 微信标准字段：交易时间、交易类型、收支或其他、交易方式、金额、交易对方；`其他` 保留整笔记录但不计入数字汇总。交易单号、商户单号只保留在 `raw_fields/raw_headers`，不作为分析字段。
- 支付宝固定表头：`收/支 / 交易对方 / 商品说明 / 收/付款方式 / 金额 / 交易订单号 / 商家订单号 / 交易时间`。
- 支付宝标准字段：收支、交易对方、商品说明、付款方式、金额、交易时间；`不计收支` 保留整笔记录但不计入数字汇总。交易订单号、商家订单号只保留在 `raw_fields/raw_headers`，不作为分析字段。
- 字段来源继续记录为 `raw_headers[列号]:原表头`，精确同列映射置信度为 `1.0`；空字段保持空，多行字段规范为单个空格，不从 `raw_text` 猜测。

## 2026-07-19 富滇银行确认字段实现

- 确认依据：`富滇银行文字字段确认.xlsx` 的 `确认字段!A4:G28`；21 项状态确认保留原建议，`currency / account_currency / verification_code` 明确排除，电子印章和验真说明按原建议排除。
- 通用文件容器：富滇解析器返回列表兼容的 `TransactionList`，文件级字段保存在 `StatementMetadata`，不重复写入每笔交易。
- 逐笔标准字段：`source_sequence / transaction_time / income / expense / raw_amount / balance / raw_balance / counterparty_account / counterparty_name / summary / remark / merchant_name`。
- 富滇来源专属字段保存在 `Transaction.source_fields`：`merchant_code / merchant_name_and_address_raw / secondary_merchant_info / postscript / remark_details`。
- 备注拆分边界：只识别明确标签 `商户代码 / 商户名称及地址 / 二级商户信息 / 附言 / 商户 / 商品 / 摘要 / 车牌`；已知字段确定性映射，其余带标签内容和标签前原文进入 `remark_details`，完整备注仍保留，不从无标签文本猜测。
- 账户和文件级字段：`account_name / account_number / statement_period_start / statement_period_end / generated_at / source_part_label / page_total`，均记录原始值、字段来源和置信度。
- 排除边界：交易币种仍存在于九列 `raw_fields` 作为原始证据，但不建立 `currency` 分析字段；账户币种、验证码、电子印章和验真说明不进入 `StatementMetadata`。
- 真实样本结果：267 笔，收入 21 笔 / `224,091.90`，支出 246 笔 / `225,511.53`，期初 `10,004.79`，期末 `8,585.16`，0 异常；数字结果与改造前一致。

## 2026-07-19 初步跨来源字段归类

- 映射口径：标准字段仅使用 `raw_headers / raw_fields` 同列精确表头；来源专属字段仅在解析器拥有确定语义时写入 `Transaction.source_fields`，同时保留来源和置信度。不得根据 `raw_text`、混合列或位置猜测字段含义。
- 已扩充的标准表头包括序号、收付方名称/账号、对方开户行、收支状态、借贷标志、付款方式和商户地点；`交易地点` 不自动映射为商户地点。
- 已统一归类的来源证据字段包括核心/交易流水号、交易凭证号、凭证号码/种类、记账/会计日期、明细标注、交易描述、交易网点、来源、全局路由号和卡号。
- 已确定的解析器补充：华夏银行对公、河北农信、邮储银行对公、交通银行对公、上海银行对公；客户明细对公的未拆分交易文本保留为来源专属原文，不再误作交易对手名称。
- 待确认项及字段覆盖检查见 `D:\Investigator PDF\outputs\field-classification-initial\初步字段解析与归类清单.xlsx`；其中的币种、渠道/方式、地点、摘要备注和对手方混合列必须逐来源确认后再实现。

## 2026-07-19 已确认 10 类来源文字字段

- 工商银行个人：固定 13 列保留摘要、对方户名、储种、渠道和序号；逐笔本方账号、币种、钞汇、地区及对方账号不进入分析字段。
- 建设银行个人：固定 7 列保留摘要、“交易地点/附言”完整原值及“对方账号与户名”完整原值；后者仅在唯一 `/` 边界存在时取斜杠后户名，不写对方账号。
- 兴业银行：固定 9 列映射摘要、支/收、交易用途和对方户名；“对方账户/对方银行”只作为混合来源字段完整保留。
- 民生银行对公：日期行和时间行确定性配对为 10 列；摘要、对方行名进入标准字段；“对方户名/账号”只在唯一 `/` 时拆分，凭证类型、凭证号码和流水号只作证据字段。
- 平安银行个人：固定 8 列映射摘要、备注和序号；交易地点为来源专属字段，交易对手信息完整保留且不拆分。
- 浦发银行个人：固定 9 列映射交易名称和对手姓名；对手账号、无效交易摘要及逐笔本方账号不进入分析字段，本方户名、账号和查询区间进入文件元数据。
- 浦发银行对公：固定 9 列映射对手机构、对手名称和备注；摘要代码原样同时保留为交易类型和 `abstract_code`，交易流水号只作证据字段。
- 中信银行个人：文本行按尾部固定三段拆为交易摘要、对方账号和对方户名；无法形成明确三段时只保留尾部原文；户名、账号、查询区间、开户日期和币种原值进入文件元数据或原始证据。
- 招商银行个人：文本行先按固定列取得交易摘要和完整对手信息；仅当末尾是明确账号时拆为账号及此前完整名称。
- 华夏银行个人：以日期词块为行锚点、按固定 x 坐标恢复 9 列；仅映射摘要、附言和来源专属交易机构，对方开户行不因通用表头自动进入分析字段。
- 所有来源继续保留 `raw_headers / raw_fields / raw_text` 和 `field_sources`；未确认或非唯一边界不猜测。
