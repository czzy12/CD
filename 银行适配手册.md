# 银行流水PDF提取 — 银行适配手册

## 当前实际支持与回归覆盖（2026-05-30）

当前代码不是纯 JSON 配置驱动，核心解析器位于 `bankflow_v2/*.py`，统一回归入口为：

```powershell
python tools\regression.py --all
```

当前统一回归覆盖 14 个样本：

| 银行/格式 | 类型 | 解析入口 | 当前状态 | 说明 |
|---|---|---|---|---|
| 工商银行 | 个人历史明细 | `icbc.py` | 已适配 | 支持强水印、金额/余额污染候选路径选择 |
| 工商银行 | 个人活期/定期混排 | `icbc.py` | 已纳入回归 | 统计正确，保留 13 条跨账户余额链复核提示 |
| 工商银行 | 对公旧版 A | `icbc_corp.py` | 已适配 | `借贷标志 + 发生额 + 余额` |
| 工商银行 | 对公旧版 B | `icbc_corp.py` | 已适配 | `借/贷 + 借方发生额 + 贷方发生额 + 余额` |
| 工商银行 | 企业存款对账单新版 C | `icbc_corp.py` | 已适配 | `借方发生额 / 贷方发生额 / 余额`，无单独借贷方向列 |
| 交通银行 | 6 列日期版 | `bocom.py` | 已适配 | 无具体时分秒时按页面顺序保持日内顺序 |
| 交通银行 | 11 列时间版 | `bocom.py` | 已适配 | 带交易时间 |
| 农业银行 | 个人 | `abc.py` | 已适配 | 无具体时间排序样本已纳入回归 |
| 邮储银行 | 个人历史明细 | `psbc.py` | 已适配 | 已纳入回归 |
| 招商银行 | 个人交易流水 | `cmb.py` | 已适配 | 已纳入回归 |
| 民生银行 | 对公单位账户对账单 | `cmbc_corp.py` | 已适配 | `单位账户对账单` 优先识别为对公 |
| 中信银行 | 个人账户交易明细 | `citic.py` | 已适配 | 单金额列按余额关系判断收入/支出 |
| 浦发银行 | 个人 | `spdb.py` | 已适配 | 已做专项样本测试 |
| 浦发银行 | 对公 | `spdb.py` | 已适配 | 已做专项样本测试 |
| Excel 导入 | 外部转表流水 | `excel_input.py` | 已支持 | 支持 `借贷状态`；小额余额误差降为低风险 |
| 通用 PDF 识别 | 未适配专用银行 | `generic_pdf.py` | 兜底支持 | 小额余额误差降为低风险，仍需人工核对关键指标 |

打包前建议固定执行：

```powershell
python -m py_compile bankflow_v2\summary.py bankflow_v2\auto_detect.py gui_v2.py tools\regression.py
python tools\regression.py --all
```

---

## 2026-05-31 新增/扩展格式

### 建设银行对公：活期存款明细账

- 识别关键词：`中国建设银行股份有限公司活期存款明细账`
- 解析器：`bankflow_v2/ccb_corp.py`
- 格式特点：PDF 表格可抽取，但交易不是逐行返回，而是同一列内多行换行文本。
- 处理方式：按列内换行索引拆分日期、借方、贷方、余额，再重组为逐笔交易。
- 回归样本：`0530流水1建设对公` 目录 4 份 PDF。
- 验证结果：4 PASS / 0 FAIL。

### 中国银行对公：活期明细文本流水

- 识别关键词组合：`中国银行`、`单位人民币活期`、`承前页余额`、`借方发生额`、`贷方发生额`
- 解析器：`bankflow_v2/boc_corp.py`
- 格式特点：PDF 表格抽取为空，但文本层中每笔流水为 `|` 分隔行。
- 处理方式：解析记账日、借方发生额、贷方发生额、余额，并用 `承前页余额` 验证期初。
- 回归样本：`中银对公流水2` 目录 6 份 PDF。
- 验证结果：6 PASS / 0 FAIL。

---

## 旧版配置化记录（历史参考，非当前全部实现）

以下内容来自早期配置化方案记录，部分名称仍写作 `*.json`。当前实际实现以 Python 专用解析器和统一回归为准。

## 已适配银行（早期记录：7种格式）

| # | 银行 | 配置文件 | 方式 | 日期格式 | 金额模式 | 页数 | 笔数 |
|---|------|----------|------|----------|----------|------|------|
| 1 | 工商银行(个人) | icbc.json | 表格 | YYYY-MM-DD HH:MM:SS | sign | 14 | 338 |
| 2 | 招商银行(个人) | cmb.json | 文本正则 | YYYY-MM-DD | sign | 26 | 592 |
| 3 | 建设银行(个人) | ccb.json | 表格 | YYYYMMDD | sign | 8 | 222 |
| 4 | 浦发银行(个人) | spdb.json | 表格 | YYYYMMDD | sign | 3 | 26 |
| 5 | 农业银行(个人) | abc.json | 文本正则 | YYYYMMDD HHMMSS | sign | 1/80 | 34/3339 |
| 6 | 民生银行(个人) | cmbc.json | 文本正则 | YYYY/MM/DD HH:MM:SS | sign | 6 | 116 |
| 7 | 民生银行(对公) | cmbc_corp.json | 文本配对 | YYYY/MM/DD HH:MM:SS | separate_columns | 5 | 89 |

---

## 各银行详细配置

### 1. 工商银行 (icbc.json)
- **识别**: 中国工商银行、工商银行、ICBC、工行
- **表格结构**: 每页有表头，有严重PDF乱码
- **关键列**: 交易日期(0)、交易金额(8)、余额(9)、对方名称(10)
- **特殊处理**: 余额交叉验证修正乱码粘合金额
- **乱码模式**: 大写字母(D/B/C/A/F/E)、随机数字混入表格

### 2. 招商银行 (cmb.json)
- **识别**: 招商银行、China Merchants Bank、CMB
- **文本正则**: `(?P<date>\d{4}-\d{2}-\d{2})\s+CNY\s+(?P<amount>[-\d][\d,]*\.\d{2})\s+[\d,]+\.\d{2}\s+(?P<type>\S+)\s*(?P<counterparty>.*)`
- **特殊处理**: 无表格结构，对方名称可能跨行，counterparty用 `.*` 非 `.+`

### 3. 建设银行 (ccb.json)
- **识别**: 中国建设银行、建设银行、CCB
- **表格结构**: 每页有表头，数据干净
- **关键列**: 序号(0)、摘要(1)、交易日期(2)、交易金额(3)、账户余额(4)

### 4. 浦发银行 (spdb.json)
- **识别**: 浦发银行、浦东发展银行、SPDB
- **表格结构**: 仅首页有表头，后续页需缓存列匹配
- **关键列**: 交易日期(0)、交易金额(4)、余额(5)、对方名称(6)

### 5. 农业银行 (abc.json)
- **识别**: 中国农业银行、农业银行、ABC
- **文本正则**: `(?P<date>\d{8})\s+(?P<time>\d{6})\s+(?P<type>\S+)\s+(?P<amount>[+-]?[\d,]+\.\d{2})\s+[\d,]+\.\d{2}\s*(?P<counterparty>.*)`
- **特殊处理**: 金额必须有正负号且两位小数(`\d{2}`)，过滤乱码行, 时间字段用于排序

### 6. 民生银行(个人) (cmbc.json)
- **识别**: 中国民生银行、民生银行、CMBC、民生
- **文本正则**: `(?P<date>\d{4}/\d{2}/\d{2})\s+(?P<time>\d{2}:\d{2}:\d{2})\s+(?P<type>.+)\s+(?P<amount>[+-]?[\d,]+\.\d{2})\s+[\d,]+\.\d{2}\s+(?P<counterparty>.*)`
- **特殊处理**: 斜杠日期格式 YYYY/MM/DD

### 7. 民生银行(对公) (cmbc_corp.json)
- **识别**: 单位账户对账单、借方发生额、贷方发生额、中国民生银行
- **文本正则**: `(?P<date>\d{4}/\d{2}/\d{2})\s+(?P<type>.+?)\s+(?P<amount_debit>[\d,]+\.\d{2})\s+(?P<amount_credit>[\d,]+\.\d{2})\s+[\d,]+\.\d{2}\s+\S+\s+(?P<counterparty>.+)`
- **特殊处理**: 
  - 每两行配对（日期在首行，时间在次行）
  - 借方=收入、贷方=支出（对公户借贷方向与个人户相反！）
  - 配置项 `"debit_is": "收入"` 控制此行为

---

## 金额模式说明

| 模式 | 配置值 | 说明 | 使用银行 |
|------|--------|------|----------|
| 正负号 | sign | 单列金额，正=收入/负=支出 | 工商、招商、建设、浦发、农业、民生(个人) |
| 收支分列 | separate_columns | 两列：收入金额+支出金额 | 民生(对公) |
| 方向列 | direction_column | 金额列+方向文字列(进/出/借/贷) | 暂无 |
| 借贷标志 | debit_credit | 金额列+借贷标志列 | 暂无 |

### 借贷方向配置

借贷方向因银行而异，由配置控制：
```json
"amount_rules": {
  "debit_is": "支出",   // 借方含义(个人户默认)
  "credit_is": "收入",  // 贷方含义(个人户默认)
  "direction_mapping": {
    "借": "支出", "贷": "收入"  // 文字→方向映射
  }
}
```
对公户民生将 `debit_is` 设为 `"收入"`。

---

## 核心修复记录

### 2026-05-30 工行对公企业存款对账单新版式

**现象**: 4 份 `中国工商银行企业存款对账单` 被识别为工行对公，但专用解析返回 0 笔，GUI 回退到通用识别后出现大量余额不连续。

**原因**: 表头只有 `借方发生额 / 贷方发生额 / 余额`，没有旧版工行对公依赖的 `借/贷` 或 `借贷标志` 列。

**修复**:

- 自动识别新增关键词：`中国工商银行企业存款对账单`
- `icbc_corp.py` 新增 format C
- format C 按 `借方发生额=支出`、`贷方发生额=收入`、`余额=余额链校验`
- 4 份样本加入 `tools/regression_cases.json`

**验证**: 4 份李翠样本全部 0 异常，统一回归 `14 PASS / 0 FAIL`。

### 2026-05-29 中信、民生对公、Excel 导入与小额余额差异

**中信个人**:

- 新增 `citic.py`
- 支持 `账户交易明细 / Transaction details`
- PDF 文本层只有单金额列时，使用上一笔余额判断收入或支出

**民生对公**:

- `单位账户对账单` 优先识别为 `民生银行对公`
- 避免被通用民生关键字误识别为个人

**Excel 导入**:

- 支持 `借贷状态`
- 保留银行原始借/贷列口径
- 借方负数冲正仍按支出列口径统计，方便和人工透视表一致

**小额余额差异**:

- Excel 导入和通用 PDF 识别中，0.99 元以内余额连续性差异标为 `低风险`
- 低风险不再把文件状态打为 `需复核`
- 专用银行解析器仍保持严格复核标准

### 1. PDF乱码金额 — `clean_number` 三次迭代
**现象**: 工商银行PDF金额列混入随机字符（如 `04-59.90`、`40:5-29.70`）
**v1**: `re.search` 第一个匹配 → 乱码前缀被误认为金额
**v2**: 优先匹配 `[+-]\d+\.\d{2}` → 部分解决
**v3**: `re.findall` 取最后一个匹配 → 解决大部分
**v4**: 余额交叉验证 → 解决数字粘合（`1426.19` → `146.19`）

### 2. 余额交叉验证
**文件**: core/extractor.py `_validate_with_balance`
**原理**: `余额变动 = 当前余额 - 上笔余额`，与解析金额对比
**安全机制**: 比值限制0.05x~20x，过滤不可靠余额

### 3. 银行检测加权
**现象**: 短关键词（"工行"、"ABC"）在乱码中误匹配
**修复**: 改为关键词长度加权 `score = sum(len(kw) for kw in matched)`

### 4. 四舍五入误差
**现象**: 月度值先舍入再求和 → 合计行与余额行不一致
**修复**: 合计行从原始DataFrame计算，不累加月度值

### 5. 跨页表头缺失
**现象**: 浦发仅首页有表头
**修复**: `_cached_col_match` 缓存首页匹配

### 6. 跨行文本配对
**现象**: 民生对公PDF每笔交易跨两行（日期/时间分离）
**修复**: `text_line_pair: true` 配置项，相邻行合并

### 7. 借贷方向反转
**现象**: 个人户借方=支出，对公户借方=收入
**修复**: 配置项 `debit_is` 控制

### 8. 文本行乱码过滤
**现象**: 农业、民生PDF乱码行被正则误匹配
**修复**: 验证日期+时间前缀格式，不匹配则跳过

### 9. 时间排序
**现象**: 同日交易未按时间排序
**修复**: 捕获time字段，复合排序

### 10. 表格复制
**现象**: QTableWidget Ctrl+C只复制当前格
**修复**: 子类化CopyableTable，遍历选中区生成制表符文本

---

## 新增银行流程

1. 复制 `configs/_template.json` 为新文件
2. 检查PDF: 有表格→表格模式，无表格→文本正则模式
3. 确定: 日期格式、金额模式(sign/separate_columns/direction_column)
4. 文本模式: 写 `text_line_regex`，注意 counterparty 用 `.*`
5. 如跨行: 设 `text_line_pair: true`
6. 如借贷分离: 确认 `debit_is`/`credit_is` 方向
7. 测试: 先用 `python main.py xxx.pdf --date-range all` 验证
8. 对比正确数据，确认金额和方向无误

---

## 技术栈与依赖

### 编程语言
Python 3.12+

### 第三方库

| 库 | 版本 | 用途 | 使用位置 |
|------|------|------|----------|
| pdfplumber | >=0.10 | PDF文本提取、表格检测 | core/extractor.py, core/pipeline.py |
| pandas | >=2.0 | DataFrame数据处理、分组汇总、排序 | core/extractor.py, output/aggregator.py |
| PyQt6 | >=6.5 | GUI桌面界面（窗口、表格、拖拽、线程） | gui_app.py |
| openpyxl | >=3.1 | Excel文件读写（pandas依赖） | output/aggregator.py |

### 标准库

| 库 | 用途 |
|------|------|
| re | 正则表达式：文本行匹配、金额清洗、日期解析、银行检测 |
| json | 银行配置文件读写（configs/*.json） |
| argparse | 命令行参数解析（main.py） |
| pathlib | 文件路径处理 |
| datetime | 日期时间计算、格式转换 |
| sys / os | 系统路径、文件操作 |

### 核心技术方案

| 技术 | 说明 |
|------|------|
| **双模式提取** | 表格模式(pdfplumber表格) + 文本模式(正则匹配)，自动回退 |
| **语义列匹配** | 关键词模糊匹配表头行，自动定位各语义列（日期/金额/余额/对方） |
| **银行自动识别** | PDF文本扫描关键词，长度加权计分，自动匹配银行配置 |
| **金额解析器** | 4种模式(sign/separate_columns/direction_column/debit_credit)，清洗乱码字符 |
| **日期解析器** | 6种格式兼容(YYYYMMDD/YYYY-MM-DD/YYYY/MM/DD+时间)，乱码前缀过滤 |
| **余额交叉验证** | 余额变动反推真实金额，修正数字粘合错误，比值限制确保安全 |
| **跨页缓存** | 首页列匹配缓存，后续页复用（浦发、工商） |
| **文本行配对** | 跨行交易合并（民生对公：日期首行+时间次行） |
| **乱码过滤** | 日期前缀格式验证，拒绝乱码行（农业、民生） |
| **GUI异步处理** | QThread后台线程，进度信号，界面不阻塞 |
| **表格复制** | CopyableTable子类化，Ctrl+C遍历选中区生成TSV文本 |
| **配置驱动** | 新银行只需加JSON配置，核心引擎不动 |

### 安装命令

```powershell
pip install pdfplumber pandas openpyxl PyQt6
```

### 程序结构

```
bank_流水/
├── gui_app.py          # GUI主程序（PyQt6）
├── main.py             # CLI命令行入口（argparse）
├── 启动GUI.bat          # Windows双击启动
├── requirements.txt    # 依赖清单
├── core/               # 核心引擎
│   ├── pipeline.py     # 全流程编排（检测→提取→过滤）
│   ├── extractor.py    # 交易提取器（表格/文本双模式）
│   ├── date_parser.py  # 多格式日期解析
│   ├── amount_parser.py# 金额解析（4种模式）
│   └── column_matcher.py# 语义列匹配
├── output/             # 输出模块
│   └── aggregator.py   # 汇总计算（月度/合计/月均）
├── configs/            # 银行配置（JSON驱动）
│   ├── _template.json  # 新银行模板
│   ├── icbc.json       # 工商银行
│   ├── cmb.json        # 招商银行
│   ├── ccb.json        # 建设银行
│   ├── spdb.json       # 浦发银行
│   ├── abc.json        # 农业银行
│   ├── cmbc.json       # 民生银行(个人)
│   └── cmbc_corp.json  # 民生银行(对公)
└── output/             # 运行时输出目录
```
