# PDF银行流水项目 - 对话上下文摘要

## 项目来源
用户是风控专员，原本用 WPS 手动处理：PDF转Excel → 删除印章/多余字段 → 手动拉透视表，每个客户约8分钟。

## 已确认的需求
- 不同银行PDF流水自动提取为Excel，自动区分收入/支出
- 日期格式兼容（20260505 / 2026-05-05 等）
- 字段顺序/叫法每个银行不同，需要分别适配
- 按月汇总 + 按交易类型汇总 + 总体统计（含收入-支出余额）
- 最终目标：拖拽式桌面应用，双击打开，拖入PDF点击处理

## 当前进度

### 已完成（commit 4685db2 + 78eb743）
```
bank_流水/
├── configs/spdb.json       # 浦发银行配置
├── configs/_template.json  # 新银行模板
├── core.py                 # 银行自动检测(首页关键词) + 表格提取
├── report.py               # 4个sheet报表(明细/按月/按类型/总统计)
├── main.py                 # 命令行入口
├── 拖拽处理.bat            # Windows拖放
└── output/
```

### 技术栈
pdfplumber + pandas + openpyxl，纯Python，无外部依赖。

### 验证结果
浦发银行PDF（3页26笔）100%准确提取，收支方向正确。

### 待做
1. GUI界面开发
2. 更多银行适配（等用户提供PDF样本）
3. 每月独立抬头流水的处理
4. 双重验证机制
5. 打包为单个.exe

## 关键讨论
- 用户认为8分钟/客户可以接受，代码方案的价值取决于月处理量
- 收入/支出通过金额正负号区分（浦发正=收入、负=支出）
- 银行适配方式：复制模板 → 填列映射 → 加入检测关键词，核心引擎不动
- GUI技术待讨论：PyQt6 vs pywebview
- 多PDF处理：每个PDF单独输出Excel

## 已完成对话内容
对话1：可行性分析、工作量估算、用户手动流程对比
对话2：浦发PDF样本测试，26笔数据提取+汇总
对话3：项目搭建(git init) + 核心引擎(core.py) + 报表模块(report.py)
对话4：命令行入口(main.py) + 拖拽bat + 实测验证 + 提交git
对话5：跨PC同步方案（CLAUDE.md + Git）